---
phase: 03-visualization-export
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - Project.toml
  - Manifest.toml
autonomous: true
requirements:
  - VIZ-01
  - EKS-01
  - EKS-03
tags:
  - julia
  - dependencies
  - project-toml
  - blocking

must_haves:
  truths:
    - "Project.toml [compat] ma GLMakie = \"0.13\" (NIE \"0.24\" — to wersja Makie, nie GLMakie)"
    - "Project.toml [deps] zawiera GLMakie, Makie, Observables, ProgressMeter (przeniesione z [extras] dla pierwszych trzech, dodane dla ProgressMeter)"
    - "Project.toml [extras] NIE zawiera juz GLMakie, Makie, Observables (zostaja tam tylko test-only deps: Aqua, JET, BenchmarkTools, PerformanceTestTools, Serialization, StableRNGs, Test, Unicode)"
    - "Manifest.toml zostal zregenerowany przez Pkg.add i odzwierciedla nowe runtime deps (commit per PROJECT D-25 — to aplikacja, nie biblioteka)"
    - "Komenda `julia --project=. -e 'using GLMakie; using Makie; using Observables; using ProgressMeter; println(\"OK\")'` exits 0"
  artifacts:
    - path: "Project.toml"
      provides: "Runtime deps + compat dla Phase 3"
      contains: "GLMakie = \"0.13\""
    - path: "Manifest.toml"
      provides: "Pinned versions wszystkich transitive deps (FFMPEG_jll, FreeType, ColorTypes, ...) — reprodukowalna instalacja"
      contains: "GLMakie"
  key_links:
    - from: "Project.toml [deps]"
      to: "Manifest.toml"
      via: "Pkg.add(...) regeneration"
      pattern: "GLMakie"
    - from: "Project.toml [compat]"
      to: "instalowalna wersja GLMakie 0.13.x"
      via: "Pkg resolver"
      pattern: "GLMakie = \"0.13\""
---

<objective>
**WAVE 0 — BLOKER dla wszystkich pozostalych planow Phase 3.**

Naprawic krytyczny blad w `Project.toml [compat]`: `GLMakie = "0.24"` to wersja Makie, NIE GLMakie. GLMakie uzywa wlasnej numeracji (obecnie 0.13.10 sparowane z Makie 0.24.10). Z `"0.24"` w compat, `Pkg.add("GLMakie")` zglasza `Unsatisfiable requirements detected for package GLMakie: restricted to versions 0.24 — no versions left` — to BLOKUJE wszystkie nastepne plany.

Po fixie naprawy:
1. Przeniesc GLMakie/Makie/Observables z `[extras]` do `[deps]` (Phase 3 konsumuje je w runtime, nie tylko w testach).
2. Dodac ProgressMeter do `[deps]` + `[compat]`.
3. Wykonac `Pkg.add(["GLMakie", "Observables", "ProgressMeter"])` w aktywnym env aby zregenerowac `Manifest.toml`.
4. Skomitowac oba pliki (PROJECT D-25 — aplikacja, Manifest commitowany).

Purpose: Pokrywa REQ VIZ-01 (umozliwia `using GLMakie`), EKS-01 (umozliwia `Makie.record()`), EKS-03 (umozliwia `using ProgressMeter`). Bez tego planu zadne `using GLMakie` w Phase 3 nie zadziala.

Output:
- Project.toml — sekcje [deps], [compat], [extras] zaktualizowane (per RESEARCH Q11 + Pitfall A).
- Manifest.toml — zregenerowany, wszystkie transitive deps (FFMPEG_jll przez Makie, FreeType, ColorTypes, GLFW, ...) zapisane.
- Dowod: `using GLMakie, Makie, Observables, ProgressMeter` exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-visualization-export/03-CONTEXT.md
@.planning/phases/03-visualization-export/03-RESEARCH.md
@.planning/phases/03-visualization-export/03-PATTERNS.md
@CLAUDE.md
@Project.toml

<interfaces>
<!-- Aktualny stan Project.toml (przed zmianami) — executor potwierdzi czytajac plik. -->

Aktualne [deps] (linie 6-10):
```toml
[deps]
GeometryBasics = "5c1252a2-5f33-56bf-86c9-59e7332b4326"
Random = "9a3f8284-a2c9-5f02-9a11-845980a1fd5c"
ChunkSplitters = "ae650224-84b6-46f8-82ea-d812ca08434e"
Statistics = "10745b16-79ce-11e8-11f9-7d13ad32a3b2"
```

Aktualne [compat] (linie 12-26) — UWAGA: GLMakie = "0.24" to BLAD:
```toml
[compat]
julia = "1.10"
GLMakie = "0.24"      # ← BLAD: GLMakie uzywa wlasnej numeracji 0.13.x
Makie = "0.24"        # ← OK
GeometryBasics = "0.5"
Observables = "0.5"
StableRNGs = "1.0"
Aqua = "0.8.14"
JET = "0.9, 0.10, 0.11"
BenchmarkTools = "1.6"
ChunkSplitters = "3"
PerformanceTestTools = "0.1"
Serialization = "1"
Test = "1"
Unicode = "1"
```

Aktualne [extras] (linie 28-39) — GLMakie/Makie/Observables tam BO byly placeholdery dla Phase 3:
```toml
[extras]
Aqua = "..."
BenchmarkTools = "..."
GLMakie = "e9467ef8-e4e7-5192-8a1a-b1aee30e663a"
JET = "..."
Makie = "ee78f7c6-11fb-53f2-987a-cfe4a2b5a57a"
Observables = "510215fc-4207-5dde-b226-833fc4488ee2"
PerformanceTestTools = "..."
Serialization = "..."
StableRNGs = "..."
Test = "..."
Unicode = "..."
```

Aktualne [targets] (linia 41-42) — pozostaje BEZ ZMIAN:
```toml
[targets]
test = ["Aqua", "JET", "PerformanceTestTools", "Serialization", "StableRNGs", "Test", "Unicode"]
```

UUIDs do uzycia (z RESEARCH.md Standard Stack + 03-CONTEXT.md):
- GLMakie: `e9467ef8-e4e7-5192-8a1a-b1aee30e663a` (compat: `"0.13"`)
- Makie: `ee78f7c6-11fb-53f2-987a-cfe4a2b5a57a` (compat: `"0.24"`)
- Observables: `510215fc-4207-5dde-b226-833fc4488ee2` (compat: `"0.5"`)
- ProgressMeter: `92933f4c-e287-5a05-a399-4b506db050ca` (compat: `"1"`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Naprawic Project.toml — przeniesc GLMakie/Makie/Observables do [deps], dodac ProgressMeter, fix compat GLMakie="0.13"</name>
  <files>Project.toml</files>
  <read_first>
    - Project.toml (aktualny stan przed edycja — kazdy line do potwierdzenia)
    - .planning/phases/03-visualization-export/03-RESEARCH.md sekcja "Q11: Project.toml / Manifest.toml Mechanics — CRITICAL BUG" (linie ~602-620)
    - .planning/phases/03-visualization-export/03-RESEARCH.md sekcja "Pitfall A: GLMakie = \"0.24\" w [compat] blokuje instalacje" (linie ~673-677)
    - .planning/phases/03-visualization-export/03-PATTERNS.md sekcja "`Project.toml` (config, extend existing)" (linie ~241-294)
    - .planning/phases/03-visualization-export/03-CONTEXT.md sekcja "Integration Points → `Project.toml [deps]`" (linie ~136-144)
  </read_first>
  <action>
    Edit `Project.toml` w trzech sekcjach jednoczesnie. Zachowac formatowanie (TOML — alfabetyczna kolejnosc w obrebie sekcji, kazdy klucz w nowej linii).

    **Krok 1: Sekcja [deps] (linie 6-10) — DODAC 4 nowe linie:**

    Po zmianie [deps] ma zawierac (w kolejnosci alfabetycznej):
    ```toml
    [deps]
    ChunkSplitters = "ae650224-84b6-46f8-82ea-d812ca08434e"
    GLMakie = "e9467ef8-e4e7-5192-8a1a-b1aee30e663a"
    GeometryBasics = "5c1252a2-5f33-56bf-86c9-59e7332b4326"
    Makie = "ee78f7c6-11fb-53f2-987a-cfe4a2b5a57a"
    Observables = "510215fc-4207-5dde-b226-833fc4488ee2"
    ProgressMeter = "92933f4c-e287-5a05-a399-4b506db050ca"
    Random = "9a3f8284-a2c9-5f02-9a11-845980a1fd5c"
    Statistics = "10745b16-79ce-11e8-11f9-7d13ad32a3b2"
    ```

    **Krok 2: Sekcja [compat] (linie 12-26) — KRYTYCZNA ZMIANA `GLMakie = "0.24"` → `GLMakie = "0.13"` + DODAC ProgressMeter:**

    Po zmianie [compat] ma zawierac (zachowac istniejaca kolejnosc + dodac jedna linie):
    ```toml
    [compat]
    julia = "1.10"
    GLMakie = "0.13"        # FIXED: GLMakie 0.13.10 pairs with Makie 0.24.10 (NIE "0.24" — to wersja Makie)
    Makie = "0.24"
    GeometryBasics = "0.5"
    Observables = "0.5"
    ProgressMeter = "1"     # NEW Phase 3 (EKS-03)
    StableRNGs = "1.0"
    Aqua = "0.8.14"
    JET = "0.9, 0.10, 0.11"
    BenchmarkTools = "1.6"
    ChunkSplitters = "3"
    PerformanceTestTools = "0.1"
    Serialization = "1"
    Test = "1"
    Unicode = "1"
    ```

    UWAGA: zmiana wartosci `GLMakie = "0.24"` na `GLMakie = "0.13"` to JEDNA edycja JEDNEJ linii. Cala reszta [compat] zostaje bez zmian. ProgressMeter dodany jako nowa linia.

    **Krok 3: Sekcja [extras] (linie 28-39) — USUNAC GLMakie, Makie, Observables (sa teraz w [deps]):**

    Po zmianie [extras] ma zawierac TYLKO test-only deps:
    ```toml
    [extras]
    Aqua = "4c88cf16-eb10-579e-8560-4a9242c79595"
    BenchmarkTools = "6e4b80f9-dd63-53aa-95a3-0cdb28fa8baf"
    JET = "c3a54625-cd67-489e-a8e7-0a5a0ff4e31b"
    PerformanceTestTools = "dc46b164-d16f-48ec-a853-60448fc869fe"
    Serialization = "9e88b42a-f829-5b0c-bbe9-9e923198166b"
    StableRNGs = "860ef19b-820b-49d6-a774-d7a799459cd3"
    Test = "8dfed614-e22c-5e08-85e1-65c5234f0b40"
    Unicode = "4ec0a83e-493e-50e2-b9ac-8f72acf5a8f5"
    ```

    **Krok 4: Sekcja [targets] (linia 41-42) — BEZ ZMIAN.** GLMakie/Makie/Observables NIE sa potrzebne dla `Pkg.test()` per D-14 (testy headless).

    Konwencje:
    - Plik UTF-8, LF, no BOM (encoding hygiene Phase 1 D-21).
    - TOML: kazdy klucz w nowej linii, brak trailing comma.
    - W [deps] kolejnosc alfabetyczna (po nazwie pakietu).
  </action>
  <verify>
    <automated>julia --project=. -e 'using TOML; t = TOML.parsefile("Project.toml"); @assert haskey(t["deps"], "GLMakie") "GLMakie not in [deps]"; @assert haskey(t["deps"], "Makie") "Makie not in [deps]"; @assert haskey(t["deps"], "Observables") "Observables not in [deps]"; @assert haskey(t["deps"], "ProgressMeter") "ProgressMeter not in [deps]"; @assert t["compat"]["GLMakie"] == "0.13" "GLMakie compat must be 0.13, got: $(t[\"compat\"][\"GLMakie\"])"; @assert t["compat"]["Makie"] == "0.24" "Makie compat must be 0.24"; @assert haskey(t["compat"], "ProgressMeter") "ProgressMeter not in [compat]"; @assert !haskey(t["extras"], "GLMakie") "GLMakie still in [extras] (should be moved to [deps])"; @assert !haskey(t["extras"], "Makie") "Makie still in [extras]"; @assert !haskey(t["extras"], "Observables") "Observables still in [extras]"; @assert haskey(t["extras"], "Aqua") "Aqua removed from [extras] (should stay)"; println("OK Project.toml: GLMakie=", t["compat"]["GLMakie"], ", deps=", length(t["deps"]), " keys")'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^GLMakie = ' Project.toml` zwraca 2 (raz w [deps], raz w [compat])
    - `grep -c '^Makie = ' Project.toml` zwraca 2 (raz w [deps], raz w [compat])
    - `grep -c '^Observables = ' Project.toml` zwraca 2 (raz w [deps], raz w [compat])
    - `grep -c '^ProgressMeter = ' Project.toml` zwraca 2 (raz w [deps], raz w [compat])
    - `grep -c 'GLMakie = "0.13"' Project.toml` zwraca 1 (KRYTYCZNE — fix compat)
    - `grep -c 'GLMakie = "0.24"' Project.toml` zwraca 0 (stara wartosc usunieta)
    - W sekcji [extras]: `grep -A 20 '^\[extras\]' Project.toml | grep -c '^GLMakie '` zwraca 0
    - Plik nadal jest poprawny TOML: `julia -e 'using TOML; TOML.parsefile("Project.toml")'` exits 0
  </acceptance_criteria>
  <done>
    Project.toml ma poprawne [deps] (8 wpisow runtime), poprawne [compat] (z `GLMakie = "0.13"`), oczyszczone [extras] (8 test-only). Plik jest valid TOML. Zaden `Pkg.add("GLMakie")` nie zglasza juz `Unsatisfiable requirements`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Pkg.add — zregenerowac Manifest.toml z nowymi runtime deps</name>
  <files>Manifest.toml</files>
  <read_first>
    - Project.toml (po Task 1 — aby potwierdzic ze [deps] zawiera GLMakie/Makie/Observables/ProgressMeter)
    - .planning/phases/03-visualization-export/03-RESEARCH.md sekcja "Standard Stack → Installation (after compat fix)" (linie ~131-138)
    - .planning/phases/03-visualization-export/03-CONTEXT.md sekcja "Integration Points → `Manifest.toml`" (linie ~145-148) — Manifest commitowany per PROJECT D-25 (aplikacja)
  </read_first>
  <action>
    Wykonac `Pkg.add` aby zregenerowac `Manifest.toml` z nowymi runtime deps. To zainstaluje GLMakie 0.13.x + Makie 0.24.x + Observables 0.5.x + ProgressMeter 1.x razem z ~50+ transitywnymi pakietami (FFMPEG_jll, FreeType_jll, GLFW_jll, ColorTypes, ...).

    Komenda:
    ```bash
    julia --project=. -e 'using Pkg; Pkg.add(["GLMakie", "Makie", "Observables", "ProgressMeter"])'
    ```

    Oczekiwany rezultat:
    - Pkg resolver znajdzie GLMakie 0.13.x (najnowsza w compat `"0.13"` — prawdopodobnie 0.13.10).
    - Manifest.toml zostanie nadpisany z pinami wszystkich nowych deps.
    - Operacja kompilacji moze trwac dlugo (60-150s przy pierwszym precompile GLMakie — TTFP issue per RESEARCH Q14 / Pitfall 14). To NORMALNE.

    Jezeli `Pkg.add` zglasi `Unsatisfiable requirements` — oznacza to ze Task 1 nie zostal poprawnie wykonany. Wrocic do Task 1 i sprawdzic `[compat]` GLMakie.

    Po zakonczeniu Pkg.add, zweryfikowac smoke import:
    ```bash
    julia --project=. -e 'using GLMakie; using Makie; using Observables; using ProgressMeter; println("Wszystkie pakiety dostepne: GLMakie ", pkgversion(GLMakie), ", Makie ", pkgversion(Makie), ", Observables ", pkgversion(Observables), ", ProgressMeter ", pkgversion(ProgressMeter))'
    ```

    Oczekiwany output (wersje moga sie roznic w obrebie majora):
    ```
    Wszystkie pakiety dostepne: GLMakie 0.13.10, Makie 0.24.10, Observables 0.5.5, ProgressMeter 1.11.0
    ```

    **WAZNE:** NIE dodawac `FFMPEG_jll` jako direct dep (per RESEARCH Standard Stack — pulled transitively by Makie). NIE dodawac `GeometryBasics` (juz w [deps] z Phase 1).

    Konwencje:
    - Manifest.toml MUSI byc commitowany (PROJECT D-25: to aplikacja, nie biblioteka — pinned versions dla reprodukowalnego demo).
    - Po sukcesie Pkg.add, `git status Manifest.toml` powinien pokazac modified.
  </action>
  <verify>
    <automated>julia --project=. -e 'using GLMakie; using Makie; using Observables; using ProgressMeter; @assert pkgversion(GLMakie) >= v"0.13" "GLMakie version too old"; @assert pkgversion(GLMakie) < v"0.14" "GLMakie version too new for compat 0.13"; @assert pkgversion(Makie) >= v"0.24" "Makie version too old"; @assert pkgversion(Observables) >= v"0.5" "Observables version too old"; @assert pkgversion(ProgressMeter) >= v"1" "ProgressMeter version too old"; println("OK loaded: GLMakie ", pkgversion(GLMakie), ", Makie ", pkgversion(Makie), ", Observables ", pkgversion(Observables), ", ProgressMeter ", pkgversion(ProgressMeter))'</automated>
  </verify>
  <acceptance_criteria>
    - `Manifest.toml` istnieje i ma > 1000 linii (transitive deps)
    - `grep -c '^name = "GLMakie"' Manifest.toml` zwraca 1
    - `grep -c '^name = "Makie"' Manifest.toml` zwraca 1
    - `grep -c '^name = "Observables"' Manifest.toml` zwraca 1
    - `grep -c '^name = "ProgressMeter"' Manifest.toml` zwraca 1
    - `grep -c '^name = "FFMPEG_jll"' Manifest.toml` zwraca 1 (transitive via Makie — sanity check)
    - Wersja GLMakie w Manifest.toml miesci sie w 0.13.x: `grep -A 2 '^name = "GLMakie"$' Manifest.toml | grep '^version' | grep -E '"0\.13\.'`
    - Smoke import dziala bez bledow (verify command)
    - `julia --project=. -e 'using Pkg; Pkg.test()'` nadal exits 0 (Phase 1+2 testy nie regresowaly)
  </acceptance_criteria>
  <done>
    Manifest.toml zregenerowany z GLMakie 0.13.x + Makie 0.24.x + Observables 0.5.x + ProgressMeter 1.x i wszystkimi transitive deps. Smoke import wszystkich 4 pakietow exit 0. Phase 1+2 testy nadal zielone (regression-free). Manifest gotowy do commitu.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pkg resolver -> Internet | Pkg.add pobiera pakiety z General registry przez HTTPS; uniezalezniamy sie od dostepnosci registry |
| Manifest.toml commit | Manifest pinuje wersje na poziomie hash — chroni przed cichym update'em transitive deps po `Pkg.update()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Manifest.toml regeneration | mitigate | Commit Manifest po Pkg.add — zmiany w transitive deps wymagaja explicit `Pkg.update` + commit |
| T-03-02 | Information Disclosure | Pkg.add pobiera kod z internetu | accept | Standardowa praktyka Julii; General registry zaufany; brak alternatywy w v1 |
| T-03-03 | Denial of Service | GLMakie precompile czas (60-150s pierwszy raz) | accept | TTFP udokumentowany w D-08 + RESEARCH Pitfall 14; user widzi @info message w Phase 3 |

Severity: brak high. Dependency management — standardowe ryzyko Julia ekosystemu.
</threat_model>

<verification>
**Plan-level integration check (po obu taskach):**

```bash
# 1. TOML walidacja + sprawdzenie kluczy
julia -e '
using TOML
t = TOML.parsefile("Project.toml")
println("=== [deps] ===")
for k in sort(collect(keys(t["deps"])))
    println("  ", k)
end
println("=== [compat] GLMakie/Makie/ProgressMeter ===")
println("  GLMakie  = ", get(t["compat"], "GLMakie", "MISSING"))
println("  Makie    = ", get(t["compat"], "Makie", "MISSING"))
println("  ProgressMeter = ", get(t["compat"], "ProgressMeter", "MISSING"))
@assert t["compat"]["GLMakie"] == "0.13"
@assert haskey(t["deps"], "GLMakie")
@assert haskey(t["deps"], "ProgressMeter")
@assert !haskey(t["extras"], "GLMakie")
println("OK Project.toml")
'

# 2. Smoke import (BLOKER dla Wave 1 — bez tego nic nie ruszy)
julia --project=. -e '
using GLMakie, Makie, Observables, ProgressMeter
println("OK pakiety: GLMakie=", pkgversion(GLMakie), " Makie=", pkgversion(Makie), " Observables=", pkgversion(Observables), " ProgressMeter=", pkgversion(ProgressMeter))
'

# 3. Pkg.test - regresja Phase 1+2
julia --project=. -e 'using Pkg; Pkg.test()'
```

Wszystkie 3 komendy MUSZA exit 0 zanim Wave 1 (plan 03-01) moze ruszyc.
</verification>

<success_criteria>
1. **Project.toml ma `GLMakie = "0.13"` w [compat]** — bez tego `Pkg.add("GLMakie")` zglasza Unsatisfiable.
2. **GLMakie, Makie, Observables, ProgressMeter w [deps]** — runtime deps Phase 3.
3. **GLMakie, Makie, Observables usuniete z [extras]** — nie sa juz placeholderami.
4. **Manifest.toml zregenerowany i zawiera wszystkie nowe pakiety + transitive (FFMPEG_jll itd.).**
5. **Smoke import 4 pakietow w aktywnym env exit 0.**
6. **Pkg.test() nadal exit 0 (Phase 1+2 brak regresji 222/222 PASS).**
</success_criteria>

<output>
After completion, create `.planning/phases/03-visualization-export/03-00-SUMMARY.md` zawierajace:

- Wersje zainstalowanych pakietow (GLMakie, Makie, Observables, ProgressMeter) — output `pkgversion(...)`.
- Liczbe transitive deps w Manifest.toml (sanity check rzedu 50+).
- Czas trwania pierwszego `Pkg.add` (sekundy — TTFP reference dla Phase 4 demo).
- Czy `Pkg.test()` przechodzi bez regresji (222/222 lub aktualny baseline).
- Czy smoke import 4 pakietow dziala.
</output>

## Source coverage audit

| Source ID | Type | Status | Note |
|-----------|------|--------|------|
| VIZ-01 | REQ | partial | Umozliwia `using GLMakie` (faktyczne window-opening w plan 03-02..03-04) |
| EKS-01 | REQ | partial | Umozliwia `Makie.record()` (faktyczne export branch w plan 03-04) |
| EKS-03 | REQ | partial | Umozliwia `using ProgressMeter` (faktyczne progress bar w plan 03-04) |
| RESEARCH Q11 | RESEARCH | COVERED | Critical fix GLMakie compat 0.24 → 0.13 |
| RESEARCH Pitfall A | RESEARCH | COVERED | Wave 0 fix per recommendation |
| CONTEXT D-XX | CONTEXT | n/a | Wave 0 jest pre-flight; D-XX adresowane w pozniejszych planach |
| PROJECT D-25 | CONTEXT | COVERED | Manifest commitowany jako application |
