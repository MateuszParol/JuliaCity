---
phase: 03-visualization-export
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - src/wizualizacja.jl
autonomous: true
requirements:
  - EKS-01
  - EKS-02
  - EKS-03
  - EKS-04
  - VIZ-05
tags:
  - julia
  - visualization
  - export
  - record
  - progressmeter

must_haves:
  truths:
    - "src/wizualizacja.jl zawiera helper `_export_loop` realizujacy `Makie.record(fig, sciezka, 1:n_klatek; framerate=fps) do frame_i ... end` blocking export"
    - "_export_loop sprawdza `isfile(sciezka)` PRZED record() i rzuca polski error per D-10 (EKS-04 — twardy error, brak nadpisania)"
    - "_export_loop liczy `n_klatek = liczba_krokow div kroki_na_klatke` (D-12) i uzywa ProgressMeter.Progress + next!(prog) (EKS-03)"
    - "_export_loop freezuje ostatnia klatke gdy SA hits liczba_krokow przed wyczerpaniem n_klatek (D-12 — sa_zakonczono Ref, brak SA stepow, Observable nie zmienia sie)"
    - "Format pliku auto-detect z extensji .mp4/.gif/.webm — Makie.record() builtin (EKS-02, RESEARCH Q9)"
    - "Branch `eksport isa String` w wizualizuj() wywoluje _export_loop"
    - "Polish @info message PRZED record() — diagnostyka dla user'a ze terminal blokuje sie podczas record() (D-09 + Pitfall 6 mitigation)"
  artifacts:
    - path: "src/wizualizacja.jl"
      provides: "_export_loop helper + eksport isa String branch zaimplementowany"
      contains: "_export_loop"
      min_lines: 320
  key_links:
    - from: "_export_loop"
      to: "Makie.record"
      via: "Makie.record(fig, sciezka, 1:n_klatek; framerate=fps) do frame_i ... end"
      pattern: "Makie\\.record\\(fig"
    - from: "_export_loop"
      to: "ProgressMeter"
      via: "Progress(n_klatek; desc=...) + next!(prog) + finish!(prog)"
      pattern: "Progress\\("
    - from: "wizualizuj eksport isa String branch"
      to: "_export_loop"
      via: "_export_loop(fig, stan, params, alg, obs.obs_trasa, obs.obs_historia, obs.obs_overlay, eksport; ...)"
      pattern: "_export_loop\\("
---

<objective>
Wypelnic branch `eksport isa String` w `wizualizuj()`: dodac internal helper `_export_loop` ktory realizuje blocking `Makie.record()` z ProgressMeter, file-exists hard-fail (D-10), freeze-last-frame gdy SA konczy wczesniej (D-12), i polski @info message przed startem (D-09).

Plan ten realizuje wszystkie 4 REQ-IDy EKS (EKS-01 record() entry point, EKS-02 extension detection auto, EKS-03 ProgressMeter, EKS-04 safe overwrite — twardy error). Format wykrywany automatycznie przez Makie z extensji pliku — RESEARCH Q9. Throttling identycznie jak w live mode (`kroki_na_klatke` per klatka, single Observable notify per record callback).

Hard-fail wrapper (D-13) i GOTOWE overlay (D-06) sa OSOBNYM planem (03-05) — eksport branch tutaj zaklada ze GLMakie dziala.

Purpose: Pokrywa EKS-01..04 (kompletny eksport flow), VIZ-05 (throttling identyczne jak live — single Observable notify per klatka), LANG-02 (UI strings po polsku — @info, error, ProgressMeter desc).

Output:
- src/wizualizacja.jl rozszerzony o `_export_loop` helper (~70 linii) + zaimplementowany branch `eksport isa String` w wizualizuj() (wywolanie _export_loop).
- Manual smoke test: `wizualizuj(stan, params, alg; liczba_krokow=200, kroki_na_klatke=20, eksport="test.mp4")` na dev machine produkuje plik `test.mp4`, ProgressMeter widoczny w terminalu. Drugi call ze sama sciezka → polski error per D-10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-visualization-export/03-CONTEXT.md
@.planning/phases/03-visualization-export/03-RESEARCH.md
@.planning/phases/03-visualization-export/03-PATTERNS.md
@.planning/phases/03-visualization-export/03-03-SUMMARY.md
@CLAUDE.md
@src/wizualizacja.jl

<interfaces>
<!-- Skopiowane patterns z RESEARCH dla executora — NIE musi szukac. -->

From RESEARCH Pattern 5 (Export Path z ProgressMeter) — linie ~321-349:

```julia
function _export_loop(fig, stan, params, alg, obs_trasa, obs_ene_historia,
                      overlay_obs, sciezka, fps, kroki_na_klatke, liczba_krokow)
    isfile(sciezka) && error("Plik 'sciezka' juz istnieje. Usun go recznie lub wybierz inna nazwe pliku.")

    n_klatek = liczba_krokow ÷ kroki_na_klatke
    sa_zakonczono = Ref(false)

    @info "Eksport do sciezka — moze potrwac kilka minut, terminal nie reaguje, postep ponizej:"
    prog = Progress(n_klatek; desc="Eksport animacji: ", dt=0.5)

    Makie.record(fig, sciezka, 1:n_klatek; framerate=fps) do frame_i
        if !sa_zakonczono[]
            for _ in 1:kroki_na_klatke
                if stan.iteracja >= liczba_krokow
                    sa_zakonczono[] = true
                    break
                end
                symuluj_krok!(stan, params, alg)
            end
        end
        # Freeze last frame gdy SA skonczylo wczesniej (D-12)
        obs_trasa[] = _trasa_do_punkty(stan)
        push!(obs_ene_historia.val, Point2f(stan.iteracja, stan.energia))
        notify(obs_ene_historia)
        next!(prog)
    end
    finish!(prog)
end
```

UWAGA: w docelowej wersji uzywamy DOSLOWNYCH polskich wiadomosci z poprawnymi diakrytykami:
- error: `"Plik '$sciezka' już istnieje. Usuń go ręcznie lub wybierz inną nazwę pliku."` (D-10)
- @info: `"Eksport do $sciezka — może potrwać kilka minut, terminal nie reaguje, postęp poniżej:"` (D-09)
- Progress desc: `"Eksport animacji: "` (LANG-02)

From RESEARCH Q3 (record() semantics):
- `Makie.record(fig, "output.mp4", 1:n_klatek; framerate=30) do frame_i ... end` — callback per klatka.
- Format wykrywany z extensji: .mp4, .gif, .webm, .mkv. Caller NIE musi przekazywac dodatkowego format= parametru (EKS-02).
- FFMPEG_jll transitive — nie dodawac jako direct dep (juz Phase 1+plan 03-00 wszystko OK).
- `framerate=fps` kontroluje playback FPS w pliku wynikowym.
- Callback synchroniczny; kazde wywolanie = 1 klatka.

From RESEARCH Q4 (ProgressMeter w record()):

```julia
prog = Progress(n_klatek; desc="Eksport animacji: ", dt=0.5)
Makie.record(fig, sciezka, 1:n_klatek; framerate=fps) do i
    # SA steps + Observable update
    next!(prog)
end
finish!(prog)
```

`dt=0.5` — minimalna przerwa miedzy aktualizacjami display (0.5s). Bez `dt` kazde `next!()` emituje linie.

From RESEARCH Q9 (record() file extension detection):
- AUTOMATYCZNE — Makie wykrywa format z extensji.
- .mp4 → H.264 przez FFMPEG_jll
- .gif → Animated GIF przez FFMPEG_jll
- .webm → VP8/VP9 przez FFMPEG_jll
- Caller NIE musi przekazywac dodatkowego `format=` parametru (EKS-02).

From CONTEXT D-09 (single API entry point, branch eksport isa String):
- Blocking trybie — `Makie.record(...)` z `1:n_klatek` iteratorem.
- ProgressMeter w terminalu (Pitfall 6 mitigation).

From CONTEXT D-10 (file-exists policy — twardy error):
- `isfile(sciezka) && error(...)` PRZED `Makie.record(...)`.
- Brak `nadpisz::Bool` kwarg w v1 (KISS, YAGNI).

From CONTEXT D-11 (FPS unified — fps arg dla live i eksport):
- Default 30. Brak osobnego eksport_fps.
- Used dla `framerate=fps` w record().

From CONTEXT D-12 (eksport dlugosc):
- `n_klatek = liczba_krokow ÷ kroki_na_klatke` (integer division — odrzuca remainder krokow).
- Dla `liczba_krokow=50_000`, `kroki_na_klatke=50` → 1000 klatek = 33s @30fps.
- Jezeli SA hits patience early, pozostale klatki pokazuja FREEZE ostatniego stanu (record wymaga deterministycznego frame count → freeze last frame przez kontynuacje Observable update bez SA stepow).

From RESEARCH Pattern 5 + Code Examples — `sa_zakonczono = Ref(false)` jest WAZNE:
- Gdy `stan.iteracja >= liczba_krokow`, ustawia sa_zakonczono[] = true.
- Pozostale wywolania callback'a NIE wykonuja `symuluj_krok!`.
- Observable update nadal sie dzieje (push do historia, _trasa_do_punkty(stan)) — ale nic sie nie zmienia, wiec klatki sa identyczne (FREEZE).

From CONTEXT "Code Insights → Reusable Assets":
- `symuluj_krok!(stan, params, alg)` jest zero-alloc (Phase 2 TEST-03).
- `stan.iteracja` += 1 per `symuluj_krok!`.

From plan 03-03 — `_live_loop` juz zdefiniowany, body wizualizuj() ma branching:

```julia
if eksport === nothing
    display(fig)
    _live_loop(fig, stan, params, alg, obs.obs_trasa, obs.obs_historia, obs.obs_overlay; ...)
else
    error("Eksport nie jest jeszcze zaimplementowany — wypelnienie w planie 03-04.")
end
```

Plan 03-04 zastapi placeholder error wywolaniem `_export_loop`.

From plan 03-02 — helpery `_trasa_do_punkty`, `_zbuduj_overlay_string` dostepne; `obs.obs_trasa`, `obs.obs_historia`, `obs.obs_overlay` w NamedTuple.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dodac helper _export_loop + zaimplementowac branch eksport isa String w wizualizuj()</name>
  <files>src/wizualizacja.jl</files>
  <read_first>
    - src/wizualizacja.jl (po plan 03-03 — ma _live_loop + live branch zaimplementowany; eksport branch placeholder)
    - .planning/phases/03-visualization-export/03-RESEARCH.md sekcja "Pattern 5: Export Path z ProgressMeter" (linie ~321-349)
    - .planning/phases/03-visualization-export/03-RESEARCH.md sekcja "Q3: Makie.record() Semantics" (linie ~520-535)
    - .planning/phases/03-visualization-export/03-RESEARCH.md sekcja "Q4: ProgressMeter w record()" (linie ~538-549)
    - .planning/phases/03-visualization-export/03-RESEARCH.md sekcja "Q9: record() File Extension Detection" (linie ~582-590)
    - .planning/phases/03-visualization-export/03-RESEARCH.md sekcja "Code Examples → ProgressMeter z record()" (linie ~774-789)
    - .planning/phases/03-visualization-export/03-RESEARCH.md sekcja "Pitfall D: Makie.record() nadpisuje plik bez ostrzezenia" (linie ~688-690)
    - .planning/phases/03-visualization-export/03-CONTEXT.md sekcja "decisions D-09, D-10, D-11, D-12" (linie ~44-50)
    - .planning/phases/03-visualization-export/03-PATTERNS.md sekcja "Error handling pattern" (linie 117-137)
    - .planning/phases/03-visualization-export/03-03-SUMMARY.md (output _live_loop sygnatura — referencja dla _export_loop similar shape)
  </read_first>
  <action>
    Edytowac `src/wizualizacja.jl` — dodac NOWY helper `_export_loop` PO `_live_loop` (PRZED docstring `wizualizuj`), nastepnie ZASTAPIC placeholder error w branchu `eksport isa String` (else branch w wizualizuj() body) wywolaniem `_export_loop(...)`.

    ---

    **Helper: `_export_loop`**

    Polish komentarz blokowy nad funkcja (skopiowac doslownie):

    ```
    Eksport renderloop (D-09 branch eksport isa String): blocking Makie.record()
    z ProgressMeter (EKS-03). Sprawdza isfile(sciezka) PRZED record() i rzuca polski
    error per D-10/EKS-04 (twardy error, brak overwrite). Liczy n_klatek = liczba_krokow
    div kroki_na_klatke (D-12). Format pliku auto-detect z extensji .mp4/.gif/.webm
    (RESEARCH Q9, EKS-02). Freeze ostatniej klatki gdy SA hits liczba_krokow przed
    wyczerpaniem n_klatek (D-12 — sa_zakonczono Ref).

    Throttling identyczne jak _live_loop: kroki_na_klatke SA krokow per record callback,
    1 notify per Observable per klatka (VIZ-05 + Pitfall 5). Brak rolling FPS — overlay
    pokazuje tylko stan + alg metryki (FPS/ETA bez sensu w blocking record).
    ```

    Body funkcji:

    ```julia
    function _export_loop(fig, stan::StanSymulacji, params::Parametry, alg::Algorytm,
                          obs_trasa::Observable{Vector{Point2f}},
                          obs_historia::Observable{Vector{Point2f}},
                          obs_overlay::Observable{String},
                          sciezka::String;
                          liczba_krokow::Int, fps::Int, kroki_na_klatke::Int)
        # D-10 / EKS-04: file-exists hard error (Pitfall D mitigation).
        # Polski user-facing error (LANG-02). Diakrytyki: "już", "Usuń", "ręcznie", "nazwę".
        isfile(sciezka) && error(
            "Plik '$sciezka' już istnieje. Usuń go ręcznie lub wybierz inną nazwę pliku."
        )

        # D-12: n_klatek = liczba_krokow / kroki_na_klatke (integer division).
        n_klatek = liczba_krokow ÷ kroki_na_klatke
        @assert n_klatek > 0 "n_klatek must be positive (liczba_krokow >= kroki_na_klatke required)"

        # D-12: gdy SA konczy sie wczesniej, pozostale klatki freeze ostatni stan.
        sa_zakonczono = Ref(false)

        # D-09: polski @info PRZED record() (Pitfall 6 mitigation — record() blokuje terminal).
        @info "Eksport do $sciezka — może potrwać kilka minut, terminal nie reaguje, postęp poniżej:"

        # EKS-03: ProgressMeter z dt=0.5s (RESEARCH Q4 — bez dt kazde next! emituje linie).
        prog = Progress(n_klatek; desc="Eksport animacji: ", dt=0.5)

        # EKS-01 + EKS-02: Makie.record(fig, sciezka, 1:n_klatek; framerate=fps) do i ... end.
        # Format wykrywany z extensji sciezka (RESEARCH Q9). FFMPEG_jll transitive via Makie.
        Makie.record(fig, sciezka, 1:n_klatek; framerate=fps) do frame_i
            if !sa_zakonczono[]
                for _ in 1:kroki_na_klatke
                    if stan.iteracja >= liczba_krokow
                        sa_zakonczono[] = true
                        break
                    end
                    symuluj_krok!(stan, params, alg)
                end
            end
            # Update Observables (1 per Observable per klatka — VIZ-05 / Pitfall 5).
            # Gdy sa_zakonczono, stan nie zmienia sie -> Observable wartosc identyczna ->
            # klatka renderowana jako FREEZE poprzedniego stanu (D-12).
            obs_trasa[] = _trasa_do_punkty(stan)
            push!(obs_historia.val, Point2f(Float32(stan.iteracja), Float32(stan.energia)))
            notify(obs_historia)

            # Overlay update (D-04) — bez fps_est/eta_sec/accept_rate (NaN — w eksport
            # te metryki nie maja sensu: blocking record, brak rolling window dla swiezych
            # delta).
            obs_overlay[] = _zbuduj_overlay_string(stan, alg, NaN, NaN, NaN)

            next!(prog)
        end
        finish!(prog)

        return nothing
    end
    ```

    ---

    **Branch eksport isa String — zastapic placeholder error w wizualizuj()**

    W body `wizualizuj()` (z plan 03-03), znajdz fragment:

    ```julia
    if eksport === nothing
        display(fig)
        _live_loop(fig, stan, params, alg, obs.obs_trasa, obs.obs_historia, obs.obs_overlay;
                   liczba_krokow=liczba_krokow, fps=fps, kroki_na_klatke=kroki_na_klatke)
    else
        error("Eksport nie jest jeszcze zaimplementowany — wypelnienie w planie 03-04.")
    end
    ```

    Zastapic na:

    ```julia
    if eksport === nothing
        # Live mode: otworz okno + uruchom renderloop (blokujace).
        display(fig)
        _live_loop(fig, stan, params, alg, obs.obs_trasa, obs.obs_historia, obs.obs_overlay;
                   liczba_krokow=liczba_krokow, fps=fps, kroki_na_klatke=kroki_na_klatke)
    else
        # Export mode: blocking Makie.record() do pliku eksport (D-09, D-10, D-12).
        # NIE wywolujemy display(fig) — record() automatycznie sluzy do off-screen renderu.
        _export_loop(fig, stan, params, alg, obs.obs_trasa, obs.obs_historia, obs.obs_overlay,
                     eksport;
                     liczba_krokow=liczba_krokow, fps=fps, kroki_na_klatke=kroki_na_klatke)
    end
    ```

    UWAGA: NIE wywolujemy `display(fig)` PRZED `_export_loop` — `Makie.record()` samodzielnie radzi sobie z renderem. `display(fig)` przed record() bywa pomocne na niektorych setupach (Discourse 2024), ale w naszym przypadku redundantne i moze powodowac niepotrzebne otwieranie okna w eksport mode.

    ---

    **WAZNE — UWAGI implementacyjne:**

    - `_export_loop` nie liczy rolling FPS / ETA / accept-rate — w blocking record() te metryki nie odzwierciedlaja real wallclock per klatka (record wymaga sterownika frame'ow). Overlay pokazuje tylko core fields (Iteracja, Energia, Temperatura, Alfa); pola FPS/Pozostalo/Akceptacja jako "—" (NaN sentinel w `_zbuduj_overlay_string`).
    - Polskie diakrytyki w wiadomosciach: "już", "Usuń", "ręcznie", "nazwę", "może", "potrwać", "postęp", "poniżej", "Eksport animacji" — wszystkie poprawne NFC (Phase 1 D-21).
    - `Makie.record(fig, sciezka, 1:n_klatek; framerate=fps) do frame_i ... end` — `frame_i` to Int z iteratora 1..n_klatek; nie uzywamy `frame_i` wewnatrz callback'a (state w `stan` + `obs_*`), ale parametr jest wymagany przez API.
    - `Point2f(Float32(stan.iteracja), Float32(stan.energia))` — explicit Float32 conversion (Point2f to Point2{Float32}).
    - NIE dodawac `format=` kwarg do `Makie.record(...)` — auto-detect z extensji (RESEARCH Q9, EKS-02).
    - Manifest.toml ma juz FFMPEG_jll transitive (plan 03-00) — `Makie.record(...)` na .mp4/.gif uruchomi sie bez doinstalowywania.
    - Body wizualizuj() po wprowadzeniu zmian: with_theme(theme_dark()) do ... end ma zarowno live branch (z _live_loop) jak i eksport branch (z _export_loop). Oba korzystaja z `obs` NamedTuple zwroconego z _init_observables.

    Konwencje:
    - Polish komentarze blokowe nad helperem.
    - English asserts (`@assert n_klatek > 0 "n_klatek must be positive..."`).
    - Polish error/info messages z diakrytykami (LANG-02 user-facing).
    - ASCII identyfikatory (`sa_zakonczono`, `n_klatek`, `prog`, `frame_i`, `sciezka`).
    - UTF-8 NFC, no BOM (Phase 1 D-21).
  </action>
  <verify>
    <automated>julia --project=. -e 'using JuliaCity; src = read("src/wizualizacja.jl", String); @assert occursin("function _export_loop", src) "_export_loop not defined"; @assert occursin("isfile(sciezka)", src) "file-exists guard missing — D-10"; @assert occursin("juz istnieje", src) || occursin("ż istnieje", src) "polish file-exists error message missing"; @assert occursin("Makie.record(fig", src) "Makie.record() call missing — EKS-01"; @assert occursin("framerate=fps", src) "framerate kwarg missing"; @assert occursin("Progress(n_klatek", src) "Progress() init missing — EKS-03"; @assert occursin("next!(prog)", src) "next!(prog) missing — EKS-03"; @assert occursin("finish!(prog)", src) "finish!(prog) missing"; @assert occursin("sa_zakonczono", src) "freeze flag missing — D-12"; @assert occursin("Eksport do", src) "polish @info message missing — D-09"; @assert occursin("_export_loop(fig, stan, params, alg", src) "_export_loop call missing in body"; @assert !occursin("Eksport nie jest jeszcze zaimplementowany", src) "eksport placeholder error still present — should be replaced"; @assert occursin("n_klatek = liczba_krokow", src) "n_klatek calculation missing — D-12"; println("OK _export_loop defined + wired into eksport isa String branch")'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^function _export_loop(' src/wizualizacja.jl` zwraca 1
    - `grep -c 'isfile(sciezka)' src/wizualizacja.jl` zwraca 1 (D-10 file-exists guard)
    - `grep -c 'już istnieje' src/wizualizacja.jl` zwraca 1 (polish error message D-10)
    - `grep -c 'Makie.record(fig' src/wizualizacja.jl` zwraca 1 (EKS-01)
    - `grep -c 'framerate=fps' src/wizualizacja.jl` zwraca 1 (D-11 unified fps)
    - `grep -c 'Progress(n_klatek' src/wizualizacja.jl` zwraca 1 (EKS-03)
    - `grep -c 'next!(prog)' src/wizualizacja.jl` zwraca 1 (EKS-03)
    - `grep -c 'finish!(prog)' src/wizualizacja.jl` zwraca 1
    - `grep -c 'sa_zakonczono' src/wizualizacja.jl` zwraca przynajmniej 3 (init Ref + check `[]` + set true)
    - `grep -c 'n_klatek = liczba_krokow' src/wizualizacja.jl` zwraca 1 (D-12)
    - `grep -c 'Eksport do' src/wizualizacja.jl` zwraca przynajmniej 1 (polish @info)
    - `grep -c 'może potrwać' src/wizualizacja.jl` zwraca 1 (D-09 polish info diacritics)
    - `grep -c 'Eksport animacji:' src/wizualizacja.jl` zwraca 1 (Progress desc)
    - `grep -c '_export_loop(fig, stan, params, alg' src/wizualizacja.jl` zwraca 1 (call site w wizualizuj body)
    - Branch eksport: placeholder usuniety: `grep -c 'Eksport nie jest jeszcze zaimplementowany' src/wizualizacja.jl` zwraca 0
    - Branch live: zachowany: `grep -c '_live_loop(fig, stan, params, alg' src/wizualizacja.jl` zwraca 1
    - `grep -c 'format=' src/wizualizacja.jl` zwraca 0 (auto-detect z extensji per RESEARCH Q9)
    - Plik UTF-8 NFC: `julia -e 'using Unicode; @assert Unicode.normalize(read("src/wizualizacja.jl", String), :NFC) == read("src/wizualizacja.jl", String)'`
    - Plik ma >= 320 linii: `awk 'END {print NR}' src/wizualizacja.jl` >= 320
    - Pkg.test() nadal exit 0 (testy headless — nie wywoluja wizualizuj)
    - VIZ-06 zachowany: `grep -rl '^using GLMakie' src/` zwraca tylko src/wizualizacja.jl
  </acceptance_criteria>
  <done>
    src/wizualizacja.jl ma _export_loop helper realizujacy `Makie.record(...)` z ProgressMeter, file-exists hard-fail (D-10/EKS-04), freeze-last-frame Ref (D-12), polish @info + error msg (LANG-02). Body wizualizuj() w branchu eksport isa String wywoluje _export_loop(...). Branch live zachowany (z plan 03-03). Phase 1+2 testy zielone.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-supplied `eksport::String` path | Path traversal: user moze podac `../../../etc/foo.mp4` — akceptowane (V5 Input Validation partial); brak sandbox dla local CLI tool |
| `isfile(sciezka)` PRZED record() | Atomic guard: race condition gdy plik powstanie miedzy isfile() a record(); akceptowane (single-user dev tool) |
| `Makie.record()` blokuje terminal | Akceptowane (D-09 mitigation z @info) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-15 | Tampering | Makie.record() nadpisuje plik bez ostrzezenia | mitigate | `isfile(sciezka) && error(...)` PRZED record() — D-10 / EKS-04 / Pitfall D |
| T-03-16 | Tampering | n_klatek == 0 przy `liczba_krokow < kroki_na_klatke` | mitigate | `@assert n_klatek > 0` z english msg |
| T-03-17 | Information Disclosure | Polish error message expose path | accept | Standard practice — user widzi ktora sciezka kolizjowala |
| T-03-18 | Denial of Service | record() blokuje terminal bez feedback | mitigate | @info message PRZED + ProgressMeter z dt=0.5 (D-09 + EKS-03) |

Severity: brak high. Local-only tool, predictable file ops.
</threat_model>

<verification>
**Plan-level integration check:**

```bash
# 1. _export_loop staticly defined + wired
julia --project=. -e '
using JuliaCity
src = read("src/wizualizacja.jl", String)
@assert occursin("function _export_loop", src)
@assert occursin("Makie.record(fig", src)
@assert occursin("isfile(sciezka)", src)
@assert occursin("Progress(n_klatek", src)
@assert occursin("next!(prog)", src)
@assert occursin("finish!(prog)", src)
@assert occursin("sa_zakonczono", src)
@assert occursin("_export_loop(fig, stan, params, alg", src)
@assert !occursin("Eksport nie jest jeszcze", src)
@assert occursin("_live_loop(fig, stan, params, alg", src)  # live branch preserved
println("OK _export_loop wired; live branch preserved")
'

# 2. VIZ-06 sanity preserved
matches=$(grep -rl "^using GLMakie" src/ 2>/dev/null | wc -l)
test "$matches" = "1" || { echo "FAIL VIZ-06: $matches files use GLMakie"; exit 1; }
echo "OK VIZ-06 preserved"

# 3. Phase 1+2 regression
julia --project=. -e 'using Pkg; Pkg.test()'

# 4. (MANUAL — dev machine z GUI) Smoke export mode
# Test 1: eksport sukces
# rm -f /tmp/test_eksport.mp4 2>/dev/null
# julia --project=. -e '
# using JuliaCity
# pkty = generuj_punkty(50; seed=42)
# stan = StanSymulacji(pkty); inicjuj_nn!(stan)
# alg = SimAnnealing(stan); stan.temperatura = alg.T_zero
# params = Parametry(liczba_krokow=400)
# wizualizuj(stan, params, alg; liczba_krokow=400, kroki_na_klatke=20, fps=30, eksport="/tmp/test_eksport.mp4")
# # Oczekiwane: @info wyswietlone, ProgressMeter dziala, plik /tmp/test_eksport.mp4 powstaje (~1s 30fps)
# '
#
# Test 2: drugi call → polski error D-10
# julia --project=. -e '
# using JuliaCity
# pkty = generuj_punkty(50; seed=42)
# stan = StanSymulacji(pkty); inicjuj_nn!(stan)
# alg = SimAnnealing(stan); stan.temperatura = alg.T_zero
# params = Parametry(liczba_krokow=400)
# try
#     wizualizuj(stan, params, alg; liczba_krokow=400, kroki_na_klatke=20, fps=30, eksport="/tmp/test_eksport.mp4")
# catch e
#     msg = sprint(showerror, e)
#     println("Got error: ", msg)
#     @assert occursin("juz istnieje", msg) || occursin("już istnieje", msg) "expected polish file-exists error"
#     println("OK D-10 file-exists guard rzuca polski error")
# end
# '
```

Komendy 1, 2, 3 musza exit 0 automatycznie. Komendy 4 to manual smoke (D-14 — runtests.jl nie testuje wizualizacji; D-15 — CI bez OpenGL).
</verification>

<success_criteria>
1. **_export_loop helper zdefiniowany** — sygnatura z Observable typed parameters + sciezka::String + kwargs (liczba_krokow, fps, kroki_na_klatke).
2. **isfile() hard-fail PRZED record()** — D-10 / EKS-04 / Pitfall D, polski error message z diakrytykami.
3. **Makie.record(fig, sciezka, 1:n_klatek; framerate=fps) do frame_i ... end** — EKS-01 + EKS-02 (auto-detect format z extensji).
4. **n_klatek = liczba_krokow div kroki_na_klatke** — D-12.
5. **ProgressMeter zintegrowany** — Progress + next!() + finish!() — EKS-03; dt=0.5s + polish desc "Eksport animacji: ".
6. **Freeze last frame** — `sa_zakonczono = Ref(false)` semantyka per D-12.
7. **Polish @info "Eksport do {sciezka} — może potrwać kilka minut..."** — D-09 / Pitfall 6 mitigation.
8. **Branch eksport isa String wywoluje _export_loop** — placeholder error usuniety.
9. **Branch live zachowany** — _live_loop z plan 03-03 dziala; oba branche wspoldzialaja.
10. **Phase 1+2 testy zielone, VIZ-06 zachowany.**
</success_criteria>

<output>
After completion, create `.planning/phases/03-visualization-export/03-04-SUMMARY.md` zawierajace:

- Liczbe linii w `src/wizualizacja.jl` (sanity check >= 320)
- Sygnatura `_export_loop` (kompletna)
- Polski error message z D-10 (literalna kopia — referencja dla plan 03-05 i Phase 4)
- Polski @info message z D-09 (literalna kopia)
- Czy manual smoke test sie udal (boolean — wypelnione recznie po execute, oba: sukces eksportu + D-10 trigger przy drugim call)
- Branching matrix: live mode = ZAIMPLEMENTOWANY (plan 03-03), eksport = ZAIMPLEMENTOWANY (plan 03-04). Hard-fail wrapper + GOTOWE = plan 03-05.
- Czy Pkg.test() exit 0 (regression check)
</output>

## Source coverage audit

| Source ID | Type | Status | Note |
|-----------|------|--------|------|
| EKS-01 | REQ | COVERED | Makie.record() w _export_loop |
| EKS-02 | REQ | COVERED | Auto-detect z extensji per RESEARCH Q9 (NIE format= kwarg) |
| EKS-03 | REQ | COVERED | ProgressMeter.Progress + next!() + finish!() z polish desc |
| EKS-04 | REQ | COVERED | isfile() hard-fail z polish error D-10 |
| VIZ-05 | REQ | COVERED | kroki_na_klatke per record callback (1 notify per Observable per klatka) |
| LANG-02 | REQ | COVERED | @info, error, ProgressMeter desc — wszystko po polsku z diakrytykami |
| RESEARCH Pattern 5 (Export z ProgressMeter) | RESEARCH | COVERED | _export_loop |
| RESEARCH Q3 (record() semantics) | RESEARCH | COVERED | API doslownie z dokumentacji |
| RESEARCH Q4 (ProgressMeter w record) | RESEARCH | COVERED | dt=0.5s, polish desc |
| RESEARCH Q9 (extension auto-detect) | RESEARCH | COVERED | Brak format= kwarg |
| RESEARCH Pitfall 6 (record blocking) | RESEARCH | COVERED | @info + ProgressMeter |
| RESEARCH Pitfall D (overwrite without warning) | RESEARCH | COVERED | isfile() hard-fail |
| CONTEXT D-09 (single API, eksport branch) | CONTEXT | COVERED | _export_loop wywolane w `else` branch |
| CONTEXT D-10 (file-exists hard error) | CONTEXT | COVERED | isfile() z polish msg |
| CONTEXT D-11 (unified fps) | CONTEXT | COVERED | framerate=fps (ten sam fps dla live i eksport) |
| CONTEXT D-12 (n_klatek + freeze) | CONTEXT | COVERED | div + sa_zakonczono Ref |
| CONTEXT D-13 (hard-fail GLMakie) | CONTEXT | DEFERRED | plan 03-05 doda try/catch wrapper |
| CONTEXT D-06 (GOTOWE overlay) | CONTEXT | DEFERRED | plan 03-05 doda |
| CONTEXT D-08 (TTFP @info) | CONTEXT | DEFERRED | plan 03-05 doda |
