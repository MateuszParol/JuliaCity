---
phase: 03-visualization-export
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/wizualizacja.jl
autonomous: true
requirements:
  - VIZ-02
  - VIZ-03
  - VIZ-04
  - VIZ-07
tags:
  - julia
  - visualization
  - figure-setup
  - observables

must_haves:
  truths:
    - "src/wizualizacja.jl zawiera helper `_setup_figure` budujacy dual-panel Figure (D-01)"
    - "Lewy panel `ax_trasa` ma `aspect=AxisAspect(1)` i polski tytul/etykiety osi (D-03, VIZ-04)"
    - "Prawy panel `ax_energia` ma polski tytul/etykiety osi"
    - "NN baseline jest renderowane jako szara przerywana linia raz przed live loopem (D-02, alpha=0.3)"
    - "Helper `_trasa_do_punkty(stan)::Vector{Point2f}` konwertuje stan.trasa do Point2f z domknieciem cyklu (n+1 punktow)"
    - "Observable architecture: obs_trasa::Observable{Vector{Point2f}}, obs_historia::Observable{Vector{Point2f}}, obs_overlay::Observable{String} — wszystkie KONKRETNE typy (NIE Any — Pitfall 5)"
    - "scatter! + lines! podlaczone do obs_trasa; lines! podlaczone do obs_historia"
    - "Body wizualizuj() wywoluje _setup_figure (gdy eksport===nothing branch sie nie aktywuje, placeholder error pozostaje dla planu 03-03)"
  artifacts:
    - path: "src/wizualizacja.jl"
      provides: "Figure setup + Observables architecture + NN baseline overlay"
      contains: "_setup_figure"
      min_lines: 180
  key_links:
    - from: "_setup_figure"
      to: "GLMakie Figure/Axis/AxisAspect"
      via: "Figure(size=(1400,700)) + Axis(fig[1,N]; ...)"
      pattern: "Figure\\(size"
    - from: "_setup_figure"
      to: "Phase 2 trasa_nn"
      via: "nn = trasa_nn(stan.D; start=1) — NN baseline overlay"
      pattern: "trasa_nn\\(stan\\.D"
    - from: "obs_trasa"
      to: "scatter!/lines! plots"
      via: "scatter!(ax_trasa, obs_trasa) + lines!(ax_trasa, obs_trasa)"
      pattern: "scatter!\\(ax_trasa, obs_trasa"
---

<objective>
Wypelnic czesc body `wizualizuj()` ktora buduje GLMakie Figure: dual-panel layout (D-01), `theme_dark()` + aspect 1:1 (D-03), polskie tytuly/etykiety osi (VIZ-04), NN baseline overlay (D-02), oraz cala Observable architecture (obs_trasa dla VIZ-02/VIZ-03, obs_historia dla wykresu energii w prawym panelu, obs_overlay dla tekstowego overlay'u VIZ-04 — wszystkie KONKRETNE typy per RESEARCH Q12 Pitfall).

Plan ten dostarcza WYZNACZONE helpery (`_setup_figure`, `_trasa_do_punkty`, `_zbuduj_overlay_string`) plus inicjalizacje Observables. Body `wizualizuj()` wywoluje te helpery wewnatrz `with_theme(theme_dark()) do ... end` (D-03 + RESEARCH Pattern 7), ale BRANCH na live/eksport pozostaje w plan 03-03 (live) + 03-04 (eksport). Po tym planie placeholder error w wizualizuj() pozostaje, ale pojedyncze helpery sa testowalne osobno.

Purpose: Pokrywa VIZ-02 (route as Observable{Vector{Point2f}} + cycle closure), VIZ-03 (scatter! z markersize), VIZ-04 (polskie etykiety/tytuly + dark theme), VIZ-07 (diakrytyki ą/ę/ł renderuja sie poprawnie — sprawdzane przez D-21 NFC encoding + Q5 font finding), LANG-02 (UI strings po polsku).

Output:
- src/wizualizacja.jl rozszerzony o internal helpery `_setup_figure`, `_trasa_do_punkty`, `_zbuduj_overlay_string`, `_init_observables` (cztery prefiksowane `_` per CONTEXT.md "Established Patterns").
- Body `wizualizuj()` zaczyna sie od TTFP @info (D-08 — placeholder polish msg), nastepnie `with_theme(theme_dark()) do ... end`, w srodku ktorego wywolywany jest `_setup_figure` + `_init_observables`. Branch na live/eksport pozostaje placeholder (`error("...")`) do planu 03-03.
- Dowod: `_setup_figure(stan, nn_trasa)` zwraca `(fig, ax_trasa, ax_energia)` gdzie ax_trasa.aspect to AxisAspect(1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-visualization-export/03-CONTEXT.md
@.planning/phases/03-visualization-export/03-RESEARCH.md
@.planning/phases/03-visualization-export/03-PATTERNS.md
@.planning/phases/03-visualization-export/03-01-SUMMARY.md
@CLAUDE.md
@src/wizualizacja.jl
@src/baselines.jl
@src/typy.jl

<interfaces>
<!-- Skopiowane patterns z RESEARCH dla executora — NIE musi szukac. -->

From RESEARCH Pattern 1 (Figure Setup with Dual Panel) — linie ~190-217:
```julia
function _setup_figure(stan::StanSymulacji, nn_trasa::Vector{Int})
    fig = Figure(size=(1400, 700))

    ax_trasa = Axis(fig[1, 1];
        title = "Trasa TSP — bona mydlana (N=$(length(stan.punkty)))",
        xlabel = "Wspolrzedna X",
        ylabel = "Wspolrzedna Y",
        aspect = AxisAspect(1))

    ax_energia = Axis(fig[1, 2];
        title = "Energia trasy vs iteracja",
        xlabel = "Iteracja",
        ylabel = "Energia (dlugosc trasy)")

    colsize!(fig.layout, 1, Relative(0.6))   # lewy panel 60%, prawy 40%

    return fig, ax_trasa, ax_energia
end
```
UWAGA: w docelowym kodzie tytuly/etykiety MUSZA byc po polsku Z DIAKRYTYKAMI: "Trasa TSP — błona mydlana (N=...)", "Współrzędna X", "Współrzędna Y", "Energia (długość trasy)" (Phase 1 D-21 NFC encoding + RESEARCH Q5 potwierdza ze TeX Gyre Heros Makie pokrywa Latin Extended).

From RESEARCH Pattern 3 (Observable Update — Vector + Cycle Closure) — linie ~252-271:
```julia
function _trasa_do_punkty(stan::StanSymulacji)::Vector{Point2f}
    n = length(stan.trasa)
    pts = Vector{Point2f}(undef, n + 1)   # +1 dla zamkniecia cyklu
    @inbounds for k in 1:n
        pts[k] = Point2f(stan.punkty[stan.trasa[k]])
    end
    pts[n + 1] = pts[1]  # zamkniecie cyklu Hamiltona (VIZ-02)
    return pts
end
```

From RESEARCH Pattern 2 (Observable Architecture, Type-Stable) — linie ~221-247 + Q12 type-stability:
```julia
# Inicjalizacja w `_init_observables` helper:
obs_trasa    = Observable(_trasa_do_punkty(stan))    # Observable{Vector{Point2f}}
obs_historia = Observable([Point2f(0f0, Float32(stan.energia))])  # Observable{Vector{Point2f}}
obs_overlay  = Observable(_zbuduj_overlay_string(stan, alg, NaN, NaN, NaN))  # Observable{String}

# Podlaczenie do plotow (po wywolaniu _setup_figure):
scatter!(ax_trasa, obs_trasa; markersize=5, color=:cyan)
lines!(ax_trasa, obs_trasa; color=:white, linewidth=1.5)
lines!(ax_energia, obs_historia; color=:orange, linewidth=2)
```

From RESEARCH Q1 + A3 (Observable Architecture decision):
- **DECYZJA: jeden Observable{String} dla calego 7-pola overlay'u** (separator `\n`).
- D-04 mowi "kazde pole wrap'owane w Observable{String}" — Opcja B (jeden multiline Observable) osiaga ten sam efekt user-facing przy mniejszym koszcie (1 notify per klatka vs 7).
- Plan ten realizuje Opcje B; udokumentowac w komentarzu kodu.

From RESEARCH Pattern 7 (Theme Scoping) — linie ~388-400:
```julia
with_theme(theme_dark()) do
    fig, ax_trasa, ax_energia = _setup_figure(stan, nn_trasa)
    ax_trasa.aspect = AxisAspect(1)
    # ... cala logika wewnatrz
end
```

From CONTEXT D-04 — 7 pol overlay'u (DOSLOWNY format do uzycia w `_zbuduj_overlay_string`):
```
Iteracja: {stan.iteracja}
Energia: {round(stan.energia, digits=4)}
Temperatura: {round(stan.temperatura, digits=6)}
Alfa: {alg.alfa}
FPS: {fps_estimate}      # NaN gdy brak danych jeszcze
Pozostalo: {eta_sec}     # sekundy ETA
Akceptacja worsening: {accept_rate * 100}%
```
Polskie diakrytyki w nazwach pol: "Pozostało", "Akceptacja", "Współrzędna" — uzywac w UI stringach (LANG-02), w identyfikatorach Julia jednak ASCII (LANG-04, BOOT-04).

From CONTEXT D-02 — NN baseline overlay (RESEARCH Pattern 2 fragment):
```julia
nn_trasa = trasa_nn(stan.D; start=1)   # juz dostepne — Phase 2 inicjuj_nn! to ustawia
nn_points = Point2f[Point2f(stan.punkty[i]) for i in nn_trasa]
push!(nn_points, nn_points[1])   # zamkniecie cyklu
lines!(ax_trasa, nn_points; color=:gray, linestyle=:dash, alpha=0.3)
# UWAGA: brak Observable — to STATYCZNE overlay (raz renderowane, nie aktualizowane)
```

From RESEARCH Q7 (linestyle=:dash potwierdzony dla GLMakie 0.13.x).

From CONTEXT "Code Insights → Reusable Assets":
- `stan.punkty::Vector{Punkt2D}` (Punkt2D = Point2{Float64}) — konwersja przez Point2f(...) per element.
- `stan.trasa::Vector{Int}` to permutacja 1:n.
- `stan.energia::Float64` — cache, READ-ONLY w wizualizacji.
- `stan.iteracja::Int` — licznik mutowany przez symuluj_krok!.
- `stan.temperatura::Float64` — temperature SA.
- `alg.alfa::Float64` — geometric cooling factor (gdy alg::SimAnnealing).

WAZNE: alg ma typ abstract `Algorytm`. Pole `.alfa` istnieje tylko dla `SimAnnealing`. Helper `_zbuduj_overlay_string` musi obslugiwac to:
```julia
alfa_val = alg isa SimAnnealing ? alg.alfa : NaN
```
lub uzyc `hasproperty(alg, :alfa) ? alg.alfa : NaN`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dodac internal helpery (_trasa_do_punkty, _zbuduj_overlay_string, _setup_figure, _init_observables) i czesciowe body wizualizuj()</name>
  <files>src/wizualizacja.jl</files>
  <read_first>
    - src/wizualizacja.jl (po plan 03-01 — header + imports + sygnatura wizualizuj z placeholder body do zastapienia)
    - src/typy.jl (pole struktury StanSymulacji, Punkt2D = Point2{Float64})
    - src/baselines.jl (sygnatura trasa_nn(D; start=1))
    - src/algorytmy/simulowane_wyzarzanie.jl (struct SimAnnealing — pole `.alfa::Float64`)
    - .planning/phases/03-visualization-export/03-RESEARCH.md sekcja "Pattern 1: Figure Setup with Dual Panel" (linie ~190-217)
    - .planning/phases/03-visualization-export/03-RESEARCH.md sekcja "Pattern 2: Observable Architecture (Type-Stable)" (linie ~221-247)
    - .planning/phases/03-visualization-export/03-RESEARCH.md sekcja "Pattern 3: Observable Update — Vector + Cycle Closure" (linie ~252-271)
    - .planning/phases/03-visualization-export/03-RESEARCH.md sekcja "Pattern 7: Theme Scoping" (linie ~388-400)
    - .planning/phases/03-visualization-export/03-RESEARCH.md sekcja "Q1 + A3 (Observable Architecture decision)" (linie ~493-502, ~817 A3)
    - .planning/phases/03-visualization-export/03-RESEARCH.md sekcja "Q5: Polish Diacritics" (linie ~552-557)
    - .planning/phases/03-visualization-export/03-RESEARCH.md sekcja "Q7: NN Baseline Overlay" (linie ~566-572)
    - .planning/phases/03-visualization-export/03-RESEARCH.md sekcja "Q8: Point2f Conversion" (linie ~575-580)
    - .planning/phases/03-visualization-export/03-RESEARCH.md sekcja "Q12: Type-Stability i Observable Closures" (linie ~624-645)
    - .planning/phases/03-visualization-export/03-CONTEXT.md sekcja "decisions D-01..D-04" (linie ~22-33)
    - .planning/phases/03-visualization-export/03-PATTERNS.md sekcja "Internal helper naming convention" (linie 108-115)
  </read_first>
  <action>
    Edytowac `src/wizualizacja.jl` — dodac 4 internal helpery (prefiks `_` per PATTERNS.md "dwustopniowe API public/internal") PRZED definicja `wizualizuj()`. Nastepnie zastapic placeholder body w `wizualizuj()` czesciowym body ktore wywoluje helpery, ale pozostawia branching live/eksport jako placeholder do planu 03-03.

    **Struktura pliku po zmianach (kolejnosc):**
    1. Header comment (BEZ ZMIAN — z plan 03-01)
    2. Top-level `using` (BEZ ZMIAN — z plan 03-01)
    3. NEW: helper `_trasa_do_punkty(stan)::Vector{Point2f}`
    4. NEW: helper `_zbuduj_overlay_string(stan, alg, fps_est, eta_sec, accept_rate)::String`
    5. NEW: helper `_setup_figure(stan, nn_trasa)::Tuple{Figure, Axis, Axis}`
    6. NEW: helper `_init_observables(stan, alg, ax_trasa, ax_energia)::NamedTuple` — zwraca (obs_trasa, obs_historia, obs_overlay)
    7. Polish docstring `wizualizuj` (BEZ ZMIAN — z plan 03-01)
    8. CZESCIOWO ZASTAPIONE body `wizualizuj()` — wywoluje helpery, branching pozostaje placeholder

    ---

    **Helper 1: `_trasa_do_punkty`**

    Polish komentarz nad funkcja: "Konwersja stan.trasa (Vector{Int}) -> Vector{Point2f} z domknieciem cyklu Hamiltona (n+1 punktow). Alokuje raz per klatka — akceptowalne (~8KB dla N=1000). RESEARCH Pattern 3 + Q8."

    ```julia
    function _trasa_do_punkty(stan::StanSymulacji)::Vector{Point2f}
        n = length(stan.trasa)
        pts = Vector{Point2f}(undef, n + 1)   # +1 dla zamkniecia cyklu (VIZ-02)
        @inbounds for k in 1:n
            pts[k] = Point2f(stan.punkty[stan.trasa[k]])
        end
        pts[n + 1] = pts[1]  # zamkniecie cyklu
        return pts
    end
    ```

    `@inbounds` OK — `n = length(stan.trasa)`, dostep `stan.punkty[stan.trasa[k]]` jest w granicach gdy `stan.trasa` to permutacja 1:n (invariant Phase 2 ALG-08). Pre-alokacja przez `undef` jest szybsza niz broadcast.

    ---

    **Helper 2: `_zbuduj_overlay_string`**

    Polish komentarz: "Buduje multiline string overlay'u (D-04 — 7 pol). Per RESEARCH Q1 + A3 — uzywamy JEDNEGO Observable{String} z `\\n` separatorem (Opcja B), zamiast 7 osobnych Observables — minimalizuje liczbe Makie notify per klatka do 1. fps_est/eta_sec/accept_rate moga byc NaN przed zebraniem danych."

    ```julia
    function _zbuduj_overlay_string(stan::StanSymulacji, alg::Algorytm,
                                     fps_est::Float64, eta_sec::Float64,
                                     accept_rate::Float64)::String
        # alg.alfa istnieje tylko dla SimAnnealing — degraduj do NaN dla innych <:Algorytm
        alfa_val = hasproperty(alg, :alfa) ? getproperty(alg, :alfa) : NaN

        # Format: latwo czytelne pola, polskie diakrytyki (Phase 1 D-21 NFC OK).
        fps_str  = isnan(fps_est)    ? "—" : string(round(fps_est; digits=1))
        eta_str  = isnan(eta_sec)    ? "—" : string(round(Int, eta_sec), " s")
        acc_str  = isnan(accept_rate) ? "—" : string(round(accept_rate * 100; digits=1), "%")

        return """Iteracja: $(stan.iteracja)
        Energia: $(round(stan.energia; digits=4))
        Temperatura: $(round(stan.temperatura; digits=6))
        Alfa: $(alfa_val)
        FPS: $(fps_str)
        Pozostało: $(eta_str)
        Akceptacja worsening: $(acc_str)"""
    end
    ```

    Pola — DOSLOWNIE z CONTEXT D-04 (z polskimi diakrytykami w "Pozostało" i "Akceptacja worsening"). NaN sentinel dla nie-zebranych jeszcze danych (pierwsza klatka).

    ---

    **Helper 3: `_setup_figure`**

    Polish komentarz: "Buduje dual-panel Figure (D-01): lewy panel `ax_trasa` z aspect 1:1 (D-03) na 2D trase + NN baseline overlay (D-02); prawy panel `ax_energia` na krzywa energii. Renderuje NN baseline jako szara przerywana linia (raz, statyczne — bez Observable). Polskie tytuly/etykiety (VIZ-04). RESEARCH Pattern 1 + Q7."

    ```julia
    function _setup_figure(stan::StanSymulacji, nn_trasa::Vector{Int})
        n_punktow = length(stan.punkty)
        fig = Figure(size=(1400, 700))

        # Lewy panel — trasa SA z aspect 1:1 (domena [0,1]² unit square, D-03)
        ax_trasa = Axis(fig[1, 1];
            title  = "Trasa TSP — błona mydlana (N=$(n_punktow))",
            xlabel = "Współrzędna X",
            ylabel = "Współrzędna Y",
            aspect = AxisAspect(1))

        # Prawy panel — wykres energii vs iteracja
        ax_energia = Axis(fig[1, 2];
            title  = "Energia trasy vs iteracja",
            xlabel = "Iteracja",
            ylabel = "Energia (długość trasy)")

        # Lewy panel ~60% szerokosci, prawy ~40% (D-01: szerokość prawego ~40-50% lewego)
        colsize!(fig.layout, 1, Relative(0.6))

        # NN baseline overlay (D-02) — szara przerywana linia, raz, statyczne (brak Observable).
        # Pokazuje "od czego startujemy" (Roadmap SC #4 erratum z planu 02-14: ratio ≤ 0.95).
        nn_points = Vector{Point2f}(undef, length(nn_trasa) + 1)
        @inbounds for k in 1:length(nn_trasa)
            nn_points[k] = Point2f(stan.punkty[nn_trasa[k]])
        end
        nn_points[end] = nn_points[1]  # zamkniecie cyklu
        lines!(ax_trasa, nn_points; color=:gray, linestyle=:dash, alpha=0.3, linewidth=1)

        return fig, ax_trasa, ax_energia
    end
    ```

    ---

    **Helper 4: `_init_observables`**

    Polish komentarz: "Tworzy KONKRETNIE-typowane Observables (RESEARCH Q12 — unika Observable{Any} closure boxing pitfall) i podlacza je do plotow w juz utworzonych ax_trasa/ax_energia. Zwraca NamedTuple z 3 obserwabli."

    ```julia
    function _init_observables(stan::StanSymulacji, alg::Algorytm,
                                ax_trasa::Axis, ax_energia::Axis)
        # Konkretnie typowane Observables (Pitfall 5 + Q12)
        obs_trasa::Observable{Vector{Point2f}} =
            Observable(_trasa_do_punkty(stan))
        obs_historia::Observable{Vector{Point2f}} =
            Observable([Point2f(Float32(stan.iteracja), Float32(stan.energia))])
        obs_overlay::Observable{String} =
            Observable(_zbuduj_overlay_string(stan, alg, NaN, NaN, NaN))

        # Podlaczenie do lewego panelu — scatter (VIZ-03) + lines (VIZ-02)
        scatter!(ax_trasa, obs_trasa; markersize=5, color=:cyan)
        lines!(ax_trasa, obs_trasa; color=:white, linewidth=1.5)

        # Podlaczenie do prawego panelu — krzywa energii
        lines!(ax_energia, obs_historia; color=:orange, linewidth=2)

        # Overlay tekstowy w lewym gornym rogu lewego panelu (D-04, top-left).
        # Dane coordinates [0,1]² (ax_trasa ma aspect 1:1, autolimits dadza ~[0,1]).
        # Uzywamy text!() zamiast textlabel!() (A5 — textlabel! moze nie byc dostepne
        # we wszystkich GLMakie 0.13.x). Brak background — czytelnosc na dark theme.
        text!(ax_trasa, obs_overlay;
            position = Point2f(0.02, 0.98),
            align = (:left, :top),
            fontsize = 11,
            color = :white,
            space = :data)

        return (; obs_trasa, obs_historia, obs_overlay)
    end
    ```

    Pozycja `Point2f(0.02, 0.98)` w danych coordinates — punkty leza w [0,1]² wiec to top-left z 2% padding.

    ---

    **CZESCIOWE body wizualizuj() — zastapic placeholder error fragmentem ktory buduje figure, ale branch pozostawia placeholder dla plan 03-03:**

    Zastapic istniejace body (z plan 03-01: `error("Wizualizacja nie jest jeszcze zaimplementowana...")`) na:

    ```julia
    function wizualizuj(stan::StanSymulacji, params::Parametry, alg::Algorytm;
                        liczba_krokow::Int=params.liczba_krokow,
                        fps::Int=30,
                        kroki_na_klatke::Int=50,
                        eksport::Union{Nothing,String}=nothing)::Nothing
        # Walidacja argumentow internal (LANG-04 — angielskie asserty)
        @assert liczba_krokow > 0 "liczba_krokow must be positive"
        @assert fps > 0 "fps must be positive"
        @assert kroki_na_klatke > 0 "kroki_na_klatke must be positive"

        # NN baseline trasa — uzywana dla overlay (D-02). Plan 03-05 pokaze ratio
        # w "GOTOWE" overlay (energia/energia_nn). Liczone raz (deterministyczne D-15).
        nn_trasa = trasa_nn(stan.D; start=1)

        # Theme scoped z with_theme — auto-reset po wyjsciu nawet przy throw (RESEARCH Pattern 7 + Pitfall E).
        with_theme(theme_dark()) do
            fig, ax_trasa, ax_energia = _setup_figure(stan, nn_trasa)
            obs = _init_observables(stan, alg, ax_trasa, ax_energia)

            # Branching live vs eksport (D-09) — wypelniane w plan 03-03 (live) i 03-04 (eksport).
            if eksport === nothing
                error("Live renderloop nie jest jeszcze zaimplementowany — wypelnienie w planie 03-03.")
            else
                error("Eksport nie jest jeszcze zaimplementowany — wypelnienie w planie 03-04.")
            end
        end
        return nothing
    end
    ```

    ---

    **WAZNE — UWAGI implementacyjne:**

    - `with_theme(theme_dark()) do ... end` — scope-owana zmiana motywu, auto-reset (D-03 + Pattern 7). NIE uzywac `set_theme!(theme_dark())` (zanieczyszcza globalny stan po powrocie z `wizualizuj()` — Pitfall E).
    - Wszystkie polskie stringi MUSZA miec poprawne diakrytyki (ą ę ł ó ś ź ż ć ń) — Phase 1 D-21 NFC encoding gwarantuje ze `Unicode.normalize(content, :NFC) == content`. RESEARCH Q5 potwierdza ze TeX Gyre Heros Makie pokrywa Latin Extended.
    - `Observable{T}` jest dostepne via `using GLMakie` (re-eksport przez Makie — RESEARCH State of the Art "Observables.jl zintegrowane z Makie | Makie 0.15+").
    - `Point2f`, `AxisAspect`, `Relative`, `Figure`, `Axis`, `colsize!`, `scatter!`, `lines!`, `text!`, `with_theme`, `theme_dark` — wszystko w scope przez `using GLMakie`.
    - Konwencje: Polish docstring + komentarze (LANG-01); English asserts (LANG-04); ASCII identyfikatory (`fps_est`, `eta_sec`, `accept_rate`, `nn_trasa`, `nn_points` — bez diakrytykow w nazwach kodu); user-facing UI strings z polskimi diakrytykami (LANG-02).
  </action>
  <verify>
    <automated>julia --project=. -e 'using JuliaCity; using GeometryBasics: Point2f; pkty = generuj_punkty(20; seed=42); stan = StanSymulacji(pkty); inicjuj_nn!(stan); alg = SimAnnealing(stan); params = Parametry(liczba_krokow=100); pts = JuliaCity._trasa_do_punkty(stan); @assert length(pts) == 21 "expected 21 points (20 + cycle closure), got $(length(pts))"; @assert pts[end] == pts[1] "cycle not closed"; @assert eltype(pts) == Point2f "expected Point2f, got $(eltype(pts))"; ovr = JuliaCity._zbuduj_overlay_string(stan, alg, NaN, NaN, NaN); @assert occursin("Iteracja:", ovr) "missing Iteracja field"; @assert occursin("Pozostało:", ovr) "missing Pozostało field (polish diacritic)"; @assert occursin("Akceptacja worsening:", ovr) "missing Akceptacja field"; @assert count("\n", ovr) == 6 "expected 7 fields (6 newlines)"; println("OK helpers: pts len=", length(pts), ", overlay fields=", count("\n", ovr) + 1)'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^function _trasa_do_punkty(' src/wizualizacja.jl` zwraca 1
    - `grep -c '^function _zbuduj_overlay_string(' src/wizualizacja.jl` zwraca 1
    - `grep -c '^function _setup_figure(' src/wizualizacja.jl` zwraca 1
    - `grep -c '^function _init_observables(' src/wizualizacja.jl` zwraca 1
    - `grep -c 'AxisAspect(1)' src/wizualizacja.jl` zwraca 1 (D-03)
    - `grep -c 'theme_dark' src/wizualizacja.jl` zwraca 1 (with_theme(theme_dark()))
    - `grep -c 'with_theme(' src/wizualizacja.jl` zwraca 1 (NIE set_theme!)
    - `grep -c 'set_theme!' src/wizualizacja.jl` zwraca 0 (Pattern 7 + Pitfall E)
    - `grep -c 'linestyle=:dash' src/wizualizacja.jl` zwraca 1 (D-02 NN baseline)
    - `grep -c 'alpha=0\.3' src/wizualizacja.jl` zwraca 1 (D-02 NN baseline)
    - `grep -c 'colsize!' src/wizualizacja.jl` zwraca 1 (D-01 layout)
    - `grep -c 'Observable{Vector{Point2f}}' src/wizualizacja.jl` zwraca przynajmniej 2 (obs_trasa, obs_historia — typed annotations)
    - `grep -c 'Observable{String}' src/wizualizacja.jl` zwraca 1 (obs_overlay)
    - `grep -c 'scatter!(ax_trasa' src/wizualizacja.jl` zwraca 1 (VIZ-03)
    - `grep -c 'lines!(ax_trasa' src/wizualizacja.jl` zwraca przynajmniej 2 (NN baseline + obs_trasa, VIZ-02)
    - `grep -c 'lines!(ax_energia' src/wizualizacja.jl` zwraca 1 (energy chart)
    - Polskie diakrytyki w UI stringach: `grep -E 'błona|Współrzędna|długość|Pozostało|Akceptacja' src/wizualizacja.jl | wc -l` zwraca przynajmniej 5
    - `grep -c '@assert.*must be positive' src/wizualizacja.jl` zwraca przynajmniej 3 (3 walidacje argumentow)
    - Brak set_theme! lub display(fig) (jeszcze nie aktywowane — plan 03-03/03-04): `grep -c 'display(fig)' src/wizualizacja.jl` zwraca 0
    - Plik UTF-8 NFC: smoke test `julia -e 'using Unicode; @assert Unicode.normalize(read("src/wizualizacja.jl", String), :NFC) == read("src/wizualizacja.jl", String) "NFC violation"'`
    - Pkg.test() nadal exits 0 (testy sa headless — wizualizuj() nie wywolywane)
    - Helpery dzialaja standalone (verify automated)
  </acceptance_criteria>
  <done>
    src/wizualizacja.jl ma 4 internal helpery (_trasa_do_punkty, _zbuduj_overlay_string, _setup_figure, _init_observables) — wszystkie KONKRETNIE typowane, polish komentarze, English asserts. Body wizualizuj() wywoluje with_theme(theme_dark()) do ... end + _setup_figure + _init_observables, ale branch live/eksport zostawia placeholder error po polsku (do plan 03-03/03-04). Polskie diakrytyki w UI stringach poprawne (NFC encoding). NIE wywolano jeszcze display(fig) ani Makie.record().
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stan/params/alg → helpery | Helpery zakladaja invarianty Phase 2 (sort(stan.trasa)==1:n, energia >0, n>0); naruszenie → @inbounds out-of-bounds → segfault |
| Polish UI strings → GLMakie text renderer | Diakrytyki przechodza przez FreeType (Q5); A1 assumes pelne pokrycie Latin Extended-A — wizualne potwierdzenie potrzebne (manual smoke) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | _trasa_do_punkty z @inbounds przy invalid trasa | mitigate | Phase 2 ALG-08 invariant (sort(trasa)==1:n) gwarantuje bezpieczne indeksowanie |
| T-03-08 | Tampering | argumenty wizualizuj() (liczba_krokow, fps, kroki_na_klatke <= 0) | mitigate | 3 @assert na poczatku body — explicit walidacja |
| T-03-09 | Information Disclosure | overlay leaks stan.energia/iteracja/temperatura | accept | Wizualizacja JEST z definicji do pokazywania wewnetrznych metryk; brak sekretow |
| T-03-10 | Tampering | with_theme nie resetuje przy throw | accept | with_theme uzywa try/finally wewnetrznie (RESEARCH Pattern 7 — auto-reset) |

Severity: brak high. Visualization-only, lokalne dane, brak network surface.
</threat_model>

<verification>
**Plan-level integration check:**

```bash
# 1. Helpery dzialaja standalone (bez GLMakie display)
julia --project=. -e '
using JuliaCity
using GeometryBasics: Point2f

pkty = generuj_punkty(20; seed=42)
stan = StanSymulacji(pkty)
inicjuj_nn!(stan)
alg = SimAnnealing(stan)
params = Parametry(liczba_krokow=100)

# _trasa_do_punkty
pts = JuliaCity._trasa_do_punkty(stan)
@assert length(pts) == 21 "expected 21 points (20 + cycle closure)"
@assert pts[end] == pts[1] "cycle not closed (last != first)"
@assert eltype(pts) == Point2f "expected Vector{Point2f}"

# _zbuduj_overlay_string
ovr = JuliaCity._zbuduj_overlay_string(stan, alg, 30.5, 12.0, 0.42)
@assert occursin("Iteracja: 0", ovr)
@assert occursin("Pozostało: 12 s", ovr)
@assert occursin("Akceptacja worsening: 42.0%", ovr)
@assert occursin("FPS: 30.5", ovr)
println("OK helpers basic")

# Polish diacritics roundtrip (NFC)
using Unicode
@assert Unicode.normalize(ovr, :NFC) == ovr "overlay string NFC violation"
println("OK NFC encoding overlay")
'

# 2. Body wizualizuj() rzuca polski placeholder error w obu branches (live + eksport)
julia --project=. -e '
using JuliaCity

pkty = generuj_punkty(20; seed=42)
stan = StanSymulacji(pkty)
inicjuj_nn!(stan)
alg = SimAnnealing(stan)
params = Parametry(liczba_krokow=100)

# UWAGA: w tym tescie wizualizuj() faktycznie ladowuje GLMakie + tworzy Figure + wywoluje
# placeholder error. Na headless CI to bedzie GLFWError, ale tylko po with_theme/Figure
# init — co dokladnie chcemy walidowac w plan 03-05 (hard-fail wrapper). Tu testujemy
# czy SAM error message po polsku jest podany (gdy GUI dostepny) lub catched (gdy headless).

# Branch live (eksport=nothing)
caught_live = false
try
    wizualizuj(stan, params, alg; liczba_krokow=10)
catch e
    msg = sprint(showerror, e)
    if occursin("Live renderloop", msg) || occursin("OpenGL", msg) || occursin("GLFW", msg) || occursin("display", msg)
        caught_live = true
    else
        rethrow(e)
    end
end
@assert caught_live "expected polish placeholder OR GLMakie display error, got something else"
println("OK live branch placeholder/display error")
'

# 3. VIZ-06 sanity preserved
matches=$(grep -rl "^using GLMakie" src/ 2>/dev/null | wc -l)
test "$matches" = "1" || { echo "FAIL VIZ-06: $matches files use GLMakie"; exit 1; }
echo "OK VIZ-06 preserved"

# 4. Phase 1+2 regression
julia --project=. -e 'using Pkg; Pkg.test()'
```

Komendy 1, 3, 4 musza exit 0. Komenda 2 moze byc skip-owana na headless cloud (CI bez X), ale na dev machine Windows MUSI exit 0.
</verification>

<success_criteria>
1. **4 internal helpery zdefiniowane** — `_trasa_do_punkty`, `_zbuduj_overlay_string`, `_setup_figure`, `_init_observables` (prefiks `_` per PATTERNS).
2. **Dual-panel layout** — Figure z 2 Axis (D-01), `colsize!(fig.layout, 1, Relative(0.6))` (60/40 split).
3. **Aspect 1:1 + dark theme** — `ax_trasa.aspect = AxisAspect(1)` + `with_theme(theme_dark()) do ... end` (D-03, RESEARCH Pattern 7).
4. **NN baseline overlay** — `lines!(ax_trasa, nn_points; color=:gray, linestyle=:dash, alpha=0.3)` (D-02).
5. **Observable architecture KONKRETNIE typowana** — `Observable{Vector{Point2f}}` (x2: obs_trasa, obs_historia), `Observable{String}` (obs_overlay) — RESEARCH Q12 + Pitfall 5.
6. **Polskie tytuly/etykiety/overlay z diakrytykami** — `błona mydlana`, `Współrzędna X/Y`, `długość trasy`, `Pozostało`, `Akceptacja worsening`. NFC encoding preserved (Phase 1 D-21).
7. **scatter! + lines! podlaczone do obs_trasa** (VIZ-02 + VIZ-03), `lines!` podlaczony do obs_historia (energy chart).
8. **Body wizualizuj() walidacja argumentow + with_theme + branching placeholder** — branch live i eksport rzucaja polski placeholder error do wypelnienia w plan 03-03/03-04.
9. **Phase 1+2 testy zielone, VIZ-06 invariant zachowany.**
</success_criteria>

<output>
After completion, create `.planning/phases/03-visualization-export/03-02-SUMMARY.md` zawierajace:

- Liczbe linii w `src/wizualizacja.jl` (sanity check >= 180)
- Lista 4 internal helperow z sygnaturami
- Output `_zbuduj_overlay_string` dla N=20 fixture (literalna multiline string — referencja dla plan 03-03)
- Czy NFC encoding zachowany (boolean)
- Liczba `Observable{...}` z konkretnymi typami (powinno byc 3)
- Sanity check: `_trasa_do_punkty` zwraca n+1 punktow z domknietym cyklem (boolean)
- Czy Pkg.test() exit 0 (regression check)
</output>

## Source coverage audit

| Source ID | Type | Status | Note |
|-----------|------|--------|------|
| VIZ-02 | REQ | COVERED | obs_trasa::Observable{Vector{Point2f}}, lines!(ax_trasa, obs_trasa) z domknietym cyklem (n+1) w `_trasa_do_punkty` |
| VIZ-03 | REQ | COVERED | scatter!(ax_trasa, obs_trasa; markersize=5) — punkty czytelne dla N=1000 |
| VIZ-04 | REQ | COVERED | Polish title/xlabel/ylabel w obu Axis; overlay w `_zbuduj_overlay_string` z 7 polami po polsku |
| VIZ-07 | REQ | COVERED | Polskie diakrytyki (błona, Współrzędna, długość, Pozostało, Akceptacja) — NFC encoding zachowany; RESEARCH Q5 potwierdza font |
| LANG-02 | REQ | COVERED | UI strings (titles/labels/overlay) po polsku |
| RESEARCH Pattern 1 (Figure setup) | RESEARCH | COVERED | `_setup_figure` |
| RESEARCH Pattern 2 (Observable architecture) | RESEARCH | COVERED | `_init_observables` z konkretnymi typami |
| RESEARCH Pattern 3 (Vector + cycle closure) | RESEARCH | COVERED | `_trasa_do_punkty` z n+1 |
| RESEARCH Pattern 7 (with_theme scoping) | RESEARCH | COVERED | `with_theme(theme_dark()) do ... end` |
| RESEARCH Q1 + A3 (one Observable for overlay) | RESEARCH | COVERED | Single Observable{String} z `\n` separatorem (Opcja B) — udokumentowane w komentarzu |
| RESEARCH Q5 (Polish diacritics) | RESEARCH | COVERED | Diakrytyki w UI stringach + NFC test w verify |
| RESEARCH Q7 (linestyle=:dash) | RESEARCH | COVERED | NN baseline `linestyle=:dash` |
| RESEARCH Q8 (Point2f conversion) | RESEARCH | COVERED | `Point2f(stan.punkty[...])` per element |
| RESEARCH Q12 (type stability) | RESEARCH | COVERED | KONKRETNE Observable{Vector{Point2f}}, Observable{String} |
| CONTEXT D-01 (dual-panel) | CONTEXT | COVERED | Figure[1,1]+Figure[1,2] |
| CONTEXT D-02 (NN baseline) | CONTEXT | COVERED | `lines!` szara dash alpha 0.3 |
| CONTEXT D-03 (dark theme + aspect 1:1) | CONTEXT | COVERED | with_theme + AxisAspect(1) |
| CONTEXT D-04 (7-pol overlay) | CONTEXT | COVERED | `_zbuduj_overlay_string` z 7 polami w polskim |
| CONTEXT D-09 (single API entry) | CONTEXT | partial | Branching wstepnie obecny; pelne live/eksport w plan 03-03/03-04 |
| CONTEXT "Established Patterns" (`_` prefix internal) | CONTEXT | COVERED | 4 helpery z prefiksem `_` |
