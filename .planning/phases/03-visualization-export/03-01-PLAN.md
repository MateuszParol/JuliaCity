---
phase: 03-visualization-export
plan: 01
type: execute
wave: 1
depends_on: [03-00]
files_modified:
  - src/wizualizacja.jl
  - src/JuliaCity.jl
autonomous: true
requirements:
  - VIZ-01
  - VIZ-04
  - VIZ-06
tags:
  - julia
  - visualization
  - skeleton
  - module-integration

must_haves:
  truths:
    - "src/wizualizacja.jl istnieje i jest JEDYNYM plikiem w src/ z `using GLMakie`"
    - "src/wizualizacja.jl deklaruje funkcje publiczna `wizualizuj` z pelna sygnatura per CONTEXT D-Discretion"
    - "src/JuliaCity.jl include-uje wizualizacja.jl po algorytmy/simulowane_wyzarzanie.jl (topologiczna kolejnosc)"
    - "src/JuliaCity.jl eksportuje `wizualizuj` w liscie `export ...`"
    - "`using JuliaCity; wizualizuj isa Function` jest true (funkcja istnieje, ale wywolanie placeholder body rzuca explicit error)"
    - "Polish docstring + komentarze, ASCII identifiers (kroki_na_klatke, liczba_krokow, eksport)"
  artifacts:
    - path: "src/wizualizacja.jl"
      provides: "Skeleton modulu wizualizacji + sygnatura wizualizuj()"
      contains: "function wizualizuj("
      min_lines: 60
    - path: "src/JuliaCity.jl"
      provides: "include + export dla wizualizacja.jl"
      contains: "include(\"wizualizacja.jl\")"
  key_links:
    - from: "src/JuliaCity.jl"
      to: "src/wizualizacja.jl"
      via: "include() w topologicznej kolejnosci po algorytmy/"
      pattern: "include\\(\"wizualizacja\\.jl\"\\)"
    - from: "src/wizualizacja.jl"
      to: "GLMakie/Makie/Observables/ProgressMeter"
      via: "top-level using statements"
      pattern: "using GLMakie"
---

<objective>
Utworzyc szkielet `src/wizualizacja.jl` — pierwszy i JEDYNY plik w `src/` ktory importuje `GLMakie` (per VIZ-06 LOCKED). Zadeklarowac funkcje publiczna `wizualizuj()` z PELNA sygnatura zdefiniowana w CONTEXT D-Discretion, polish docstring per PATTERNS.md (analog `src/baselines.jl`), oraz placeholder body (`error("not yet implemented — wizualizuj() body jest budowane w planach 03-02..03-05")`) — to pozwala na wczesne pokrycie VIZ-06 grep guard (plan 03-06) ZANIM dojdzie cala logika.

Zintegrowac z `src/JuliaCity.jl`: dodac `include("wizualizacja.jl")` po istniejacym `include("algorytmy/simulowane_wyzarzanie.jl")` oraz dodac `wizualizuj` do listy `export`.

Purpose: Pokrywa VIZ-06 (jedyny `using GLMakie` w `src/`) wczesnie — pozwala plan 03-06 dodac grep guard test bez czekania az logika bedzie gotowa. Pokrywa VIZ-01 (deklaracja sygnatury) i VIZ-04/LANG-01/LANG-02 (polish docstring). Dostarcza stabilne API surface ktore plan 03-02..03-05 wypelniaja inkrementalnie bez zmieniania sygnatury/eksportu.

Output:
- `src/wizualizacja.jl` (NEW) — header comment, `using GLMakie/Makie/Observables/ProgressMeter/GeometryBasics`, polish docstring, sygnatura `function wizualizuj(...)::Nothing` z placeholder body `error("...")`.
- `src/JuliaCity.jl` (MODIFIED) — dodac jeden `include` + jeden symbol w `export`.
- Dowod: `using JuliaCity; @assert wizualizuj isa Function` exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-visualization-export/03-CONTEXT.md
@.planning/phases/03-visualization-export/03-RESEARCH.md
@.planning/phases/03-visualization-export/03-PATTERNS.md
@.planning/phases/03-visualization-export/03-00-SUMMARY.md
@CLAUDE.md
@src/JuliaCity.jl
@src/typy.jl
@src/baselines.jl
@src/algorytmy/simulowane_wyzarzanie.jl

<interfaces>
<!-- Kontrakty z istniejacego kodu (Phase 1+2). Executor NIE musi szukac. -->

From src/typy.jl:
```julia
const Punkt2D = Point2{Float64}
abstract type Algorytm end
mutable struct StanSymulacji{R<:AbstractRNG}
    const punkty::Vector{Punkt2D}
    const D::Matrix{Float64}
    const rng::R
    trasa::Vector{Int}
    energia::Float64
    temperatura::Float64
    iteracja::Int
end
Base.@kwdef struct Parametry
    liczba_krokow::Int = 50_000
end
```

From src/baselines.jl + src/algorytmy/simulowane_wyzarzanie.jl (Phase 2):
```julia
function trasa_nn(D::Matrix{Float64}; start::Int=1)::Vector{Int}
function inicjuj_nn!(stan::StanSymulacji)
function symuluj_krok!(stan::StanSymulacji, params::Parametry, alg::SimAnnealing)
function uruchom_sa!(stan::StanSymulacji, params::Parametry, alg::SimAnnealing; ...)
struct SimAnnealing{R<:AbstractRNG} <: Algorytm
    T_zero::Float64
    alfa::Float64
    cierpliwosc::Int
    rng::R
end
```

From src/JuliaCity.jl (lines 41-45) — current export list:
```julia
export Punkt2D, StanSymulacji, Algorytm, generuj_punkty,
       Parametry, SimAnnealing,
       oblicz_macierz_dystans!, oblicz_energie, delta_energii, kalibruj_T0,
       trasa_nn, inicjuj_nn!,
       symuluj_krok!, uruchom_sa!
```

From CONTEXT.md "Claude's Discretion" (linia 72) — KANONICZNA SYGNATURA do skopiowania DOSLOWNIE:
```julia
function wizualizuj(stan::StanSymulacji, params::Parametry, alg::Algorytm;
                    liczba_krokow::Int=params.liczba_krokow,
                    fps::Int=30,
                    kroki_na_klatke::Int=50,
                    eksport::Union{Nothing,String}=nothing)::Nothing
```

Wymagane top-level imports w src/wizualizacja.jl (RESEARCH Pattern 9):
```julia
using GLMakie                       # VIZ-06: jedyne miejsce w src/ z tym importem
using ProgressMeter                 # EKS-03: pasek postepu eksportu
using GeometryBasics: Point2f       # Float32 dla Makie GPU pipeline (Q8 conversion)
```
NIE dodawac `using Observables` osobno — `Observable` jest re-eksportowane przez `using GLMakie` (Makie 0.15+ integration per RESEARCH State of the Art).
NIE dodawac `using Makie` — `using GLMakie` daje `Makie.*` w scope.

From PATTERNS.md "src/JuliaCity.jl (module root, extend existing)":
- Add line po istniejacym include: `include("wizualizacja.jl")` (linia ~38 w obecnym pliku)
- Extend export: dodac `wizualizuj` do koncowej linii export (linia ~45)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Utworzyc src/wizualizacja.jl ze szkieletem (header, using, sygnatura wizualizuj z placeholder body)</name>
  <files>src/wizualizacja.jl</files>
  <read_first>
    - src/baselines.jl (analog file-header pattern + polish docstring + REQ coverage comment + return nothing)
    - src/algorytmy/simulowane_wyzarzanie.jl (analog: nested file convention dla zlozonych modulow)
    - src/typy.jl (potwierdzic shape StanSymulacji, Parametry, Algorytm)
    - .planning/phases/03-visualization-export/03-RESEARCH.md sekcja "Pattern 9: Module Integration (VIZ-06)" (linie ~427-440)
    - .planning/phases/03-visualization-export/03-RESEARCH.md sekcja "Code Examples → Pelna sygnatura funkcji publicznej" (linie ~707-736)
    - .planning/phases/03-visualization-export/03-CONTEXT.md sekcja "Claude's Discretion → Sygnatura kompletna" (linia 72)
    - .planning/phases/03-visualization-export/03-CONTEXT.md sekcja "Code Insights → Established Patterns" (linie 124-129)
    - .planning/phases/03-visualization-export/03-PATTERNS.md sekcja "src/wizualizacja.jl" (linie 23-178)
  </read_first>
  <action>
    Utworzyc NOWY plik `src/wizualizacja.jl` (UTF-8, LF, no BOM, NFC normalized; ASCII filename per BOOT-04 / Phase 1 D-23).

    **Struktura pliku (w tej kolejnosci):**

    **1. Header comment block** (mirror `src/baselines.jl` linie 1-11 + dodanie VIZ-06 + carry-forward dependencies):

    ```julia
    # Wizualizacja TSP — okno GLMakie + eksport MP4/GIF.
    # Pokrywa REQ VIZ-01..07, EKS-01..04 (11 wymagan).
    #
    # WAZNE (VIZ-06 LOCKED): src/wizualizacja.jl jest JEDYNYM plikiem w src/
    # importujacym `using GLMakie`. Core (`punkty.jl`, `energia.jl`, `baselines.jl`,
    # `algorytmy/`, `typy.jl`) pozostaje pure-headless — `runtests.jl` nie wymaga
    # OpenGL. Dowod: `grep -rl "using GLMakie" src/` zwraca tylko ten plik.
    #
    # Architektura (D-09):
    #   - eksport === nothing  → live renderloop (display(fig) + while isopen(fig))
    #   - eksport isa String   → blocking Makie.record() + ProgressMeter
    #
    # Decyzje uzytkownika (15 LOCKED w 03-CONTEXT.md):
    #   D-01 dual-panel; D-02 NN baseline szara linia; D-03 dark theme + aspect 1:1;
    #   D-04 7-pol overlay po polsku; D-05 KROKI_NA_KLATKE=50; D-06 GOTOWE freeze;
    #   D-09 single API entry; D-10 file-exists hard error; D-11 unified fps;
    #   D-12 export = liczba_krokow / KROKI_NA_KLATKE klatek; D-13 GLMakie hard-fail polski.
    #
    # Asercje wewnetrzne po angielsku per LANG-04. Komentarze polskie per LANG-01.
    # Komunikaty user-facing (error msg, @info, overlay strings) po polsku per LANG-02.
    #
    # Zaleznosci (`StanSymulacji`, `Parametry`, `Algorytm`, `SimAnnealing`,
    # `symuluj_krok!`, `trasa_nn`) sa w scope'ie modulu — typy.jl + baselines.jl +
    # algorytmy/simulowane_wyzarzanie.jl sa include-owane wczesniej w JuliaCity.jl.
    ```

    **2. Imports block** (top-level, PRZED docstring funkcji):

    ```julia
    using GLMakie                       # VIZ-06: jedyne miejsce w src/ z tym importem
    using ProgressMeter                 # EKS-03: pasek postepu eksportu
    using GeometryBasics: Point2f       # Float32 dla Makie GPU pipeline (RESEARCH Q8)
    # Observable jest re-eksportowane przez using GLMakie (Makie 0.15+ integration).
    # Makie.record(), set_theme!, with_theme, theme_dark, Figure, Axis, AxisAspect,
    # Relative, colsize!, scatter!, lines!, text!, textlabel! — wszystko w scope
    # przez `using GLMakie`.
    ```

    **3. Polish docstring + sygnatura funkcji publicznej** (skopiowac DOSLOWNIE z RESEARCH "Code Examples → Pelna sygnatura funkcji publicznej" linie 707-736):

    ```julia
    """
        wizualizuj(stan, params, alg; liczba_krokow, fps, kroki_na_klatke, eksport)

    Animuje proces wyzarzania TSP w oknie GLMakie lub eksportuje animacje do pliku.

    Otwiera okno GLMakie z dual-panel layoutem: lewy panel pokazuje trase SA
    (punkty + linia cyklu Hamiltona) z NN baseline jako szara przerywana linia
    (D-02); prawy panel pokazuje krzywa energia(iteracja) w czasie rzeczywistym.
    Overlay tekstowy w lewym gornym rogu pokazuje 7 pol: Iteracja, Energia,
    Temperatura, Alfa, FPS, Pozostalo, Akceptacja worsening (D-04). Wszystko
    po polsku z poprawnym renderowaniem diakrytykow (D-03 dark theme).

    Throttled updates: Observable aktualizowany raz na `kroki_na_klatke` SA
    krokow (default 50, per D-05) — okno pozostaje responsywne.

    # Argumenty pozycyjne
    - `stan::StanSymulacji` — zainicjowany stan SA (po `inicjuj_nn!` i ustawieniu `stan.temperatura`)
    - `params::Parametry` — parametry symulacji (m.in. `liczba_krokow`)
    - `alg::Algorytm` — algorytm (np. `SimAnnealing`)

    # Slowa kluczowe
    - `liczba_krokow::Int=params.liczba_krokow` — liczba krokow SA do wykonania
    - `fps::Int=30` — klatki na sekunde (live i eksport — unified per D-11)
    - `kroki_na_klatke::Int=50` — krokow SA miedzy aktualizacjami Observables (throttling, VIZ-05)
    - `eksport::Union{Nothing,String}=nothing` — sciezka do pliku MP4/GIF lub `nothing` dla live okna

    # Zachowanie (D-09 — single API entry point)
    - `eksport=nothing`: otwiera okno GLMakie, animuje w czasie rzeczywistym, czeka na zamkniecie
      (window pozostaje otwarty po SA stop z overlay'em "GOTOWE", D-06).
    - `eksport="sciezka.mp4"` lub `"sciezka.gif"`: zapisuje animacje (blokujace; postep w terminalu
      przez ProgressMeter, EKS-03). Format wykrywany z extensji (EKS-02). Twardy error gdy plik
      docelowy juz istnieje (D-10, EKS-04).

    Wymaga aktywnego kontekstu OpenGL. Headless cloud (CI, Docker bez X) NIE jest wspierany w v1
    (D-13 — rzuca polski blad diagnostyczny).
    """
    function wizualizuj(stan::StanSymulacji, params::Parametry, alg::Algorytm;
                        liczba_krokow::Int=params.liczba_krokow,
                        fps::Int=30,
                        kroki_na_klatke::Int=50,
                        eksport::Union{Nothing,String}=nothing)::Nothing
        # Body bedzie wypelniony w planach 03-02 (figure setup), 03-03 (live loop),
        # 03-04 (export branch), 03-05 (hard-fail wrapper + GOTOWE overlay).
        error("Wizualizacja nie jest jeszcze zaimplementowana — wypelnienie body w planach 03-02..03-05.")
        return nothing
    end
    ```

    **WAZNE — placeholder body:**
    - Body MUSI byc explicit `error("Wizualizacja nie jest jeszcze zaimplementowana — wypelnienie body w planach 03-02..03-05.")`. NIE pozostawiac pustej funkcji.
    - To pozwala plan 03-06 dodac VIZ-06 grep guard test bez ryzyka ze ktos wywola wizualizuj() z polowicznym body.
    - `return nothing` po `error()` to dead code, ale wymagane przez `::Nothing` annotation per Pitfall B z Phase 2 (`@inferred ::Nothing` wymaga literal `return nothing`). Mimo ze `error` rzuca, kompilator i tak inferuje powrot.

    **Konwencje:**
    - Polish prose docstring + Polish komentarze inline (LANG-01).
    - English assert/error messages tylko dla internal asserts (LANG-04). User-facing error w `wizualizuj()` body jest po polsku (zob. plan 03-05).
    - ASCII identyfikatory (`kroki_na_klatke`, `liczba_krokow`, `eksport` — bez diakrytykow w nazwach kwarg per BOOT-04 / D-23).
    - Plik UTF-8, LF, no BOM (Phase 1 D-21 encoding hygiene — `Unicode.normalize(content, :NFC) == content`).
    - Filename `wizualizacja.jl` (NIE `wizualizacja-glmakie.jl` — D-23 ASCII-only, krotka nazwa).
    - NIE deklarowac `module ...` — plik jest `include`-owany do `module JuliaCity` (jak `baselines.jl`).
    - NIE robic `export` w tym pliku — eksport idzie z `JuliaCity.jl` (Task 2).
  </action>
  <verify>
    <automated>julia --project=. -e 'using JuliaCity; @assert isdefined(JuliaCity, :wizualizuj) "wizualizuj not defined"; @assert wizualizuj isa Function "wizualizuj not a Function"; m = collect(methods(wizualizuj)); @assert length(m) == 1 "expected 1 method, got $(length(m))"; @assert hasmethod(wizualizuj, Tuple{StanSymulacji, Parametry, Algorytm}) "wizualizuj signature mismatch"; println("OK wizualizuj declared, methods=", length(m))'</automated>
  </verify>
  <acceptance_criteria>
    - Plik `src/wizualizacja.jl` istnieje
    - `grep -c '^using GLMakie$' src/wizualizacja.jl` zwraca 1
    - `grep -c '^using ProgressMeter$' src/wizualizacja.jl` zwraca 1
    - `grep -c 'using GeometryBasics: Point2f' src/wizualizacja.jl` zwraca 1
    - `grep -c '^function wizualizuj(' src/wizualizacja.jl` zwraca 1
    - `grep -c 'kroki_na_klatke::Int=50' src/wizualizacja.jl` zwraca 1
    - `grep -c 'eksport::Union{Nothing,String}=nothing' src/wizualizacja.jl` zwraca 1
    - `grep -c 'fps::Int=30' src/wizualizacja.jl` zwraca 1
    - `grep -c 'liczba_krokow::Int=params.liczba_krokow' src/wizualizacja.jl` zwraca 1
    - `grep -c '::Nothing' src/wizualizacja.jl` zwraca przynajmniej 1 (return type annotation)
    - `grep -c 'error(' src/wizualizacja.jl` zwraca przynajmniej 1 (placeholder)
    - Plik ma >= 60 linii (header + using + docstring + sygnatura): `awk 'END {print NR}' src/wizualizacja.jl` >= 60
    - Plik UTF-8 NFC bez BOM (po wireing-u w Task 2, runtests encoding hygiene zielony)
  </acceptance_criteria>
  <done>
    src/wizualizacja.jl istnieje z header comment, top-level using GLMakie/ProgressMeter/GeometryBasics, polish docstring + sygnatura `wizualizuj()` z full kwarg list per CONTEXT D-Discretion, placeholder body `error("not yet implemented...")`. Plik UTF-8 NFC bez BOM, polish komentarze, ASCII identifiers. Wszystko gotowe do wireing-u w Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire src/wizualizacja.jl do src/JuliaCity.jl (include + export)</name>
  <files>src/JuliaCity.jl</files>
  <read_first>
    - src/JuliaCity.jl (aktualny stan przed edycja — kazda linia do potwierdzenia)
    - .planning/phases/03-visualization-export/03-PATTERNS.md sekcja "src/JuliaCity.jl (module root, extend existing)" (linie 180-237)
    - .planning/phases/03-visualization-export/03-CONTEXT.md sekcja "Integration Points → src/JuliaCity.jl" (linie 132-136)
  </read_first>
  <action>
    Edytowac `src/JuliaCity.jl` w dwoch miejscach: dodac `include("wizualizacja.jl")` po istniejacym `include("algorytmy/simulowane_wyzarzanie.jl")` oraz rozszerzyc export o `wizualizuj`.

    **Krok 1: Dodac include po linii 38 (`include("algorytmy/simulowane_wyzarzanie.jl")`):**

    Po istniejacym bloku include (linie 26-38), dodac:
    ```julia

    # Wizualizacja GLMakie + eksport MP4/GIF (REQ VIZ-01..07, EKS-01..04) — Phase 3
    # WAZNE: `using GLMakie` zyje wewnatrz wizualizacja.jl (VIZ-06 LOCKED)
    include("wizualizacja.jl")
    ```

    **Krok 2: Rozszerzyc export (linie 41-45) o `wizualizuj`:**

    Aktualny export:
    ```julia
    export Punkt2D, StanSymulacji, Algorytm, generuj_punkty,
           Parametry, SimAnnealing,
           oblicz_macierz_dystans!, oblicz_energie, delta_energii, kalibruj_T0,
           trasa_nn, inicjuj_nn!,
           symuluj_krok!, uruchom_sa!
    ```

    Po zmianie:
    ```julia
    export Punkt2D, StanSymulacji, Algorytm, generuj_punkty,
           Parametry, SimAnnealing,
           oblicz_macierz_dystans!, oblicz_energie, delta_energii, kalibruj_T0,
           trasa_nn, inicjuj_nn!,
           symuluj_krok!, uruchom_sa!,
           wizualizuj
    ```

    Dodac `wizualizuj` jako nowa linia z indentem zgodnym z istniejacymi (`       `) lub na koncu istniejacej ostatniej linii — preferowane: nowa linia per RESEARCH State of the Art clean format.

    **Krok 3: Module-level docstring (linia 13)** — placeholder juz istnieje:
    ```julia
    - `wizualizuj(stan, params, alg; ...)` — animacja GLMakie (Phase 3)
    ```
    Pozostaje BEZ ZMIAN (juz tam jest, sprawdzic w czytaniu).

    **WAZNE — NIE robic:**
    - NIE dodawac `using GLMakie` w `JuliaCity.jl` (lamie VIZ-06).
    - NIE dodawac `using Observables` ani `using ProgressMeter` (zyja w wizualizacja.jl).
    - NIE zmieniac topologicznej kolejnosci pozostalych include.

    **Topologiczna kolejnosc po zmianie:**
    1. typy.jl
    2. punkty.jl
    3. energia.jl
    4. baselines.jl
    5. algorytmy/simulowane_wyzarzanie.jl
    6. wizualizacja.jl ← NEW

    Konwencje:
    - Polish komentarz nad blokiem `include("wizualizacja.jl")` zgodnie z istniejaca konwencja (mirror linii 25, 28, 31, 34, 37).
    - Plik UTF-8, LF, no BOM (juz jest, nie regresowac).
  </action>
  <verify>
    <automated>julia --project=. -e 'using JuliaCity; @assert isdefined(JuliaCity, :wizualizuj) "wizualizuj not exported"; @assert :wizualizuj in names(JuliaCity) "wizualizuj not in public API names list"; @assert wizualizuj isa Function "wizualizuj not callable"; pkty = generuj_punkty(10; seed=42); stan = StanSymulacji(pkty); inicjuj_nn!(stan); alg = SimAnnealing(stan); params = Parametry(liczba_krokow=100); try wizualizuj(stan, params, alg; liczba_krokow=10) catch e; @assert occursin("nie jest jeszcze zaimplementowana", sprint(showerror, e)) "expected polish placeholder error, got: $(sprint(showerror, e))"; println("OK wizualizuj() rzuca placeholder error po polsku"); end'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'include("wizualizacja.jl")' src/JuliaCity.jl` zwraca 1
    - `grep -c 'wizualizuj' src/JuliaCity.jl` zwraca przynajmniej 2 (1 export + 1 docstring placeholder)
    - W liscie eksportu: `awk '/^export/,/^$/' src/JuliaCity.jl | grep -c wizualizuj` zwraca 1
    - Topologiczna kolejnosc: `grep -n 'include' src/JuliaCity.jl` pokazuje wizualizacja.jl jako OSTATNI include
    - `julia --project=. -e 'using JuliaCity; println(:wizualizuj in names(JuliaCity))'` zwraca `true`
    - `julia --project=. -e 'using Pkg; Pkg.test()'` nadal exits 0 (Phase 1+2 testy headless — wizualizuj() nie jest wywolywane)
    - `:wizualizuj in names(JuliaCity)` zwraca true
  </acceptance_criteria>
  <done>
    src/JuliaCity.jl ma `include("wizualizacja.jl")` jako ostatni include w topologicznej kolejnosci, oraz `wizualizuj` w liscie export. `using JuliaCity` daje wizualizuj jako Function. Wywolanie wizualizuj(...) rzuca polski placeholder error. Phase 1+2 testy nadal zielone.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public API → wizualizuj() | wizualizuj() to public; argumenty (stan, params, alg) zaufane (z modulu); kwarg `eksport::String` to user-supplied path — walidacja w plan 03-04 |
| `using GLMakie` impact | Top-level `using GLMakie` ladowane na `using JuliaCity` — TTFP overhead 60-150s pierwszy raz, sekundy potem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | wizualizuj() body placeholder error | mitigate | Explicit `error("not yet implemented")` zamiast empty body — zapobiega cichym call site |
| T-03-05 | Denial of Service | TTFP GLMakie load przy `using JuliaCity` | accept | Udokumentowane w D-08 (TTFP @info message w plan 03-05); nieuniknione przy `using GLMakie` |
| T-03-06 | Tampering | wizualizacja.jl modyfikuje cos w core src/ | mitigate | Explicit komentarz w header: "Pure additive — Phase 1+2 PHASE COMPLETE preserved"; testy regression-free |

Severity: brak high. Skeleton-only, brak zewnetrznego ataku surface.
</threat_model>

<verification>
**Plan-level integration check:**

```bash
# 1. Skeleton smoke + placeholder error
julia --project=. -e '
using JuliaCity
@assert isdefined(JuliaCity, :wizualizuj)
@assert wizualizuj isa Function
@assert :wizualizuj in names(JuliaCity)
println("OK wizualizuj is exported and callable")

# Sygnatura: wywolanie z prawidlowymi argumentami → placeholder error
pkty = generuj_punkty(10; seed=42)
stan = StanSymulacji(pkty)
inicjuj_nn!(stan)
alg = SimAnnealing(stan)
params = Parametry(liczba_krokow=100)

caught = false
try
    wizualizuj(stan, params, alg; liczba_krokow=10)
catch e
    msg = sprint(showerror, e)
    @assert occursin("nie jest jeszcze zaimplementowana", msg) "expected polish placeholder, got: $msg"
    caught = true
end
@assert caught "wizualizuj() did not throw placeholder error"
println("OK placeholder body rzuca polski error")
'

# 2. VIZ-06: jedyny `using GLMakie` w src/ (sanity check, formal test w plan 03-06)
matches=$(grep -rl "^using GLMakie" src/ 2>/dev/null | wc -l)
test "$matches" = "1" || { echo "FAIL VIZ-06: $matches files use GLMakie (expected 1)"; exit 1; }
echo "OK VIZ-06: only src/wizualizacja.jl imports GLMakie"

# 3. Phase 1+2 regression
julia --project=. -e 'using Pkg; Pkg.test()'
```

Wszystkie 3 musza exit 0.
</verification>

<success_criteria>
1. **src/wizualizacja.jl istnieje** — header comment, using GLMakie/ProgressMeter/GeometryBasics, sygnatura wizualizuj() z full kwarg list, placeholder error body.
2. **VIZ-06 spelnione** — `using GLMakie` tylko w wizualizacja.jl (sanity check; formal test w plan 03-06).
3. **wizualizuj jest exported** z `module JuliaCity` — `:wizualizuj in names(JuliaCity)` true.
4. **Wywolanie wizualizuj(stan, params, alg; ...) rzuca polski placeholder error** "Wizualizacja nie jest jeszcze zaimplementowana...".
5. **Phase 1+2 testy zielone** — Pkg.test() exit 0, brak regresji.
6. **Polish docstring, ASCII identifiers, UTF-8 NFC bez BOM** — encoding hygiene preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/03-visualization-export/03-01-SUMMARY.md` zawierajace:

- Liczbe linii w `src/wizualizacja.jl` (sanity check >= 60)
- Lista top-level `using` statements w wizualizacja.jl (powinna byc 3)
- Czy `wizualizuj` jest w `names(JuliaCity)` (boolean true)
- Tresc placeholder error message (dla referencji w plan 03-05 — zostanie zastapiony pelnym body)
- Czy Pkg.test() exit 0 (regression check)
- Sanity check VIZ-06: liczba plikow w src/ z `using GLMakie` (powinno byc 1)
</output>

## Source coverage audit

| Source ID | Type | Status | Note |
|-----------|------|--------|------|
| VIZ-01 | REQ | partial | Sygnatura `wizualizuj()` zadeklarowana; pelny renderloop w plan 03-03 |
| VIZ-04 | REQ | partial | Polish docstring + komentarze + identifiers; UI strings (overlay) w plan 03-02..03-03 |
| VIZ-06 | REQ | COVERED | `using GLMakie` ZAINSTALOWANE w wizualizacja.jl jako jedynym pliku src/; formal grep guard test w plan 03-06 |
| LANG-01 | REQ | COVERED | Polish komentarze + docstring w wizualizacja.jl |
| LANG-02 | REQ | partial | Polish placeholder error msg; UI strings w plan 03-02..03-05 |
| RESEARCH Pattern 9 (Module Integration) | RESEARCH | COVERED | include + export added per pattern |
| RESEARCH "Pelna sygnatura" Code Example | RESEARCH | COVERED | Sygnatura skopiowana doslownie |
| PATTERNS "src/wizualizacja.jl" header pattern | PATTERNS | COVERED | Header comment block w stylu baselines.jl |
| PATTERNS "src/JuliaCity.jl extend" | PATTERNS | COVERED | Two-line addition |
| CONTEXT D-Discretion (sygnatura) | CONTEXT | COVERED | Skopiowana doslownie |
| CONTEXT D-09 (single API entry) | CONTEXT | partial | Sygnatura ma kwarg `eksport`; branching logic w plan 03-04 |
