---
phase: 03-visualization-export
plan: 06
type: execute
wave: 6
depends_on: [03-05]
files_modified:
  - test/runtests.jl
autonomous: true
requirements:
  - VIZ-06
tags:
  - julia
  - testing
  - viz-06-grep-guard
  - headless

must_haves:
  truths:
    - "test/runtests.jl ma nowy `@testset \"VIZ-06: GLMakie isolation\"` ktory grepem sprawdza ze tylko src/wizualizacja.jl zawiera `using GLMakie`"
    - "Test jest grep-level (czyta pliki jako String, NIE wymaga OpenGL — pure headless, zgodne z D-14)"
    - "Test uzywa pkgdir(JuliaCity) jako anchor (mirror wzorca z encoding hygiene testset linie 25-88)"
    - "Test rzuca @test fail gdy KTORYS plik w src/ inny niz wizualizacja.jl wprowadzi `using GLMakie`"
    - "Pkg.test() exit 0 (test przechodzi: jest dokladnie 1 plik z `using GLMakie`)"
  artifacts:
    - path: "test/runtests.jl"
      provides: "VIZ-06 grep guard testset"
      contains: "VIZ-06"
  key_links:
    - from: "test/runtests.jl::@testset VIZ-06"
      to: "src/wizualizacja.jl"
      via: "walkdir(src) + read(file) + count(occursin(...)) — single match expected"
      pattern: "using GLMakie"
---

<objective>
Dodac `@testset "VIZ-06: GLMakie isolation"` do `test/runtests.jl` ktory programatycznie weryfikuje invariant `src/wizualizacja.jl` jest jedynym plikiem w `src/` zawierajacym `using GLMakie`. Pokrywa REQ VIZ-06 jako test (do tej pory tylko grep checki w verify sections poszczegolnych planow).

Test jest grep-level (`read(file, String)` + `occursin(...)`) — NIE wymaga OpenGL, NIE instancjuje GLMakie. Bezpieczny dla headless CI (D-14, D-15). Uzywa identycznego patternu jak istniejacy `@testset "encoding hygiene"` (linie 25-88 w runtests.jl): `pkgdir(JuliaCity)` jako anchor + `walkdir(src/)` + `read(file)`.

To finalny plan Phase 3 — Wave 6 jest waveset czystego testing/CI guard, brak zmian w `src/`. Po tym planie wszystkie 11 REQ-IDow Phase 3 (VIZ-01..07, EKS-01..04) sa zaimplementowane lub testowo-pokryte.

Purpose: Pokrywa VIZ-06 (formal test isolation guard — dotychczas tylko verify w innych planach).

Output:
- test/runtests.jl rozszerzony o jeden nowy `@testset` (~25 linii) — grep guard.
- Pkg.test() exit 0 (test PASSes — invariant zachowany).
- Phase 3 COMPLETE: 11/11 REQ-IDow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-visualization-export/03-CONTEXT.md
@.planning/phases/03-visualization-export/03-RESEARCH.md
@.planning/phases/03-visualization-export/03-PATTERNS.md
@.planning/phases/03-visualization-export/03-05-SUMMARY.md
@CLAUDE.md
@test/runtests.jl
@src/wizualizacja.jl

<interfaces>
<!-- Wzorzec do skopiowania z istniejacego runtests.jl. -->

From test/runtests.jl linie 25-88 — istniejacy `@testset "encoding hygiene"` walka po `src/`:

```julia
@testset "encoding hygiene (BOOT-03, D-21)" begin
    repo_root = pkgdir(JuliaCity)
    katalogi = [joinpath(repo_root, "src"), joinpath(repo_root, "test")]
    rozszerzenia = (".jl", ".toml", ".md")

    pliki = String[]
    for kat in katalogi
        isdir(kat) || continue
        for (root, _, files) in walkdir(kat)
            for f in files
                if any(endswith(f, ext) for ext in rozszerzenia)
                    push!(pliki, joinpath(root, f))
                end
            end
        end
    end
    # ... testy per plik
end
```

KLUCZ: `pkgdir(JuliaCity)` jako anchor (Pkg.test sandbox env w innym katalogu — bez tego walkdir trafia w zly src/).

From RESEARCH "Validation Architecture → Automatable guards":
```bash
# VIZ-06: tylko wizualizacja.jl importuje GLMakie
grep -rl "using GLMakie" src/
# Oczekiwany output: tylko src/wizualizacja.jl
```

Test ma to samo zrobic w Julia code: walkdir(src/) → filter .jl → read each → count `using GLMakie` matches.

From PATTERNS.md sekcja "test/runtests.jl (optional VIZ-06 grep guard)" linie 298-331:
```julia
@testset "VIZ-06: GLMakie isolation" begin
    repo_root = pkgdir(JuliaCity)
    src_dir = joinpath(repo_root, "src")

    # Walka po wszystkich .jl w src/ + algorytmy/
    pliki_jl = String[]
    for (root, _, files) in walkdir(src_dir)
        for f in files
            endswith(f, ".jl") && push!(pliki_jl, joinpath(root, f))
        end
    end

    # Zlicz pliki z `using GLMakie` (top-level statement)
    pliki_z_glmakie = String[]
    for plik in pliki_jl
        tresc = read(plik, String)
        # Match line-anchored "using GLMakie" (top-level) — unika false-positive
        # w komentarzach lub docstring
        for linia in eachline(IOBuffer(tresc))
            if startswith(strip(linia), "using GLMakie")
                push!(pliki_z_glmakie, plik)
                break
            end
        end
    end

    # Hard assertion: dokladnie 1 plik = src/wizualizacja.jl
    @test length(pliki_z_glmakie) == 1
    @test endswith(pliki_z_glmakie[1], "wizualizacja.jl")
end
```

UWAGA: PATTERNS.md ma wstepny outline — plan 03-06 finalize z dodaniem szczegolow:
- Komentarze polskie + reference do REQ VIZ-06.
- pkgdir(JuliaCity) jako anchor (krytyczne — Pkg.test sandbox).
- Per-line iteration (nie occursin na calym pliku) — eliminuje matche w docstringach.
- 2 asercje: count == 1 ORAZ filename ends z wizualizacja.jl.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dodac @testset "VIZ-06: GLMakie isolation" do test/runtests.jl</name>
  <files>test/runtests.jl</files>
  <read_first>
    - test/runtests.jl (caly plik — szczegolnie linie 25-88 dla wzorca encoding hygiene + linie 211-227 dla wzorca Aqua testset, czyli gdzie wstrzyknac nowy @testset — preferowane PRZED Aqua dla logicznej grupacji "structural guards")
    - src/wizualizacja.jl (potwierdzic ze plik zawiera `using GLMakie` na top-level)
    - .planning/phases/03-visualization-export/03-PATTERNS.md sekcja "test/runtests.jl (optional VIZ-06 grep guard)" (linie 298-331)
    - .planning/phases/03-visualization-export/03-CONTEXT.md sekcja "decisions D-14" (linie 59-60) — runtests.jl NIE testuje wizualizacji (interaktywnej); grep guard JEST DOZWOLONY bo nie instancjuje GLMakie
    - .planning/phases/03-visualization-export/03-RESEARCH.md sekcja "Validation Architecture → Automatable guards" (linie ~870-878)
  </read_first>
  <action>
    Edytowac `test/runtests.jl` — wstrzyknac nowy `@testset "VIZ-06: GLMakie isolation"` PRZED `@testset "Aqua.jl quality (TEST-06)"` (linia ~211 w aktualnym pliku — sekcja "9. Aqua.jl quality gate"). Logiczne grupowanie: encoding hygiene → wizualizacja isolation → Aqua = trzy structural quality gates obok siebie.

    Lokalizacja: po `include("test_symulacja.jl")` (linia ~198 w aktualnym pliku) i PRZED komentarzem `# 9. Aqua.jl quality gate (TEST-06)` (linia ~200).

    **Nowy testset (kompletny kod do wstrzykniecia):**

    ```julia
    # ─────────────────────────────────────────────────────────────────────────
    # 9b. VIZ-06: GLMakie isolation guard — Phase 3.
    #     Sprawdza ze src/wizualizacja.jl jest JEDYNYM plikiem w src/ z `using GLMakie`.
    #     Test jest grep-level (czyta pliki jako String) — NIE wymaga OpenGL, bezpieczny
    #     dla headless CI (D-14, D-15). Pattern mirror encoding hygiene testset (linie 25-88):
    #     pkgdir(JuliaCity) jako anchor (Pkg.test sandbox env w innym katalogu).
    #     Per-line iteration eliminuje false-positives w docstringach lub komentarzach.
    # ─────────────────────────────────────────────────────────────────────────
    @testset "VIZ-06: GLMakie isolation" begin
        repo_root = pkgdir(JuliaCity)
        src_dir = joinpath(repo_root, "src")
        @test isdir(src_dir)   # sanity: anchor poprawnie znalezione

        # Walka po wszystkich .jl w src/ (+ podkatalogi typu algorytmy/)
        pliki_jl = String[]
        for (root, _, files) in walkdir(src_dir)
            for f in files
                endswith(f, ".jl") && push!(pliki_jl, joinpath(root, f))
            end
        end
        @test !isempty(pliki_jl)   # sanity: znaleziono pliki

        # Zlicz pliki z `using GLMakie` top-level (per-line match — unika false-positive
        # w komentarzach/docstringach typu "VIZ-06: only file with `using GLMakie`")
        pliki_z_glmakie = String[]
        for plik in pliki_jl
            tresc = read(plik, String)
            for linia in eachline(IOBuffer(tresc))
                if startswith(strip(linia), "using GLMakie")
                    push!(pliki_z_glmakie, plik)
                    break  # jeden match per plik wystarczy
                end
            end
        end

        # Hard assertion: dokladnie 1 plik z `using GLMakie` w src/ — i jest to wizualizacja.jl
        @test length(pliki_z_glmakie) == 1
        if length(pliki_z_glmakie) == 1
            @test endswith(pliki_z_glmakie[1], "wizualizacja.jl")
        else
            # Diagnostyka: pokaz wszystkie pliki ktore lamia VIZ-06 invariant
            for p in pliki_z_glmakie
                @info "VIZ-06 violation: $p contains `using GLMakie`"
            end
        end
    end
    ```

    ---

    **WAZNE — UWAGI implementacyjne:**

    - `startswith(strip(linia), "using GLMakie")` — anchor na poczatek linii (po stripie whitespace) — eliminuje matche w komentarzach typu `# uses using GLMakie...` lub w docstringach. Match: `using GLMakie` ORAZ `using GLMakie: SomeSymbol` (importy nazwane).
    - `break` po pierwszym matchu w pliku — efektywnosc, kazdy plik liczy sie raz.
    - Diagnostyka `@info` w else branch pomaga gdy ktos akcydentalnie doda `using GLMakie` do innego pliku — log pokazuje ktory plik narusza invariant.
    - Test jest GREP-LEVEL — czyta pliki jako String, NIE wykonuje `using GLMakie`. To zgodne z D-14 (runtests.jl nie testuje wizualizacji) — VIZ-06 isolation jest STRUCTURAL invariant, nie behavioral.
    - Bezpieczne dla headless CI: brak GL context, brak display, brak FFmpeg — tylko I/O na .jl files.

    Konwencje:
    - Polish komentarze blokowe + per-line.
    - English assert messages (`@test isdir(src_dir)` — diagnostyka Julia default).
    - ASCII identyfikatory (`pliki_jl`, `pliki_z_glmakie`, `tresc`, `linia`, `plik`, `repo_root`, `src_dir`).
    - UTF-8 NFC, no BOM (Phase 1 D-21 — runtests.jl juz jest UTF-8, nie regresowac).
    - Numeracja sekcji w runtests.jl: aktualnie 1..10, dodajemy "9b" (zostawiamy 10. Aqua + 11. JET bez renumerowania — minimalna zmiana).
  </action>
  <verify>
    <automated>julia --project=. -e 'using Pkg; Pkg.test()'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@testset "VIZ-06: GLMakie isolation"' test/runtests.jl` zwraca 1
    - `grep -c 'pkgdir(JuliaCity)' test/runtests.jl` zwraca przynajmniej 2 (encoding hygiene + nowy VIZ-06 testset)
    - `grep -c 'using GLMakie' test/runtests.jl` zwraca przynajmniej 1 (literal w teste — nie WIRELESS using; w komentarzu lub stringu)
    - `grep -c 'pliki_z_glmakie' test/runtests.jl` zwraca przynajmniej 3 (init, push, length check)
    - `grep -c 'wizualizacja.jl' test/runtests.jl` zwraca przynajmniej 1 (endswith assertion)
    - `grep -c 'walkdir(src_dir)' test/runtests.jl` zwraca 1
    - Test umieszczony PRZED Aqua testset: `awk '/@testset "VIZ-06/{viz_line=NR} /Aqua\.test_all/{aqua_line=NR} END {if(viz_line < aqua_line) print "OK"; else print "FAIL"}' test/runtests.jl` zwraca "OK"
    - Pkg.test() exit 0 — VIZ-06 test PASSes
    - Liczba testsetow w runtests.jl wzrosla o 1: `grep -c '^    @testset' test/runtests.jl` zwraca 9 (bylo 8 — encoding, generuj_punkty, no_global_rng, StanSymulacji, Wave 0, Aqua, JET + 1 NEW VIZ-06)
    - Plik UTF-8 NFC: `julia -e 'using Unicode; @assert Unicode.normalize(read("test/runtests.jl", String), :NFC) == read("test/runtests.jl", String)'`
    - W src/ tylko 1 plik z `using GLMakie` — sanity (test sam to weryfikuje, ale double-check)
  </acceptance_criteria>
  <done>
    test/runtests.jl rozszerzony o `@testset "VIZ-06: GLMakie isolation"` ktory grep-level (read+per-line check) weryfikuje ze tylko src/wizualizacja.jl ma `using GLMakie`. Test jest headless-safe (brak instancjacji GLMakie). Pkg.test() exit 0. Phase 3 COMPLETE: 11/11 REQ-IDow zaimplementowanych lub testowo-pokrytych.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test reads filesystem | `pkgdir(JuliaCity)` + `walkdir(src/)` + `read(file)` — pure I/O, no exec |
| `startswith(strip(linia), "using GLMakie")` | Substring match — false positive theoretically possible jezeli ktos napisze `using GLMakie` w kodzie nie-importujacym, ale anchor na poczatku linii (`startswith` po strip) eliminuje vast majority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-23 | Tampering | Test pomija plik (false negative) | mitigate | walkdir() rekurencyjnie po podkatalogach (algorytmy/); `endswith(f, ".jl")` filtr |
| T-03-24 | Tampering | False positive w docstring/komentarzu | mitigate | `startswith(strip(linia), "using GLMakie")` — anchor na poczatku linii po strip |
| T-03-25 | Tampering | pkgdir(JuliaCity) returns nothing w niestandardowym setup | accept | `@test isdir(src_dir)` early sanity check |

Severity: brak high. Test invariant — reads only.
</threat_model>

<verification>
**Plan-level integration check:**

```bash
# 1. Test obecny w runtests.jl
julia --project=. -e '
src = read("test/runtests.jl", String)
@assert occursin("@testset \"VIZ-06: GLMakie isolation\"", src) "VIZ-06 testset missing"
@assert occursin("pliki_z_glmakie", src) "pliki_z_glmakie variable missing"
@assert occursin("walkdir(src_dir)", src) "walkdir missing"
@assert occursin("wizualizacja.jl", src) "wizualizacja.jl filename check missing"
println("OK VIZ-06 testset structurally present")
'

# 2. Test PASSes — Pkg.test() exit 0
julia --project=. -e 'using Pkg; Pkg.test()'

# 3. Sanity: src/ ma dokladnie 1 plik z `using GLMakie`
matches=$(grep -rl "^using GLMakie" src/ 2>/dev/null | wc -l)
test "$matches" = "1" || { echo "FAIL: $matches files with using GLMakie (expected 1)"; exit 1; }
echo "OK src/ ma dokladnie 1 plik z using GLMakie (= src/wizualizacja.jl)"

# 4. Sanity: test FAILS gdy invariant zlamane (rzeczywista weryfikacja siaty)
# Symulacja: dodaj `using GLMakie` do innego pliku, oczekuj test failure, potem revert.
# (Manual — nie automatyzujemy bo wymaga modify+revert na zywym src/)
```

Komendy 1, 2, 3 musza exit 0.
</verification>

<success_criteria>
1. **`@testset "VIZ-06: GLMakie isolation"` dodany** do test/runtests.jl PRZED Aqua testset (logiczne grupowanie structural guards).
2. **Test jest headless-safe** — pkgdir + walkdir + read + per-line check; brak instancjacji GLMakie.
3. **Test PASSes** — Pkg.test() exit 0; dokladnie 1 plik z `using GLMakie` (= src/wizualizacja.jl).
4. **Diagnostyka @info gdy violation** — pomaga przyszlym programistom widziec ktory plik narusza invariant.
5. **Phase 1+2 testy zachowane** — brak regresji w pozostalych testsetach.
6. **Phase 3 COMPLETE: 11/11 REQ-IDow** (VIZ-01..07 + EKS-01..04) zaimplementowane lub testowo-pokryte.
</success_criteria>

<output>
After completion, create `.planning/phases/03-visualization-export/03-06-SUMMARY.md` zawierajace:

- Numer i pozycje nowego testsetu w runtests.jl (np. "9b. VIZ-06: GLMakie isolation, po include test_symulacja.jl, przed Aqua")
- Liczbe testsetow ogolem przed/po (powinno byc +1)
- Output Pkg.test() (last 20 linii — pokazuje ile @test PASS, czy są failures)
- Sanity: liczba plikow w src/ z `using GLMakie` (powinno byc 1)
- Coverage Phase 3: 11/11 REQ-IDow (VIZ-01..07, EKS-01..04) — tabela mapping ktora plan zaimplementowala kazdy REQ
- **Phase 3 COMPLETE marker** — wszystkie 5 ROADMAP success criteria osiagniete (sprawdzic linie 95-100 ROADMAP.md):
  1. wizualizuj otwiera GLMakie window — manual smoke (plan 03-03)
  2. Polskie tytuly/etykiety/overlay z diakrytykami — D-04 + D-21 NFC (plan 03-02)
  3. Throttling Observable updates — D-05 KROKI_NA_KLATKE=50 (plan 03-03)
  4. wizualizacja.jl jedyny `using GLMakie` — TEN plan VIZ-06 grep guard
  5. eksport=mp4/gif z ProgressMeter + safe overwrite — plan 03-04 (EKS-01..04)
</output>

## Source coverage audit

| Source ID | Type | Status | Note |
|-----------|------|--------|------|
| VIZ-06 | REQ | COVERED | Formal test (do tej pory tylko verify w innych planach) — `@testset "VIZ-06: GLMakie isolation"` |
| RESEARCH "Validation Architecture → Automatable guards" | RESEARCH | COVERED | Test implementuje rekomendowany grep guard w Julii zamiast bash |
| PATTERNS "test/runtests.jl VIZ-06 grep guard" | PATTERNS | COVERED | Skopiowany wzorzec z PATTERNS.md + dodane szczegoly |
| CONTEXT D-14 (runtests.jl bez wizualizacji) | CONTEXT | RESPECTED | Test grep-level, NIE instancjuje GLMakie |
| CONTEXT D-15 (CI bez OpenGL) | CONTEXT | RESPECTED | Test bezpieczny dla headless CI |
