---
phase: 03-visualization-export
plan: 05
type: execute
wave: 5
depends_on: [03-04]
files_modified:
  - src/wizualizacja.jl
autonomous: true
requirements:
  - VIZ-01
  - VIZ-04
  - VIZ-05
tags:
  - julia
  - visualization
  - error-handling
  - hard-fail
  - ttfp
  - finalization

must_haves:
  truths:
    - "Body wizualizuj() jest opakowane w try/catch ktory catchuje GLMakie/GLFW/OpenGL/X11/display errors i re-rzuca jako polski error per D-13"
    - "Polski error message D-13 jest doslowny (z RESEARCH Pattern 6) — `GLMakie wymaga aktywnego kontekstu OpenGL...`"
    - "PRZED with_theme jest @info `Ladowanie GLMakie (pierwsze uruchomienie moze trwac 60+ s — kompilacja JIT)...` (D-08 TTFP grace overlay)"
    - "PO `display(fig)` w live mode jest @info `Wizualizacja gotowa, rozpoczynam symulacje...` (D-08)"
    - "Po SA stop w _live_loop jest GOTOWE overlay text! z ratio: `GOTOWE - ratio: {energia/energia_nn}` (D-06)"
    - "Window pozostaje otwarty po GOTOWE; user zamyka recznie; brak auto-close, brak loop replay (D-06)"
    - "Branch eksport NIE dodaje GOTOWE overlay (eksport konczy sie zapisem pliku, nie czeka na user)"
  artifacts:
    - path: "src/wizualizacja.jl"
      provides: "Hard-fail try/catch wrapper + TTFP @info + GOTOWE overlay"
      contains: "GLMakie wymaga aktywnego kontekstu OpenGL"
      min_lines: 360
  key_links:
    - from: "wizualizuj() body"
      to: "try/catch wrapper"
      via: "try ... _wizualizuj_impl(...) ... catch e ... rethrow polish error"
      pattern: "try"
    - from: "_live_loop"
      to: "GOTOWE overlay"
      via: "energia_nn liczone w wizualizuj() body przed petla; po _live_loop dodaje text! z ratio"
      pattern: "GOTOWE"
    - from: "wizualizuj() opening"
      to: "TTFP @info"
      via: "@info Ladowanie GLMakie..."
      pattern: "Ładowanie GLMakie"
---

<objective>
Doszlifowac `wizualizuj()` o trzy ostatnie elementy CONTEXT decisions:

1. **D-13: Hard-fail GLMakie wrapper** — opakowac body w try/catch ktory catchuje GLMakie/GLFW/OpenGL/X11/display errors (po stringu w `sprint(showerror, e)`) i re-rzuca jako polski error diagnostyczny. To jest finalna polska "user-facing failure mode" gdy uzytkownik probuje uruchomic na headless cloud bez OpenGL.

2. **D-08: TTFP grace overlay** — dwa @info messages: PRZED `with_theme(theme_dark())` ("Ladowanie GLMakie..."), PO `display(fig)` w live mode ("Wizualizacja gotowa..."). Mitigacja Pitfall 14 (sysimage trap rejected — TTFP 60-150s pierwszy raz w czystym REPL).

3. **D-06: GOTOWE overlay po zakonczeniu SA (live mode tylko)** — w branchu live, po `_live_loop()` zwroceniu (SA hits liczba_krokow lub user zamknie okno), dodac `text!` overlay `"GOTOWE — ratio: {round(stan.energia/energia_nn, digits=4)}"` ktory pozostaje wyrenderowany. Window pozostaje otwarty (user zamyka recznie). Brak auto-close, brak loop replay.

`energia_nn` jest liczone raz przed `with_theme do ... end` (uzywajac `oblicz_energie(stan.D, nn_trasa, bufor)` na NN baseline z plan 03-02).

**WAZNE — refactoring strategy:** Aby try/catch byl czysty, wydziel `_wizualizuj_impl` jako internal helper przyjmujacy te same argumenty co wizualizuj(). `wizualizuj()` body redukuje sie do: TTFP @info + try { _wizualizuj_impl(...) } catch e { rethrow polish }. To dokladnie wzorzec z RESEARCH Pattern 6.

Purpose: Pokrywa CONTEXT D-06 (GOTOWE overlay), D-08 (TTFP grace), D-13 (hard-fail polski error). VIZ-04 (UI po polsku — komunikaty), LANG-02 (UI strings polish).

Output:
- src/wizualizacja.jl finalna wersja: hard-fail wrapper + TTFP messages + GOTOWE overlay. Wszystkie 15 CONTEXT decisions zaimplementowane.
- Manual smoke: live mode pokazuje GOTOWE overlay po stop; eksport nie. Headless (xvfb-stop) -> polski D-13 error.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-visualization-export/03-CONTEXT.md
@.planning/phases/03-visualization-export/03-RESEARCH.md
@.planning/phases/03-visualization-export/03-PATTERNS.md
@.planning/phases/03-visualization-export/03-04-SUMMARY.md
@CLAUDE.md
@src/wizualizacja.jl

<interfaces>
<!-- Skopiowane patterns z RESEARCH dla executora — NIE musi szukac. -->

From RESEARCH Pattern 6 (Hard-Fail GLMakie Catch, D-13) — linie ~358-381:

```julia
function wizualizuj(stan::StanSymulacji, params::Parametry, alg::Algorytm;
                    liczba_krokow::Int=params.liczba_krokow,
                    fps::Int=30,
                    kroki_na_klatke::Int=50,
                    eksport::Union{Nothing,String}=nothing)::Nothing
    @info "Ładowanie GLMakie (pierwsze uruchomienie może trwać 60+ s — kompilacja JIT)..."
    try
        _wizualizuj_impl(stan, params, alg; liczba_krokow, fps, kroki_na_klatke, eksport)
    catch e
        # GLMakie rzuca GLFW.GLFWError lub InitError gdy brak OpenGL/display
        msg = sprint(showerror, e)
        if contains(msg, "GLFW") || contains(msg, "OpenGL") || contains(msg, "display") ||
           contains(msg, "X11") || contains(msg, "GLMakie") || isa(e, InitError)
            error("GLMakie wymaga aktywnego kontekstu OpenGL. Brak displayu? " *
                  "Spróbuj `xvfb-run -a julia ...` na Linuksie albo uruchom lokalnie z GUI. " *
                  "Headless cloud (CI, Docker bez X) NIE jest wspierany w wersji v1.")
        else
            rethrow(e)
        end
    end
    return nothing
end
```

From CONTEXT D-13 (literalna polish wiadomosc — DOSLOWNIE skopiowac):
```
GLMakie wymaga aktywnego kontekstu OpenGL. Brak displayu? Spróbuj `xvfb-run -a julia ...` na Linuksie albo uruchom lokalnie z GUI. Headless cloud (CI, Docker bez X) NIE jest wspierany w wersji v1.
```

From CONTEXT D-08 (TTFP grace overlay — dwa @info messages):
- PRZED `display(fig)` (lub przed with_theme): `@info "Ładowanie GLMakie (pierwsze uruchomienie może trwać 60+ s — kompilacja JIT)..."`
- PO `display(fig)`: `@info "Wizualizacja gotowa, rozpoczynam symulację..."`

From CONTEXT D-06 (GOTOWE overlay po SA stop, live mode):
- Po `_live_loop()` zakonczeniu (stan.iteracja >= liczba_krokow lub user zamknie okno).
- Dodac `text!` overlay: `"GOTOWE — ratio: {round(stan.energia/energia_nn, digits=4)}"`
- Window pozostaje otwarty; user zamyka recznie. Brak auto-close, brak loop replay.

From CONTEXT D-06 + Specifics — uzywamy "GOTOWE" (krotkie polskie slowo, alternative "ZAKOŃCZONE" za dlugie).

From RESEARCH Pitfall E (with_theme wewnatrz try/catch):
- WAZNE: `try/catch` wrapper na ZEWNATRZ `with_theme do`. Hierarchia: outer try/catch → with_theme do → display(fig) → loop.
- Dlatego refactoring na _wizualizuj_impl: try/catch w `wizualizuj()`, with_theme w `_wizualizuj_impl()`.

From RESEARCH Pitfall 14 (TTFP Surprise):
- D-08 mitigation. Czas pierwszego ladowania GLMakie: 60-150s na zimnym REPL; po precompilation cache (Julia 1.9+): 5-15s.

From CONTEXT D-02 + Code Insights — `energia_nn` liczone raz przed live loop:
```julia
nn_trasa = trasa_nn(stan.D; start=1)   # juz w wizualizuj() body z plan 03-02
bufor = zeros(Float64, Threads.nthreads())
energia_nn = oblicz_energie(stan.D, nn_trasa, bufor)
```
Uzywa `oblicz_energie(D, trasa, bufor)::Float64` z Phase 2 (3-arg signature).

UWAGA: w plan 03-02 `nn_trasa = trasa_nn(stan.D; start=1)` jest juz w body wizualizuj(). Plan 03-05 dodaje `energia_nn` obok i przekazuje do _live_loop (lub do nowego helpera `_dodaj_gotowe_overlay`).

From RESEARCH Q10 (GLMakie Headless Exception Types):
- GLFW.GLFWError z VERSION_UNAVAILABLE
- InitError owijajacy GLFW.GLFWError
- Wiadomosci zawieraja: "X11: Failed to open display", "GLX: Failed to create context", "glfwInit failed"
- Robust catch: sprawdzaj `sprint(showerror, e)` zawierajace "GLFW"/"OpenGL"/"X11"/"display".

Aktualna struktura wizualizuj() (po plan 03-04):
```julia
function wizualizuj(stan, params, alg; liczba_krokow, fps, kroki_na_klatke, eksport)::Nothing
    @assert ... walidacje
    nn_trasa = trasa_nn(stan.D; start=1)
    with_theme(theme_dark()) do
        fig, ax_trasa, ax_energia = _setup_figure(stan, nn_trasa)
        obs = _init_observables(stan, alg, ax_trasa, ax_energia)
        if eksport === nothing
            display(fig)
            _live_loop(fig, stan, params, alg, obs.obs_trasa, obs.obs_historia, obs.obs_overlay; ...)
        else
            _export_loop(fig, stan, params, alg, ..., eksport; ...)
        end
    end
    return nothing
end
```

Po plan 03-05 struktura bedzie:
```julia
function wizualizuj(stan, params, alg; liczba_krokow, fps, kroki_na_klatke, eksport)::Nothing
    @info "Ładowanie GLMakie..." # D-08
    try
        _wizualizuj_impl(stan, params, alg; liczba_krokow, fps, kroki_na_klatke, eksport)
    catch e
        msg = sprint(showerror, e)
        if contains(msg, "GLFW") || contains(msg, "OpenGL") || contains(msg, "display") ||
           contains(msg, "X11") || contains(msg, "GLMakie") || isa(e, InitError)
            error("GLMakie wymaga aktywnego kontekstu OpenGL. ...")  # D-13 doslownie
        else
            rethrow(e)
        end
    end
    return nothing
end

function _wizualizuj_impl(stan, params, alg; liczba_krokow, fps, kroki_na_klatke, eksport)
    @assert ... walidacje  # przeniesione z wizualizuj()
    nn_trasa = trasa_nn(stan.D; start=1)
    bufor = zeros(Float64, Threads.nthreads())
    energia_nn = oblicz_energie(stan.D, nn_trasa, bufor)  # D-06: dla GOTOWE ratio
    with_theme(theme_dark()) do
        fig, ax_trasa, ax_energia = _setup_figure(stan, nn_trasa)
        obs = _init_observables(stan, alg, ax_trasa, ax_energia)
        if eksport === nothing
            display(fig)
            @info "Wizualizacja gotowa, rozpoczynam symulację..."  # D-08 second @info
            _live_loop(fig, stan, params, alg, obs.obs_trasa, obs.obs_historia, obs.obs_overlay; ...)
            # D-06: GOTOWE overlay po SA stop (live mode tylko)
            _dodaj_gotowe_overlay!(ax_trasa, stan, energia_nn)
            wait(fig.scene)  # czeka na zamkniecie okna przez user'a (nie zwraca dopoki user nie zamknie)
        else
            _export_loop(fig, stan, params, alg, ..., eksport; ...)
        end
    end
    return nothing
end

function _dodaj_gotowe_overlay!(ax_trasa, stan, energia_nn)
    ratio = round(stan.energia / energia_nn; digits=4)
    text!(ax_trasa, "GOTOWE — ratio: $ratio";
          position = Point2f(0.5, 0.5),
          align = (:center, :center),
          fontsize = 24,
          color = :yellow,
          space = :data)
    return nothing
end
```

UWAGA dot. `wait(fig.scene)`:
- Domyslnie display(fig) NIE blokuje gdy ostatnia operacja w skrypcie. W Julia REPL okno trzymaja Julia event loop.
- W skrypcie (gdy `_live_loop` zwroci, wizualizuj() konczy sie), okno moze sie zamknac PRZED tym jak user zobaczy GOTOWE. `wait(fig.scene)` (lub `wait(events(fig).window_open)`) blokuje az user zamknie okno — to dokladnie chcemy dla GOTOWE.
- ALTERNATYWA: w `_live_loop` po petli `while isopen(fig)`, gdy `stan.iteracja >= liczba_krokow` ale `isopen(fig)` jeszcze true, kontynuowac `sleep(1/fps)` w nowej petli `while isopen(fig)` (tylko event loop, brak SA). Pozwala GOTOWE pozostac wyrenderowany az user zamknie okno.

Wybrac PROSTSZE: dodac `_dodaj_gotowe_overlay!` PRZED ostatecznym czekaniem; uzyc `while isopen(fig); sleep(1/fps); end` jako passive event loop. Implementacja preferowana.

```julia
if eksport === nothing
    display(fig)
    @info "Wizualizacja gotowa, rozpoczynam symulację..."
    _live_loop(fig, stan, params, alg, obs.obs_trasa, obs.obs_historia, obs.obs_overlay;
               liczba_krokow=liczba_krokow, fps=fps, kroki_na_klatke=kroki_na_klatke)
    # D-06: GOTOWE overlay (tylko gdy SA dobiegl konca, NIE gdy user zamknal okno wczesniej)
    if !isopen(fig)
        # User zamknal okno przed SA stop — pomijamy GOTOWE overlay (brak okna do dodania)
    elseif stan.iteracja >= liczba_krokow
        # SA hit liczba_krokow — dodaj GOTOWE overlay i czekaj na zamkniecie okna
        _dodaj_gotowe_overlay!(ax_trasa, stan, energia_nn)
        # Passive event loop az user zamknie okno (event loop GLMakie wymaga sleep yielding)
        while isopen(fig)
            sleep(1/fps)
        end
    end
end
```

Plan implementuje powyzsza logike — tylko po SA-completion gdy okno jest jeszcze otwarte.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor wizualizuj() na try/catch wrapper + _wizualizuj_impl + dodaj _dodaj_gotowe_overlay! + TTFP @info</name>
  <files>src/wizualizacja.jl</files>
  <read_first>
    - src/wizualizacja.jl (po plan 03-04 — ma _live_loop, _export_loop, oba branche zaimplementowane)
    - .planning/phases/03-visualization-export/03-RESEARCH.md sekcja "Pattern 6: Hard-Fail GLMakie Catch (D-13)" (linie ~357-381)
    - .planning/phases/03-visualization-export/03-RESEARCH.md sekcja "Pitfall E: with_theme wewnatrz try/catch" (linie ~692-695)
    - .planning/phases/03-visualization-export/03-RESEARCH.md sekcja "Pitfall 14: TTFP Surprise" (linie ~485-487)
    - .planning/phases/03-visualization-export/03-RESEARCH.md sekcja "Q10: GLMakie Headless Exception Types" (linie ~592-600)
    - .planning/phases/03-visualization-export/03-CONTEXT.md sekcja "decisions D-06, D-08, D-13" (linie ~38-39, 40, 54-58)
    - src/energia.jl (sygnatura `oblicz_energie(D::Matrix, trasa::Vector{Int}, bufor::Vector{Float64})::Float64` — uzyte do liczenia energia_nn)
  </read_first>
  <action>
    Edytowac `src/wizualizacja.jl` w czterech krokach:

    ---

    **Krok 1: Wydziel `_wizualizuj_impl` z body wizualizuj()**

    Aktualna struktura body wizualizuj() (po plan 03-04):
    ```julia
    function wizualizuj(stan, params, alg; ...)::Nothing
        @assert ... walidacje
        nn_trasa = trasa_nn(stan.D; start=1)
        with_theme(theme_dark()) do
            fig, ax_trasa, ax_energia = _setup_figure(stan, nn_trasa)
            obs = _init_observables(stan, alg, ax_trasa, ax_energia)
            if eksport === nothing
                display(fig)
                _live_loop(...)
            else
                _export_loop(...)
            end
        end
        return nothing
    end
    ```

    Przeniesc CALA logike do nowej funkcji `_wizualizuj_impl` (po `_export_loop`, PRZED docstring `wizualizuj`):

    ```julia
    function _wizualizuj_impl(stan::StanSymulacji, params::Parametry, alg::Algorytm;
                              liczba_krokow::Int, fps::Int, kroki_na_klatke::Int,
                              eksport::Union{Nothing,String})
        # Walidacja argumentow internal (LANG-04 — angielskie asserty)
        @assert liczba_krokow > 0 "liczba_krokow must be positive"
        @assert fps > 0 "fps must be positive"
        @assert kroki_na_klatke > 0 "kroki_na_klatke must be positive"

        # NN baseline: trasa + energia. Energia uzywana w GOTOWE overlay (D-06 ratio).
        # Liczone raz, deterministyczne (D-15 start=1). Phase 2 oblicz_energie 3-arg signature.
        nn_trasa = trasa_nn(stan.D; start=1)
        bufor = zeros(Float64, Threads.nthreads())
        energia_nn = oblicz_energie(stan.D, nn_trasa, bufor)

        # Theme scoped (D-03 + Pattern 7 + Pitfall E — try/catch jest na zewnatrz)
        with_theme(theme_dark()) do
            fig, ax_trasa, ax_energia = _setup_figure(stan, nn_trasa)
            obs = _init_observables(stan, alg, ax_trasa, ax_energia)

            if eksport === nothing
                # Live mode: otworz okno + uruchom renderloop (blokujace).
                display(fig)
                # D-08: drugi @info PO display(fig)
                @info "Wizualizacja gotowa, rozpoczynam symulację..."
                _live_loop(fig, stan, params, alg, obs.obs_trasa, obs.obs_historia, obs.obs_overlay;
                           liczba_krokow=liczba_krokow, fps=fps, kroki_na_klatke=kroki_na_klatke)
                # D-06: GOTOWE overlay tylko gdy SA dobiegl konca i okno jest jeszcze otwarte.
                if isopen(fig) && stan.iteracja >= liczba_krokow
                    _dodaj_gotowe_overlay!(ax_trasa, stan, energia_nn)
                    # Passive event loop — czekamy az user zamknie okno (D-06: brak auto-close).
                    while isopen(fig)
                        sleep(1/fps)
                    end
                end
            else
                # Export mode: blocking Makie.record() do pliku eksport.
                _export_loop(fig, stan, params, alg, obs.obs_trasa, obs.obs_historia, obs.obs_overlay,
                             eksport;
                             liczba_krokow=liczba_krokow, fps=fps, kroki_na_klatke=kroki_na_klatke)
            end
        end
        return nothing
    end
    ```

    UWAGA — zmiany vs poprzednia wersja:
    - Walidacja argumentow PRZENIESIONA z wizualizuj() do _wizualizuj_impl.
    - `nn_trasa` nadal liczone raz; PLUS `bufor` + `energia_nn` (NEW dla D-06 GOTOWE ratio).
    - Po `_live_loop()` — IF SA hits liczba_krokow + okno jeszcze otwarte → GOTOWE overlay + passive event loop.
    - Drugi @info "Wizualizacja gotowa" (D-08) PO display(fig) w live branch.

    ---

    **Krok 2: Dodaj internal helper `_dodaj_gotowe_overlay!`**

    PRZED `_wizualizuj_impl`, dodac:

    ```julia
    # Dodaje GOTOWE overlay na srodku ax_trasa po zakonczeniu SA (D-06).
    # Wywolane gdy SA hits liczba_krokow + okno jest jeszcze otwarte. Zolty kolor
    # dla widocznosci na dark theme; centered position w danych coordinates [0,1]².
    function _dodaj_gotowe_overlay!(ax_trasa::Axis, stan::StanSymulacji, energia_nn::Float64)
        ratio = round(stan.energia / energia_nn; digits=4)
        text!(ax_trasa, "GOTOWE — ratio: $ratio";
              position = Point2f(0.5, 0.5),
              align = (:center, :center),
              fontsize = 24,
              color = :yellow,
              space = :data)
        return nothing
    end
    ```

    `space=:data` — pozycja w data coordinates (ax_trasa ma autolimits ~[0,1]²). Centered.

    ---

    **Krok 3: Zastapic body wizualizuj() na try/catch wrapper z TTFP @info**

    Zastapic CALE body funkcji `wizualizuj()` (od linii za docstring do `return nothing`) na:

    ```julia
    function wizualizuj(stan::StanSymulacji, params::Parametry, alg::Algorytm;
                        liczba_krokow::Int=params.liczba_krokow,
                        fps::Int=30,
                        kroki_na_klatke::Int=50,
                        eksport::Union{Nothing,String}=nothing)::Nothing
        # D-08: TTFP grace overlay — pierwszy @info PRZED with_theme/display.
        # Pitfall 14: czystego REPL TTFP GLMakie 60-150s; precompile cache 5-15s.
        @info "Ładowanie GLMakie (pierwsze uruchomienie może trwać 60+ s — kompilacja JIT)..."

        # D-13: Hard-fail wrapper dla GLMakie/OpenGL/X11/display errors.
        # Pitfall E: try/catch NA ZEWNATRZ with_theme — with_theme musi byc w impl
        # zeby auto-reset dzialal nawet przy throw wewnatrz with_theme block'u.
        try
            _wizualizuj_impl(stan, params, alg;
                             liczba_krokow=liczba_krokow, fps=fps, kroki_na_klatke=kroki_na_klatke,
                             eksport=eksport)
        catch e
            # Q10: GLMakie rzuca GLFW.GLFWError lub InitError przy braku OpenGL/displayu.
            # Sprawdzamy po stringu (GLFW nie eksportowane do scope'u) ORAZ typie InitError.
            msg = sprint(showerror, e)
            if contains(msg, "GLFW") || contains(msg, "OpenGL") || contains(msg, "display") ||
               contains(msg, "X11") || contains(msg, "GLMakie") || isa(e, InitError)
                # D-13: doslowny polish error (CONTEXT.md). Diakrytyki: "Spróbuj", "albo".
                error("GLMakie wymaga aktywnego kontekstu OpenGL. Brak displayu? " *
                      "Spróbuj `xvfb-run -a julia ...` na Linuksie albo uruchom lokalnie z GUI. " *
                      "Headless cloud (CI, Docker bez X) NIE jest wspierany w wersji v1.")
            else
                # Inny blad (np. ArgumentError z @assert) — propagujemy bez zmian.
                rethrow(e)
            end
        end
        return nothing
    end
    ```

    UWAGA — zmiany vs poprzednia wersja:
    - Walidacja argumentow USUNIETA (przeniesiona do _wizualizuj_impl).
    - Dodany TTFP @info na poczatku.
    - Cala logika opakowana w try/catch.
    - catch sprawdza sprint(showerror, e) po stringu (GLFW/OpenGL/display/X11/GLMakie) ORAZ isa(e, InitError).
    - Polski D-13 error message DOSLOWNIE (z poprawnymi diakrytykami: "Spróbuj", "ręcznie").
    - Inne errors (np. ArgumentError) propagowane bez zmian (rethrow).

    ---

    **Krok 4: WAZNE — kolejnosc helperow w pliku**

    Po wszystkich zmianach kolejnosc internal helperow w pliku (od gory do dolu):
    1. _trasa_do_punkty (z plan 03-02)
    2. _zbuduj_overlay_string (z plan 03-02)
    3. _setup_figure (z plan 03-02)
    4. _init_observables (z plan 03-02)
    5. _live_loop (z plan 03-03)
    6. _export_loop (z plan 03-04)
    7. _dodaj_gotowe_overlay! (NEW — plan 03-05)
    8. _wizualizuj_impl (NEW — plan 03-05)
    9. wizualizuj (publiczne, opakowane w try/catch — plan 03-05 finalize)

    Julia inferuje funkcje przy include — kolejnosc nie wymusza topologicznego porzadku, ale czytelnosc preferuje "od najmniejszego do najwiekszego".

    Konwencje:
    - Polish komentarze blokowe nad helperami.
    - English asserts (`@assert ... must be positive`).
    - Polish UI strings z diakrytykami: "Ładowanie", "może", "trwać", "Wizualizacja", "rozpoczynam", "symulację", "Spróbuj", "Linuksie", "lokalnie", "Headless", "wspierany", "wersji" (D-08 + D-13).
    - "GOTOWE" overlay text — krotko, czytelnie, z polskim "—" (em dash, nie hyphen).
    - ASCII identyfikatory (`energia_nn`, `bufor`, `ratio`).
    - UTF-8 NFC, no BOM (Phase 1 D-21).
  </action>
  <verify>
    <automated>julia --project=. -e 'using JuliaCity; src = read("src/wizualizacja.jl", String); @assert occursin("function _wizualizuj_impl", src) "_wizualizuj_impl not defined"; @assert occursin("function _dodaj_gotowe_overlay!", src) "_dodaj_gotowe_overlay! not defined"; @assert occursin("Ładowanie GLMakie", src) "TTFP @info missing — D-08"; @assert occursin("Wizualizacja gotowa", src) "second TTFP @info missing — D-08"; @assert occursin("GLMakie wymaga aktywnego kontekstu OpenGL", src) "D-13 polish hard-fail message missing"; @assert occursin("xvfb-run", src) "D-13 xvfb hint missing"; @assert occursin("GOTOWE", src) "D-06 GOTOWE overlay missing"; @assert occursin("ratio", src) "D-06 ratio in GOTOWE missing"; @assert occursin("energia_nn", src) "energia_nn computation missing — D-06"; @assert occursin("oblicz_energie(stan.D, nn_trasa, bufor)", src) "energia_nn calc missing"; @assert occursin("try", src) "try/catch wrapper missing — D-13"; @assert occursin("rethrow(e)", src) "rethrow for non-GLMakie errors missing — D-13"; @assert occursin("isa(e, InitError)", src) "InitError catch missing — Q10"; @assert occursin("contains(msg", src) "string match catch missing — Q10"; @assert occursin("_wizualizuj_impl(stan, params, alg", src) "wizualizuj does not delegate to impl"; println("OK plan 03-05 wired: hard-fail wrapper + TTFP + GOTOWE")'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^function _wizualizuj_impl(' src/wizualizacja.jl` zwraca 1 (refactored impl)
    - `grep -c '^function _dodaj_gotowe_overlay!(' src/wizualizacja.jl` zwraca 1 (D-06 helper)
    - `grep -c '^function wizualizuj(' src/wizualizacja.jl` zwraca 1 (public wrapper)
    - `grep -c 'Ładowanie GLMakie' src/wizualizacja.jl` zwraca 1 (D-08 first @info)
    - `grep -c 'Wizualizacja gotowa' src/wizualizacja.jl` zwraca 1 (D-08 second @info)
    - `grep -c 'GLMakie wymaga aktywnego kontekstu OpenGL' src/wizualizacja.jl` zwraca 1 (D-13 message)
    - `grep -c 'xvfb-run' src/wizualizacja.jl` zwraca 1 (D-13 hint)
    - `grep -c 'GOTOWE' src/wizualizacja.jl` zwraca przynajmniej 1 (D-06 overlay text)
    - `grep -c 'energia_nn' src/wizualizacja.jl` zwraca przynajmniej 3 (compute + pass + ratio)
    - `grep -c 'oblicz_energie(stan.D, nn_trasa, bufor)' src/wizualizacja.jl` zwraca 1
    - `grep -c '^    try$' src/wizualizacja.jl` zwraca przynajmniej 1 (try/catch wrapper)
    - `grep -c 'rethrow(e)' src/wizualizacja.jl` zwraca 1 (non-GLMakie propagation)
    - `grep -c 'isa(e, InitError)' src/wizualizacja.jl` zwraca 1 (Q10 type check)
    - `grep -c 'sprint(showerror, e)' src/wizualizacja.jl` zwraca 1 (Q10 string match base)
    - `grep -c 'contains(msg' src/wizualizacja.jl` zwraca przynajmniej 5 (5 string checks: GLFW, OpenGL, display, X11, GLMakie)
    - `grep -c '_wizualizuj_impl(stan, params, alg' src/wizualizacja.jl` zwraca 1 (delegacja)
    - `grep -c 'while isopen(fig)' src/wizualizacja.jl` zwraca przynajmniej 2 (1 w _live_loop + 1 passive event loop po GOTOWE)
    - Plik UTF-8 NFC: `julia -e 'using Unicode; @assert Unicode.normalize(read("src/wizualizacja.jl", String), :NFC) == read("src/wizualizacja.jl", String)'`
    - Plik ma >= 360 linii: `awk 'END {print NR}' src/wizualizacja.jl` >= 360
    - Pkg.test() nadal exit 0 (testy headless — nie wywoluja wizualizuj)
    - VIZ-06 zachowany: `grep -rl '^using GLMakie' src/` zwraca tylko src/wizualizacja.jl
    - Branch live: `_live_loop(...)` nadal wywolywane (z plan 03-03)
    - Branch eksport: `_export_loop(...)` nadal wywolywane (z plan 03-04)
  </acceptance_criteria>
  <done>
    src/wizualizacja.jl ma kompletny stack helperow + publiczna wizualizuj() opakowane w hard-fail try/catch. _wizualizuj_impl trzyma logike biznesowa (TTFP+with_theme+branching+GOTOWE). _dodaj_gotowe_overlay! tworzy GOTOWE text! z ratio energia/energia_nn po SA stop. TTFP @info messages PRZED with_theme i PO display(fig). Polski D-13 error gdy GLMakie zglasza GLFW/OpenGL/X11/display. Phase 1+2 testy zielone, VIZ-06 zachowany. Wszystkie 15 CONTEXT decisions D-01..D-15 zaimplementowane.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| try/catch boundary | Catch tylko GLMakie-related; inne errors (ArgumentError, BoundsError, ...) propagowane przez rethrow |
| `contains(msg, ...)` substring match | Akceptujemy false positives (np. user files containing "OpenGL" w komunikacie) — domena visualization-only, wystarczajace |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-19 | Tampering | catch swallow non-GLMakie errors | mitigate | Explicit `rethrow(e)` w else branch — propagate non-matching errors |
| T-03-20 | Information Disclosure | sprint(showerror, e) leaks stack trace | accept | Standard Julia error reporting; no PII; user wlasny system |
| T-03-21 | Denial of Service | passive event loop after GOTOWE blokuje az do user close | accept | D-06 mandate (window stays open); user explicit close |
| T-03-22 | Tampering | with_theme nie resetuje przy throw wewnatrz | mitigate | Pitfall E + Pattern 7: with_theme uzywa try/finally wewnetrznie; NASZE try/catch jest na zewnatrz, nie wplywa |

Severity: brak high. Visualization error reporting — local dev tool.
</threat_model>

<verification>
**Plan-level integration check:**

```bash
# 1. Statyczne struktury obecne
julia --project=. -e '
using JuliaCity
src = read("src/wizualizacja.jl", String)
@assert occursin("function _wizualizuj_impl", src)
@assert occursin("function _dodaj_gotowe_overlay!", src)
@assert occursin("Ładowanie GLMakie", src)
@assert occursin("Wizualizacja gotowa", src)
@assert occursin("GLMakie wymaga aktywnego kontekstu OpenGL", src)
@assert occursin("xvfb-run", src)
@assert occursin("GOTOWE", src)
@assert occursin("energia_nn", src)
@assert occursin("isa(e, InitError)", src)
@assert occursin("rethrow(e)", src)
@assert occursin("_wizualizuj_impl(stan, params, alg", src)
println("OK static structure correct")
'

# 2. VIZ-06 sanity preserved
matches=$(grep -rl "^using GLMakie" src/ 2>/dev/null | wc -l)
test "$matches" = "1" || { echo "FAIL VIZ-06: $matches files use GLMakie"; exit 1; }
echo "OK VIZ-06 preserved"

# 3. Phase 1+2 regression
julia --project=. -e 'using Pkg; Pkg.test()'

# 4. (MANUAL — dev machine z GUI) Live mode + GOTOWE overlay
# julia --project=. -e '
# using JuliaCity
# pkty = generuj_punkty(50; seed=42)
# stan = StanSymulacji(pkty); inicjuj_nn!(stan)
# alg = SimAnnealing(stan); stan.temperatura = alg.T_zero
# params = Parametry(liczba_krokow=400)
# wizualizuj(stan, params, alg; liczba_krokow=400, kroki_na_klatke=20, fps=30)
# # Oczekiwane: 2 @info messages (TTFP + Wizualizacja gotowa), animacja, po stopie
# # GOTOWE overlay z ratio (np. 0.92xx), okno czeka na zamkniecie.
# '

# 5. (MANUAL — headless smoke) D-13 polski error
# JULIA_GR_PROVIDER=GR DISPLAY= julia --project=. -e '
# using JuliaCity
# pkty = generuj_punkty(50; seed=42)
# stan = StanSymulacji(pkty); inicjuj_nn!(stan)
# alg = SimAnnealing(stan); stan.temperatura = alg.T_zero
# params = Parametry(liczba_krokow=400)
# try
#     wizualizuj(stan, params, alg; liczba_krokow=400)
# catch e
#     msg = sprint(showerror, e)
#     println("Got: ", msg)
#     @assert occursin("GLMakie wymaga aktywnego kontekstu OpenGL", msg) "expected D-13 polish error"
#     println("OK D-13 polski hard-fail")
# end
# '
```

Komendy 1, 2, 3 musza exit 0 automatycznie. Komendy 4, 5 to manual smoke.
</verification>

<success_criteria>
1. **wizualizuj() opakowane w try/catch** — `_wizualizuj_impl` trzyma core logic; `wizualizuj` wrapper.
2. **D-13 hard-fail polski error** — DOSLOWNA wiadomosc z CONTEXT.md, sprawdzenie po stringu (GLFW/OpenGL/X11/display/GLMakie) + isa(e, InitError); inne errors przez rethrow.
3. **D-08 dwa TTFP @info** — "Ładowanie GLMakie..." PRZED with_theme; "Wizualizacja gotowa..." PO display(fig) w live branch.
4. **D-06 GOTOWE overlay (live mode tylko)** — `text!` z `"GOTOWE — ratio: {ratio}"` po SA stop; window pozostaje otwarty (passive event loop).
5. **energia_nn liczone raz** — `oblicz_energie(stan.D, nn_trasa, bufor)` w `_wizualizuj_impl` przed with_theme.
6. **Wszystkie 15 CONTEXT decisions D-01..D-15 zaimplementowane** w wizualizacja.jl.
7. **Phase 1+2 testy zielone, VIZ-06 zachowany.**
</success_criteria>

<output>
After completion, create `.planning/phases/03-visualization-export/03-05-SUMMARY.md` zawierajace:

- Liczbe linii w `src/wizualizacja.jl` (sanity check >= 360 — finalna liczba)
- Lista wszystkich 8 internal helperow + sygnatury (od _trasa_do_punkty do _wizualizuj_impl)
- Kompletna sygnatura publicznej `wizualizuj()` (z type annotations)
- D-13 polski error message (literalna kopia)
- D-08 TTFP messages (literalna kopia obu)
- D-06 GOTOWE overlay text format (literalny)
- Coverage matrix: D-01..D-15 → ktora funkcja realizuje (tabela)
- Czy manual smoke testy sie udaly (3 booleany: live+GOTOWE, eksport, headless+D-13)
- Czy Pkg.test() exit 0 (regression check)
</output>

## Source coverage audit

| Source ID | Type | Status | Note |
|-----------|------|--------|------|
| VIZ-01 | REQ | COVERED | Pelna funkcja wizualizuj() z hard-fail wrapper, TTFP grace, GOTOWE — finalna |
| VIZ-04 | REQ | COVERED | TTFP messages + GOTOWE + D-13 — wszystko po polsku |
| LANG-02 | REQ | COVERED | Polish UI strings finalize: TTFP, GOTOWE, D-13 z diakrytykami |
| RESEARCH Pattern 6 (Hard-Fail GLMakie Catch) | RESEARCH | COVERED | wizualizuj() try/catch wrapper |
| RESEARCH Pitfall E (with_theme + try/catch) | RESEARCH | COVERED | try/catch NA ZEWNATRZ with_theme; with_theme w _wizualizuj_impl |
| RESEARCH Pitfall 14 (TTFP Surprise) | RESEARCH | COVERED | @info "Ładowanie..." PRZED with_theme |
| RESEARCH Q10 (GLMakie Headless Exception Types) | RESEARCH | COVERED | sprint(showerror,e) string match + isa(e,InitError) |
| CONTEXT D-06 (GOTOWE freeze + ratio) | CONTEXT | COVERED | _dodaj_gotowe_overlay! z ratio energia/energia_nn |
| CONTEXT D-08 (TTFP grace overlay) | CONTEXT | COVERED | dwa @info messages |
| CONTEXT D-13 (hard-fail polish msg) | CONTEXT | COVERED | DOSLOWNY error string |
| CONTEXT D-14 (runtests.jl bez wizualizacji) | CONTEXT | RESPECTED | wizualizuj() nie wywolywane w testach (poza grep guard plan 03-06) |
| CONTEXT D-15 (CI bez GLMakie) | CONTEXT | RESPECTED | Brak zmian w CI |
| WSZYSTKIE D-01..D-15 (final coverage) | CONTEXT | COVERED | 15/15 CONTEXT decisions zaimplementowanych w wizualizacja.jl |
