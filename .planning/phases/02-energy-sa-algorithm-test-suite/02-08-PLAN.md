---
phase: 02-energy-sa-algorithm-test-suite
plan: 08
type: execute
wave: 8
depends_on: [02-06, 02-07]
files_modified:
  - test/runtests.jl
autonomous: true
gap_closure: true
requirements:
  - TEST-06
tags:
  - julia
  - aqua
  - test-orchestration
  - quality-gate
  - bug-fix

must_haves:
  truths:
    - "test/runtests.jl Aqua testset has check_extras as a TOP-LEVEL kwarg of Aqua.test_all (NOT nested inside deps_compat)"
    - "check_extras ignore list contains the 4 phase-future-use extras: BenchmarkTools, GLMakie, Makie, Observables"
    - "Project.toml [extras] keeps all 4 future-use entries (per CONTEXT.md guidance — Phase 3 viz, Phase 4 bench)"
    - "deps_compat ignore list keeps :Random and :Statistics (stdlib without compat entries — Pattern D)"
    - "grep -E 'check_extras\\s*=' test/runtests.jl returns exactly 1 occurrence (top-level only — no duplicates, no nested)"
  artifacts:
    - path: "test/runtests.jl"
      provides: "Naprawiony Aqua testset z check_extras na top-level z ignore list dla 4 unused extras"
      contains: "check_extras = (ignore"
  key_links:
    - from: "test/runtests.jl::Aqua testset"
      to: "Aqua.test_all top-level kwargs"
      via: "check_extras hoisted from deps_compat sub-tuple to top-level kwarg"
      pattern: "check_extras\\s*=\\s*\\(ignore"
    - from: "test/runtests.jl::Aqua testset"
      to: "Project.toml [extras]"
      via: "ignore list explicitly names BenchmarkTools, GLMakie, Makie, Observables"
      pattern: ":BenchmarkTools.*:GLMakie.*:Makie.*:Observables"
---

<objective>
Naprawic BLOCKER BL-02 + IN-04 — `check_extras` w `test/runtests.jl::Aqua.test_all` jest zagniezdzone WEWNATRZ tuple `deps_compat`, podczas gdy `check_extras` jest osobnym top-level kwarg-iem Aqua. Aqua silently ignoruje misplaced config i uruchamia `check_extras` z defaultami — failure na 4 unused extras (BenchmarkTools, GLMakie, Makie, Observables) ktore sa w Project.toml [extras] ale NIE w [targets].test.

Per CONTEXT.md te 4 deps SA INTENCJONALNIE w [extras] — sa potrzebne w Phase 3 (GLMakie/Makie/Observables — wizualizacja) i Phase 4 (BenchmarkTools — benchmarks). Wlasciwy fix: dodac je do `check_extras` ignore list (NIE usuwac z Project.toml).

Purpose: TEST-06 (`Aqua.test_all` clean) jest jednym z 5 success criteria phase 2. Bez fixu Aqua zglosi 4 false-positives po regeneracji Manifest.toml + uruchomieniu `Pkg.test()`.

Output:
- test/runtests.jl: testset Aqua przebudowany z `check_extras` jako top-level kwarg
- Ignore list rozszerzona o `:BenchmarkTools, :GLMakie, :Makie, :Observables` (utrzymuje :Test, :Unicode dla bezpieczenstwa)
- deps_compat zachowany (`ignore = [:Random, :Statistics]`) — NIE jest dotykany shape `deps_compat` poza usunieciem nested check_extras
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-CONTEXT.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-VERIFICATION.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-REVIEW.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-06-SUMMARY.md
@CLAUDE.md
@Project.toml
@test/runtests.jl

<interfaces>
<!-- BEFORE-state Aqua testset (test/runtests.jl, lines 211-218): -->
@testset "Aqua.jl quality (TEST-06)" begin
    Aqua.test_all(JuliaCity;
        ambiguities = (recursive = false,),
        stale_deps = false,
        deps_compat = (ignore = [:Random, :Statistics],
                       check_extras = (ignore = [:Test, :Unicode],)),    # <-- BUG: nested!
    )
end

<!-- Aqua API: top-level kwargs to Aqua.test_all are independent. From Aqua docs:
     - ambiguities, stale_deps, deps_compat, project_extras, piracies, persistent_tasks,
       unbound_args, undefined_exports, project_toml_formatting, deps_compat,
       check_extras  ALL TOP-LEVEL.
     - deps_compat takes its own NamedTuple: (ignore=[symbols], check_extras=...)?
       NO — Aqua's deps_compat does NOT have a check_extras sub-key. The misplaced
       config is silently dropped, and check_extras runs with defaults. -->

<!-- Project.toml [extras] (current state): -->
[extras]
Aqua = "..."
BenchmarkTools = "..."   # future Phase 4 (bench/)
GLMakie = "..."          # future Phase 3 (viz)
JET = "..."
Makie = "..."            # future Phase 3 (viz)
Observables = "..."      # future Phase 3 (viz)
PerformanceTestTools = "..."
StableRNGs = "..."
Test = "..."
Unicode = "..."

<!-- [targets].test (current state — 6 entries): -->
test = ["Aqua", "JET", "PerformanceTestTools", "StableRNGs", "Test", "Unicode"]

<!-- Aqua's check_extras flags entries in [extras] that are not in any [targets].* list.
     Without explicit ignore: 4 extras (BenchmarkTools, GLMakie, Makie, Observables)
     are flagged as test failures. -->

<!-- AFTER-state target shape (correct Aqua API per BL-02 fix): -->
@testset "Aqua.jl quality (TEST-06)" begin
    Aqua.test_all(JuliaCity;
        ambiguities = (recursive = false,),
        stale_deps = false,
        deps_compat = (ignore = [:Random, :Statistics],),
        check_extras = (ignore = [:Test, :Unicode, :BenchmarkTools, :GLMakie, :Makie, :Observables],),
    )
end
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure Aqua testset — hoist check_extras to top level + extend ignore list</name>
  <files>test/runtests.jl</files>
  <read_first>
    - test/runtests.jl (full file — find the Aqua testset around lines 211-218)
    - Project.toml (confirm [extras] still has BenchmarkTools, GLMakie, Makie, Observables — base for ignore list)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-REVIEW.md (BL-02, IN-04 — full fix recipe)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-CONTEXT.md (rationale: future-Phase 3/4 deps stay in [extras])
  </read_first>
  <action>
    Edit `test/runtests.jl`. Locate the Aqua testset block at lines 211-218:

    BEFORE (exact text — 8 lines including testset wrappers):
    ```julia
        @testset "Aqua.jl quality (TEST-06)" begin
            Aqua.test_all(JuliaCity;
                ambiguities = (recursive = false,),
                stale_deps = false,
                deps_compat = (ignore = [:Random, :Statistics],
                               check_extras = (ignore = [:Test, :Unicode],)),
            )
        end
    ```

    AFTER (exact replacement — 9 lines):
    ```julia
        @testset "Aqua.jl quality (TEST-06)" begin
            # BL-02 + IN-04 (gap-closure 02-08): check_extras hoisted from deps_compat
            # sub-tuple to top-level kwarg. Ignore list extended to BenchmarkTools/GLMakie/
            # Makie/Observables — INTENCJONALNIE w [extras] dla Phase 3 (viz) i Phase 4 (bench)
            # per CONTEXT.md, NIE usuwane z Project.toml. Pattern D: stdlib :Random/:Statistics
            # bez compat entry → deps_compat ignore.
            Aqua.test_all(JuliaCity;
                ambiguities = (recursive = false,),
                stale_deps = false,
                deps_compat = (ignore = [:Random, :Statistics],),
                check_extras = (ignore = [:Test, :Unicode, :BenchmarkTools, :GLMakie, :Makie, :Observables],),
            )
        end
    ```

    Specific structural changes:
    1. `deps_compat = (ignore = [:Random, :Statistics],` — close NamedTuple with `,)` instead of carrying into nested `check_extras`. Note the trailing comma after `]` to mark single-element NamedTuple syntax `(ignore = ..., )`.
    2. Add new top-level kwarg `check_extras = (ignore = [...],)` AFTER `deps_compat` (and after `ambiguities` and `stale_deps`).
    3. The `check_extras` ignore list contains 6 symbols: `:Test, :Unicode, :BenchmarkTools, :GLMakie, :Makie, :Observables`.
    4. Polish rationale comment block (5 lines) added inside the testset, before the `Aqua.test_all` call.

    Do NOT modify any other parts of `test/runtests.jl`:
    - Encoding hygiene testset (lines ~25-88) untouched.
    - generuj_punkty / StanSymulacji testsets untouched.
    - Wave 0 StableRNG smoke untouched.
    - 3 `include(...)` lines for test_energia.jl, test_baselines.jl, test_symulacja.jl untouched.
    - JET TEST-07 testset (after the Aqua block) untouched.
    - File header comment block untouched.

    Do NOT modify `Project.toml` — the 4 unused extras stay there per CONTEXT.md (Phase 3/4 dependencies).
  </action>
  <verify>
    <automated>grep -c "check_extras = (ignore = \[:Test, :Unicode, :BenchmarkTools, :GLMakie, :Makie, :Observables\]" test/runtests.jl</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'check_extras = (ignore = \[:Test, :Unicode, :BenchmarkTools, :GLMakie, :Makie, :Observables\]' test/runtests.jl` returns exactly 1
    - `grep -c "deps_compat = (ignore = \[:Random, :Statistics\]" test/runtests.jl` returns exactly 1
    - `grep -cE '^\s*check_extras\s*=' test/runtests.jl` returns exactly 1 (no duplicate, no nested)
    - The line `deps_compat = (ignore = [:Random, :Statistics],),` (with comma+close-paren+comma terminator) exists exactly once: `grep -c 'deps_compat = (ignore = \[:Random, :Statistics\],),' test/runtests.jl` returns 1
    - The OLD malformed pattern `check_extras = (ignore = [:Test, :Unicode],))` (note double close-paren — was nested) returns 0: `grep -c 'check_extras = (ignore = \[:Test, :Unicode\],))' test/runtests.jl` == 0
    - JET testset still present after Aqua: `grep -c "JET type stability (TEST-07)" test/runtests.jl` returns >= 1
    - 3 includes still present: `grep -cE 'include\("test_(energia|baselines|symulacja)\.jl"\)' test/runtests.jl` returns 3
    - `grep -c "BL-02 + IN-04 (gap-closure 02-08)" test/runtests.jl` returns 1 (rationale comment present)
  </acceptance_criteria>
  <done>
    test/runtests.jl Aqua testset has `check_extras` as a TOP-LEVEL kwarg (not nested in deps_compat). Ignore list explicitly names the 4 future-use extras (BenchmarkTools, GLMakie, Makie, Observables) plus :Test, :Unicode. deps_compat retains :Random, :Statistics. Project.toml unchanged. All other testsets untouched.
  </done>
</task>

</tasks>

<verification>
After Task 1:
1. `grep -cE '^\s*check_extras\s*=' test/runtests.jl` returns 1 (top-level, not nested)
2. `grep -c ':BenchmarkTools' test/runtests.jl` returns 1 (in ignore list)
3. `grep -c 'check_extras = (ignore = \[:Test, :Unicode\],))' test/runtests.jl` returns 0 (malformed nested form removed)
4. Project.toml [extras] still has all 10 entries — `grep -c 'BenchmarkTools = ' Project.toml` returns 1; analog dla GLMakie/Makie/Observables.

Runtime verification (deferred to plan 02-13):
- `julia --project=. -e 'using Pkg; Pkg.test()'` exit 0; Aqua testset Pass.
</verification>

<success_criteria>
- BL-02 + IN-04 fixed: check_extras top-level kwarg, ignore list complete
- Project.toml NIE jest modyfikowane (per CONTEXT.md Phase 3/4 future use)
- TEST-06 unblocked at code level (runtime confirmation in plan 02-13)
- Inne testsety w runtests.jl niezmienione (encoding/PKT/JET/includes)
</success_criteria>

<output>
After completion, create `.planning/phases/02-energy-sa-algorithm-test-suite/02-08-SUMMARY.md` documenting:
- Aqua testset before/after structure
- Ignore list rationale (4 extras stay in Project.toml; ignored at Aqua level only)
- Note: runtime confirmation deferred to plan 02-13
</output>
