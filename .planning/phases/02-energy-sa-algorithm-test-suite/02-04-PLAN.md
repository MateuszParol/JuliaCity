---
phase: 02-energy-sa-algorithm-test-suite
plan: 04
type: execute
wave: 4
depends_on: [02-02, 02-03]
files_modified:
  - src/algorytmy/simulowane_wyzarzanie.jl
  - src/algorytmy/.gitkeep
  - src/JuliaCity.jl
autonomous: true
requirements:
  - ALG-01
  - ALG-02
  - ALG-03
  - ALG-06
  - ALG-07
  - ALG-08
tags:
  - julia
  - algorithm
  - simulated-annealing
  - hot-path
  - zero-alloc

must_haves:
  truths:
    - "src/algorytmy/simulowane_wyzarzanie.jl zawiera struct SimAnnealing <: Algorytm z 3 polami (T_zero, alfa, cierpliwosc)"
    - "SimAnnealing(stan; alfa=0.9999, cierpliwosc=5000, T_zero=kalibruj_T0(stan)) konstruktor (D-02, D-03)"
    - "symuluj_krok!(stan, params, alg::SimAnnealing) jest type-stable, zero-alloc po rozgrzewce, zwraca Nothing"
    - "Po kazdym kroku sort(stan.trasa) == 1:n (Hamilton invariant - ALG-08)"
    - "Same master seed + ten sam plik kodu -> identyczna trajektoria stan.trasa (ALG-07 via D-09/D-12 - single master RNG)"
    - "uruchom_sa!(stan, params, alg) outer-loop helper implementuje ALG-06 stop criterion (D-04): exit gdy licznik_bez_poprawy >= alg.cierpliwosc OR stan.iteracja >= params.liczba_krokow; reset licznika tylko przy strict Δ < 0"
    - "src/JuliaCity.jl include-uje algorytmy/simulowane_wyzarzanie.jl po baselines.jl i eksportuje uruchom_sa!"
    - "src/algorytmy/.gitkeep zostal usuniety (pierwszy plik w katalogu)"
  artifacts:
    - path: "src/algorytmy/simulowane_wyzarzanie.jl"
      provides: "Wariant SimAnnealing + symuluj_krok! + uruchom_sa! outer loop (ALG-06 stop)"
      contains: "struct SimAnnealing"
      min_lines: 90
    - path: "src/JuliaCity.jl"
      provides: "include + export dla simulowane_wyzarzanie.jl"
      contains: "include(\"algorytmy/simulowane_wyzarzanie.jl\")"
  key_links:
    - from: "src/algorytmy/simulowane_wyzarzanie.jl::symuluj_krok!"
      to: "src/energia.jl::delta_energii"
      via: "delta = delta_energii(stan, i, j)"
      pattern: "delta_energii\\(stan, i, j\\)"
    - from: "src/algorytmy/simulowane_wyzarzanie.jl::symuluj_krok!"
      to: "stan.rng (Xoshiro)"
      via: "rand(stan.rng, ...) - Metropolis acceptance"
      pattern: "rand\\(stan\\.rng"
    - from: "src/algorytmy/simulowane_wyzarzanie.jl::SimAnnealing constructor"
      to: "src/energia.jl::kalibruj_T0"
      via: "T_zero=kalibruj_T0(stan) default kwarg"
      pattern: "T_zero.*kalibruj_T0"
---

<objective>
Zaimplementowac wariant SimAnnealing (struct + 2 konstruktory) oraz funkcje `symuluj_krok!` realizujaca jeden krok SA: uniform random pair, delta 2-opt, Metropolis acceptance, in-place reverse, geometric cooling. Type-stable, zero-alloc po rozgrzewce. Pokrywa REQ ALG-01, ALG-02, ALG-03, ALG-06 (patience interpretacja), ALG-07 (single master RNG -> identical trajectory), ALG-08 (Hamilton invariant).

Purpose: Hot path symulacji - wywolywany 50_000 razy. Musi byc type-stable (TEST-02), zero-alloc (TEST-03), Hamilton invariant po kazdym kroku (TEST-01, ALG-08), deterministyczny dla single master seed (TEST-04, ALG-07 per D-09/D-12).

Output:
- src/algorytmy/simulowane_wyzarzanie.jl - nowy plik z struct SimAnnealing + 2 konstruktorami + symuluj_krok! + uruchom_sa! (outer-loop helper z ALG-06 stop criterion per D-04)
- src/algorytmy/.gitkeep - usuniety (pierwszy plik dolacza)
- src/JuliaCity.jl - include + eksport SimAnnealing, symuluj_krok!, uruchom_sa!
- Dowod: po `for _ in 1:1000; symuluj_krok!(stan, params, alg); end` mamy `sort(stan.trasa) == 1:n`; uruchom_sa! z malym cierpliwosc=10 koniczy wczesniej niz params.liczba_krokow (sprawdzane w Plan 02-05)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-CONTEXT.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-RESEARCH.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-PATTERNS.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-02-SUMMARY.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-03-SUMMARY.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@src/typy.jl
@src/energia.jl
@src/baselines.jl
@src/JuliaCity.jl

<interfaces>
<!-- Kontrakty z 02-02 + 02-03. Executor NIE musi szukac. -->

From src/typy.jl (LOCKED):
```julia
abstract type Algorytm end
mutable struct StanSymulacji{R<:AbstractRNG}
    const punkty::Vector{Punkt2D}
    const D::Matrix{Float64}
    const rng::R
    trasa::Vector{Int}             # symuluj_krok! mutates via reverse!
    energia::Float64               # symuluj_krok! mutates via += delta
    temperatura::Float64           # symuluj_krok! mutates via *= alfa
    iteracja::Int                  # symuluj_krok! mutates via += 1
end
Base.@kwdef struct Parametry
    liczba_krokow::Int = 50_000
end
```

From src/energia.jl (Plan 02-02):
```julia
function delta_energii(stan::StanSymulacji, i::Int, j::Int)::Float64
function kalibruj_T0(stan::StanSymulacji; n_probek::Int=1000, rng=stan.rng)::Float64
```

From src/baselines.jl (Plan 02-03):
```julia
function inicjuj_nn!(stan::StanSymulacji)
```

From CONTEXT.md D-02 (defaults - LOCKED):
- alfa = 0.9999 (geometric cooling factor)
- cierpliwosc = 5000 (stagnation patience)
- liczba_krokow = 50_000 (Parametry default)

From CONTEXT.md D-04 (cooling location): stan.temperatura *= alfa AFTER Metropolis test, BEFORE iteracja += 1.

From CONTEXT.md D-05/D-06/D-07 (2-opt mechanika - LOCKED):
- i = rand(stan.rng, 1:(n-1))
- j = rand(stan.rng, (i+2):n)  - excludes adjacent (j >= i+2)
- delta = delta_energii(stan, i, j)
- accept iff delta < 0.0 || rand(stan.rng) < exp(-delta / stan.temperatura)
- on accept: reverse!(view(stan.trasa, (i+1):j))  AND  stan.energia += delta

From CONTEXT.md D-09 (single master RNG - LOCKED): brak per-thread RNG. symuluj_krok! uzywa wylacznie stan.rng. ALG-07 interpretowane jako "single master seed -> identical trajectory" (TEST-04 D-12).

From RESEARCH Pattern 2 (symuluj_krok! pattern - linie 184-209):
```julia
function symuluj_krok!(stan::StanSymulacji, params::Parametry, alg::SimAnnealing)
    n = length(stan.trasa)
    i = rand(stan.rng, 1:(n - 1))
    j = rand(stan.rng, (i + 2):n)
    @assert 1 <= i < j <= n "i, j out of range"

    delta = delta_energii(stan, i, j)
    zaakceptowano = delta < 0.0 || rand(stan.rng) < exp(-delta / stan.temperatura)
    if zaakceptowano
        reverse!(view(stan.trasa, (i + 1):j))
        stan.energia += delta
    end
    stan.temperatura *= alg.alfa
    stan.iteracja += 1
    return nothing
end
```

From RESEARCH §Phase Requirements (linia 727) - ALG-06 (stagnation patience):
"counter w closure scope outer loop (NIE pole Stan/Parametry), bo symuluj_krok! to JEDEN krok - outer loop w examples/ owns stop logic. **Phase 2 nie dodaje stop loop do symuluj_krok!**; cierpliwosc jest pole SimAnnealing (D-01) ale konsumowane przez outer loop w examples/ (Phase 4) i tests (Plan 02-05)."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Utworzyc src/algorytmy/simulowane_wyzarzanie.jl + usunac .gitkeep</name>
  <files>src/algorytmy/simulowane_wyzarzanie.jl, src/algorytmy/.gitkeep</files>
  <read_first>
    - src/typy.jl (StanSymulacji, Algorytm, Parametry shape)
    - src/energia.jl (delta_energii, kalibruj_T0 sygnatury - z 02-02 SUMMARY)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-RESEARCH.md sekcje "Pattern 2: 2-opt move + uniform random pair" (linie 181-214) i "Pitfall A: @allocated" (linie 290-320) i "Pitfall B: @inferred Union returns" (linie 324-336)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-CONTEXT.md decyzje D-01 (Parametry vs SimAnnealing), D-02 (defaults), D-03 (kalibracja T0 w default kwarg), D-04 (patience reset only Delta<0; cooling po Metropolis), D-05..D-07 (2-opt mechanika), D-09 (single master RNG), D-12 (TEST-04 semantyka)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-PATTERNS.md sekcja "src/algorytmy/simulowane_wyzarzanie.jl (service / event-driven)" (linie 99-194)
    - .planning/research/PITFALLS.md Pitfall 1 (type-instability via abstract fields - SimAnnealing fields MUST be concrete)
  </read_first>
  <action>
    **A. Utworzyc katalog struktury:** `src/algorytmy/` istnieje (z Phase 1, ma `.gitkeep`). Po utworzeniu nowego pliku `simulowane_wyzarzanie.jl` w tym katalogu, **USUNAC** plik `.gitkeep` (jest juz nie potrzebny - katalog ma teraz prawdziwy plik).

    **B. Utworzyc NOWY plik src/algorytmy/simulowane_wyzarzanie.jl** (UTF-8, LF, no BOM, NFC normalized; ASCII filename per BOOT-04 - "simulowane_wyzarzanie" NIE "symulowane_wyżarzanie"):

    Top-of-file komentarz:
    ```julia
    # Wariant SimAnnealing - simulowane wyzarzanie (Simulated Annealing) z 2-opt + Metropolis.
    # Pokrywa REQ ALG-01, ALG-02, ALG-03, ALG-06 (patience field), ALG-07, ALG-08.
    #
    # Hyperparametry zyja w SimAnnealing (D-01: algorytm-specyficzne).
    # Parametry trzymaja pola niezalezne od algorytmu (D-01) - liczba_krokow.
    # Outer loop w examples/Phase 4 + testy Plan 02-05 owns stop logic
    # (cierpliwosc + liczba_krokow konsumowane tam).
    #
    # Hot path: symuluj_krok! type-stable, zero-alloc po rozgrzewce (TEST-03, ALG-03).
    # Single master RNG: stan.rng - brak per-thread (D-09).
    # Asercje wewnetrzne po angielsku per LANG-04. Komentarze polskie per LANG-01.
    ```

    Diakrytyki w komentarzach prose mozna uzywac (NFC) - np. "wyżarzanie" w docstring jest OK; ale **filename i identyfikatory** - tylko ASCII.

    NIE dodawac `using` statementow - wszystko w scope przez `src/JuliaCity.jl`.

    **C. Definicja struct SimAnnealing <: Algorytm**

    Polish docstring z REQ ALG-01 + cross-ref D-01/D-02. Wszystkie pola CONCRETE types (Pitfall 1):
    ```julia
    """
        SimAnnealing <: Algorytm

    Wariant Simulowane Wyżarzanie - geometric cooling z patience stop. Hyperparametry
    zyja w strukturze (D-01) - Parametry trzyma pola niezalezne od algorytmu.

    # Pola
    - T_zero::Float64  - poczatkowa temperatura (kalibrowana przez kalibruj_T0, D-03)
    - alfa::Float64    - wspolczynnik geometric cooling, default 0.9999 (D-02)
    - cierpliwosc::Int - stagnation patience threshold, default 5000 (D-02);
                         outer loop konsumuje to pole (D-04: reset tylko przy Delta<0)
    """
    struct SimAnnealing <: Algorytm
        T_zero::Float64
        alfa::Float64
        cierpliwosc::Int
    end
    ```

    **D. Konstruktor 1 (positional, dla testow):**
    Konstruktor domyslny od Julia istnieje juz dla pol (T_zero::Float64, alfa::Float64, cierpliwosc::Int) - czyli `SimAnnealing(0.5, 0.9999, 5000)` dziala. NIE definiujemy explicit positional ctor - default wystarcza.

    **E. Konstruktor 2 (kwarg z auto-kalibracja T0):**
    Polish docstring z REQ ALG-05 + D-03 (kalibruj_T0 jako default kwarg).
    Sygnatura:
    ```julia
    function SimAnnealing(stan::StanSymulacji;
                          alfa::Float64=0.9999,
                          cierpliwosc::Int=5000,
                          T_zero::Float64=kalibruj_T0(stan))
        return SimAnnealing(T_zero, alfa, cierpliwosc)
    end
    ```

    UWAGA: T_zero=kalibruj_T0(stan) jest evaluated ZA KAZDYM RAZEM gdy konstruktor jest wywolany BEZ explicit T_zero - to oznacza ze caller musi miec stan z wypelniona stan.D (czyli musial wywolac inicjuj_nn!(stan) wczesniej). Jezeli stan.D jest niewypelniona, kalibruj_T0 da bzdurne wyniki ale nie wybuchnie - to jest udokumentowane (D-03 + D-14: "po inicjuj_nn!").

    **WAŻNE: Ustawienie stan.temperatura** - po skonstruowaniu SimAnnealing, caller musi ustawic `stan.temperatura = alg.T_zero` recznie (bo SimAnnealing jest immutable struct, nie wie o stan). To czesc workflow init w examples/Phase 4 i tests Plan 02-05. Udokumentowac to w docstring konstruktora kwarg:
    "Po wywolaniu konstruktora caller musi ustawic `stan.temperatura = alg.T_zero` przed pierwszym symuluj_krok!."

    **F. Funkcja symuluj_krok!**

    Polish docstring z REQ ALG-02, ALG-03, ALG-08. Doslownie pattern z RESEARCH Pattern 2:
    ```julia
    function symuluj_krok!(stan::StanSymulacji, params::Parametry, alg::SimAnnealing)
        n = length(stan.trasa)
        i = rand(stan.rng, 1:(n - 1))
        j = rand(stan.rng, (i + 2):n)
        @assert 1 <= i < j <= n "i, j out of range"

        delta = delta_energii(stan, i, j)
        zaakceptowano = delta < 0.0 || rand(stan.rng) < exp(-delta / stan.temperatura)
        if zaakceptowano
            reverse!(view(stan.trasa, (i + 1):j))
            stan.energia += delta
        end
        stan.temperatura *= alg.alfa
        stan.iteracja += 1
        return nothing
    end
    ```

    Hot-path discipline:
    - `n = length(stan.trasa)` raz na poczatku.
    - Direct field access (NIE getfield).
    - `return nothing` literal (Pitfall B - @inferred ::Nothing wymaga literal).
    - NIE uzywaj keyword args w hot path (juz spelnione - 3 positional args + alg::SimAnnealing).
    - `params` jest PRESENT ale NIE UZYWANY w symuluj_krok! sam w sobie - to bo outer loop konsumuje params.liczba_krokow + alg.cierpliwosc. Plan 02-05 (testy) i Phase 4 (examples) implementuja outer loop.

    **NIE uzywac @inbounds w symuluj_krok!** - assercja sprawdza i,j; reverse!(view) jest bounds-safe; delta_energii ma swoje asercje.

    **G. Konwencje:**
    - Polish docstring + Polish komentarze inline (z diakrytykami w NFC).
    - English asserts.
    - ASCII identifiers (`zaakceptowano`, `delta`, `alfa`, `cierpliwosc`).
    - `return nothing` explicit.
  </action>
  <verify>
    <automated>julia --project=. -e 'using JuliaCity, Random; pkty = generuj_punkty(20; seed=42); stan = StanSymulacji(pkty; rng=Xoshiro(42)); inicjuj_nn!(stan); alg = SimAnnealing(stan); stan.temperatura = alg.T_zero; params = Parametry(liczba_krokow=100); for _ in 1:100; symuluj_krok!(stan, params, alg); end; @assert sort(stan.trasa) == collect(1:20) "Hamilton invariant broken: $(sort(stan.trasa))"; @assert stan.iteracja == 100 "iteracja count off"; @assert stan.temperatura < alg.T_zero "temperature should have decreased"; println("OK symuluj_krok! 100 steps - Hamilton invariant preserved, iteracja=", stan.iteracja, " T0=", round(alg.T_zero, digits=4), " T_final=", round(stan.temperatura, digits=4))'</automated>
  </verify>
  <acceptance_criteria>
    - Plik src/algorytmy/simulowane_wyzarzanie.jl istnieje
    - Plik src/algorytmy/.gitkeep NIE istnieje (usuniety)
    - `grep -c '^struct SimAnnealing' src/algorytmy/simulowane_wyzarzanie.jl` zwraca 1
    - `grep -c 'function SimAnnealing(stan::StanSymulacji' src/algorytmy/simulowane_wyzarzanie.jl` zwraca 1 (kwarg ctor)
    - `grep -c '^function symuluj_krok!' src/algorytmy/simulowane_wyzarzanie.jl` zwraca 1
    - `grep -c 'reverse!(view(stan.trasa' src/algorytmy/simulowane_wyzarzanie.jl` zwraca 1 (D-07)
    - `grep -c 'rand(stan.rng' src/algorytmy/simulowane_wyzarzanie.jl` zwraca przynajmniej 3 (i, j, Metropolis)
    - `grep -c 'delta_energii(stan, i, j)' src/algorytmy/simulowane_wyzarzanie.jl` zwraca 1
    - Cooling step uses `*=` (NIE `.=`): `julia --project=. -e 'src=read("src/algorytmy/simulowane_wyzarzanie.jl",String); @assert occursin("stan.temperatura *= alg.alfa", src) && !occursin("stan.temperatura .= alg.alfa", src)'` exits 0
    - `grep -c 'return nothing' src/algorytmy/simulowane_wyzarzanie.jl` zwraca przynajmniej 1
    - `grep -c 'kalibruj_T0' src/algorytmy/simulowane_wyzarzanie.jl` zwraca 1 (default kwarg)
    - Plik ma >= 60 linii: `awk 'END {print NR}' src/algorytmy/simulowane_wyzarzanie.jl` >= 60
    - Po Task 2 wired: 100 krokow nie lamie Hamilton invariant
  </acceptance_criteria>
  <done>
    src/algorytmy/simulowane_wyzarzanie.jl zawiera struct SimAnnealing <: Algorytm (T_zero, alfa, cierpliwosc) + kwarg constructor SimAnnealing(stan; ...) z auto-kalibracja T_zero + symuluj_krok!. .gitkeep usuniety. Po 100 krokach Hamilton invariant zachowany.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire src/algorytmy/simulowane_wyzarzanie.jl do src/JuliaCity.jl</name>
  <files>src/JuliaCity.jl</files>
  <read_first>
    - src/JuliaCity.jl (po edycjach z 02-01..02-03)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-PATTERNS.md sekcja "src/JuliaCity.jl (MODIFIED)" (linie 422-491)
  </read_first>
  <action>
    Modyfikacja `src/JuliaCity.jl` - dodac `include("algorytmy/simulowane_wyzarzanie.jl")` PO `include("baselines.jl")`. Topological order: `typy -> punkty -> energia -> baselines -> algorytmy/simulowane_wyzarzanie`.

    Final state po wszystkich modyfikacjach 02-01..02-04:

    ```julia
    module JuliaCity

    # Zewnetrzne zaleznosci runtime
    using GeometryBasics: Point2
    using Random
    using ChunkSplitters
    using Statistics: std

    # Typy domenowe (Punkt2D, Algorytm, StanSymulacji, Parametry)
    include("typy.jl")

    # Generator punktow testowych (PKT-01..04)
    include("punkty.jl")

    # Energia + macierz dystansow + delta + kalibracja T0 (Phase 2 Plan 02-02)
    include("energia.jl")

    # Baseline NN (Phase 2 Plan 02-03)
    include("baselines.jl")

    # Algorytmy <:Algorytm (Holy-traits dispatch) - Phase 2 Plan 02-04
    include("algorytmy/simulowane_wyzarzanie.jl")

    # Eksport publicznego API
    export Punkt2D, StanSymulacji, Algorytm, generuj_punkty,
           Parametry, SimAnnealing,
           oblicz_macierz_dystans!, oblicz_energie, delta_energii, kalibruj_T0,
           trasa_nn, inicjuj_nn!,
           symuluj_krok!

    end # module
    ```

    Konwencje:
    - Polish prose komentarz nad blokiem `include("algorytmy/...")`.
    - Topologiczna kolejnosc: simulowane_wyzarzanie.jl PO baselines.jl (zaleznosci: uzywa delta_energii i kalibruj_T0 z energia.jl - juz dostepne; NIE uzywa nicego z baselines.jl bezposrednio, ale dla porzadku konceptualnego (najpierw init flow, potem algorytm) zostawiamy ta kolejnosc).
    - Eksport SimAnnealing i symuluj_krok! dodany do listy.
  </action>
  <verify>
    <automated>julia --project=. -e 'using JuliaCity, Random; @assert isdefined(JuliaCity, :SimAnnealing); @assert isdefined(JuliaCity, :symuluj_krok!); @assert SimAnnealing <: Algorytm; pkty = generuj_punkty(20; seed=42); stan = StanSymulacji(pkty; rng=Xoshiro(42)); inicjuj_nn!(stan); alg = SimAnnealing(stan; alfa=0.9999, cierpliwosc=5000); @assert alg.alfa == 0.9999; @assert alg.cierpliwosc == 5000; @assert alg.T_zero > 0; stan.temperatura = alg.T_zero; symuluj_krok!(stan, Parametry(), alg); @assert stan.iteracja == 1; @assert sort(stan.trasa) == collect(1:20); println("OK SimAnnealing + symuluj_krok! exports work, T_zero=", round(alg.T_zero, digits=4))'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'include("algorytmy/simulowane_wyzarzanie.jl")' src/JuliaCity.jl` zwraca 1
    - `grep -c 'SimAnnealing' src/JuliaCity.jl` zwraca przynajmniej 1 (export)
    - `grep -c 'symuluj_krok!' src/JuliaCity.jl` zwraca przynajmniej 1 (export)
    - Topologiczna kolejnosc include w pliku: typy < punkty < energia < baselines < algorytmy/...
    - Phase 1 testy nadal przechodza: `julia --project=. -e 'using Pkg; Pkg.test()'` exits 0
    - SimAnnealing <: Algorytm: `julia --project=. -e 'using JuliaCity; @assert SimAnnealing <: Algorytm'` exits 0
    - Default constructor for SimAnnealing(stan) works: `julia --project=. -e 'using JuliaCity; pkty=generuj_punkty(20;seed=1); stan=StanSymulacji(pkty); inicjuj_nn!(stan); alg=SimAnnealing(stan); @assert alg.alfa == 0.9999 && alg.cierpliwosc == 5000'` exits 0
  </acceptance_criteria>
  <done>
    src/JuliaCity.jl ma include dla algorytmy/simulowane_wyzarzanie.jl w topologicznej kolejnosci. Eksportuje SimAnnealing, symuluj_krok!, uruchom_sa! (Task 3). Pkg.test() przechodzi.
  </done>
</task>

<task type="auto">
  <name>Task 3: Dodac uruchom_sa! outer-loop helper z ALG-06 stop criterion</name>
  <files>src/algorytmy/simulowane_wyzarzanie.jl, src/JuliaCity.jl</files>
  <read_first>
    - src/algorytmy/simulowane_wyzarzanie.jl (po Task 1+2 - struct SimAnnealing + symuluj_krok! gotowe)
    - src/JuliaCity.jl (po Task 2 - eksport SimAnnealing, symuluj_krok! juz dodany)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-CONTEXT.md decyzja D-04 (stagnation patience reset tylko przy Δ < 0; stop: licznik_bez_poprawy >= alg.cierpliwosc OR stan.iteracja >= params.liczba_krokow)
    - .planning/REQUIREMENTS.md ALG-06 (Kryterium stopu: stagnation patience)
    - .planning/phases/01-bootstrap-core-types-points/01-CONTEXT.md D-06 (StanSymulacji shape LOCKED - NIE dodajemy pol; licznik_bez_poprawy zyje LOKALNIE w uruchom_sa!)
  </read_first>
  <action>
    **Cel:** Dostarczyc ALG-06 (stagnation patience stop) substantywnie - NIE odraczac do Phase 4. ALG-06 / D-04 wymaga konkretnego kryterium stopu zlozonego z dwoch warunkow OR; symuluj_krok! to jeden krok i NIE moze konsumowac patience (single-step contract). Outer-loop helper `uruchom_sa!` opakowuje petle, trzyma `licznik_bez_poprawy` LOKALNIE (NIE jako pole Stan/Parametry - Phase 1 D-06 shape LOCKED), i implementuje stop logic doslownie z D-04.

    **A. Dodac funkcje `uruchom_sa!` do `src/algorytmy/simulowane_wyzarzanie.jl`** PO `symuluj_krok!`:

    ```julia
    """
        uruchom_sa!(stan::StanSymulacji, params::Parametry, alg::SimAnnealing) -> Int

    Outer-loop helper realizujacy kryterium stopu z REQ ALG-06 / D-04:

      - licznik `licznik_bez_poprawy` startuje od 0 (lokalny, NIE pole Stan - shape LOCKED)
      - po kazdym `symuluj_krok!` sprawdzamy: czy `stan.energia` faktycznie zmalalo
        wzgledem ostatniego best-known minimum? jezeli TAK (strict Δ < 0) -> reset licznika do 0
        (D-04: "reset tylko przy Δ < 0"); akceptacja worsening przez Metropolis NIE resetuje
      - stop gdy `licznik_bez_poprawy >= alg.cierpliwosc` LUB `stan.iteracja >= params.liczba_krokow`

    Caller MUSI wywolac `inicjuj_nn!(stan)` i ustawic `stan.temperatura = alg.T_zero` PRZED
    wywolaniem (analogicznie do `symuluj_krok!`).

    Zwraca liczbe wykonanych krokow (rowna `stan.iteracja - iteracja_start`).

    # Argumenty
    - `stan::StanSymulacji` - mutowany in-place
    - `params::Parametry` - hard cap przez `params.liczba_krokow`
    - `alg::SimAnnealing` - konsumuje `alg.cierpliwosc`
    """
    function uruchom_sa!(stan::StanSymulacji, params::Parametry, alg::SimAnnealing)::Int
        iteracja_start = stan.iteracja
        # Snapshot best-known energy at entry; rosnie tylko gdy delta < 0 zostanie zaakceptowane
        # i stan.energia spadnie ponizej tej wartosci.
        energia_min = stan.energia
        licznik_bez_poprawy = 0

        while stan.iteracja < params.liczba_krokow && licznik_bez_poprawy < alg.cierpliwosc
            symuluj_krok!(stan, params, alg)
            # D-04: reset TYLKO przy strict improvement (delta < 0).
            # Akceptacja Metropolis przy delta >= 0 NIE resetuje (to eksploracja, nie postep).
            if stan.energia < energia_min
                energia_min = stan.energia
                licznik_bez_poprawy = 0
            else
                licznik_bez_poprawy += 1
            end
        end
        return stan.iteracja - iteracja_start
    end
    ```

    Konwencje (per CLAUDE.md + Phase 1 D-22..24):
    - Polish docstring (NFC, diakrytyki dozwolone w prose).
    - ASCII identyfikatory: `licznik_bez_poprawy`, `energia_min`, `iteracja_start`.
    - English asercji NIE potrzebne tutaj - logika jest defensywna sama w sobie (caller-error w temperaturze powoduje zachowanie greedy ale nie crash).
    - Type-stable: returns `Int`. NIE wywolujemy zadnych @inferred testow tutaj (Plan 02-05 pokrywa).
    - Zero-alloc po rozgrzewce: petla while + 3 lokalne zmienne Int/Float64 + symuluj_krok! (zero-alloc per Task 1) -> brak alokacji.

    **B. Dodac `uruchom_sa!` do listy eksportow w `src/JuliaCity.jl`:**

    Aktualizuj export-line tak by zawierala `uruchom_sa!` po `symuluj_krok!`:
    ```julia
    export Punkt2D, StanSymulacji, Algorytm, generuj_punkty,
           Parametry, SimAnnealing,
           oblicz_macierz_dystans!, oblicz_energie, delta_energii, kalibruj_T0,
           trasa_nn, inicjuj_nn!,
           symuluj_krok!, uruchom_sa!
    ```

    **C. Sanity smoke test inline w trakcie wykonania (NIE commit-owac do test/, to jest kontrola wykonania):**

    ```bash
    julia --project=. -e '
    using JuliaCity, Random
    pkty = generuj_punkty(20; seed=42)
    stan = StanSymulacji(pkty; rng=Xoshiro(42))
    inicjuj_nn!(stan)
    alg = SimAnnealing(stan; alfa=0.9999, cierpliwosc=10)  # mala cierpliwosc -> wczesny stop
    stan.temperatura = alg.T_zero
    params = Parametry(liczba_krokow=10_000)
    n_krokow = uruchom_sa!(stan, params, alg)
    @assert n_krokow < params.liczba_krokow "patience nie zatrzymal petli (n=$n_krokow >= $(params.liczba_krokow))"
    @assert stan.iteracja == n_krokow "iteracja off"
    @assert sort(stan.trasa) == collect(1:20) "Hamilton invariant zlamany"
    println("OK uruchom_sa! patience=10 -> n_krokow=", n_krokow, " < ", params.liczba_krokow, " (early stop dziala); iteracja=", stan.iteracja)
    '
    ```

    Jezeli ten sanity smoke FAILUJE, debug na miejscu (nie kontynuowac do Plan 02-05) - typowe przyczyny: (a) literowka `licznik_bez_poprawy` vs `licznik_bez_poprawy` wewnatrz, (b) zly operator porownania (`>` vs `>=`), (c) reset w zlym branchu (powinno byc tylko gdy `stan.energia < energia_min`, NIE przy kazdej akceptacji).
  </action>
  <verify>
    <automated>julia --project=. -e 'using JuliaCity, Random; @assert isdefined(JuliaCity, :uruchom_sa!) "uruchom_sa! missing from JuliaCity exports"; pkty = generuj_punkty(20; seed=42); stan = StanSymulacji(pkty; rng=Xoshiro(42)); inicjuj_nn!(stan); alg = SimAnnealing(stan; alfa=0.9999, cierpliwosc=10); stan.temperatura = alg.T_zero; params = Parametry(liczba_krokow=10_000); n = uruchom_sa!(stan, params, alg); @assert n isa Int "uruchom_sa! should return Int, got $(typeof(n))"; @assert n < params.liczba_krokow "patience early-stop not triggered: n=$n vs liczba_krokow=$(params.liczba_krokow)"; @assert stan.iteracja == n; @assert sort(stan.trasa) == collect(1:20); println("OK uruchom_sa! ALG-06 patience early-stop: n=", n, " (< 10000); Hamilton OK")'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^function uruchom_sa!' src/algorytmy/simulowane_wyzarzanie.jl` zwraca 1
    - `grep -c 'licznik_bez_poprawy' src/algorytmy/simulowane_wyzarzanie.jl` zwraca przynajmniej 4 (init, decrement check, reset, inkrement)
    - `grep -c 'energia_min' src/algorytmy/simulowane_wyzarzanie.jl` zwraca przynajmniej 3 (init, comparison, update)
    - `grep -c 'while stan.iteracja < params.liczba_krokow && licznik_bez_poprawy < alg.cierpliwosc' src/algorytmy/simulowane_wyzarzanie.jl` zwraca 1 (D-04 stop dokladnie)
    - `grep -c 'uruchom_sa!' src/JuliaCity.jl` zwraca przynajmniej 1 (export)
    - `julia --project=. -e 'using JuliaCity; @assert isdefined(JuliaCity, :uruchom_sa!)'` exits 0
    - Plik src/algorytmy/simulowane_wyzarzanie.jl ma >= 90 linii (z dodatkiem uruchom_sa!): `awk 'END {print NR}' src/algorytmy/simulowane_wyzarzanie.jl` >= 90
    - Smoke test patience=10 powoduje wczesny stop (n_krokow < 10_000): smoke z verify exits 0
    - Phase 1 testy nadal przechodza: `julia --project=. -e 'using Pkg; Pkg.test()'` exits 0
  </acceptance_criteria>
  <done>
    src/algorytmy/simulowane_wyzarzanie.jl zawiera funkcje `uruchom_sa!` ktora implementuje ALG-06 stop criterion zgodnie z D-04 (reset licznik_bez_poprawy tylko przy strict Δ < 0; stop OR-of: licznik >= cierpliwosc | iteracja >= liczba_krokow). Eksportowana z JuliaCity. Smoke test z patience=10 dowodzi early-stop dziala.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| symuluj_krok! -> stan internals | Mutates stan.trasa, stan.energia, stan.temperatura, stan.iteracja. Wymaga ze stan jest sprawnie zainicjowany (po inicjuj_nn! i ustawieniu stan.temperatura = alg.T_zero) |
| stan.rng -> Metropolis | Deterministyczne dla single master seed (D-09). Multi-thread determinism gwarantowany przez D-12 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering | symuluj_krok! z niezainicjowana stan (temperatura == 0) | accept | exp(-delta/0) = exp(-Inf) = 0; Metropolis nigdy nie zaakceptuje worsening; algorytm bedzie greedy, ale Hamilton invariant zachowany. Test plan 02-05 sprawdza Init flow before krok |
| T-02-11 | Tampering | i, j poza zakresem | mitigate | @assert 1 <= i < j <= n; rand(stan.rng, 1:(n-1)) i rand(stan.rng, (i+2):n) gwarantuja zakres |
| T-02-12 | Information Disclosure | stan.rng state leak | accept | Nie ma sekretow; stan.rng to deterministic Xoshiro |

Severity: brak high. Algorytmiczna funkcja, brak network/secrets.
</threat_model>

<verification>
Per-task automated checks (powyzej w `<verify>` blokach).

**Plan-level integration check:**

```bash
# Smoke test - symuluj_krok! 1000 krokow + Hamilton invariant
julia --project=. -e '
using JuliaCity, Random
pkty = generuj_punkty(50; seed=42)
stan = StanSymulacji(pkty; rng=Xoshiro(42))
inicjuj_nn!(stan)
energia_nn = stan.energia
alg = SimAnnealing(stan; alfa=0.9999, cierpliwosc=5000)
stan.temperatura = alg.T_zero
params = Parametry(liczba_krokow=1000)
for k in 1:1000
    symuluj_krok!(stan, params, alg)
    if k % 100 == 0
        @assert sort(stan.trasa) == collect(1:50) "Hamilton invariant broken at k=$k"
    end
end
@assert stan.iteracja == 1000
@assert stan.temperatura < alg.T_zero
@assert stan.energia <= energia_nn  # SA powinno zmniejszac energie (ale moze byc rownie)
println("OK Plan 02-04: 1000 krokow, Hamilton OK, energia: ", round(energia_nn, digits=4), " -> ", round(stan.energia, digits=4))
'

# Pkg.test - wszystkie istniejace testy nadal zielone
julia --project=. -e 'using Pkg; Pkg.test()'

# Determinism smoke (Plan 02-05 robi pelny test, tu tylko sanity)
julia --project=. -e '
using JuliaCity, Random
function run_sa(seed, krokow)
    pkty = generuj_punkty(20; seed=42)
    stan = StanSymulacji(pkty; rng=Xoshiro(seed))
    inicjuj_nn!(stan)
    alg = SimAnnealing(stan)
    stan.temperatura = alg.T_zero
    params = Parametry(liczba_krokow=krokow)
    for _ in 1:krokow
        symuluj_krok!(stan, params, alg)
    end
    return copy(stan.trasa), stan.energia
end
t1, e1 = run_sa(42, 500)
t2, e2 = run_sa(42, 500)
@assert t1 == t2 "determinism broken: trasa differs"
@assert e1 == e2 "determinism broken: energia differs"
println("OK Determinism: same Xoshiro(42) -> same trajectory")
'
```
</verification>

<success_criteria>
1. **src/algorytmy/simulowane_wyzarzanie.jl istnieje, zawiera struct SimAnnealing <: Algorytm + kwarg constructor + symuluj_krok! + uruchom_sa! (outer-loop helper). UTF-8 NFC bez BOM, ASCII filename.**
2. **src/algorytmy/.gitkeep zostal usuniety (pierwszy plik dotaczyl).**
3. **SimAnnealing pola: T_zero::Float64, alfa::Float64, cierpliwosc::Int (wszystkie concrete - Pitfall 1).**
4. **Konstruktor SimAnnealing(stan; alfa=0.9999, cierpliwosc=5000, T_zero=kalibruj_T0(stan)) dziala (D-03 default kwarg).**
5. **symuluj_krok! mutuje stan in-place (trasa, energia, temperatura, iteracja); cooling AFTER Metropolis (D-04); reverse!(view) (D-07); return nothing literal.**
6. **Po 1000 krokow Hamilton invariant zachowany (sort(stan.trasa) == 1:n).**
7. **Determinizm same-process: same seed -> same trajektoria (single master RNG per D-09).**
8. **src/JuliaCity.jl: include + eksport SimAnnealing, symuluj_krok!, uruchom_sa!.**
9. **Pkg.test() przechodzi (Phase 1 + Wave 0 testy zachowane).**
10. **uruchom_sa!(stan, params, alg) implementuje ALG-06 stop criterion per D-04 substantywnie: licznik_bez_poprawy lokalny (nie pole Stan), reset tylko przy strict Δ < 0, stop gdy `licznik_bez_poprawy >= alg.cierpliwosc OR stan.iteracja >= params.liczba_krokow`. Smoke z patience=10 dowodzi early-stop. Test ALG-06 jest w Plan 02-05.**
</success_criteria>

<output>
After completion, create `.planning/phases/02-energy-sa-algorithm-test-suite/02-04-SUMMARY.md` zawierajace:

- Liczbe linii w `src/algorytmy/simulowane_wyzarzanie.jl` (sanity check >= 60)
- T_zero kalibrowany dla N=20 seed=42 (jako referencja dla TEST-08 golden value w 02-05)
- Energia po 1000 krokach SA na N=20 seed=42 (sanity check, NIE golden value - golden bedzie w 02-05 z StableRNG(42))
- Czy `.gitkeep` zostal usuniety
- Czy SimAnnealing <: Algorytm (smoke check Holy-traits)
</output>
