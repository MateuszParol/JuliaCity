---
phase: 02-energy-sa-algorithm-test-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Project.toml
  - src/typy.jl
  - src/JuliaCity.jl
  - test/runtests.jl
autonomous: true
requirements:
  - ALG-01
tags:
  - julia
  - dependencies
  - types
  - smoke-test

must_haves:
  truths:
    - "Project.toml deklaruje ChunkSplitters i Statistics w [deps] z poprawnymi UUID"
    - "Compat dla ChunkSplitters jest ustawiony na \"3\"; Statistics nie ma compat (stdlib)"
    - "src/typy.jl eksportuje strukturę Parametry z polem liczba_krokow::Int = 50_000"
    - "src/JuliaCity.jl importuje ChunkSplitters i Statistics.std oraz eksportuje Parametry"
    - "test/runtests.jl zawiera testset \"Wave 0: StableRNG ↔ Punkt2D smoke\" który przechodzi"
  artifacts:
    - path: "Project.toml"
      provides: "ChunkSplitters + Statistics w [deps]; ChunkSplitters w [compat]"
      contains: "ChunkSplitters = \"ae650224-84b6-46f8-82ea-d812ca08434e\""
    - path: "src/typy.jl"
      provides: "Base.@kwdef struct Parametry z liczba_krokow"
      contains: "Base.@kwdef struct Parametry"
    - path: "src/JuliaCity.jl"
      provides: "module-level using ChunkSplitters i using Statistics: std; export Parametry"
      contains: "using ChunkSplitters"
    - path: "test/runtests.jl"
      provides: "Wave 0 smoke testset weryfikujący StableRNG ↔ Punkt2D"
      contains: "Wave 0: StableRNG"
  key_links:
    - from: "src/typy.jl::Parametry"
      to: "src/JuliaCity.jl::export"
      via: "include + export"
      pattern: "export.*Parametry"
    - from: "test/runtests.jl::Wave 0 smoke"
      to: "src/punkty.jl::generuj_punkty(n, rng::AbstractRNG)"
      via: "rand(StableRNG(42), Punkt2D, n) dispatch"
      pattern: "generuj_punkty\\(5, StableRNG"
---

<objective>
Wprowadzić fundament Phase 2: zależności (`ChunkSplitters`, `Statistics`), strukturę `Parametry` w `src/typy.jl`, oraz Wave 0 smoke test weryfikujący że `rand(StableRNG(42), Punkt2D, n)` faktycznie działa (krytyczne dla TEST-08 golden values).

Purpose: Wszystkie kolejne plany (02-02..02-06) zakładają że ChunkSplitters jest dostępny w `using`-zone `JuliaCity` oraz że `Statistics.std` jest wywoływalny w `kalibruj_T0`. Smoke test odbywa się NA POCZĄTKU bo jeśli `rand(StableRNG, Punkt2D, n)` failuje, musimy zmienić `src/punkty.jl::generuj_punkty(n, rng)` PRZED napisaniem TEST-08 referenced-value test. Pokrywa też ALG-01 częściowo (deklaracja `Parametry`).

Output:
- `Project.toml` z dodanymi `ChunkSplitters`/`Statistics` w `[deps]` i `ChunkSplitters = "3"` w `[compat]`
- `src/typy.jl` z dodaną `Base.@kwdef struct Parametry`
- `src/JuliaCity.jl` z `using ChunkSplitters`, `using Statistics: std`, eksport `Parametry`
- `test/runtests.jl` z dodanym Wave 0 smoke testsetem
- Dowód: `julia --project=. -e 'using Pkg; Pkg.test()'` zielony (Phase 1 testy + Wave 0)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-CONTEXT.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-RESEARCH.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-PATTERNS.md
@CLAUDE.md
@Project.toml
@src/typy.jl
@src/JuliaCity.jl
@src/punkty.jl
@test/runtests.jl

<interfaces>
<!-- Kontrakty Phase 1 które rozszerzamy. Executor NIE musi szukać; wszystko jest tutaj. -->

From src/typy.jl:
```julia
const Punkt2D = Point2{Float64}
abstract type Algorytm end
mutable struct StanSymulacji{R<:AbstractRNG}
    const punkty::Vector{Punkt2D}
    const D::Matrix{Float64}
    const rng::R
    trasa::Vector{Int}
    energia::Float64
    temperatura::Float64
    iteracja::Int
end
function StanSymulacji(punkty::Vector{Punkt2D}; rng::R=Xoshiro(42)) where {R<:AbstractRNG}
```

From src/JuliaCity.jl:
```julia
module JuliaCity
using GeometryBasics: Point2
using Random
include("typy.jl")
include("punkty.jl")
export Punkt2D, StanSymulacji, Algorytm, generuj_punkty
end
```

From src/punkty.jl:
```julia
function generuj_punkty(n::Int=1000; seed::Integer=42)::Vector{Punkt2D}
function generuj_punkty(n::Int, rng::AbstractRNG)::Vector{Punkt2D}
```

CONTEXT.md D-01 (Parametry shape):
```julia
Base.@kwdef struct Parametry
    liczba_krokow::Int = 50_000
end
```

UUID-y zweryfikowane w RESEARCH.md §Sources:
- ChunkSplitters: `ae650224-84b6-46f8-82ea-d812ca08434e` (compat = "3")
- Statistics: `10745b16-79ce-11e8-11f9-7d13ad32a3b2` (stdlib, brak compat)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dodać ChunkSplitters i Statistics do Project.toml</name>
  <files>Project.toml</files>
  <read_first>
    - Project.toml (cały plik — 34 linii)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-RESEARCH.md sekcje "Standard Stack" + "Critical version note" + "Sources" (linie 36-77 + 740-745)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-PATTERNS.md sekcja "Project.toml (MODIFIED)" (linie 606-678)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-CONTEXT.md decyzje D-11 (ChunkSplitters lock) + sekcja "Integration Points" (linie 240-246)
  </read_first>
  <action>
    Zmodyfikować `Project.toml`:

    1. **Dodać do `[deps]`** (alfabetyczna kolejność — wstawić ChunkSplitters PRZED GeometryBasics, Statistics PO Random):
       ```toml
       ChunkSplitters = "ae650224-84b6-46f8-82ea-d812ca08434e"
       GeometryBasics = "5c1252a2-5f33-56bf-86c9-59e7332b4326"
       Random = "9a3f8284-a2c9-5f02-9a11-845980a1fd5c"
       Statistics = "10745b16-79ce-11e8-11f9-7d13ad32a3b2"
       ```

    2. **Dodać do `[compat]`** (alfabetyczna kolejność, `julia` na początku — wstawić ChunkSplitters PRZED GLMakie):
       ```toml
       [compat]
       julia = "1.10"
       Aqua = "0.8.14"
       BenchmarkTools = "1.6"
       ChunkSplitters = "3"
       GLMakie = "0.24"
       GeometryBasics = "0.5"
       JET = "0.9"
       Makie = "0.24"
       Observables = "0.5"
       StableRNGs = "1.0"
       ```
       **WAŻNE — JET pozostaje przy 0.9** (NIE bumpować na 0.11 — research-flagged: 0.11 wymaga Julia 1.12, a my mamy locked julia="1.10"). Jeśli istniejący compat ma inną kolejność, zachować formatowanie Phase 1 (julia=`"1.10"` pierwsze, reszta alfabetycznie LUB w istniejącej kolejności — nie reorganizować ponad to co konieczne; tylko dodać ChunkSplitters w odpowiednim miejscu).

    3. **NIE modyfikować** `[extras]` ani `[targets]` w tym plan-ie — `PerformanceTestTools` przyjdzie w 02-06.

    4. **NIE modyfikować** `Statistics` w `[compat]` — Statistics to stdlib (Julia 1.10+ guarantee), Aqua wymaga `deps_compat = (ignore = [:Random, :Statistics], ...)` zamiast wpisu w `[compat]` (per Pattern D w 02-PATTERNS.md i RESEARCH §Wave 0 Gaps line 700).

    5. Po edycji uruchomić `julia --project=. -e 'using Pkg; Pkg.instantiate(); Pkg.resolve()'` aby Manifest.toml się zaktualizował z nowymi depami. Jeśli `Pkg.instantiate()` failuje ("ChunkSplitters not registered" - mało prawdopodobne, ale możliwe), zweryfikować UUID przez `julia --project=. -e 'using Pkg; Pkg.add("ChunkSplitters")'`.
  </action>
  <verify>
    <automated>julia --project=. -e 'using Pkg; v = Pkg.dependencies()[Base.UUID("ae650224-84b6-46f8-82ea-d812ca08434e")].version; @assert v >= v"3.0.0" "ChunkSplitters version too old: $v"; println("OK ChunkSplitters: ", v)'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^ChunkSplitters = "ae650224' Project.toml` zwraca 1
    - `grep -c '^Statistics = "10745b16' Project.toml` zwraca 1
    - `grep '^ChunkSplitters = "3"' Project.toml` zwraca jedną linię (compat entry)
    - `grep '^Statistics' Project.toml | grep -v '^Statistics = "10745b16'` zwraca pusty output (NIE ma compat entry dla Statistics)
    - `julia --project=. -e 'using Pkg; Pkg.instantiate(); using ChunkSplitters; using Statistics; println("loaded ok")'` exits 0
    - JET nadal na 0.9: `grep '^JET = "0.9"' Project.toml` zwraca jedną linię
  </acceptance_criteria>
  <done>
    Project.toml ma ChunkSplitters i Statistics w [deps] z poprawnymi UUID, ChunkSplitters w [compat]="3", Statistics nie ma compat. `Pkg.instantiate()` przechodzi. JET pozostaje na 0.9.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dodać Parametry do src/typy.jl + rozbudować imports w src/JuliaCity.jl</name>
  <files>src/typy.jl, src/JuliaCity.jl</files>
  <read_first>
    - src/typy.jl (cały plik — 78 linii)
    - src/JuliaCity.jl (cały plik — 33 linii)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-CONTEXT.md decyzje D-01 + D-02 (Parametry shape + defaults) + sekcja "Integration Points" linia 238
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-PATTERNS.md sekcja "src/typy.jl (MODIFIED)" (linie 494-525) i "src/JuliaCity.jl (MODIFIED)" (linie 422-491)
    - .planning/phases/01-bootstrap-core-types-points/01-CONTEXT.md sekcje D-22..D-24 (Polish/English split, ASCII identifiers)
  </read_first>
  <action>
    **A. Modyfikacja `src/typy.jl` — dodać `Parametry` AFTER `StanSymulacji` block:**

    Na końcu pliku (po linii 78, czyli po definicji konstruktora `StanSymulacji`), dodać:

    ```julia

    """
        Parametry

    Hiperparametry niezależne od algorytmu (D-01). Slot na `kroki_na_klatke` w Phase 3
    (wizualizacja). `liczba_krokow` to budżet kroków SA — domyślnie 50_000 dla N=1000
    (D-02: α^50_000 ≈ 6.7×10⁻³ daje schemat chłodzenia z ~80% acceptance ratio na
    starcie do ~1% pod koniec).

    # Pola
    - `liczba_krokow::Int = 50_000` — twardy cap kroków (D-02, D-04)
    """
    Base.@kwdef struct Parametry
        liczba_krokow::Int = 50_000
    end
    ```

    Konwencje (per 01-CONTEXT.md D-22..D-24):
    - Docstring po polsku z polskimi diakrytykami (NFC).
    - Identyfikatory ASCII (`liczba_krokow`, NIE `liczba_kroków`).
    - Brak modyfikacji `StanSymulacji` (Phase 1 D-06 hard lock).

    **B. Modyfikacja `src/JuliaCity.jl` — dodać using imports + rozszerzyć export:**

    Zaktualizować plik tak by sekcja `using` zawierała:
    ```julia
    # Zewnętrzne zależności runtime
    using GeometryBasics: Point2
    using Random
    using ChunkSplitters                       # Phase 2 (D-11) — chunked threading
    using Statistics: std                      # Phase 2 — kalibruj_T0 używa std()
    ```

    Eksport rozszerzyć o `Parametry`:
    ```julia
    export Punkt2D, StanSymulacji, Algorytm, generuj_punkty,
           Parametry
    ```

    NIE dodawać jeszcze `include("energia.jl")` ani innych — to przyjdzie w 02-02..02-04.
    NIE dodawać do exportu jeszcze `oblicz_energie`, `SimAnnealing`, etc. — będą dodawane stopniowo w kolejnych planach.

    Zachować Polish prose comments nad każdym blokiem `include` / `export` (per Phase 1 konwencja).
  </action>
  <verify>
    <automated>julia --project=. -e 'using JuliaCity; @assert isdefined(JuliaCity, :Parametry) "Parametry missing"; p = JuliaCity.Parametry(); @assert p.liczba_krokow == 50_000 "wrong default"; @assert isdefined(JuliaCity, :ChunkSplitters) "ChunkSplitters not in scope" || true; println("OK Parametry default: ", p.liczba_krokow)'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Base.@kwdef struct Parametry' src/typy.jl` zwraca 1
    - `grep -c 'liczba_krokow::Int = 50_000' src/typy.jl` zwraca 1
    - `grep -c 'using ChunkSplitters' src/JuliaCity.jl` zwraca 1
    - `grep -c 'using Statistics: std' src/JuliaCity.jl` zwraca 1
    - `grep -c 'Parametry' src/JuliaCity.jl` zwraca przynajmniej 1 (export linia)
    - `julia --project=. -e 'using JuliaCity; p = Parametry(); @assert p.liczba_krokow == 50_000'` exits 0
    - `julia --project=. -e 'using JuliaCity; p = Parametry(liczba_krokow=100); @assert p.liczba_krokow == 100'` exits 0 (kwarg constructor works)
    - StanSymulacji shape niezmieniony: `julia --project=. -e 'using JuliaCity; pkty=generuj_punkty(5;seed=1); s=StanSymulacji(pkty); @assert s.iteracja==0 && s.energia==0.0'` exits 0
  </acceptance_criteria>
  <done>
    `Parametry` jest dostępna jako `JuliaCity.Parametry`, default `liczba_krokow=50_000`, kwarg constructor działa. `using ChunkSplitters` i `using Statistics: std` są w `src/JuliaCity.jl`. `StanSymulacji` shape niezmieniony.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 smoke test — StableRNG ↔ Punkt2D w test/runtests.jl</name>
  <files>test/runtests.jl</files>
  <read_first>
    - test/runtests.jl (cały plik — 195 linii)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-RESEARCH.md sekcja "Pitfall E: rand(StableRNG(42), Punkt2D, 20)" (linie 361-384)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-PATTERNS.md sekcja "Wave 0 smoke" (linie 366-373) i "Test fixture pattern (StableRNG vs Xoshiro)" (linie 754-781)
    - src/punkty.jl (cały plik — 54 linii) — generuj_punkty(n, rng::AbstractRNG) używa rand(rng, Punkt2D, n)
  </read_first>
  <action>
    **Cel:** Zweryfikować EMPIRYCZNIE że `rand(StableRNG(42), Punkt2D, n)` działa (community-evidence — research flagged jako MEDIUM confidence). Jeśli failuje, ten test failuje I wymaga modyfikacji `src/punkty.jl` (fallback comprehension `[Punkt2D(rand(rng,Float64), rand(rng,Float64)) for _ in 1:n]`) PRZED napisaniem TEST-08 golden values w 02-05.

    **Modyfikacja `test/runtests.jl`:**

    1. **Dodać `using StableRNGs`** do top-of-file imports (linia ~13, po `using JET`):
       ```julia
       using StableRNGs
       ```

    2. **Dodać nowy testset PRZED `@testset "Aqua.jl quality"`** (czyli w okolicy linii 158-160, po sekcji 4 StanSymulacji konstruktor — przed sekcją Aqua). Dodać sekcję dzielącą i testset:

       ```julia
       # ─────────────────────────────────────────────────────────────────────────
       # 5. Wave 0 smoke: StableRNG ↔ Punkt2D dispatch (Phase 2 Plan 02-01)
       # Krytyczny PRZED TEST-08 (golden value w Phase 2 Plan 02-05).
       # Research-flagged jako MEDIUM confidence (Pitfall E w 02-RESEARCH.md):
       # `rand(StableRNG, Point2{Float64}, n)` działa via GeometryBasics' Random.SamplerType
       # dispatch — community-evidence, niezweryfikowane oficjalnie. Ten test gwarantuje
       # że Phase 1 src/punkty.jl::generuj_punkty(n, rng) NIE wymaga fallbacku.
       # ─────────────────────────────────────────────────────────────────────────
       @testset "Wave 0: StableRNG ↔ Punkt2D smoke (Plan 02-01)" begin
           pkty = generuj_punkty(5, StableRNG(42))
           @test eltype(pkty) == Punkt2D
           @test length(pkty) == 5
           # determinizm w obrębie tej samej wersji StableRNGs
           @test generuj_punkty(5, StableRNG(42)) == generuj_punkty(5, StableRNG(42))
           # dwa różne seedy dają różne wyniki
           @test generuj_punkty(5, StableRNG(42)) != generuj_punkty(5, StableRNG(43))
           # punkty w [0,1]² (jak generuj_punkty z Xoshiro)
           @test all(p -> 0.0 <= p[1] <= 1.0 && 0.0 <= p[2] <= 1.0, pkty)
       end
       ```

       **WAŻNE — RENUMEROWAĆ pozostałe sekcje:** "5. Aqua.jl" → "6. Aqua.jl", "6. JET smoke" → "7. JET smoke" (zachować spójność komentarzy section-divider).

    3. **NIE modyfikować** istniejących testsetów (encoding hygiene, generuj_punkty, RNG isolation, StanSymulacji konstruktor, Aqua, JET smoke). Tylko wstawić Wave 0 między sekcję 4 a Aqua, oraz przenumerować Aqua/JET.

    4. **Jeśli ten test failuje** (Wave 0 redzieje wyjątek `MethodError: no method matching rand(::StableRNG, ::Random.SamplerType{Point2{Float64}})`):
       - **STOP**, nie kontynuować plan-em 02-02.
       - Zgłosić problem do orchestratora przez wynik wykonania (test fail).
       - Mitigacja jest w `src/punkty.jl` — zastąpić `return rand(rng, Punkt2D, n)` przez `return [Punkt2D(rand(rng, Float64), rand(rng, Float64)) for _ in 1:n]`. Ale to zmieni stream (dwa skalarne calls) i może przesunąć Phase 1 PKT golden values — wymaga osobnej decyzji.
       - **Jeśli test PRZECHODZI**: Phase 1 implementacja jest OK, kontynuujemy Phase 2 bez zmian w `src/punkty.jl`.
  </action>
  <verify>
    <automated>julia --project=. -e 'using Pkg; Pkg.test()' 2>&1 | grep -E "Test Summary:|Wave 0|Pass:|Fail:" | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'using StableRNGs' test/runtests.jl` zwraca 1
    - `grep -c 'Wave 0: StableRNG' test/runtests.jl` zwraca przynajmniej 1
    - `grep -c 'generuj_punkty(5, StableRNG(42))' test/runtests.jl` zwraca przynajmniej 2 (test smoke + determinism)
    - `julia --project=. -e 'using Pkg; Pkg.test()'` exits 0 (wszystkie istniejące Phase 1 testy + Wave 0 smoke przechodzą)
    - Output testu zawiera linię `Wave 0: StableRNG ↔ Punkt2D smoke (Plan 02-01)` z `Pass:` (np. 5 passes)
    - Sekcje numerowane: encoding=1, generuj_punkty=2, no global RNG=3, StanSymulacji=4, Wave 0=5, Aqua=6, JET=7 (lub odpowiednio)
  </acceptance_criteria>
  <done>
    `test/runtests.jl` zawiera Wave 0 smoke testset, który zielony zatwierdza że `rand(StableRNG(42), Punkt2D, n)` działa. Wszystkie inne Phase 1 testy zachowane bez modyfikacji semantyki. `Pkg.test()` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → src code | Wave 0 smoke testuje że `rand(StableRNG, Punkt2D, n)` nie wybucha; brak external boundary |
| Project.toml → Pkg resolver | Dodanie deps wymaga że UUID-y są poprawne; resolver odrzuca błędne UUID |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Project.toml UUID | accept | UUID-y zweryfikowane w RESEARCH.md §Sources line 743; `Pkg.instantiate()` katastrofuje przy mismatch |
| T-02-02 | Information Disclosure | Wave 0 smoke test fail trail | accept | Test fail jest sygnałem dla executora żeby przejść do mitigacji (fallback comprehension w `src/punkty.jl`) — to nie security risk |
| T-02-03 | Denial of Service | `Parametry` constructor z bardzo dużym `liczba_krokow` | accept | `Parametry(liczba_krokow=10^9)` to legalny ale głupi user input; nie jest atak; brak validation needed |

Severity assessment: brak high. Phase 2 jest pure-algorithmic + tests — żadne external input, żadne secrets, żadne network. Block-on-high passes.
</threat_model>

<verification>
**Per-task automated checks** (powyżej w `<verify>` blokach każdego task-u).

**Plan-level integration check:**

```bash
julia --project=. -e 'using Pkg; Pkg.instantiate(); Pkg.resolve(); Pkg.test()'
```

Musi zwrócić exit code 0 z output zawierającym:
- Wszystkie Phase 1 testy (encoding, PKT, StanSymulacji, Aqua, JET smoke) — zielone
- Wave 0 testset zielony (5 passes minimum)

**Sanity checks:**

```bash
# UUID + version dla ChunkSplitters
julia --project=. -e 'using Pkg; v = Pkg.dependencies()[Base.UUID("ae650224-84b6-46f8-82ea-d812ca08434e")].version; @assert v >= v"3.0.0"; println("ChunkSplitters: ", v)'

# Parametry default
julia --project=. -e 'using JuliaCity; p = Parametry(); @assert p.liczba_krokow == 50_000; println("Parametry OK")'

# Statistics dostępne
julia --project=. -e 'using JuliaCity; @assert std([1.0,2.0,3.0]) ≈ 1.0; println("Statistics.std OK")'
```
</verification>

<success_criteria>
1. **Project.toml zawiera ChunkSplitters w [deps] (UUID `ae650224-...`) i [compat] (`"3"`); Statistics w [deps] (UUID `10745b16-...`), brak compat dla Statistics; JET nadal na "0.9".**
2. **`julia --project=. -e 'using Pkg; Pkg.instantiate()'` exits 0 — wszystkie deps dostępne.**
3. **`src/typy.jl` zawiera `Base.@kwdef struct Parametry` z `liczba_krokow::Int = 50_000`; StanSymulacji shape niezmieniony.**
4. **`src/JuliaCity.jl` zawiera `using ChunkSplitters` i `using Statistics: std`; eksport rozszerzony o `Parametry`.**
5. **`test/runtests.jl` zawiera Wave 0 smoke testset weryfikujący `rand(StableRNG(42), Punkt2D, n)`; wszystkie Phase 1 testy przechodzą; `Pkg.test()` exits 0.**
</success_criteria>

<output>
After completion, create `.planning/phases/02-energy-sa-algorithm-test-suite/02-01-SUMMARY.md` zawierające:

- Czy Wave 0 smoke przeszedł (jeśli nie — opis fallbacku zastosowanego w `src/punkty.jl`)
- Wersję ChunkSplitters wybraną przez resolver (z `Pkg.dependencies()`)
- Czy Manifest.toml został zaktualizowany (`git diff Manifest.toml | wc -l`)
- Lista sekcji w `test/runtests.jl` (numerowanie po wstawieniu Wave 0)
</output>
