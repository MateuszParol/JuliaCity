---
phase: 02-energy-sa-algorithm-test-suite
plan: 05
type: execute
wave: 5
depends_on: [02-02, 02-03, 02-04]
files_modified:
  - test/test_energia.jl
  - test/test_baselines.jl
  - test/test_symulacja.jl
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
  - TEST-05
  - TEST-08
  - ALG-08
tags:
  - julia
  - tests
  - golden-value
  - allocation
  - determinism

must_haves:
  truths:
    - "test/test_energia.jl pokrywa ENE-01..05 + czesciowo TEST-02/TEST-03 (pure functions)"
    - "test/test_baselines.jl pokrywa ALG-04 + TEST-05 (NN-baseline-beat ≥10%)"
    - "test/test_symulacja.jl pokrywa ALG-01..03/06..08 + TEST-01/04/08"
    - "Kazdy test_*.jl plik moze byc wlaczony przez include z runtests.jl (sibling paths)"
    - "Test N=4 jednostkowy kwadrat: oblicz_energie(...) ≈ 4.0 (Roadmap SC-1)"
    - "TEST-03 @allocated == 0 uzywa helper function _alloc_test_helper (Pitfall A)"
    - "TEST-04 multi-thread determinism uzywa PerformanceTestTools.@include_foreach + in-process fast fallback"
    - "TEST-08 golden value: hardcoded reference dla StableRNG(42) N=20 1000 krokow (D-17)"
  artifacts:
    - path: "test/test_energia.jl"
      provides: "Testset dla oblicz_energie/delta_energii/kalibruj_T0/oblicz_macierz_dystans!"
      contains: "@testset"
      min_lines: 80
    - path: "test/test_baselines.jl"
      provides: "Testset dla trasa_nn + inicjuj_nn! + TEST-05 NN-baseline-beat"
      contains: "TEST-05"
      min_lines: 60
    - path: "test/test_symulacja.jl"
      provides: "Testset dla SimAnnealing + symuluj_krok! + TEST-01/03/04/08"
      contains: "TEST-08"
      min_lines: 100
  key_links:
    - from: "test/test_symulacja.jl::TEST-04"
      to: "PerformanceTestTools.@include_foreach"
      via: "subprocess z env JULIA_NUM_THREADS"
      pattern: "@include_foreach"
    - from: "test/test_energia.jl::ENE-03"
      to: "_alloc_test_helper(...)"
      via: "wrapping @allocated w funkcji helper (Pitfall A)"
      pattern: "_alloc_test_helper"
    - from: "test/test_baselines.jl::TEST-05"
      to: "stan.energia / energia_nn <= 0.9"
      via: "asercja jakosci SA vs NN"
      pattern: "energia_nn"
---

<objective>
Utworzyc 3 nowe pliki testowe pokrywajace REQ TEST-01..05, TEST-08 oraz ENE-* / ALG-* na poziomie unit. TEST-06 (Aqua) i TEST-07 (JET) sa orchestrowane w runtests.jl (Plan 02-06). Sa to uniwersalne pliki - moga byc wlaczone przez `include("test_energia.jl")` z runtests.jl LUB uruchamiane standalone.

Purpose: Daje Phase 2 wlasnie ta gwarancje ktora goal-criterion #5 wymaga: zero failures w `julia --project=. test/runtests.jl` z testset-ami dla Hamilton, @inferred, @allocated==0, multi-thread determinism, NN-baseline-beat, golden-value StableRNG. To jest serce TDD-equivalent quality gate dla Phase 2.

Output:
- test/test_energia.jl - ENE-01..05 + TEST-02 czesciowo + TEST-03 czesciowo (oblicz_energie < 4096B)
- test/test_baselines.jl - ALG-04 + TEST-05 (NN-baseline-beat ≥10% pod NN dla N=1000 seed=42)
- test/test_symulacja.jl - ALG-01..03/06..08 + TEST-01 (Hamilton) + TEST-04 (multi-thread) + TEST-08 (golden)
- TEST-08 golden values: HARDCODED po lokalnym wykonaniu fixture w trakcie execution (executor MUSI to zrobic w tym planie)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-CONTEXT.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-RESEARCH.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-PATTERNS.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-02-SUMMARY.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-03-SUMMARY.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-04-SUMMARY.md
@CLAUDE.md
@src/typy.jl
@src/punkty.jl
@src/energia.jl
@src/baselines.jl
@src/algorytmy/simulowane_wyzarzanie.jl
@test/runtests.jl

<interfaces>
<!-- Pelne API surface po 02-01..02-04. Executor ma wszystko. -->

Public API exported z JuliaCity:
```julia
const Punkt2D = Point2{Float64}
abstract type Algorytm end
mutable struct StanSymulacji{R<:AbstractRNG}
function StanSymulacji(punkty; rng=Xoshiro(42))

Base.@kwdef struct Parametry
    liczba_krokow::Int = 50_000
end

function generuj_punkty(n=1000; seed=42)
function generuj_punkty(n, rng::AbstractRNG)

function oblicz_macierz_dystans!(stan)::Nothing
function oblicz_energie(D::Matrix{Float64}, trasa::Vector{Int}, bufor::Vector{Float64})::Float64
function oblicz_energie(punkty::Vector{Punkt2D}, trasa::Vector{Int})::Float64
function delta_energii(stan, i, j)::Float64
function kalibruj_T0(stan; n_probek=1000, rng=stan.rng)::Float64

function trasa_nn(D; start=1)::Vector{Int}
function inicjuj_nn!(stan)::Nothing

struct SimAnnealing <: Algorytm
    T_zero::Float64
    alfa::Float64
    cierpliwosc::Int
end
function SimAnnealing(stan; alfa=0.9999, cierpliwosc=5000, T_zero=kalibruj_T0(stan))
function symuluj_krok!(stan, params, alg::SimAnnealing)::Nothing
```

CONTEXT.md D-12 (TEST-04 semantyka - LOCKED):
- `stan_1.trasa == stan_n.trasa` (bit-identical)
- `isapprox(stan_1.energia, stan_n.energia; rtol=1e-12)` (sub-ULP differ tolerated)

CONTEXT.md D-16 (Tiered fixtures):
- N=4 jednostkowy kwadrat: oblicz_energie([Punkt2D(0,0),Punkt2D(1,0),Punkt2D(1,1),Punkt2D(0,1)], [1,2,3,4]) ≈ 4.0
- N=20 + StableRNG(42): TEST-08 golden + TEST-02 @inferred + TEST-03 @allocated==0
- N=1000 + Xoshiro(42): TEST-05 NN-baseline-beat + TEST-04 multi-thread

CONTEXT.md D-17 (TEST-08 pattern):
```julia
using StableRNGs
punkty = generuj_punkty(20, StableRNG(42))
stan = StanSymulacji(punkty; rng=StableRNG(42))
inicjuj_nn!(stan)
alg = SimAnnealing(stan; alfa=0.9999, cierpliwosc=5000, T_zero=kalibruj_T0(stan; rng=stan.rng))
stan.temperatura = alg.T_zero  # MANUAL po SimAnnealing constructor
params = Parametry(liczba_krokow=1000)
for _ in 1:1000; symuluj_krok!(stan, params, alg); end
@test stan.trasa == [HARDCODED_REF]   # Executor lokalnie generuje raz
@test isapprox(stan.energia, HARDCODED_FLOAT; rtol=1e-6)
```

RESEARCH Pitfall A (@allocated helper - LOCKED):
```julia
function _alloc_test_helper(stan, params, alg)
    return @allocated symuluj_krok!(stan, params, alg)
end
# warmup
for _ in 1:3
    symuluj_krok!(stan, params, alg)
end
@test _alloc_test_helper(stan, params, alg) == 0
```

RESEARCH Pitfall G (TEST-05 CI cost - rekomendacja):
liczba_krokow=20_000 (start; podnies do 50_000 jezeli failuje na CI)

RESEARCH Open Question 3 (TEST-04 vs TEST-05 budgets):
- TEST-04: liczba_krokow=5_000 (determinizm sprawdzalny szybciej, nie wymaga konwergencji)
- TEST-05: liczba_krokow=20_000 (jakosc; jest deterministic single-seed - binary outcome, no flakiness)

RESEARCH Example 3 (TEST-04 PerformanceTestTools pattern - linie 491-530):
```julia
using PerformanceTestTools, Serialization

sa_run_script = """
using JuliaCity, Random, Serialization
punkty = generuj_punkty(1000; seed=42)
stan = StanSymulacji(punkty; rng=Xoshiro(42))
inicjuj_nn!(stan)
alg = SimAnnealing(stan)
stan.temperatura = alg.T_zero
params = Parametry(liczba_krokow=5_000)
for _ in 1:params.liczba_krokow
    symuluj_krok!(stan, params, alg)
end
out_path = ENV["JC_OUT"]
serialize(out_path, (trasa=stan.trasa, energia=stan.energia))
"""

script_path = tempname() * ".jl"
write(script_path, sa_run_script)
out_1 = tempname() * ".jls"
out_n = tempname() * ".jls"

PerformanceTestTools.@include_foreach(
    script_path,
    [
        ["JULIA_NUM_THREADS" => "1", "JC_OUT" => out_1],
        ["JULIA_NUM_THREADS" => "8", "JC_OUT" => out_n],
    ]
)
r1 = deserialize(out_1)
rn = deserialize(out_n)
@test r1.trasa == rn.trasa
@test isapprox(r1.energia, rn.energia; rtol=1e-12)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Utworzyc test/test_energia.jl (ENE-01..05 + TEST-02/03 czesciowo)</name>
  <files>test/test_energia.jl</files>
  <read_first>
    - test/runtests.jl (lines 89-156 - testset structure analog z Phase 1)
    - src/energia.jl (cale - sygnatury 4 funkcji)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-PATTERNS.md sekcja "test/test_energia.jl (test / unit)" (linie 234-294)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-RESEARCH.md sekcje "Pitfall A: @allocated" (linie 290-320), "Pitfall B: @inferred Union" (linie 324-336), "Phase Requirements -> Test Map" (linie 660-700)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-CONTEXT.md decyzje D-08 (energy cache invariant), D-16 (tiered fixtures)
  </read_first>
  <action>
    Utworzyc NOWY plik `test/test_energia.jl` (UTF-8, LF, no BOM, NFC; ASCII filename per BOOT-04).

    Top-of-file komentarz:
    ```julia
    # Testset dla src/energia.jl - pokrywa REQ ENE-01..05 + TEST-02/03 czesciowo (pure functions).
    # Wlaczany przez include("test_energia.jl") z test/runtests.jl (Plan 02-06).
    # Mozna tez uruchomic standalone: include("test/test_energia.jl") po `using JuliaCity, Test, Random`.
    # Asercje wewnetrzne po angielsku per LANG-04.
    ```

    Struktura: jeden top-level `@testset "energia (ENE-01..05)"` zawierajacy sub-testset-y.

    **Defensywne imports** (per PATTERNS.md - allows standalone run):
    ```julia
    using Test
    using JuliaCity
    using Random
    using Random: Xoshiro
    ```

    **Sub-testset 1: oblicz_energie - N=4 jednostkowy kwadrat (ENE-01, Roadmap SC-1):**
    ```julia
    @testset "oblicz_energie - jednostkowy kwadrat (ENE-01, Roadmap SC-1)" begin
        punkty = [Punkt2D(0.0, 0.0), Punkt2D(1.0, 0.0), Punkt2D(1.0, 1.0), Punkt2D(0.0, 1.0)]
        trasa = [1, 2, 3, 4]
        @test oblicz_energie(punkty, trasa) ≈ 4.0
        # Permutacja - kazdy cykl Hamiltona daje ta sama energie (cykl jest symetryczny)
        @test oblicz_energie(punkty, [2, 3, 4, 1]) ≈ 4.0
    end
    ```

    **Sub-testset 2: oblicz_energie type-stable + alloc (ENE-02, ENE-03):**
    ```julia
    @testset "oblicz_energie type-stable + < 4096 B (ENE-02, ENE-03)" begin
        punkty = generuj_punkty(20; seed=42)
        trasa = collect(1:20)

        # ENE-02: type-stable
        @test @inferred(oblicz_energie(punkty, trasa)) isa Float64

        # 3-arg hot-path version
        n = length(punkty)
        D = Matrix{Float64}(undef, n, n)
        @inbounds for j in 1:n, i in 1:j-1
            dx = punkty[i][1] - punkty[j][1]
            dy = punkty[i][2] - punkty[j][2]
            D[i,j] = D[j,i] = sqrt(dx*dx + dy*dy)
        end
        @inbounds for j in 1:n; D[j,j] = 0.0; end
        bufor = zeros(Float64, Threads.nthreads())

        @test @inferred(oblicz_energie(D, trasa, bufor)) isa Float64

        # ENE-03: < 4096 B po rozgrzewce. Wrap @allocated w funkcji helper (Pitfall A).
        function _alloc_3arg(D, trasa, bufor)
            return @allocated oblicz_energie(D, trasa, bufor)
        end
        # warmup
        for _ in 1:3
            oblicz_energie(D, trasa, bufor)
        end
        # Hot-path version: powinno byc 0 alloc po warmup (per D-10 - bufor pre-alokowany).
        # Tolerujemy <4096 B per ENE-03 (margines dla closure / @threads bookkeeping).
        @test _alloc_3arg(D, trasa, bufor) < 4096
    end
    ```

    **Sub-testset 3: oblicz_energie chunked threading (ENE-05):**
    ```julia
    @testset "oblicz_energie chunked threading (ENE-05)" begin
        # Strukturalny check - source uzywa Threads.@threads :static + ChunkSplitters.
        # Verifikacja behavioralna: roznych nthreads daje ta sama wartosc (do sub-ULP).
        # Z punktu widzenia testu: liczymy ta sama energie 2x i sprawdzamy ze jest bliska.
        punkty = generuj_punkty(100; seed=42)
        trasa = shuffle!(Xoshiro(7), collect(1:100))
        e1 = oblicz_energie(punkty, trasa)
        e2 = oblicz_energie(punkty, trasa)
        @test e1 == e2  # in-process determinism (no threading variation in single call)
        # multi-thread determinism per JULIA_NUM_THREADS test jest w test_symulacja.jl (TEST-04).
    end
    ```

    **Sub-testset 4: delta_energii O(1) + zero-alloc (ENE-04):**
    ```julia
    @testset "delta_energii O(1) + zero-alloc (ENE-04)" begin
        punkty = generuj_punkty(20; seed=42)
        stan = StanSymulacji(punkty; rng=Xoshiro(42))
        inicjuj_nn!(stan)

        # type-stable
        @test @inferred(delta_energii(stan, 5, 17)) isa Float64

        # zero-alloc po rozgrzewce (helper function - Pitfall A)
        function _alloc_delta(stan, i, j)
            return @allocated delta_energii(stan, i, j)
        end
        for _ in 1:3
            delta_energii(stan, 5, 17)
        end
        @test _alloc_delta(stan, 5, 17) == 0
    end
    ```

    **Sub-testset 5: cache invariant (D-08 implied, ENE-04):**
    ```julia
    @testset "cache invariant: stan.energia += delta zgadza sie z oblicz_energie" begin
        # Po SA krokach z delta-update, stan.energia powinno byc bliskie wynikowi
        # ponownie obliczonemu z oblicz_energie (drift kumulowany +=).
        punkty = generuj_punkty(20; seed=42)
        stan = StanSymulacji(punkty; rng=Xoshiro(42))
        inicjuj_nn!(stan)
        alg = SimAnnealing(stan)
        stan.temperatura = alg.T_zero
        params = Parametry(liczba_krokow=500)
        for _ in 1:500
            symuluj_krok!(stan, params, alg)
        end
        bufor = zeros(Float64, Threads.nthreads())
        e_recompute = oblicz_energie(stan.D, stan.trasa, bufor)
        @test isapprox(stan.energia, e_recompute; rtol=1e-10)
    end
    ```

    **Sub-testset 6: kalibruj_T0 (ALG-05):**
    ```julia
    @testset "kalibruj_T0 zwraca rozsadna wartosc (ALG-05)" begin
        punkty = generuj_punkty(20; seed=42)
        stan = StanSymulacji(punkty; rng=Xoshiro(42))
        inicjuj_nn!(stan)
        T0 = kalibruj_T0(stan; n_probek=1000)
        @test T0 isa Float64
        @test T0 > 0  # 2σ na worsening delts musi byc > 0
        @test T0 < 10  # rozsadny upper bound dla N=20 jednostkowy kwadrat (max delta ≈ 2*sqrt(2))
    end
    ```

    **Sub-testset 7: oblicz_macierz_dystans! (D-08, fundament dla delta_energii):**
    ```julia
    @testset "oblicz_macierz_dystans! - symetria + diagonal" begin
        punkty = generuj_punkty(10; seed=1)
        stan = StanSymulacji(punkty)
        oblicz_macierz_dystans!(stan)
        @test size(stan.D) == (10, 10)
        @test all(stan.D[i, i] == 0.0 for i in 1:10)  # diagonal
        @test all(stan.D[i, j] == stan.D[j, i] for i in 1:10, j in 1:10)  # symmetry
        # spot check: D[1,2] = euklidesowa odleglosc punkty[1] do punkty[2]
        p1, p2 = punkty[1], punkty[2]
        @test stan.D[1, 2] ≈ sqrt((p1[1]-p2[1])^2 + (p1[2]-p2[2])^2)
    end
    ```

    Konwencje:
    - Polish komentarze + sekcyjne dividery `# ──...──` przed niektorymi testset-ami (mirror runtests.jl style).
    - English asserts message (np. `"expected 4.0"`).
    - Kazdy testset ma nazwe formatu `"<funkcja> (REQ-ID-A, REQ-ID-B)"`.
  </action>
  <verify>
    <automated>julia --project=. -e 'using Test, JuliaCity, Random; include("test/test_energia.jl")' 2>&1 | grep -E "Pass:|Fail:|Error:|Test Summary:" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Plik test/test_energia.jl istnieje, >= 80 linii
    - `grep -c '^@testset' test/test_energia.jl` zwraca przynajmniej 6 (jeden top-level + 5+ sub-testset, lub 6+ sub-testset bez top-level)
    - `grep -c 'oblicz_energie' test/test_energia.jl` zwraca przynajmniej 5 (uzycia w roznych testset-ach)
    - `grep -c 'delta_energii' test/test_energia.jl` zwraca przynajmniej 2
    - `grep -c 'kalibruj_T0' test/test_energia.jl` zwraca przynajmniej 1
    - `grep -c '_alloc_' test/test_energia.jl` zwraca przynajmniej 2 (helper functions per Pitfall A)
    - `grep -c '@inferred' test/test_energia.jl` zwraca przynajmniej 2
    - `grep -c 'Punkt2D(0.0' test/test_energia.jl` zwraca przynajmniej 1 (N=4 fixture)
    - `julia --project=. -e 'using Test, JuliaCity, Random; include("test/test_energia.jl")'` exits 0 ze wszystkimi @test passing (zero Fails, zero Errors)
  </acceptance_criteria>
  <done>
    test/test_energia.jl zawiera testset-y dla wszystkich ENE-01..05 + TEST-02/03 czesciowo. Wszystkie testset-y zielone gdy uruchamiane standalone przez include. Polish docs + English asserts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Utworzyc test/test_baselines.jl (ALG-04 + TEST-05)</name>
  <files>test/test_baselines.jl</files>
  <read_first>
    - test/runtests.jl (testset structure)
    - src/baselines.jl (trasa_nn + inicjuj_nn! sygnatury)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-PATTERNS.md sekcja "test/test_baselines.jl (test / unit + integ)" (linie 378-419)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-RESEARCH.md sekcja "Pitfall G: NN-baseline-beat fixture cost" (linie 405-417)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-CONTEXT.md decyzje D-14 (dual entry points), D-15 (start=1), D-16 (tiered fixtures - N=1000 dla TEST-05)
  </read_first>
  <action>
    Utworzyc NOWY plik `test/test_baselines.jl` (UTF-8, LF, no BOM, NFC; ASCII filename).

    Top-of-file komentarz:
    ```julia
    # Testset dla src/baselines.jl - pokrywa REQ ALG-04 + TEST-05 (NN-baseline-beat ≥10%).
    # Wlaczany przez include("test_baselines.jl") z test/runtests.jl (Plan 02-06).
    # TEST-05 jest CI-tezki (~30s na N=1000); rekomendacja Pitfall G:
    # liczba_krokow=20_000 (start); podnies do 50_000 jezeli single-seed deterministic test fail.
    ```

    Defensywne imports:
    ```julia
    using Test
    using JuliaCity
    using Random
    using Random: Xoshiro
    ```

    **Sub-testset 1: trasa_nn jest permutacja 1:n (ALG-04):**
    ```julia
    @testset "trasa_nn - permutacja 1:n (ALG-04)" begin
        # Maly fixture - 5 punktow w roznych miejscach
        punkty = [Punkt2D(0.0, 0.0), Punkt2D(1.0, 0.0), Punkt2D(2.0, 0.0),
                  Punkt2D(3.0, 0.0), Punkt2D(4.0, 0.0)]
        # D recznie - punkty na linii prostej
        n = 5
        D = Matrix{Float64}(undef, n, n)
        for i in 1:n, j in 1:n
            D[i, j] = abs(i - j) * 1.0
        end
        # NN z start=1: powinno byc [1, 2, 3, 4, 5] (greedy idzie po sasiadach)
        trasa = trasa_nn(D; start=1)
        @test trasa == [1, 2, 3, 4, 5]
        @test sort(trasa) == collect(1:5)  # permutacja invariant

        # NN z start=3: greedy idzie do najblizszego sasiada (2 lub 4 - tie); sprawdz tylko
        # ze permutacja
        trasa3 = trasa_nn(D; start=3)
        @test sort(trasa3) == collect(1:5)
        @test trasa3[1] == 3
    end
    ```

    **Sub-testset 2: trasa_nn determinizm + asercja niekwadratowa D:**
    ```julia
    @testset "trasa_nn determinizm + walidacja" begin
        punkty = generuj_punkty(50; seed=42)
        stan = StanSymulacji(punkty)
        oblicz_macierz_dystans!(stan)
        t1 = trasa_nn(stan.D; start=1)
        t2 = trasa_nn(stan.D; start=1)
        @test t1 == t2  # determinizm (D-15: start=1, brak RNG)

        # niekwadratowa D - asercja
        D_bad = zeros(Float64, 5, 4)
        @test_throws AssertionError trasa_nn(D_bad)
    end
    ```

    **Sub-testset 3: inicjuj_nn! pelny flow (ALG-04, D-14):**
    ```julia
    @testset "inicjuj_nn! - pelny init flow (ALG-04, D-14)" begin
        punkty = generuj_punkty(20; seed=42)
        stan = StanSymulacji(punkty; rng=Xoshiro(42))

        # przed init: pre-alokowane D, trasa = collect(1:n), energia = 0
        @test stan.energia == 0.0
        @test stan.trasa == collect(1:20)

        inicjuj_nn!(stan)

        # po init: D wypelnione, trasa = NN, energia = oblicz_energie(NN)
        @test sort(stan.trasa) == collect(1:20)  # permutacja
        @test stan.energia > 0
        @test stan.iteracja == 0
        @test stan.D[1, 1] == 0.0  # diagonal
        @test stan.D[1, 2] == stan.D[2, 1]  # symmetry

        # cache invariant - stan.energia ≈ oblicz_energie(stan.D, stan.trasa, bufor)
        bufor = zeros(Float64, Threads.nthreads())
        @test isapprox(stan.energia, oblicz_energie(stan.D, stan.trasa, bufor); rtol=1e-12)
    end
    ```

    **Sub-testset 4: TEST-05 NN-baseline-beat (KRYTYCZNY):**
    ```julia
    @testset "TEST-05: NN-baseline-beat - SA ≥10% pod NN (N=1000 seed=42)" begin
        # Pitfall G: start z liczba_krokow=20_000; jezeli failuje na CI - podnies do 50_000.
        # Single-seed deterministic - binary outcome, brak flakiness.
        punkty = generuj_punkty(1000; seed=42)

        # NN baseline (pure - bez Stana)
        n = length(punkty)
        D = Matrix{Float64}(undef, n, n)
        for j in 1:n, i in 1:j-1
            dx = punkty[i][1] - punkty[j][1]
            dy = punkty[i][2] - punkty[j][2]
            d = sqrt(dx*dx + dy*dy)
            D[i, j] = d
            D[j, i] = d
        end
        for j in 1:n; D[j, j] = 0.0; end
        nn = trasa_nn(D; start=1)
        bufor = zeros(Float64, Threads.nthreads())
        energia_nn = oblicz_energie(D, nn, bufor)

        # SA run
        stan = StanSymulacji(punkty; rng=Xoshiro(42))
        inicjuj_nn!(stan)
        alg = SimAnnealing(stan)
        stan.temperatura = alg.T_zero
        params = Parametry(liczba_krokow=20_000)
        for _ in 1:params.liczba_krokow
            symuluj_krok!(stan, params, alg)
        end

        # SA musi byc ≥10% krotsze niz NN
        @test stan.energia / energia_nn <= 0.9
        @info "TEST-05: NN energia=$(round(energia_nn, digits=4)), SA energia=$(round(stan.energia, digits=4)), ratio=$(round(stan.energia/energia_nn, digits=4))"
    end
    ```

    Konwencje: Polish komentarze, English asserts. Sekcje numerowane (1, 2, 3, 4) lub `# ─` dividery.
  </action>
  <verify>
    <automated>julia --project=. -t 2 -e 'using Test, JuliaCity, Random; include("test/test_baselines.jl")' 2>&1 | grep -E "Pass:|Fail:|Error:|Test Summary:" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Plik test/test_baselines.jl istnieje, >= 60 linii
    - `grep -c '^@testset' test/test_baselines.jl` zwraca przynajmniej 4
    - `grep -c 'TEST-05' test/test_baselines.jl` zwraca przynajmniej 1
    - `grep -c 'trasa_nn' test/test_baselines.jl` zwraca przynajmniej 4
    - `grep -c 'inicjuj_nn!' test/test_baselines.jl` zwraca przynajmniej 1
    - `grep -c 'energia_nn' test/test_baselines.jl` zwraca przynajmniej 2 (TEST-05 ratio)
    - `grep -c 'liczba_krokow=20_000' test/test_baselines.jl` zwraca 1
    - `julia --project=. -t 2 -e 'using Test, JuliaCity, Random; include("test/test_baselines.jl")'` exits 0 ze wszystkimi @test passing
    - **Jezeli TEST-05 failuje** (ratio > 0.9): podnies liczba_krokow do 50_000 i rerun. Udokumentuj decyzje w 02-05-SUMMARY.md.
  </acceptance_criteria>
  <done>
    test/test_baselines.jl zawiera testset-y dla trasa_nn, inicjuj_nn!, TEST-05 NN-baseline-beat (≥10% pod NN dla N=1000 seed=42 z liczba_krokow=20_000 lub 50_000). Wszystkie zielone.
  </done>
</task>

<task type="auto">
  <name>Task 3: Utworzyc test/test_symulacja.jl (ALG-01..03/06..08 + TEST-01/04/08)</name>
  <files>test/test_symulacja.jl</files>
  <read_first>
    - test/runtests.jl (testset structure)
    - src/algorytmy/simulowane_wyzarzanie.jl (SimAnnealing + symuluj_krok! sygnatury)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-PATTERNS.md sekcja "test/test_symulacja.jl (test / unit + integ)" (linie 296-375)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-RESEARCH.md sekcje "Example 3: TEST-04 multi-thread" (linie 489-535), "Pitfall A: @allocated" (linie 290-320), "Pitfall D: TEST-04 sub-ULP" (linie 351-359)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-CONTEXT.md decyzje D-12 (TEST-04 semantyka), D-16 (N=20 + StableRNG dla TEST-08), D-17 (TEST-08 hardcoded refs)
  </read_first>
  <action>
    Utworzyc NOWY plik `test/test_symulacja.jl` (UTF-8, LF, no BOM, NFC; ASCII filename).

    Top-of-file komentarz:
    ```julia
    # Testset dla src/algorytmy/simulowane_wyzarzanie.jl - pokrywa REQ ALG-01..03/06..08 + TEST-01/04/08.
    # Wlaczany przez include("test_symulacja.jl") z test/runtests.jl (Plan 02-06).
    # TEST-04 multi-thread determinism uzywa PerformanceTestTools.@include_foreach (subprocess).
    # TEST-08 golden value: HARDCODED dla StableRNG(42) N=20 1000 krokow - zmiana algorytmu
    # wymusza aktualizacje hardcoded ref (intencjonalny alarm regresji).
    ```

    Defensywne imports:
    ```julia
    using Test
    using JuliaCity
    using Random
    using Random: Xoshiro
    using StableRNGs
    using Serialization
    using PerformanceTestTools  # TEST-04
    ```

    **Sub-testset 1: SimAnnealing struct + ctors (ALG-01):**
    ```julia
    @testset "SimAnnealing struct + ctors (ALG-01)" begin
        @test SimAnnealing <: Algorytm

        # positional default ctor
        alg = SimAnnealing(0.5, 0.9999, 5000)
        @test alg.T_zero == 0.5
        @test alg.alfa == 0.9999
        @test alg.cierpliwosc == 5000

        # kwarg ctor z auto-T_zero
        punkty = generuj_punkty(20; seed=42)
        stan = StanSymulacji(punkty; rng=Xoshiro(42))
        inicjuj_nn!(stan)
        alg2 = SimAnnealing(stan)
        @test alg2.alfa == 0.9999
        @test alg2.cierpliwosc == 5000
        @test alg2.T_zero > 0  # auto-kalibracja

        # override T_zero recznie
        alg3 = SimAnnealing(stan; T_zero=1.0)
        @test alg3.T_zero == 1.0
    end
    ```

    **Sub-testset 2: symuluj_krok! type-stable + zero-alloc (ALG-02, ALG-03, TEST-02, TEST-03):**
    ```julia
    @testset "symuluj_krok! type-stable + @allocated == 0 (ALG-02, ALG-03)" begin
        punkty = generuj_punkty(20; seed=42)
        stan = StanSymulacji(punkty; rng=Xoshiro(42))
        inicjuj_nn!(stan)
        alg = SimAnnealing(stan)
        stan.temperatura = alg.T_zero
        params = Parametry(liczba_krokow=100)

        # type-stable: @inferred ::Nothing
        @test @inferred(symuluj_krok!(stan, params, alg)) === nothing

        # warmup (Pitfall A)
        for _ in 1:3
            symuluj_krok!(stan, params, alg)
        end

        # @allocated == 0 strictly (TEST-03)
        function _alloc_krok(stan, params, alg)
            return @allocated symuluj_krok!(stan, params, alg)
        end
        @test _alloc_krok(stan, params, alg) == 0
    end
    ```

    **Sub-testset 3: TEST-01 / ALG-08 Hamilton invariant po kazdym kroku:**
    ```julia
    @testset "TEST-01 / ALG-08: Hamilton invariant po kazdym kroku" begin
        punkty = generuj_punkty(20; seed=42)
        stan = StanSymulacji(punkty; rng=Xoshiro(42))
        inicjuj_nn!(stan)
        alg = SimAnnealing(stan)
        stan.temperatura = alg.T_zero
        params = Parametry(liczba_krokow=2000)

        # Sample co 100 krokow + finalny check po 2000
        for k in 1:2000
            symuluj_krok!(stan, params, alg)
            if k % 100 == 0
                @test sort(stan.trasa) == collect(1:20)
            end
        end
        @test sort(stan.trasa) == collect(1:20)
        @test stan.iteracja == 2000
    end
    ```

    **Sub-testset 4: TEST-08 golden value StableRNG(42) N=20:**

    UWAGA - **EXECUTOR MUSI WYGENEROWAC HARDCODED REFS LOKALNIE** przed napisaniem assercji. Workflow:

    1. Najpierw napisz testset Z PLACEHOLDER asercjami:
       ```julia
       @testset "TEST-08: golden value StableRNG(42), N=20, 1000 krokow" begin
           punkty = generuj_punkty(20, StableRNG(42))
           stan = StanSymulacji(punkty; rng=StableRNG(42))
           inicjuj_nn!(stan)
           alg = SimAnnealing(stan; alfa=0.9999, cierpliwosc=5000)
           stan.temperatura = alg.T_zero
           params = Parametry(liczba_krokow=1000)
           for _ in 1:1000
               symuluj_krok!(stan, params, alg)
           end
           # Wygenerowane lokalnie podczas Plan 02-05 wykonania (executor zapisuje wynik raz).
           # Zmiana algorytmu / RNG / liczby krokow wymusza aktualizacje hardcoded refs.
           @test stan.trasa == TRASA_REF
           @test isapprox(stan.energia, ENERGIA_REF; rtol=1e-6)
       end
       ```

    2. **Uruchom raz lokalnie i zaobserwuj wartosci:**
       ```bash
       julia --project=. -e '
       using JuliaCity, StableRNGs
       punkty = generuj_punkty(20, StableRNG(42))
       stan = StanSymulacji(punkty; rng=StableRNG(42))
       inicjuj_nn!(stan)
       alg = SimAnnealing(stan; alfa=0.9999, cierpliwosc=5000)
       stan.temperatura = alg.T_zero
       params = Parametry(liczba_krokow=1000)
       for _ in 1:1000; symuluj_krok!(stan, params, alg); end
       println("TRASA_REF = ", stan.trasa)
       println("ENERGIA_REF = ", stan.energia)
       '
       ```

    3. **Zastapic placeholderami konkretnymi wartosciami:**
       ```julia
       # WARTOSCI WYGENEROWANE LOKALNIE 2026-04-29 - StableRNG(42) + Plan 02-04 implementacja.
       # Aktualizuj te wartosci jezeli zmienisz algorytm SA / RNG / liczbe krokow.
       const TRASA_REF = [<wygenerowane przez executora>]
       const ENERGIA_REF = <wygenerowane przez executora>
       ```

       (Te `const` zadeklaruj BEZPOSREDNIO przed `@testset "TEST-08"` w pliku.)

    4. **Verify ze test przechodzi po hardkodowaniu** - testset musi byc zielony.

    **Sub-testset 5: TEST-04 multi-thread determinism (subprocess + in-process):**

    Dwa podejscia per RESEARCH §Caveats (Example 3 ostatni komentarz):

    A. **In-process (low-fidelity, szybki - sanity check, ZAWSZE w testach):**
    ```julia
    @testset "TEST-04 in-process: same seed, fresh stan -> identical trajectory" begin
        function run_sa_inproc(; krokow::Int=5_000)
            punkty = generuj_punkty(1000; seed=42)
            stan = StanSymulacji(punkty; rng=Xoshiro(42))
            inicjuj_nn!(stan)
            alg = SimAnnealing(stan)
            stan.temperatura = alg.T_zero
            params = Parametry(liczba_krokow=krokow)
            for _ in 1:krokow
                symuluj_krok!(stan, params, alg)
            end
            return copy(stan.trasa), stan.energia
        end
        t1, e1 = run_sa_inproc()
        t2, e2 = run_sa_inproc()
        @test t1 == t2
        @test e1 == e2  # in-process: brak threading variation, dokladna rownosc OK
    end
    ```

    B. **Subprocess (high-fidelity, REQ TEST-04):**
    ```julia
    @testset "TEST-04 subprocess: JULIA_NUM_THREADS=1 vs 8 -> identical trajektoria" begin
        # Pattern z RESEARCH Example 3 (linie 491-530) + PerformanceTestTools.@include_foreach.
        sa_run_script = """
        using JuliaCity, Random, Serialization
        punkty = generuj_punkty(1000; seed=42)
        stan = StanSymulacji(punkty; rng=Xoshiro(42))
        inicjuj_nn!(stan)
        alg = SimAnnealing(stan)
        stan.temperatura = alg.T_zero
        params = Parametry(liczba_krokow=5_000)
        for _ in 1:params.liczba_krokow
            symuluj_krok!(stan, params, alg)
        end
        out_path = ENV["JC_OUT"]
        serialize(out_path, (trasa=stan.trasa, energia=stan.energia))
        """
        script_path = tempname() * ".jl"
        write(script_path, sa_run_script)
        out_1 = tempname() * ".jls"
        out_n = tempname() * ".jls"

        PerformanceTestTools.@include_foreach(
            script_path,
            [
                ["JULIA_NUM_THREADS" => "1", "JC_OUT" => out_1],
                ["JULIA_NUM_THREADS" => "8", "JC_OUT" => out_n],
            ]
        )
        r1 = deserialize(out_1)
        rn = deserialize(out_n)

        # D-12 / Pitfall D: trasa bit-identyczna; energia tolerujemy sub-ULP
        @test r1.trasa == rn.trasa
        @test isapprox(r1.energia, rn.energia; rtol=1e-12)
    end
    ```

    UWAGA: Subprocess test wymaga ze `PerformanceTestTools` jest dostepny (Plan 02-06 dodaje do [extras]+[targets]). Jezeli ten plan jest uruchamiany STANDALONE przed Plan 02-06, ten subprocess test rzuci `LoadError: Package PerformanceTestTools not found`. To jest OK - przed integracja runtests.jl (Plan 02-06) standalone test powinno byc uruchamiane przez:
    ```bash
    julia --project=. -e '
    using Pkg
    Pkg.add("PerformanceTestTools")  # tymczasowo, jezeli jeszcze nie w extras
    using Test, JuliaCity, Random, StableRNGs, Serialization, PerformanceTestTools
    include("test/test_symulacja.jl")
    '
    ```
    Plan 02-06 dodaje `PerformanceTestTools` do `[extras]` + `[targets].test`, po czym `Pkg.test()` rozwiazuje to automatycznie.

    Konwencje: Polish komentarze, English asserts. `@info` dla diagnostycznych infor.
  </action>
  <verify>
    <automated>julia --project=. -e 'using Pkg; "PerformanceTestTools" in keys(Pkg.project().dependencies) || Pkg.add("PerformanceTestTools"); using Test, JuliaCity, Random, StableRNGs, Serialization, PerformanceTestTools; include("test/test_symulacja.jl")' 2>&1 | grep -E "Pass:|Fail:|Error:|Test Summary:" | head -25</automated>
  </verify>
  <acceptance_criteria>
    - Plik test/test_symulacja.jl istnieje, >= 100 linii
    - `grep -c '^@testset' test/test_symulacja.jl` zwraca przynajmniej 5
    - `grep -c 'TEST-08' test/test_symulacja.jl` zwraca przynajmniej 1
    - `grep -c 'TEST-04' test/test_symulacja.jl` zwraca przynajmniej 2 (in-process + subprocess)
    - `grep -c 'TEST-01' test/test_symulacja.jl` zwraca przynajmniej 1
    - `grep -c 'TEST-03' test/test_symulacja.jl` zwraca przynajmniej 1
    - `grep -c 'StableRNG(42)' test/test_symulacja.jl` zwraca przynajmniej 2 (TEST-08 fixture)
    - `grep -c 'TRASA_REF' test/test_symulacja.jl` zwraca przynajmniej 2 (definicja `const` + uzycie)
    - `grep -c 'ENERGIA_REF' test/test_symulacja.jl` zwraca przynajmniej 2
    - `grep -c '_alloc_krok' test/test_symulacja.jl` zwraca przynajmniej 2
    - `grep -c 'PerformanceTestTools.@include_foreach' test/test_symulacja.jl` zwraca przynajmniej 1
    - **TRASA_REF i ENERGIA_REF SA HARDCODED ze WYGENEROWANYMI WARTOSCIAMI** (nie literalnymi placeholderami `<wygenerowane>`)
    - `julia --project=. -e '...include("test/test_symulacja.jl")...'` exits 0 ze wszystkimi @test passing (po hardkodowaniu refs)
  </acceptance_criteria>
  <done>
    test/test_symulacja.jl zawiera 5+ testset-y pokrywajace ALG-01..03, ALG-08, TEST-01, TEST-03, TEST-04 (oba podejscia), TEST-08 (z prawdziwymi hardcoded refs po wygenerowaniu lokalnie). Wszystkie zielone gdy uruchamiane standalone.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process -> filesystem | TEST-04 zapisuje subprocess output do `tempname()` - cleanup automatyczny przez OS, brak cross-test contamination |
| TEST-08 hardcoded refs -> regresja | Zmiana algorytmu (D-04, D-05, D-06) wymusza aktualizacje refs - INTENCJONALNY alarm regresji |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Tampering | TEST-08 hardcoded refs sa wrong | mitigate | Executor MUSI wygenerowac refs lokalnie (action item w Task 3); commit tylko jezeli test przechodzi |
| T-02-14 | DOS | TEST-04 subprocess overhead na CI (~20s extra) | accept | Akceptowalne dla pojedynczego REQ; alternatywa (in-process only) jest niska-fidelity |
| T-02-15 | DOS | TEST-05 timeout na CI (>60s na N=1000 seed=42 z 20_000 krokami) | mitigate | Pitfall G: start z 20_000 krokow; podnies do 50_000 jezeli failuje (deterministic single-seed - binary outcome) |

Severity: brak high. Test code, brak network/secrets/PII.
</threat_model>

<verification>
Per-task automated checks (powyzej w `<verify>` blokach).

**Plan-level integration check:**

```bash
# Wszystkie 3 pliki testowe uruchamialne standalone
julia --project=. -t 4 -e '
using Pkg
"PerformanceTestTools" in keys(Pkg.project().dependencies) || Pkg.add("PerformanceTestTools")
using Test, JuliaCity, Random, StableRNGs, Serialization, PerformanceTestTools
include("test/test_energia.jl")
include("test/test_baselines.jl")
include("test/test_symulacja.jl")
' 2>&1 | tail -50
```

Exit code 0 = wszystkie testy zielone. Output zawiera `Test Summary: ... Pass:` dla kazdego testset-u.

**TEST-08 referenced values are real:**

```bash
grep -A 1 'TRASA_REF =' test/test_symulacja.jl | head -2
# Oczekiwane: TRASA_REF = [3, 7, 15, ...] - lista 20 Int (nie placeholder)
```
</verification>

<success_criteria>
1. **test/test_energia.jl, test/test_baselines.jl, test/test_symulacja.jl ISTNIEJA, ASCII filenames, UTF-8 NFC.**
2. **Kazdy plik MOZE byc uruchomiony przez include - wszystkie testy zielone (Pass, zero Fail/Error).**
3. **TEST-01 Hamilton invariant - sample co 100 krokow + finalny po 2000 krokach.**
4. **TEST-02 @inferred na publicznym API (oblicz_energie, delta_energii, symuluj_krok!).**
5. **TEST-03 @allocated == 0 dla symuluj_krok! (helper function per Pitfall A).**
6. **TEST-04 dwa testset-y: in-process (sanity) + subprocess (PerformanceTestTools.@include_foreach z env JULIA_NUM_THREADS).**
7. **TEST-05 NN-baseline-beat: SA/NN ≤ 0.9 dla N=1000 seed=42 z liczba_krokow=20_000 (lub 50_000 jezeli failuje).**
8. **TEST-08 hardcoded TRASA_REF + ENERGIA_REF wygenerowane lokalnie z StableRNG(42), N=20, 1000 krokow.**
9. **Wszystkie pliki uzywaja Polish komentarze + English asserts; defensywne `using` na top-of-file.**
</success_criteria>

<output>
After completion, create `.planning/phases/02-energy-sa-algorithm-test-suite/02-05-SUMMARY.md` zawierajace:

- Liczbe linii w kazdym z 3 plikow (test_energia, test_baselines, test_symulacja)
- TEST-08 hardcoded TRASA_REF + ENERGIA_REF (wartosci do dokumentacji)
- TEST-05: ratio SA/NN dla N=1000 seed=42 (powinno byc ≤ 0.9)
- Czy podniesiono liczba_krokow z 20_000 do 50_000 (jezeli pierwsze przebicie failed)
- Czy TEST-04 subprocess test zielony (PerformanceTestTools dostepny - dodany do extras w 02-06, ale temporary Pkg.add OK dla pre-integration)
- Lista wszystkich testset-name w kazdym pliku (sanity check coverage)
</output>
