---
phase: 02-energy-sa-algorithm-test-suite
plan: 14
type: execute
wave: 11
depends_on: [02-13]
files_modified:
  - bench/diagnostyka_test05.jl
  - src/energia.jl
  - src/algorytmy/simulowane_wyzarzanie.jl
  - test/test_baselines.jl
  - .planning/phases/02-energy-sa-algorithm-test-suite/02-CONTEXT.md
  - .planning/phases/02-energy-sa-algorithm-test-suite/deferred-items.md
autonomous: true
gap_closure: true
requirements:
  - ALG-05
  - TEST-05
tags:
  - julia
  - simulated-annealing
  - algorithm-fix
  - empirical-diagnosis
  - T0-calibration

must_haves:
  truths:
    - "TEST-05 (NN-baseline-beat) PASS przy liczba_krokow=50_000 dla N=1000, seed=42"
    - "stan.energia / energia_nn <= 0.9 jako hard @test (nie @test_broken)"
    - "Pełen suite: 222 PASS, 0 FAIL, 0 ERROR, 0 BROKEN"
    - "@allocated symuluj_krok!(stan, params, alg) == 0 nadal trzymane (TEST-03 brak regresji)"
    - "JET TEST-07: 4/4 PASS dla zmodyfikowanych funkcji (kalibruj_T0 / SimAnnealing constructor)"
    - "Aqua TEST-06: 9/9 PASS"
    - "bench/diagnostyka_test05.jl zacommitowany — przyszłe regresje wykrywalne"
    - "02-CONTEXT.md D-03 zaktualizowany erratum'em z empiryczną diagnozą + wybraną techniką"
    - "deferred-items.md TEST-05 entry oznaczony RESOLVED in plan 02-14"
  artifacts:
    - path: "bench/diagnostyka_test05.jl"
      provides: "Skrypt diagnostyczny: T0 sweep, acceptance ratio, trajektoria energii dla NN-start N=1000"
      contains: "diagnostyka_test05"
    - path: "src/energia.jl OR src/algorytmy/simulowane_wyzarzanie.jl"
      provides: "Fix kalibracji T0 dla NN-start (technika wybrana w fazie B na podstawie diagnozy)"
      contains: "kalibruj_T0"
    - path: "test/test_baselines.jl"
      provides: "TEST-05 budżet zredukowany z 200_000 do 50_000 kroków + ratio <= 0.9"
      contains: "liczba_krokow=50_000"
    - path: ".planning/phases/02-energy-sa-algorithm-test-suite/02-CONTEXT.md"
      provides: "D-03 erratum z diagnozą + zmianą algorytmu"
      contains: "D-03 erratum (plan 02-14)"
  key_links:
    - from: "test/test_baselines.jl::TEST-05"
      to: "src/.../kalibruj_T0 OR SimAnnealing constructor"
      via: "Fixed T0 calibration produces ratio <= 0.9 within 50k steps"
      pattern: "ratio.*<= 0.9"
    - from: "bench/diagnostyka_test05.jl"
      to: "decision rationale in 02-14-SUMMARY.md"
      via: "Empirical data drives B1/B2/B3 choice"
      pattern: "diagnostyka"
---

<objective>
Algorytmiczne zamknięcie TEST-05 (NN-baseline-beat) — ostatni FAIL z planu 02-13 (handoff dd65a35).

**Diagnoza z handoffu (do empirycznej weryfikacji w fazie A):** Aktualne `kalibruj_T0 = 2σ(worsening_deltas_z_random_swapów)` produkuje T₀ skalibrowane dla **random tour startu**. Gdy `inicjuj_nn!` daje już lokalnie-blisko-optymalny start (NN), te same `2σ` dają acceptance ratio ~80% dla worsening swap → SA aktywnie WYRZUCA z dobrego basena energii i nigdy do niego nie wraca. Roadmap SC #4 (`SA ≥10% pod NN`) wymaga **fixu kalibracji**, nie zwiększenia budżetu.

**Empiryczne potwierdzenie (200_000 kroków, plan 02-13):**
- N=1000, NN energia=28.85, SA energia=47.74, ratio=**1.6549** (cel ≤ 0.9; SA pogarsza NN o 65%).

**Cel planu 02-14:** Zaprojektować fix algorytmiczny w 3 fazach (diagnoza → wybór → implementacja), tak aby TEST-05 przechodził przy oryginalnym budżecie **50_000 kroków** (PLAN 02-05) BEZ regresji innych testów (TEST-03 zero alloc, TEST-06 Aqua, TEST-07 JET, TEST-08 golden values).

**Trzy fazy planu:**

1. **Faza A — diagnoza empiryczna** (`bench/diagnostyka_test05.jl`): pomiar (a) `T₀ = kalibruj_T0(stan)` po `inicjuj_nn!`, (b) rozkład delta_energii dla 1000 random 2-opt swapów na NN-tour (mean/std/p50/p95), (c) acceptance ratio worsening w pierwszych 100/1000/10_000 krokach przy bieżącym T₀, (d) trajektoria `stan.energia` dla T₀ ∈ {0.01, 0.05, 0.1, 0.5, kalibruj_T0}. Output: tabela "T₀ → kroki do ratio ≤ 0.9".

2. **Faza B — wybór techniki** spośród trzech kandydatów MINIMUM-VIABLE:
   - **B1: lower fixed T0 dla NN-start** — kwarg `T_zero_dla_NN_start::Float64=0.05` w `SimAnnealing(stan; ...)` (z fallback do `kalibruj_T0(stan)` gdy random start; auto-detect przez `stan.energia` vs naive trasa)
   - **B2: Ben-Ameur (2004) auto-calibration** — iteracyjnie znajdź T₀ dające docelowy χ₀ acceptance ratio (0.5–0.8) na próbce worsening deltas; zastępuje `2σ` formułę
   - **B3: target acceptance pre-run** — po `inicjuj_nn!`, sample 200 swapów, oblicz T₀ = -mean(positive_delta) / log(χ₀) z χ₀=0.5; działa dla obu startów (random i NN)

   Decyzja w **końcu fazy A**, na podstawie danych. Hipoteza prior: **B3** (najczystsze, no magic constant, działa dla obu startów; wymaga jedynie zmiany formuły w `kalibruj_T0`).

3. **Faza C — implementacja + weryfikacja**:
   - Edit `src/energia.jl::kalibruj_T0` (lub `src/algorytmy/simulowane_wyzarzanie.jl::SimAnnealing` jeśli wybrane B1)
   - Update `02-CONTEXT.md` D-03 erratum
   - TEST-05 budżet: 200_000 → **50_000** (oryginał z PLAN 02-05)
   - Re-run pełnego suite: 222 PASS, 0 FAIL/ERROR/BROKEN
   - Sprawdzić regresję: TEST-03 zero alloc (allocations check), TEST-07 JET stability dla zmienionych funkcji, TEST-06 Aqua

Output:
- `bench/diagnostyka_test05.jl` (zacommitowany — przyszłe regresje)
- `src/energia.jl` lub `src/algorytmy/simulowane_wyzarzanie.jl` z fixem
- `test/test_baselines.jl` z `liczba_krokow=50_000` + `@test stan.energia/energia_nn <= 0.9`
- `02-CONTEXT.md` D-03 erratum
- `02-14-SUMMARY.md` z diagnozą + decyzją + Test Summary
- `deferred-items.md` TEST-05 → RESOLVED
- Aktualizacja `STATE.md` / `ROADMAP.md` (Phase 2 = 100%)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-CONTEXT.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-RESEARCH.md
@.planning/phases/02-energy-sa-algorithm-test-suite/deferred-items.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-13-PLAN.md
@CLAUDE.md
@Project.toml
@src/energia.jl
@src/algorytmy/simulowane_wyzarzanie.jl
@src/baselines.jl
@test/test_baselines.jl

<interfaces>
<!--
Bieżący stan kalibracji T₀ (src/energia.jl:186):

  function kalibruj_T0(stan; n_probek=1000, rng=stan.rng)::Float64
      # sample 1000 random 2-opt deltas, collect positive (worsening)
      # return 2.0 * std(worsening)  # D-03 LOCKED
  end

Bieżący flow TEST-05 (test/test_baselines.jl:88-127):

  punkty = generuj_punkty(1000; seed=42)
  stan = StanSymulacji(punkty; rng=Xoshiro(42))
  inicjuj_nn!(stan)               # stan.trasa = NN tour, stan.energia = NN energy
  alg = SimAnnealing(stan)        # alg.T_zero = kalibruj_T0(stan)  ← BUG: 2σ z NN-start
  stan.temperatura = alg.T_zero
  for _ in 1:200_000              # ← bumpniete z 50k w plan 02-13 (palliative)
      symuluj_krok!(stan, params, alg)
  end
  @test stan.energia / energia_nn <= 0.9  ← FAIL: ratio=1.6549

Hipoteza diagnostyczna (faza A potwierdzi/odrzuci):
  T₀_calibrated dla NN-start = 2σ(worsening_deltas) ≈ 1.0 (rząd wielkości typowej delta_energii).
  Acceptance ratio P(accept|delta>0) = exp(-delta/T₀) ≈ exp(-1.0/1.0) ≈ 0.37 ALE
  cooling jest wolne (alfa=0.9999 → T(50k)=0.0067, T(10k)=0.37) → przez pierwsze ~10k
  kroków acceptance ratio ~ 0.37 → SA accept'uje ~37% worsening swapów → wyrzuca z basena NN.

Faza B kandydaci szczegółowo:

B1 — lower T_zero dla NN start:
  function SimAnnealing(stan; alfa=0.9999, cierpliwosc=5000,
                        T_zero=auto_T0(stan))
      ...
  end
  function auto_T0(stan)
      # Heurystyka: jeżeli stan.energia jest "low" (< 0.5 * naive_random_energy)
      # to start jest NN-like, użyj T_zero=0.05; w przeciwnym razie 2σ.
      # ALE: heurystyka łatwo się myli; ALTERNATYWNIE wprowadzić explicit kwarg
      # T_zero_dla_NN_start::Float64=0.05.
  end
  PLUS: minimal change. MINUS: magic constant 0.05; user musi pamiętać kwarg.

B2 — Ben-Ameur (2004) auto-calibration (iteracyjna):
  Cel: znaleźć T₀ dające χ₀ ≈ 0.8 (lub 0.5) na zbiorze worsening deltas.
  Algorytm: iteruj T₀^(k+1) = T₀^(k) * (ln(χ̂_k) / ln(χ₀))^(1/p) (zwykle p=1)
  Zbieżność typowo 5-10 iteracji.
  PLUS: matematycznie czysty; działa dla każdego startu. MINUS: ~5x bardziej kosztowny
  niż 2σ; iteracyjny — może nie zbieżnie się zachowywać (stąd assertion + max_iter).

B3 — target acceptance pre-run (closed-form):
  Po `inicjuj_nn!`, sample N_probek swapów, zbierz positive deltas.
  T₀ = -mean(positive_deltas) / ln(χ₀)  z χ₀=0.5  →  T₀ = mean(positive_deltas) / ln(2)
  Bo: P(accept|mean_delta) = exp(-mean_delta/T₀) = χ₀  ⟹  T₀ = -mean_delta / ln(χ₀).
  PLUS: closed-form (no iteration); działa dla obu startów; jedna formuła.
  MINUS: zmienia D-03 LOCKED → wymaga erratum w CONTEXT.md.

Hipoteza wyboru (jeszcze do potwierdzenia danymi z fazy A):
  B3 — najczystsze, robust, jednolinijkowa zmiana w `kalibruj_T0`.

Faza C verification gates (po implementacji):
  julia --project=. -e 'using Pkg; Pkg.test()' → exit 0
  grep -E "Test Summary" /tmp/pkgtest.log → "222 ... 0 Fail"
  TEST-05 testset Pass; TEST-03 nadal Pass; TEST-06 Aqua nadal Pass; TEST-07 JET nadal Pass.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stworzyć skrypt diagnostyczny bench/diagnostyka_test05.jl (Faza A)</name>
  <files>bench/diagnostyka_test05.jl</files>
  <read_first>
    - src/energia.jl (sygnatura kalibruj_T0, delta_energii)
    - src/algorytmy/simulowane_wyzarzanie.jl (SimAnnealing constructor, symuluj_krok!)
    - src/baselines.jl (trasa_nn, inicjuj_nn!)
    - test/test_baselines.jl linie 88-127 (TEST-05 fixture do replikacji)
  </read_first>
  <action>
    Stworzyć katalog `bench/` (jeżeli nie istnieje) i plik `bench/diagnostyka_test05.jl`. Zawartość: skrypt jednorazowy (uruchamiany ręcznie przez `julia --project=. bench/diagnostyka_test05.jl`), który:

    1. Replikuje setup TEST-05 (N=1000, seed=42, NN start).
    2. Drukuje:
       - `T₀_calibrated = kalibruj_T0(stan)` po `inicjuj_nn!`
       - Statystyki próbki 1000 random 2-opt swapów: count(positive), mean(positive), std(positive), p50(positive), p95(positive)
       - Mean(positive)/ln(2) (kandydat T₀ dla B3 z χ₀=0.5)
       - Mean(positive)/ln(1.25) (kandydat T₀ dla B3 z χ₀=0.8)
    3. Wykonuje **5 mini-runów** SA (każdy 50_000 kroków, fresh stan), zmieniając tylko `alg.T_zero`:
       - T₀ ∈ {0.01, 0.05, 0.1, 0.5, T₀_calibrated}
       - Dla każdego: print energia_final, ratio = energia_final / energia_nn
       - Print acceptance ratio worsening w pierwszych 1000 krokach (counter w przypadkowo zmodyfikowanym `symuluj_krok!`-ekvivalent — lokalna kopia logiki w skrypcie, bez modyfikacji src/)
    4. Drukuje rekomendację: "Najlepszy T₀: {value} dający ratio={ratio} w 50_000 krokach"

    Konwencja: skrypt NIE używa @testset. Output to czyste `println` / `@info` linijki, kopiowalne do SUMMARY 02-14.

    Skrypt musi być **deterministyczny** (StableRNG dla próbkowania, Xoshiro(42) dla SA jak w TEST-05) — aby przyszłe regresje były wykrywalne.
  </action>
  <verify>
    <automated>test -f bench/diagnostyka_test05.jl &amp;&amp; grep -c "diagnostyka_test05\|N=1000\|seed=42" bench/diagnostyka_test05.jl</automated>
  </verify>
  <acceptance_criteria>
    - `bench/diagnostyka_test05.jl` istnieje
    - Plik zawiera setup `generuj_punkty(1000; seed=42)` i `inicjuj_nn!(stan)`
    - 5 mini-runów SA z różnymi T₀ wytłaczane przez `for T₀ in [0.01, 0.05, 0.1, 0.5, T₀_calibrated]`
    - Output struktury readable (cytowalne do SUMMARY)
    - Brak modyfikacji `src/` (skrypt diagnostyczny jest izolowany)
  </acceptance_criteria>
  <done>
    Skrypt diagnostyczny gotowy do uruchomienia.
  </done>
</task>

<task type="checkpoint:auto-action" gate="blocking">
  <name>Task 2: Uruchomić bench/diagnostyka_test05.jl, zebrać dane (Faza A — empirical evidence)</name>
  <what-built>
    Skrypt diagnostyczny z Task 1.
  </what-built>
  <how-to-verify>
    ```bash
    julia --project=. bench/diagnostyka_test05.jl 2>&1 | tee /tmp/diag-test05.log
    ```

    Expected output (struktura):
    ```
    [diagnostyka] T₀_calibrated (2σ) = X.XXX
    [diagnostyka] sample 1000 random 2-opt deltas, NN start:
        n_positive = N
        mean(positive) = X.XXX
        std(positive)  = X.XXX
        p50(positive)  = X.XXX
        p95(positive)  = X.XXX
    [diagnostyka] kandydat T₀ (B3, χ₀=0.5) = mean/ln2 = X.XXX
    [diagnostyka] kandydat T₀ (B3, χ₀=0.8) = mean/ln(1.25) = X.XXX
    [diagnostyka] mini-run T₀=0.01: ratio=X.XXX, acceptance_worsening_first1k=X.XX%
    [diagnostyka] mini-run T₀=0.05: ratio=X.XXX, acceptance_worsening_first1k=X.XX%
    [diagnostyka] mini-run T₀=0.1:  ratio=X.XXX, acceptance_worsening_first1k=X.XX%
    [diagnostyka] mini-run T₀=0.5:  ratio=X.XXX, acceptance_worsening_first1k=X.XX%
    [diagnostyka] mini-run T₀={cal}: ratio=X.XXX, acceptance_worsening_first1k=X.XX%
    [diagnostyka] REKOMENDACJA: T₀={best_value} → ratio={best_ratio} (50k kroków)
    ```

    Skrypt powinien się zakończyć w < 5 min (5 × 50_000 SA kroków + setup).

    Decyzja kierująca fazę B (Task 3):
    - Jeśli `T₀_calibrated` daje ratio > 1 (tj. SA pogarsza NN): potwierdza hipotezę handoff. Wybierz B3 jeżeli `mean(positive)/ln(2)` daje ratio ≤ 0.9.
    - Jeśli T₀=0.05 (B1 prior) daje ratio ≤ 0.9 ale B3 też: wybierz B3 (mniej magic constants).
    - Jeśli żadne T₀ z 5 testowanych nie daje ratio ≤ 0.9 w 50_000 kroków: trzeba albo zwiększyć budżet, albo wprowadzić cooling change (poza scope tego planu — przejdź do plan 02-15 albo zostaw 200k).
  </how-to-verify>
  <resume-signal>Wewnętrzne: po pomyślnym zakończeniu skryptu zapisać output do `/tmp/diag-test05.log` i przejść do Task 3.</resume-signal>
  <acceptance_criteria>
    - `julia --project=. bench/diagnostyka_test05.jl` exit 0
    - Output zawiera 5 mini-run rezultatów + REKOMENDACJĘ
    - Co najmniej jeden T₀ daje ratio ≤ 0.9 w 50_000 kroków (jeśli żaden — patrz contingency w how-to-verify)
    - Output zacytowany w 02-14-SUMMARY.md (Task 8)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Decyzja Faza B — wybór techniki na podstawie danych z Task 2</name>
  <files>.planning/phases/02-energy-sa-algorithm-test-suite/02-14-DECISION.md</files>
  <read_first>
    - /tmp/diag-test05.log (output Task 2)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-CONTEXT.md sekcja D-03
  </read_first>
  <action>
    Stworzyć plik `02-14-DECISION.md` (1-stronicowy, krótki):

    ```markdown
    # Plan 02-14 — Decyzja Faza B

    ## Dane z Faza A (bench/diagnostyka_test05.jl)
    [tabelka z danymi: T₀_calibrated, mean(positive), 5 mini-runów]

    ## Wybór: B{1|2|3}
    Uzasadnienie: [konkretne dane wskazujące]

    ## Szczegóły implementacji (Faza C)
    [funkcja do edycji, dokładna zmiana, miejsce w pliku]
    ```

    **Reguły wyboru:**
    - Domyślnie B3, jeżeli `mean(positive)/ln(2)` daje ratio ≤ 0.9 w 50k.
    - Jeżeli B3 nie wystarczy ale T₀=0.05 (fixed) wystarczy: B1 (kwarg) — ale dokumentuj w errata że jest to magic constant.
    - Jeżeli żadne nie wystarczy: zatrzymać się tutaj, oznaczyć plan 02-14 jako WIP, zaproponować plan 02-15 (cooling tuning lub list-based SA).
  </action>
  <verify>
    <automated>test -f .planning/phases/02-energy-sa-algorithm-test-suite/02-14-DECISION.md</automated>
  </verify>
  <acceptance_criteria>
    - `02-14-DECISION.md` istnieje
    - Wybór jednoznaczny (B1 LUB B2 LUB B3)
    - Uzasadnienie cytuje konkretne wartości z Task 2 outputu
    - Sekcja "Szczegóły implementacji" daje precyzyjne instrukcje dla Task 4
  </acceptance_criteria>
  <done>
    Decyzja zarchiwizowana, blueprint dla Task 4 gotowy.
  </done>
</task>

<task type="auto">
  <name>Task 4: Implementacja fixu (Faza C — code change)</name>
  <files>src/energia.jl OR src/algorytmy/simulowane_wyzarzanie.jl (zgodnie z decyzją Task 3)</files>
  <read_first>
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-14-DECISION.md
    - src/energia.jl (jeśli zmienia się kalibruj_T0)
    - src/algorytmy/simulowane_wyzarzanie.jl (jeśli zmienia się SimAnnealing constructor)
  </read_first>
  <action>
    Wykonać zmianę kodu zgodnie z 02-14-DECISION.md. Zachować:
    - Sygnaturę publiczną (`kalibruj_T0(stan; n_probek, rng)::Float64` LUB `SimAnnealing(stan; alfa, cierpliwosc, T_zero)`)
    - Type stability (return type adnotacja `::Float64` zostaje)
    - 3-way dispatch z WR-01 (gap-closure 02-11) jeśli edytowane jest `kalibruj_T0` — fallback dla `length(worsening) ∈ {0, 1}` musi nadal istnieć
    - Komentarze dokumentujące zmianę (referencja do plan 02-14, D-03 erratum)

    Nie usuwać poprzednich gap-closure (BL-01, WR-01) — tylko zmienić formułę zwracania T₀.
  </action>
  <verify>
    <automated>
      julia --project=. -e 'using JuliaCity; using JuliaCity: kalibruj_T0; println("kalibruj_T0 ok")'
    </automated>
  </verify>
  <acceptance_criteria>
    - Plik(i) `src/...` zmodyfikowane zgodnie z 02-14-DECISION.md
    - Komentarz odsyłający do plan 02-14 obecny
    - `using JuliaCity` ładuje się bez błędów
    - Sygnatury publiczne nieuszkodzone (compile check)
  </acceptance_criteria>
  <done>
    Fix algorytmiczny zaaplikowany.
  </done>
</task>

<task type="auto">
  <name>Task 5: Edytować test/test_baselines.jl — TEST-05 budżet 200_000 → 50_000</name>
  <files>test/test_baselines.jl</files>
  <read_first>
    - test/test_baselines.jl linie 86-127 (TEST-05 testset)
  </read_first>
  <action>
    Zmienić w `test/test_baselines.jl`:

    1. Linia 119: `params = Parametry(liczba_krokow=200_000)` → `params = Parametry(liczba_krokow=50_000)`
    2. Komentarz z linii 89-94 (Pitfall G level 2 history): zaktualizować — usunąć narrację 20k/50k/200k, wstawić krótką notkę:
       ```
       # Plan 02-14 fix: kalibruj_T0 zwraca T₀ adekwatne dla NN-start (B3 lub B1 — patrz 02-CONTEXT D-03 erratum).
       # 50_000 kroków wystarcza gdy T₀ jest poprawnie skalibrowane (oryginalny budżet z PLAN 02-05).
       ```
    3. `@info "TEST-05: ..."` zostawić bez zmian (provide ratio observation).
  </action>
  <verify>
    <automated>grep -c "liczba_krokow=50_000" test/test_baselines.jl</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "liczba_krokow=50_000" test/test_baselines.jl` returns >= 1
    - `grep -c "liczba_krokow=200_000" test/test_baselines.jl` returns 0
    - Komentarz nad TEST-05 cytuje "Plan 02-14"
    - Reszta TEST-05 testseta nieuszkodzona
  </acceptance_criteria>
  <done>
    Budżet TEST-05 wraca do oryginalnego (50_000) — możliwy dzięki fixowi z Task 4.
  </done>
</task>

<task type="checkpoint:auto-action" gate="blocking">
  <name>Task 6: Pkg.test() pełen suite — MUSI exit 0 z 222 PASS, 0 FAIL/ERROR/BROKEN</name>
  <what-built>
    Faza C complete (Task 4 + Task 5). Spodziewamy się że TEST-05 przechodzi.
  </what-built>
  <how-to-verify>
    ```bash
    julia --project=. -e 'using Pkg; Pkg.test()' 2>&1 | tee /tmp/pkgtest-02-14.log
    echo "Exit: $?"
    ```

    Expected:
    - Exit code 0
    - Test Summary: `JuliaCity | 222 PASS, 0 FAIL, 0 ERROR, 0 BROKEN`
    - TEST-05 testset: `1 PASS` (nie FAIL)
    - Aqua TEST-06: `9 PASS`
    - JET TEST-07: `4 PASS` (zmieniona funkcja musi być nadal type-stable)

    Specyficzne checks:
    ```bash
    grep -cE "0 Fail" /tmp/pkgtest-02-14.log    # MUST be >= 1
    grep -cE "0 Error" /tmp/pkgtest-02-14.log   # MUST be >= 1
    grep -cE "0 Broken" /tmp/pkgtest-02-14.log  # MUST be >= 1 (lub 0 broken in summary line)
    grep -A 2 "TEST-05: NN-baseline-beat" /tmp/pkgtest-02-14.log
    grep -A 2 "JET type stability" /tmp/pkgtest-02-14.log
    ```

    Failure modes + responses:
    - **TEST-05 nadal FAIL**: oznacza że wybór z Task 3 był błędny LUB B3 nie wystarcza. Re-uruchomić bench/diagnostyka_test05.jl, sprawdzić czy fix faktycznie się wczytuje (compile cache issues — `using Revise; Revise.revise()` lub fresh REPL). Jeśli faktycznie B3 nie wystarcza: rozważyć B1 (T_zero kwarg) albo eskalować do plan 02-15.
    - **JET TEST-07 FAIL** na zmienionej `kalibruj_T0`: dodać `::Float64` annotacje na każdym `return` branch (już są, ale weryfikuj). Jeśli nadal — dodać `@inline` adnotację lub sprawdzić czy `using Statistics` nadal w `src/energia.jl`.
    - **TEST-03 zero alloc regression**: oznacza że nowy `kalibruj_T0` allokuje wewnątrz hot path. ALE: `kalibruj_T0` jest wywoływane RAZ przed pętlą SA, więc allocations w `kalibruj_T0` są irrelevant — TEST-03 mierzy `symuluj_krok!`, nie `kalibruj_T0`. Jeśli regresja faktyczna: sprawdzić czy fix nie ruszył `delta_energii` (nie powinien).
    - **TEST-08 golden value mismatch**: TRASA_REF/ENERGIA_REF dotyczą N=20 (test_symulacja.jl), nie N=1000 (TEST-05). Fix algorytmu zmienia trajektorię → wartości N=20 też mogą się zmienić. Re-capture refs przez `julia --project=. -e 'include("...")'` — TEST-08 nie ma być golden dla aktualnego algorytmu, jest snapshot dla deterministycznej regresji. Akceptujemy update REFs jeśli TEST-08 to wymaga.
  </how-to-verify>
  <resume-signal>Wewnętrzne: po exit 0 zapisać Test Summary do /tmp/pkgtest-02-14.log i przejść do Task 7.</resume-signal>
  <acceptance_criteria>
    - `julia --project=. -e 'using Pkg; Pkg.test()'` exit 0
    - Test Summary: 222 PASS (lub więcej, jeśli dodano nowe testy), 0 FAIL, 0 ERROR, 0 BROKEN
    - TEST-05 PASS
    - Aqua TEST-06 PASS (9/9)
    - JET TEST-07 PASS (4/4)
    - TEST-03 zero alloc PASS (lub niezmienione vs poprzedni run)
    - TEST-08 PASS (lub TRASA_REF/ENERGIA_REF zaktualizowane jeśli algorytm zmienił trajektorię — udokumentowane w SUMMARY)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 7: Update 02-CONTEXT.md — D-03 erratum z empiryczną diagnozą + wybraną techniką</name>
  <files>.planning/phases/02-energy-sa-algorithm-test-suite/02-CONTEXT.md</files>
  <read_first>
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-CONTEXT.md sekcja D-03
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-14-DECISION.md
    - /tmp/diag-test05.log
  </read_first>
  <action>
    W `02-CONTEXT.md` znaleźć sekcję `D-03` (LOCKED w fazie 1) i dopisać pod nią `D-03 erratum (plan 02-14)`:

    ```markdown
    ### D-03 erratum (plan 02-14, 2026-04-30)

    **Empiryczne stwierdzenie:** Pierwotna formuła `T₀ = 2σ(worsening_deltas)` (D-03 LOCKED w fazie 1) była skalibrowana dla random tour startu. Plan 02-13 wykrył że dla NN-start (`inicjuj_nn!`) ta sama formuła daje T₀ wyrzucające SA z basena NN — TEST-05 ratio 1.65 (cel ≤ 0.9) po 200_000 krokach.

    **Diagnoza (bench/diagnostyka_test05.jl):**
    - T₀_calibrated_2σ ≈ {wartość z diag log}
    - mean(positive_deltas) na NN-start ≈ {wartość}
    - acceptance ratio worsening dla T₀_2σ w pierwszych 1000 krokach: {wartość}%
    - Mini-run T₀ ∈ {0.01, 0.05, 0.1, 0.5, T₀_2σ} → najlepsze ratio przy T₀={best}, ratio={best_ratio}

    **Zmiana:** {opis wybranej techniki B1/B2/B3}.

    **Wpływ:**
    - TEST-05 PASS przy oryginalnym budżecie 50_000 kroków (PLAN 02-05).
    - kalibruj_T0 zachowuje sygnaturę publiczną.
    - TEST-08 golden values: {bez zmian / zaktualizowane — udokumentowane}.
    ```

    Nie usuwać oryginalnego D-03 (LOCKED). Erratum jest dopiskiem.
  </action>
  <verify>
    <automated>grep -c "D-03 erratum (plan 02-14" .planning/phases/02-energy-sa-algorithm-test-suite/02-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "D-03 erratum (plan 02-14" 02-CONTEXT.md` returns 1
    - Erratum cytuje konkretne wartości z /tmp/diag-test05.log
    - Oryginalny D-03 LOCKED nieusunięty
  </acceptance_criteria>
  <done>
    Audit trail kompletny — przyszli developerzy widzą dlaczego formuła się zmieniła.
  </done>
</task>

<task type="auto">
  <name>Task 8: Stworzyć 02-14-SUMMARY.md + zaktualizować deferred-items.md</name>
  <files>
    .planning/phases/02-energy-sa-algorithm-test-suite/02-14-SUMMARY.md
    .planning/phases/02-energy-sa-algorithm-test-suite/deferred-items.md
  </files>
  <read_first>
    - /tmp/diag-test05.log
    - /tmp/pkgtest-02-14.log
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-14-DECISION.md
    - .planning/phases/02-energy-sa-algorithm-test-suite/deferred-items.md (TEST-05 entry)
  </read_first>
  <action>
    1. Stworzyć `02-14-SUMMARY.md` w stylu poprzednich (02-12-SUMMARY.md jako template):
       - Header (plan, requirements covered, files modified)
       - Streszczenie (1 paragraf)
       - Faza A: empirical diagnosis (cytat /tmp/diag-test05.log)
       - Faza B: decision (z 02-14-DECISION.md)
       - Faza C: implementation (diff podsumowany)
       - Test Summary (cytat z /tmp/pkgtest-02-14.log)
       - Phase 2 closure: 21/21 REQ-IDs runtime-verified, 5/5 ROADMAP success criteria met

    2. Edytować `deferred-items.md`:
       - W tabeli zmienić TEST-05 status z `OPEN — see "Plan 02-13 WIP handoff" below` na `RESOLVED in plan 02-14`
       - W sekcji "Resolution log" dodać entry:
         ```
         - **Plan 02-14 (algorithm fix, runtime)** — TEST-05 NN-baseline-beat domkniete.
           - Diagnoza: kalibruj_T0=2σ skalibrowane dla random startu; dla NN-start daje T₀ wyrzucające z minimum.
           - Fix: {wybrana technika B1/B2/B3}.
           - Test Summary: 222 PASS, 0 FAIL/ERROR/BROKEN.
           - Phase 2 ROADMAP success criteria #4 (SA ≥10% pod NN) empirically met.
         ```
       - Zaktualizować "Last updated" line na ISO date 2026-04-30 + plan 02-14.
       - Zostawić sekcję "Plan 02-13 WIP handoff" tak jak jest (historical record).
  </action>
  <verify>
    <automated>
      test -f .planning/phases/02-energy-sa-algorithm-test-suite/02-14-SUMMARY.md &amp;&amp; grep -c "RESOLVED in plan 02-14" .planning/phases/02-energy-sa-algorithm-test-suite/deferred-items.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `02-14-SUMMARY.md` istnieje, zawiera Test Summary z /tmp/pkgtest-02-14.log
    - `grep -c "RESOLVED in plan 02-14" deferred-items.md` returns 1
    - `grep -c "OPEN — see \"Plan 02-13 WIP handoff\"" deferred-items.md` returns 0 (status zaktualizowany)
    - Plan 02-13 WIP handoff sekcja preserved (historyczne)
  </acceptance_criteria>
  <done>
    Plan 02-14 dokumentacyjnie zamknięty.
  </done>
</task>

<task type="auto">
  <name>Task 9: Zaktualizować STATE.md i ROADMAP.md — Phase 2 = 100%</name>
  <files>
    .planning/STATE.md
    .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/STATE.md
    - .planning/ROADMAP.md (sekcja Phase 2)
  </read_first>
  <action>
    1. `STATE.md`:
       - Frontmatter: `completed_phases: 1 → 2`, `total_plans: 12 → 14`, `completed_plans: 11 → 14`, `percent: 92 → 50` (1/4 → 2/4 phases)
       - "Current Position": Phase 2 → COMPLETE. Next: Phase 3 (visualization & export).
       - "Last Action": "Phase 2 closed: TEST-05 NN-baseline-beat fixed via plan 02-14 (kalibruj_T0 erratum); Pkg.test() 222 PASS"
       - "Next Action": `/gsd-discuss-phase 3` (lub `/gsd-plan-phase 3` zależnie od preferencji)
       - "Roadmap Snapshot": [x] Phase 2

    2. `ROADMAP.md`:
       - Phase 2 sekcja: status `IN PROGRESS` → `COMPLETE` (lub odpowiednik)
       - Wave 11 entry dla plan 02-14
  </action>
  <verify>
    <automated>grep -c "Phase 2.*COMPLETE\|completed_phases: 2" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md: `completed_phases: 2`, Phase 2 oznaczona `[x]` w Roadmap Snapshot
    - ROADMAP.md: Phase 2 status COMPLETE
    - "Next Action" wskazuje Phase 3
  </acceptance_criteria>
  <done>
    Tracking docs spójne — Phase 2 oficjalnie zamknięta.
  </done>
</task>

</tasks>

<verification>
Po wszystkich 9 taskach:

1. `julia --project=. -e 'using Pkg; Pkg.test()'` exit 0
2. `grep -c "TEST-05: NN-baseline-beat" /tmp/pkgtest-02-14.log` returns 1 + sąsiedni `1 PASS`
3. `grep -c "RESOLVED in plan 02-14" deferred-items.md` returns 1
4. `grep -c "D-03 erratum (plan 02-14" 02-CONTEXT.md` returns 1
5. `test -f bench/diagnostyka_test05.jl` true
6. `test -f .planning/phases/02-energy-sa-algorithm-test-suite/02-14-SUMMARY.md` true
7. STATE.md `completed_phases: 2`

Plan 02-14 zamyka:
- TEST-05 (NN-baseline-beat ≥10%) — empirical PASS przy 50_000 kroków
- Phase 2 ROADMAP SC #4 (SA bije NN baseline)
- Phase 2 jako całość: wszystkie 21 REQ-IDs runtime-verified
</verification>

<success_criteria>
- TEST-05 PASS jako hard `@test` (nie `@test_broken`)
- Pkg.test() exit 0 z 222 PASS, 0 FAIL/ERROR/BROKEN
- bench/diagnostyka_test05.jl zacommitowany jako baseline dla regresji
- 02-CONTEXT.md D-03 erratum udokumentowany z empirycznymi danymi
- Phase 2 ROADMAP success criteria 1-5 wszystkie spełnione
- TEST-03 zero alloc nadal PASS (brak regresji w `symuluj_krok!`)
</success_criteria>

<output>
Po completion, `02-14-SUMMARY.md` zawiera:
- Empirical diagnosis (cytat z /tmp/diag-test05.log)
- Decision rationale (B1/B2/B3 wybór + dlaczego)
- Code change diff (1-2 linie w `kalibruj_T0` lub `SimAnnealing`)
- Test Summary excerpt (Pkg.test output)
- TEST-08 update note (jeśli TRASA_REF/ENERGIA_REF się zmieniły)
- Phase 2 closure marker: "Phase 2 COMPLETE — 21/21 REQ-IDs runtime-verified, 5/5 ROADMAP SC met"
- Pointer do plan 03-01 (wizualizacja)
</output>
