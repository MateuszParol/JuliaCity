---
phase: 02-energy-sa-algorithm-test-suite
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/baselines.jl
  - src/JuliaCity.jl
autonomous: true
requirements:
  - ALG-04
tags:
  - julia
  - baseline
  - nearest-neighbor

must_haves:
  truths:
    - "src/baselines.jl eksportuje 2 funkcje: trasa_nn (pure) i inicjuj_nn! (mutating wrapper)"
    - "trasa_nn(D; start=1) zwraca permutacje 1:n - sort(trasa_nn(D)) == 1:n"
    - "inicjuj_nn!(stan) wypelnia stan.D, stan.trasa, stan.energia w kolejnosci D-14"
    - "src/JuliaCity.jl include-uje baselines.jl po energia.jl"
  artifacts:
    - path: "src/baselines.jl"
      provides: "NN baseline tour + mutating Stan initializer"
      contains: "function trasa_nn"
      min_lines: 40
    - path: "src/JuliaCity.jl"
      provides: "include + export dla baselines.jl"
      contains: "include(\"baselines.jl\")"
  key_links:
    - from: "src/baselines.jl::inicjuj_nn!"
      to: "src/energia.jl::oblicz_macierz_dystans!"
      via: "wywolanie wewnatrz inicjuj_nn!"
      pattern: "oblicz_macierz_dystans!\\(stan\\)"
    - from: "src/baselines.jl::inicjuj_nn!"
      to: "src/energia.jl::oblicz_energie"
      via: "stan.energia = oblicz_energie(stan.D, stan.trasa, bufor)"
      pattern: "oblicz_energie\\(stan\\.D"
---

<objective>
Zaimplementowac NN-baseline jako pure entry point (`trasa_nn`) plus mutating wrapper (`inicjuj_nn!`) ktory inicjalizuje `stan.D`, `stan.trasa`, `stan.energia` przed startem SA.

Purpose: Pokrywa REQ ALG-04 - NN jest uzywane jako (a) start dla SA (`inicjuj_nn!` w `examples/`/testach), (b) baseline jakosci dla TEST-05 (SA musi byc >=10% krotszy niz NN). Dwa entry points (D-14): pure dla TEST-05 bez Stana, mutating dla flow init.

Output:
- src/baselines.jl - nowy plik z `trasa_nn(D; start=1)` i `inicjuj_nn!(stan)`
- src/JuliaCity.jl - include + eksport `trasa_nn`, `inicjuj_nn!`
- Dowod: po `inicjuj_nn!(stan)` mamy `sort(stan.trasa) == 1:n` oraz `stan.energia > 0`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-CONTEXT.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-RESEARCH.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-PATTERNS.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-02-SUMMARY.md
@CLAUDE.md
@src/typy.jl
@src/energia.jl
@src/JuliaCity.jl

<interfaces>
<!-- Kontrakty z 02-02 (energia.jl). Executor NIE musi szukac. -->

From src/energia.jl (utworzony w 02-02):
```julia
function oblicz_macierz_dystans!(stan::StanSymulacji)::Nothing
function oblicz_energie(D::Matrix{Float64}, trasa::Vector{Int}, bufor::Vector{Float64})::Float64
function oblicz_energie(punkty::Vector{Punkt2D}, trasa::Vector{Int})::Float64
function delta_energii(stan::StanSymulacji, i::Int, j::Int)::Float64
function kalibruj_T0(stan::StanSymulacji; n_probek::Int=1000, rng=stan.rng)::Float64
```

From CONTEXT.md D-14 (dual entry points - LOCKED):
- Pure: `trasa_nn(D::Matrix{Float64}; start::Int=1)::Vector{Int}` - uzywana w TEST-05 bez Stana
- Mutating wrapper: `inicjuj_nn!(stan)`:
  1. oblicz_macierz_dystans!(stan) - wypelnia stan.D
  2. stan.trasa = trasa_nn(stan.D; start=1)
  3. stan.energia = oblicz_energie(stan.D, stan.trasa, bufor)
  4. stan.iteracja = 0

From CONTEXT.md D-15: start=1 zawsze (brak RNG-zaleznosci). Test NN-baseline-beat: SA-z-start=1 vs NN-z-start=1.

From RESEARCH Pattern 3 (NN baseline pattern):
```julia
function trasa_nn(D::Matrix{Float64}; start::Int=1)
    n = size(D, 1)
    @assert n == size(D, 2) "D must be square"
    odwiedzone = falses(n)
    trasa = Vector{Int}(undef, n)
    trasa[1] = start
    odwiedzone[start] = true
    for k in 2:n
        biezacy = trasa[k - 1]
        najblizszy = 0
        min_dist = Inf
        @inbounds for j in 1:n
            if !odwiedzone[j] && D[biezacy, j] < min_dist
                min_dist = D[biezacy, j]
                najblizszy = j
            end
        end
        trasa[k] = najblizszy
        odwiedzone[najblizszy] = true
    end
    return trasa
end

function inicjuj_nn!(stan::StanSymulacji)
    oblicz_macierz_dystans!(stan)
    stan.trasa = trasa_nn(stan.D; start=1)
    bufor = zeros(Float64, Threads.nthreads())
    stan.energia = oblicz_energie(stan.D, stan.trasa, bufor)
    stan.iteracja = 0
    return nothing
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Utworzyc src/baselines.jl z trasa_nn + inicjuj_nn!</name>
  <files>src/baselines.jl</files>
  <read_first>
    - src/typy.jl (StanSymulacji shape)
    - src/punkty.jl (file-header convention, two-method idiom)
    - src/energia.jl (oblicz_macierz_dystans!, oblicz_energie - sygnatury z 02-02 SUMMARY)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-RESEARCH.md sekcja "Pattern 3: NN baseline z dual entry-points (D-14)" (linie 218-264)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-CONTEXT.md decyzje D-14 (dual entry points), D-15 (start=1 zawsze)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-PATTERNS.md sekcja "src/baselines.jl (service / transform + mutate)" (linie 196-231)
  </read_first>
  <action>
    Utworzyc NOWY plik `src/baselines.jl` (UTF-8, LF, no BOM, NFC normalized; ASCII filename per BOOT-04).

    Top-of-file komentarz (mirror src/punkty.jl + src/energia.jl konwencja):
    ```julia
    # NN-baseline (Nearest Neighbor) tour + mutating Stan initializer.
    # Pokrywa REQ ALG-04. Dwa entry points (D-14):
    #   - trasa_nn(D; start) - pure, uzywana w TEST-05 NN-baseline-beat bez Stana
    #   - inicjuj_nn!(stan) - mutating wrapper, wypelnia stan.D + stan.trasa + stan.energia
    # NN nie jest <:Algorytm (brak symuluj_krok!) - dlatego src/baselines.jl, nie src/algorytmy/.
    # start=1 zawsze (D-15) - brak RNG-zaleznosci.
    # Asercje wewnetrzne po angielsku per LANG-04.
    ```

    NIE dodawac `using` statementow - wszystko w scope przez `src/JuliaCity.jl`.

    **Funkcja 1: trasa_nn(D::Matrix{Float64}; start::Int=1)::Vector{Int}**

    Polish docstring z REQ ALG-04 + cross-ref D-14/D-15. `# Examples` jldoctest (skopiuj wzorzec z `src/punkty.jl`).
    Algorytm doslownie z RESEARCH Pattern 3 (linie 226-247):
    ```julia
    function trasa_nn(D::Matrix{Float64}; start::Int=1)::Vector{Int}
        n = size(D, 1)
        @assert n == size(D, 2) "D must be square"
        @assert 1 <= start <= n "start out of range"
        odwiedzone = falses(n)
        trasa = Vector{Int}(undef, n)
        trasa[1] = start
        odwiedzone[start] = true
        @inbounds for k in 2:n
            biezacy = trasa[k - 1]
            najblizszy = 0
            min_dist = Inf
            for j in 1:n
                if !odwiedzone[j] && D[biezacy, j] < min_dist
                    min_dist = D[biezacy, j]
                    najblizszy = j
                end
            end
            trasa[k] = najblizszy
            odwiedzone[najblizszy] = true
        end
        return trasa
    end
    ```

    Property invariant (verify w testach): `sort(trasa_nn(D)) == 1:n` (permutacja 1:n).

    **Funkcja 2: inicjuj_nn!(stan::StanSymulacji)::Nothing**

    Polish docstring z REQ ALG-04 + cross-ref D-14. Bang-function (mutates stan).
    Algorytm doslownie z RESEARCH Pattern 3 (linie 256-263):
    ```julia
    function inicjuj_nn!(stan::StanSymulacji)
        oblicz_macierz_dystans!(stan)                          # wypelnia stan.D
        stan.trasa = trasa_nn(stan.D; start=1)                 # D-15: start=1
        bufor = zeros(Float64, Threads.nthreads())             # alloc OK - wywolywane raz
        stan.energia = oblicz_energie(stan.D, stan.trasa, bufor)
        stan.iteracja = 0
        return nothing
    end
    ```

    Adnotacja `::Nothing` na sygnaturze NIE jest dodawana (Julia infers void return). Ale `return nothing` MUSI byc explicit (per Pitfall B - @inferred ::Nothing wymaga literal return nothing).

    Konwencje:
    - Polish prose docstring + Polish komentarze inline.
    - English asserts (`"D must be square"`, `"start out of range"`).
    - ASCII identyfikatory (`odwiedzone`, `biezacy`, `najblizszy`, `min_dist` - bez diakrytykow).
    - `@inbounds` na zewnetrznym for w trasa_nn - bezpieczne bo asercje sprawdzaja n + start.
  </action>
  <verify>
    <automated>julia --project=. -e 'using JuliaCity, Random; pkty = generuj_punkty(20; seed=42); stan = StanSymulacji(pkty); inicjuj_nn!(stan); @assert sort(stan.trasa) == collect(1:20) "trasa not a permutation"; @assert stan.energia > 0 "energia should be positive"; @assert stan.iteracja == 0 "iteracja should be reset"; @assert stan.D[1,1] == 0.0 "D diagonal not zero"; println("OK inicjuj_nn! - n=", length(stan.trasa), " energia=", round(stan.energia, digits=4))'</automated>
  </verify>
  <acceptance_criteria>
    - Plik src/baselines.jl istnieje
    - `grep -c '^function trasa_nn' src/baselines.jl` zwraca 1
    - `grep -c '^function inicjuj_nn!' src/baselines.jl` zwraca 1
    - `grep -c 'oblicz_macierz_dystans!' src/baselines.jl` zwraca przynajmniej 1
    - `grep -c 'oblicz_energie' src/baselines.jl` zwraca przynajmniej 1
    - `grep -c 'return nothing' src/baselines.jl` zwraca przynajmniej 1 (inicjuj_nn! return)
    - `grep -c 'odwiedzone' src/baselines.jl` zwraca przynajmniej 2 (init + check w petli)
    - Plik ma >= 40 linii: `awk 'END {print NR}' src/baselines.jl` >= 40
    - ASCII filename: `baselines.jl` (oczywiste)
    - Po Task 2 wired: smoke test `inicjuj_nn!` zwraca permutacje 1:n
  </acceptance_criteria>
  <done>
    src/baselines.jl zawiera trasa_nn (pure) i inicjuj_nn! (mutating). Pure funkcja zwraca Vector{Int} bedacy permutacja 1:n. Mutating wrapper wykonuje 4 kroki (oblicz_macierz_dystans! -> trasa_nn -> oblicz_energie -> reset iteracja). Wszystko po polsku w docstring/komentarzach, English asserts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire src/baselines.jl do src/JuliaCity.jl</name>
  <files>src/JuliaCity.jl</files>
  <read_first>
    - src/JuliaCity.jl (po edycjach z 02-01 i 02-02)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-PATTERNS.md sekcja "src/JuliaCity.jl (MODIFIED)" (linie 422-491) - topological order
  </read_first>
  <action>
    Modyfikacja `src/JuliaCity.jl` - dodac `include("baselines.jl")` PO `include("energia.jl")`. Topological order: `typy -> punkty -> energia -> baselines`.

    Pattern (po wszystkich modyfikacjach 02-01..02-03):

    ```julia
    module JuliaCity

    # Zewnetrzne zaleznosci runtime
    using GeometryBasics: Point2
    using Random
    using ChunkSplitters
    using Statistics: std

    # Typy domenowe (Punkt2D, Algorytm, StanSymulacji, Parametry)
    include("typy.jl")

    # Generator punktow testowych (PKT-01..04)
    include("punkty.jl")

    # Energia + macierz dystansow + delta + kalibracja T0 (Phase 2 Plan 02-02)
    include("energia.jl")

    # Baseline NN (Phase 2 Plan 02-03)
    include("baselines.jl")

    # Eksport publicznego API
    export Punkt2D, StanSymulacji, Algorytm, generuj_punkty,
           Parametry,
           oblicz_macierz_dystans!, oblicz_energie, delta_energii, kalibruj_T0,
           trasa_nn, inicjuj_nn!

    end # module
    ```

    NIE dodawac jeszcze `include("algorytmy/simulowane_wyzarzanie.jl")` - to przyjdzie w 02-04.
    NIE dodawac do exportu jeszcze `SimAnnealing`, `symuluj_krok!`.

    Konwencje:
    - Polish komentarz nad blokiem `include("baselines.jl")`.
    - Topologiczna kolejnosc: `energia.jl` PRZED `baselines.jl` (baselines.jl uzywa oblicz_macierz_dystans!, oblicz_energie z energia.jl).
  </action>
  <verify>
    <automated>julia --project=. -e 'using JuliaCity, Random; @assert isdefined(JuliaCity, :trasa_nn); @assert isdefined(JuliaCity, :inicjuj_nn!); pkty = generuj_punkty(10; seed=1); stan = StanSymulacji(pkty); inicjuj_nn!(stan); @assert sort(stan.trasa) == collect(1:10); @assert stan.energia > 0; D = stan.D; trasa = trasa_nn(D); @assert sort(trasa) == collect(1:10); println("OK trasa_nn + inicjuj_nn! exports work")'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'include("baselines.jl")' src/JuliaCity.jl` zwraca 1
    - `grep -c 'trasa_nn' src/JuliaCity.jl` zwraca przynajmniej 1 (export)
    - `grep -c 'inicjuj_nn!' src/JuliaCity.jl` zwraca przynajmniej 1 (export)
    - Topologiczna kolejnosc weryfikuje sie przez kolejnosc include w pliku: typy < punkty < energia < baselines
    - Phase 1 testy nadal przechodza: `julia --project=. -e 'using Pkg; Pkg.test()'` exits 0
    - Smoke test inicjuj_nn! przechodzi: po wywolaniu, sort(stan.trasa)==1:n i stan.energia>0
  </acceptance_criteria>
  <done>
    src/JuliaCity.jl ma include("baselines.jl") po energia.jl w topologicznej kolejnosci. Export rozszerzony o trasa_nn, inicjuj_nn!. Smoke test pokazuje ze inicjuj_nn!(stan) wypelnia stan poprawnie.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public API -> internal | trasa_nn(D) jest publiczny; jezeli D nie jest kwadratowa lub start poza zakresem, asercja katastrofuje (English message) |
| inicjuj_nn! mutates Stan | inicjuj_nn! mutates stan.D, stan.trasa, stan.energia - wymagana dyscyplina ze tylko jeden caller na raz; trywialne bo single-threaded init |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | trasa_nn dla niekwadratowej D | mitigate | @assert n == size(D, 2) "D must be square" |
| T-02-08 | Tampering | trasa_nn dla start poza 1:n | mitigate | @assert 1 <= start <= n "start out of range" |
| T-02-09 | Information Disclosure | error messages | accept | Standard Julia error message convention; nie ma sekretow w D |

Severity: brak high. Pure-algorithmic baseline, brak network/secrets.
</threat_model>

<verification>
Per-task automated checks (powyzej w `<verify>` blokach).

**Plan-level integration check:**

```bash
# Smoke test - inicjuj_nn! na N=20
julia --project=. -e '
using JuliaCity, Random
pkty = generuj_punkty(20; seed=42)
stan = StanSymulacji(pkty)
inicjuj_nn!(stan)
@assert sort(stan.trasa) == collect(1:20) "trasa not permutation"
@assert stan.energia > 0
@assert stan.iteracja == 0
@assert stan.D[1,1] == 0.0
@assert stan.D[1,2] == stan.D[2,1]  # symmetric
# trasa_nn alone (without Stan)
trasa2 = trasa_nn(stan.D; start=1)
@assert trasa2 == stan.trasa  # deterministic
@assert sort(trasa2) == collect(1:20)
println("OK Plan 02-03 integration: trasa_nn + inicjuj_nn!")
'

# Pkg.test - Phase 1 + Wave 0 nadal zielone
julia --project=. -e 'using Pkg; Pkg.test()'

# NN energy is sane (> 0, < 4*sqrt(2)*N for unit-square N points)
julia --project=. -e '
using JuliaCity
pkty = generuj_punkty(100; seed=42)
stan = StanSymulacji(pkty)
inicjuj_nn!(stan)
@assert 0 < stan.energia < 100 * sqrt(2) "NN energy out of expected range: $(stan.energia)"
println("OK NN energy on N=100: ", round(stan.energia, digits=4))
'
```
</verification>

<success_criteria>
1. **src/baselines.jl istnieje, zawiera 2 funkcje (trasa_nn, inicjuj_nn!); UTF-8 NFC bez BOM; ASCII filename.**
2. **trasa_nn(D; start=1) zwraca Vector{Int} o dlugosci size(D,1) bedacy permutacja 1:n; deterministyczny dla danego D + start.**
3. **inicjuj_nn!(stan) wypelnia stan.D, stan.trasa, stan.energia w kolejnosci D-14; ustawia stan.iteracja = 0.**
4. **src/JuliaCity.jl: include("baselines.jl") w topologicznej kolejnosci po energia.jl; export rozszerzony o trasa_nn, inicjuj_nn!.**
5. **Pkg.test() przechodzi (Phase 1 + Wave 0 testy zachowane).**
</success_criteria>

<output>
After completion, create `.planning/phases/02-energy-sa-algorithm-test-suite/02-03-SUMMARY.md` zawierajace:

- Liczbe linii w `src/baselines.jl` (sanity check >= 40)
- NN energia dla N=100 seed=42 (jako referencja dla Phase 4 quality checks)
- Czy wszystkie 2 nowe nazwy sa w `JuliaCity` exports
- Czy `inicjuj_nn!` wypelnia stan.D symetrycznie (smoke check)
</output>
