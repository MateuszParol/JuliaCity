---
phase: 02-energy-sa-algorithm-test-suite
plan: 06
type: execute
wave: 6
depends_on: [02-05]
files_modified:
  - Project.toml
  - test/runtests.jl
autonomous: true
requirements:
  - TEST-06
  - TEST-07
tags:
  - julia
  - test-orchestration
  - aqua
  - jet
  - ci

must_haves:
  truths:
    - "Project.toml [extras] zawiera PerformanceTestTools z poprawnym UUID"
    - "Project.toml [targets].test zawiera \"PerformanceTestTools\""
    - "test/runtests.jl wlacza 3 nowe pliki przez include: test_energia.jl, test_baselines.jl, test_symulacja.jl"
    - "test/runtests.jl ma rozszerzony testset Aqua.test_all (TEST-06) z deps_compat ignore [Random, Statistics]"
    - "test/runtests.jl ma testset JET.@test_opt (TEST-07) na 4 funkcjach z target_modules=(JuliaCity,)"
    - "Phase 1 stub JET smoke @report_opt zostal zastapiony pelnym TEST-07 (lub zostawiony obok jako historyczne)"
    - "julia --project=. -e 'using Pkg; Pkg.test()' exits 0 - wszystkie 21 REQ-IDow pokrytych"
  artifacts:
    - path: "Project.toml"
      provides: "[extras] + [targets].test rozszerzone o PerformanceTestTools"
      contains: "PerformanceTestTools ="
    - path: "test/runtests.jl"
      provides: "Pelny orchestrator: encoding + PKT + StanSymulacji + Wave 0 + 3 includes + Aqua + JET"
      contains: "include(\"test_energia.jl\")"
  key_links:
    - from: "test/runtests.jl"
      to: "test/test_energia.jl"
      via: "include(\"test_energia.jl\")"
      pattern: "include\\(\"test_energia\\.jl\"\\)"
    - from: "test/runtests.jl"
      to: "test/test_baselines.jl"
      via: "include(\"test_baselines.jl\")"
      pattern: "include\\(\"test_baselines\\.jl\"\\)"
    - from: "test/runtests.jl"
      to: "test/test_symulacja.jl"
      via: "include(\"test_symulacja.jl\")"
      pattern: "include\\(\"test_symulacja\\.jl\"\\)"
    - from: "test/runtests.jl::Aqua testset"
      to: "JuliaCity package"
      via: "Aqua.test_all(JuliaCity; ...)"
      pattern: "Aqua\\.test_all\\(JuliaCity"
    - from: "test/runtests.jl::JET testset"
      to: "@test_opt target_modules=(JuliaCity,)"
      via: "JET strict @test_opt na 4 publicznych funkcjach"
      pattern: "@test_opt target_modules=\\(JuliaCity,\\)"
---

<objective>
Domknac Phase 2 - finalne wpiecie wszystkiego do `test/runtests.jl` (single-command `Pkg.test()` uruchamia ALL TESTS), dodanie `PerformanceTestTools` do `[extras]`+`[targets].test`, rozszerzenie Aqua testsetu (TEST-06) i zastapienie Phase 1 JET smoke pelnym TEST-07 (`@test_opt target_modules=(JuliaCity,)` na 4 funkcjach).

Purpose: Po tym planie cala Phase 2 jest komplet - 21 REQ-IDow pokrytych, `julia --project=. -e 'using Pkg; Pkg.test()'` exits 0, CI matrix (3 OS x 3 Julia) gotowy do wykonania. To jest "delivery moment" Phase 2.

Output:
- Project.toml - [extras] + [targets].test rozszerzone o PerformanceTestTools
- test/runtests.jl - 3 nowe `include` dla test_*.jl + rozszerzone Aqua (TEST-06) + pelny JET (TEST-07)
- Dowod: `julia --project=. -e 'using Pkg; Pkg.test()'` exits 0, output zawiera Test Summary z >= 8 zielonych testset-ow (Phase 1 + Wave 0 + 3 includes + Aqua + JET)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-CONTEXT.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-RESEARCH.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-PATTERNS.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-01-SUMMARY.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-02-SUMMARY.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-03-SUMMARY.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-04-SUMMARY.md
@.planning/phases/02-energy-sa-algorithm-test-suite/02-05-SUMMARY.md
@CLAUDE.md
@Project.toml
@test/runtests.jl
@test/test_energia.jl
@test/test_baselines.jl
@test/test_symulacja.jl

<interfaces>
<!-- Pelny stan po wszystkich poprzednich planach. -->

Po Plan 02-05 mamy:
- 3 nowe test/test_*.jl pliki (uruchamiane standalone)
- 1 plan zostal: rozszerzyc test/runtests.jl + dodac PerformanceTestTools do extras
- TEST-06 (Aqua) i TEST-07 (JET) sa nadal "Phase 1 stub" - musimy je rozszerzyc o pelny check

PerformanceTestTools.jl UUID (Plan task musi zweryfikowac przez Pkg.add lokalnie):
- Probable UUID: `aaaa9989-eea8-4cf9-a8a8-cbdfa31a5ddd` (ale executor MUSI uruchomic `julia --project=. -e 'using Pkg; Pkg.add("PerformanceTestTools")'` i odczytac UUID z Manifest lub z generowanej linii w Project.toml)

Aqua.test_all kwargs (z Phase 1 + dodatki Phase 2):
```julia
Aqua.test_all(JuliaCity;
    ambiguities = (recursive = false,),
    stale_deps = false,
    deps_compat = (ignore = [:Random, :Statistics],
                   check_extras = (ignore = [:Test, :Unicode],)),
)
```

UWAGA: dodano `:Statistics` do `deps_compat ignore` (nowe Phase 2 - stdlib bez compat entry per Pattern D).
**NIE** dodawac `unbound_args=(broken=true,)` preemptywnie - Pitfall F mowi: dodaj TYLKO jezeli failuje.

JET TEST-07 pattern (z RESEARCH Example 5 linie 564-587):
```julia
@testset "JET type stability (TEST-07)" begin
    punkty = generuj_punkty(20; seed=42)
    stan = StanSymulacji(punkty; rng=Xoshiro(42))
    inicjuj_nn!(stan)
    alg = SimAnnealing(stan)
    stan.temperatura = alg.T_zero
    params = Parametry(liczba_krokow=100)
    bufor = zeros(Float64, Threads.nthreads())

    # warmup
    oblicz_energie(stan.D, stan.trasa, bufor)
    delta_energii(stan, 5, 17)
    symuluj_krok!(stan, params, alg)
    kalibruj_T0(stan; n_probek=10)

    # @test_opt z target_modules - automatyczny test fail na issue (RESEARCH §State of the Art)
    @test_opt target_modules=(JuliaCity,) oblicz_energie(stan.D, stan.trasa, bufor)
    @test_opt target_modules=(JuliaCity,) delta_energii(stan, 5, 17)
    @test_opt target_modules=(JuliaCity,) symuluj_krok!(stan, params, alg)
    @test_opt target_modules=(JuliaCity,) kalibruj_T0(stan; n_probek=10)
end
```

Phase 1 JET smoke testset (linie 180-194 w runtests.jl) jest do **zastapienia** pelnym TEST-07 (research §Pitfalls specific to runtests.jl line 601: "Phase 1 JET smoke test should be removed/replaced by the full TEST-07 testset above - no point keeping the soft-assertion smoke once we have hard @test_opt").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dodac PerformanceTestTools do Project.toml [extras] + [targets].test</name>
  <files>Project.toml</files>
  <read_first>
    - Project.toml (po Plan 02-01 powinien miec ChunkSplitters, Statistics)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-RESEARCH.md sekcja "Environment Availability" (linie 632-644) - "PerformanceTestTools.jl - Phase 2 plan musi dodac do [extras] + [targets]. UUID musi byc zweryfikowane"
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-PATTERNS.md sekcja "Project.toml (MODIFIED)" (linie 606-678)
  </read_first>
  <action>
    **A. Lokalnie dodac PerformanceTestTools do projektu:**
    ```bash
    julia --project=. -e 'using Pkg; Pkg.add("PerformanceTestTools")'
    ```
    To dopisze entry w `[deps]` (tymczasowo - przeniesiemy do `[extras]` w kroku B) i zaktualizuje Manifest.toml.

    **B. Odczytac UUID:**
    ```bash
    julia --project=. -e 'using Pkg; uuid = Pkg.dependencies()[Base.UUID(filter(p -> Pkg.dependencies()[p].name == "PerformanceTestTools", keys(Pkg.dependencies()))[1])].uuid; println("PerformanceTestTools = \"", uuid, "\"")'
    ```
    LUB prosciej:
    ```bash
    julia --project=. -e 'using Pkg; for (uuid, dep) in Pkg.dependencies(); if dep.name == "PerformanceTestTools"; println(dep.name, " = \"", uuid, "\""); end; end'
    ```

    **C. Modyfikacja Project.toml:**

    1. **PRZENIESC** wpis `PerformanceTestTools` z `[deps]` do `[extras]` (alfabetycznie - PRZED `StableRNGs`):
       ```toml
       [extras]
       Aqua = "4c88cf16-eb10-579e-8560-4a9242c79595"
       BenchmarkTools = "6e4b80f9-dd63-53aa-95a3-0cdb28fa8baf"
       GLMakie = "e9467ef8-e4e7-5192-8a1a-b1aee30e663a"
       JET = "c3a54625-cd67-489e-a8e7-0a5a0ff4e31b"
       Makie = "ee78f7c6-11fb-53f2-987a-cfe4a2b5a57a"
       Observables = "510215fc-4207-5dde-b226-833fc4488ee2"
       PerformanceTestTools = "<UUID-from-step-B>"
       StableRNGs = "860ef19b-820b-49d6-a774-d7a799459cd3"
       Test = "8dfed614-e22c-5e08-85e1-65c5234f0b40"
       Unicode = "4ec0a83e-493e-50e2-b9ac-8f72acf5a8f5"
       ```

    2. **DODAC** PerformanceTestTools do `[targets].test` (alfabetycznie - po JET, przed StableRNGs):
       ```toml
       [targets]
       test = ["Aqua", "JET", "PerformanceTestTools", "StableRNGs", "Test", "Unicode"]
       ```

    3. **NIE** dodawac `[compat]` entry dla PerformanceTestTools - test deps zwykle nie maja compat entries (chyba ze sa kapryśne; Aqua/JET/StableRNGs sa - mozna bez tez ale dla spojnosci ze Phase 1 stylem, zostawmy bez compat dla PerformanceTestTools).

    4. **Pkg.resolve()** - zaktualizowac Manifest.toml:
       ```bash
       julia --project=. -e 'using Pkg; Pkg.resolve()'
       ```

    UWAGA: Moze byc tak ze `Pkg.add("PerformanceTestTools")` w kroku A doda automatycznie `[deps]`+`[compat]` entries. Trzeba je RECZNIE PRZENIESC do `[extras]` (i usunac z `[deps]`+`[compat]` jezeli sie tam pojawily) - test-only deps zyja w `[extras]`/`[targets]`, NIE w `[deps]`.

    Verify ze finalny Project.toml ma:
    - `[deps]` zawiera tylko: ChunkSplitters, GeometryBasics, Random, Statistics (z Plan 02-01)
    - `[extras]` zawiera dodatkowo PerformanceTestTools
    - `[targets].test` zawiera "PerformanceTestTools"
  </action>
  <verify>
    <automated>julia --project=. -e 'using Pkg; deps = Pkg.project().dependencies; @assert !haskey(deps, "PerformanceTestTools") "PerformanceTestTools should be in [extras] not [deps]"; ext = readchomp(`grep -c "^PerformanceTestTools" Project.toml`); @assert parse(Int, ext) >= 1 "PerformanceTestTools missing from Project.toml"; targets = readchomp(`grep "^test = " Project.toml`); @assert occursin("PerformanceTestTools", targets) "PerformanceTestTools missing from [targets].test"; println("OK Project.toml: PerformanceTestTools w [extras]+[targets], NIE w [deps]")'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^PerformanceTestTools = ' Project.toml` zwraca dokladnie 1 (w [extras])
    - PerformanceTestTools wpis NIE wystepuje w `[deps]`: `awk '/^\[deps\]/,/^\[/' Project.toml | grep -c PerformanceTestTools` zwraca 0 (lub awk daje 1 dla samej linii `[deps]` - sprawdzic recznie)
    - Linia `test = [...]` w `[targets]` zawiera "PerformanceTestTools": `grep '^test = ' Project.toml | grep -c PerformanceTestTools` zwraca 1
    - `julia --project=. -e 'using Pkg; Pkg.test()'` zaczynamy proba - jezeli failuje na "PerformanceTestTools not found" tzn ze [extras] dziala (test loads bez problemu)
    - Manifest.toml zawiera PerformanceTestTools entry: `grep -c PerformanceTestTools Manifest.toml` zwraca przynajmniej 1
  </acceptance_criteria>
  <done>
    Project.toml [extras] zawiera PerformanceTestTools z poprawnym UUID, [targets].test zawiera "PerformanceTestTools". Manifest.toml zaktualizowany. PerformanceTestTools NIE wystepuje w [deps] ani [compat].
  </done>
</task>

<task type="auto">
  <name>Task 2: Rozszerzyc test/runtests.jl - includes + Aqua TEST-06 + JET TEST-07</name>
  <files>test/runtests.jl</files>
  <read_first>
    - test/runtests.jl (po Plan 02-01 powinien miec Wave 0 testset)
    - test/test_energia.jl (sygnatury testset-ow z Plan 02-05)
    - test/test_baselines.jl (sygnatury testset-ow)
    - test/test_symulacja.jl (sygnatury testset-ow)
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-PATTERNS.md sekcja "test/runtests.jl (MODIFIED)" (linie 527-603) - pelny pattern integracji
    - .planning/phases/02-energy-sa-algorithm-test-suite/02-RESEARCH.md sekcje "Example 4: Aqua.test_all" (linie 539-558), "Example 5: JET TEST-07" (linie 560-587), "Pitfall F: Aqua unbound_args" (linie 386-403)
  </read_first>
  <action>
    Modyfikacja `test/runtests.jl` - 3 zmiany:

    **A. Dodac 3 includes po Wave 0 testsecie**, PRZED Aqua testset.

    Sekcja po Wave 0 (numerowanie sekcji - jezeli Plan 02-01 dal Wave 0=5, to Aqua=6, JET=7 - musimy:
    - 5 = Wave 0 (z 02-01)
    - 6 = include test_energia.jl (NEW)
    - 7 = include test_baselines.jl (NEW)
    - 8 = include test_symulacja.jl (NEW)
    - 9 = Aqua.test_all (rozszerzony z Phase 1 stub)
    - 10 = JET TEST-07 (zastapuje Phase 1 smoke)

    Wzor:
    ```julia
    # ─────────────────────────────────────────────────────────────────────────
    # 6. Energia (Plan 02-02 + Plan 02-05 testset-y)
    # ─────────────────────────────────────────────────────────────────────────
    include("test_energia.jl")

    # ─────────────────────────────────────────────────────────────────────────
    # 7. Baselines + TEST-05 NN-baseline-beat (Plan 02-03 + Plan 02-05)
    # ─────────────────────────────────────────────────────────────────────────
    include("test_baselines.jl")

    # ─────────────────────────────────────────────────────────────────────────
    # 8. Symulacja SA + TEST-01/03/04/08 (Plan 02-04 + Plan 02-05)
    # ─────────────────────────────────────────────────────────────────────────
    include("test_symulacja.jl")
    ```

    **B. Rozszerzyc Aqua testset (TEST-06)** - zastapic Phase 1 stub pelnym wariantem:

    Stary stub (linie 158-175 w runtests.jl po Plan 02-01):
    ```julia
    @testset "Aqua.jl quality" begin
        deps_compat_kwargs = (ignore = [:Random], check_extras = (ignore = [:Test, :Unicode],))
        Aqua.test_all(JuliaCity; stale_deps = false, deps_compat = deps_compat_kwargs)
    end
    ```

    Nowy (TEST-06):
    ```julia
    # ─────────────────────────────────────────────────────────────────────────
    # 9. Aqua.jl quality gate (TEST-06) - rozszerzony z Phase 1 stub
    # ─────────────────────────────────────────────────────────────────────────
    @testset "Aqua.jl quality (TEST-06)" begin
        # Phase 2 dodaje :Statistics do deps_compat ignore (stdlib bez compat entry per Pattern D).
        # Trzymamy stale_deps=false do Phase 4 (BenchmarkTools wejdzie do [deps] dopiero w bench/).
        # NIE dodajemy unbound_args=(broken=true,) preemptywnie (Pitfall F):
        # tylko jezeli Aqua faktycznie zglosi false-positive na StanSymulacji{R}.
        # Jezeli to sie dzieje na pierwszym uruchomieniu, Plan 02-06 SUMMARY ma to udokumentowac
        # i w nastepnym commit dodac kwarg.
        Aqua.test_all(JuliaCity;
            ambiguities = (recursive = false,),
            stale_deps = false,
            deps_compat = (ignore = [:Random, :Statistics],
                           check_extras = (ignore = [:Test, :Unicode],)),
        )
    end
    ```

    **C. ZASTAPIC** Phase 1 JET smoke testset pelnym TEST-07:

    Stary stub (linie 180-194 w runtests.jl po Plan 02-01):
    ```julia
    @testset "JET smoke" begin
        result = @report_opt generuj_punkty(10; seed=42)
        @test result !== nothing
    end
    ```

    Nowy (TEST-07) - pattern z RESEARCH Example 5 (linie 564-587):
    ```julia
    # ─────────────────────────────────────────────────────────────────────────
    # 10. JET type stability (TEST-07) - zastepuje Phase 1 stub @report_opt
    # ─────────────────────────────────────────────────────────────────────────
    @testset "JET type stability (TEST-07)" begin
        # @test_opt automatycznie failuje testset jezeli JET znajdzie issue.
        # target_modules=(JuliaCity,) filtruje szum z Base/Core (Pitfall C).
        punkty = generuj_punkty(20; seed=42)
        stan = StanSymulacji(punkty; rng=Xoshiro(42))
        inicjuj_nn!(stan)
        alg = SimAnnealing(stan)
        stan.temperatura = alg.T_zero
        params = Parametry(liczba_krokow=100)
        bufor = zeros(Float64, Threads.nthreads())

        # warmup - JET analizuje skompilowany kod, ale dla pewnosci po pierwszej kompilacji
        oblicz_energie(stan.D, stan.trasa, bufor)
        delta_energii(stan, 5, 17)
        symuluj_krok!(stan, params, alg)
        kalibruj_T0(stan; n_probek=10)

        @test_opt target_modules=(JuliaCity,) oblicz_energie(stan.D, stan.trasa, bufor)
        @test_opt target_modules=(JuliaCity,) delta_energii(stan, 5, 17)
        @test_opt target_modules=(JuliaCity,) symuluj_krok!(stan, params, alg)
        @test_opt target_modules=(JuliaCity,) kalibruj_T0(stan; n_probek=10)
    end
    ```

    **D. Final test/runtests.jl structure** (top-of-file imports rozszerzone, sekcje w kolejnosci):
    1. encoding hygiene (Phase 1)
    2. generuj_punkty PKT (Phase 1)
    3. generuj_punkty no global RNG (Phase 1)
    4. StanSymulacji konstruktor (Phase 1)
    5. Wave 0 StableRNG ↔ Punkt2D (Plan 02-01)
    6. include test_energia.jl (NEW)
    7. include test_baselines.jl (NEW)
    8. include test_symulacja.jl (NEW)
    9. Aqua TEST-06 (rozszerzony)
    10. JET TEST-07 (zastapiony)

    UWAGI:
    - **NIE modyfikowac** sekcji 1-4 ani Wave 0 (juz dodanej w Plan 02-01).
    - Importy na top-of-file: jezeli test_symulacja.jl uzywa `using PerformanceTestTools`, to test/runtests.jl moze ale nie musi go importowac (test_symulacja.jl ma defensywny `using` i tak). Aby uniknac duplicate import, mozemy NIE dodawac PerformanceTestTools na top runtests.jl (kazdy test_*.jl ma defensywne usings).
    - Po edycji uruchomic full test suite: `julia --project=. -e 'using Pkg; Pkg.test()'`. Jezeli wszystko zielone - Phase 2 jest gotowa.
  </action>
  <verify>
    <automated>julia --project=. -t 4 -e 'using Pkg; Pkg.test()' 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'include("test_energia.jl")' test/runtests.jl` zwraca 1
    - `grep -c 'include("test_baselines.jl")' test/runtests.jl` zwraca 1
    - `grep -c 'include("test_symulacja.jl")' test/runtests.jl` zwraca 1
    - `grep -c 'TEST-06' test/runtests.jl` zwraca przynajmniej 1
    - `grep -c 'TEST-07' test/runtests.jl` zwraca przynajmniej 1
    - `grep -c '@test_opt target_modules=(JuliaCity,)' test/runtests.jl` zwraca przynajmniej 4 (oblicz_energie, delta_energii, symuluj_krok!, kalibruj_T0)
    - `grep -c 'deps_compat' test/runtests.jl` zwraca przynajmniej 1 z `:Statistics` w ignore: `grep 'deps_compat' test/runtests.jl | grep -c Statistics` zwraca przynajmniej 1
    - Phase 1 JET smoke (`@report_opt`) zostal zastapiony lub usuniety: `grep -c '@report_opt' test/runtests.jl` zwraca 0 (zastapiony przez @test_opt)
    - **`julia --project=. -e 'using Pkg; Pkg.test()'` exits 0** - finalny smoke test calej Phase 2
    - Output Pkg.test() zawiera linie `Test Summary:` dla minimum 8 testset-ow z `Pass:` (zero `Fail:`, zero `Error:`)
  </acceptance_criteria>
  <done>
    test/runtests.jl wlacza 3 nowe pliki przez include w odpowiedniej kolejnosci, ma rozszerzony Aqua testset (TEST-06) z deps_compat ignore dla [Random, Statistics], ma pelny JET TEST-07 (@test_opt na 4 funkcjach z target_modules=(JuliaCity,)). Phase 1 stub JET (@report_opt) zostal zastapiony. `Pkg.test()` exits 0 ze wszystkimi 21 REQ-IDami pokrytymi.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test orchestrator -> sub-tests | runtests.jl include-uje test_*.jl - relative paths sa bezpieczne (sibling files w test/) |
| PerformanceTestTools -> subprocess | TEST-04 odpala subprocess Julia - subprocess ma dostep do tych samych plikow co test (filesystem boundary akceptowane) |
| Aqua quality gate -> false positives | Aqua moze zglosic unbound_args na StanSymulacji{R} (Pitfall F) - test failure zostanie udokumentowany ale wyciagniety w nastepnym commit jako (broken=true,) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | Tampering | PerformanceTestTools UUID typo w Project.toml | mitigate | Executor MUSI uruchomic `Pkg.add` lokalnie i odczytac UUID z generated entry; `Pkg.resolve()` zlapie mismatch |
| T-02-17 | DOS | Pkg.test timeout na CI z TEST-04 subprocess (~60s + TEST-05 ~30s = ~90s total Phase 2 dodatki) | accept | Akceptowalne dla pojedynczego CI job; jezeli problem - Pitfall G mowi podnies/zmniejsz krok counts |
| T-02-18 | Information Disclosure | JET error output exposing internal types | accept | Standard Julia error output; brak sekretow w types |

Severity: brak high. Test orchestration code, brak network/secrets/PII.
</threat_model>

<verification>
**Plan-level integration check (FINALNY Phase 2 quality gate):**

```bash
# Single-command full test suite
julia --project=. -t 4 -e 'using Pkg; Pkg.test()' 2>&1 | tail -100
```

Expected output:
- `Test Summary: ` linie dla minimum 8 testset-ow:
  1. `encoding hygiene (BOOT-03, D-21)` - Pass
  2. `generuj_punkty (PKT-01, PKT-02, PKT-03)` - Pass
  3. `generuj_punkty no global RNG mutation` - Pass
  4. `StanSymulacji konstruktor` - Pass
  5. `Wave 0: StableRNG ↔ Punkt2D smoke` - Pass
  6. (testset-y z test_energia.jl) - Pass (multi)
  7. (testset-y z test_baselines.jl) - Pass (multi)
  8. (testset-y z test_symulacja.jl) - Pass (multi)
  9. `Aqua.jl quality (TEST-06)` - Pass
  10. `JET type stability (TEST-07)` - Pass
- Exit code 0
- Brak `Fail:` ani `Error:` linii (modulo `Broken:` jezeli ktos doda `(broken=true,)` swiadomie)

**Coverage verification (sanity):**

```bash
# Wszystkie 21 REQ-IDow Phase 2 powinny byc wymienione co najmniej raz w test/runtests.jl LUB w test/test_*.jl LUB w src/* docstrings
for req in ENE-01 ENE-02 ENE-03 ENE-04 ENE-05 \
           ALG-01 ALG-02 ALG-03 ALG-04 ALG-05 ALG-06 ALG-07 ALG-08 \
           TEST-01 TEST-02 TEST-03 TEST-04 TEST-05 TEST-06 TEST-07 TEST-08; do
    count=$(grep -rc "$req" src/ test/ 2>/dev/null | grep -v ':0$' | wc -l)
    if [ "$count" -eq 0 ]; then
        echo "MISSING: $req"
    fi
done
```
Expected: brak `MISSING:` linii.
</verification>

<success_criteria>
1. **Project.toml [extras] zawiera PerformanceTestTools z poprawnym UUID; [targets].test zawiera "PerformanceTestTools".**
2. **Manifest.toml zaktualizowany przez Pkg.resolve(); commit-able.**
3. **test/runtests.jl wlacza 3 nowe pliki przez include w sekcjach 6-8 (po Wave 0).**
4. **TEST-06 Aqua testset rozszerzony: deps_compat ignore [Random, Statistics]; ambiguities (recursive=false); stale_deps=false; check_extras ignore [Test, Unicode]. NIE preemptive (broken=true,).**
5. **TEST-07 JET testset zastepuje Phase 1 stub: @test_opt target_modules=(JuliaCity,) na 4 funkcjach (oblicz_energie, delta_energii, symuluj_krok!, kalibruj_T0).**
6. **`julia --project=. -e 'using Pkg; Pkg.test()'` exits 0** ze wszystkimi 8+ testset-ami zielonymi.
7. **Wszystkie 21 REQ-IDow Phase 2 (ENE×5, ALG×8, TEST×8) sa pokryte przez tests w `src/` lub `test/`.**
</success_criteria>

<output>
After completion, create `.planning/phases/02-energy-sa-algorithm-test-suite/02-06-SUMMARY.md` zawierajace:

- PerformanceTestTools UUID + version (z Pkg.dependencies())
- Output Pkg.test (lub samo summary - liczba pass/fail/error per testset)
- Czy Aqua zglosil unbound_args false-positive (jezeli tak: nastepny commit doda `unbound_args=(broken=true,)`)
- Czy JET TEST-07 zglosil jakiekolwiek issues (jezeli tak: rozdzielic na issue + plan naprawy)
- Coverage table 21 REQ-IDow x location (gdzie kazdy REQ jest pokryty)
- Total runtime testow (s) - sanity check dla CI budget
</output>
