---
phase: 04.1-demo-gif-hybrid-and-isopen-fix
plan: 02
type: execute
wave: 2
depends_on:
  - 04.1-01
files_modified:
  - src/wizualizacja.jl
  - examples/eksport_mp4.jl
  - assets/demo.gif
autonomous: false
requirements:
  - DEMO-02
  - DEMO-04
  - EKS-04
must_haves:
  truths:
    - "assets/demo.gif pokazuje 3-fazowa hybryde: ~5s NN-construction edge-by-edge od pustej trasy + ~0.5-1s wizualny separator + ~5s SA-2-opt optimization"
    - "Pierwsza klatka GIFa fazy NN ma <=1 krawedz (NN start od stan.trasa = [1])"
    - "Ostatnia klatka GIFa fazy SA pokazuje trase z energia < energia_nn (SA bil baseline)"
    - "Rozdzielczosc docelowa: src Figure 1920x960, downscale Lanczos do 1600px (zrodlo 16% nadmiarem dla anti-aliasingu)"
    - "Rozmiar pliku assets/demo.gif <= 1.5 MB (akceptowalny dla README embed na GitHubie)"
    - "wizualizuj() public signature NIEZMIENIONA (Phase 3 D-09 LOCKED) — figure_size jako nowy kwarg z default backwards-compat"
  artifacts:
    - path: "src/wizualizacja.jl"
      provides: "Helper _animuj_nn_construction! (incremental NN edge-by-edge yield) + nowy kwarg figure_size w wizualizuj() / _wizualizuj_impl / _setup_figure (default (1400, 700))"
      contains: "_animuj_nn_construction!"
    - path: "examples/eksport_mp4.jl"
      provides: "Skrypt-level orchestration 3 faz hybrydy (NN-construction record block + separator record block + SA optimization record block) wewnatrz JEDNEGO record() przez kwarg `frame=` LUB 3 osobne record() z postprocess concat"
      contains: "_animuj_nn_construction!"
    - path: "assets/demo.gif"
      provides: "Zregenerowany binarny artefakt po implementacji (autonomous:false — wymaga lokalnego GLMakie + commit binarny)"
      contains: "GIF89a"
  key_links:
    - from: "examples/eksport_mp4.jl::main()"
      to: "src/wizualizacja.jl::_animuj_nn_construction!"
      via: "import przez `using JuliaCity` (helper musi byc eksportowany LUB skrypt referuje przez `JuliaCity._animuj_nn_construction!`)"
      pattern: "_animuj_nn_construction!"
    - from: "src/wizualizacja.jl::_animuj_nn_construction!"
      to: "src/baselines.jl::trasa_nn"
      via: "ZGODNOSC ALGORYTMICZNA — helper reprodukuje sekwencje trasa_nn(D; start=1) z incremental yield zamiast batch zwrocenia"
      pattern: "argmin po D\\[biezacy, j\\] dla j nieodwiedzonego"
    - from: "examples/eksport_mp4.jl::SZEROKOSC_GIF"
      to: "ffmpeg downscale Lanczos do 1600px"
      via: "filtr_skala = \"fps=$FPS,scale=$SZEROKOSC_GIF:-1:flags=lanczos\""
      pattern: "SZEROKOSC_GIF = 1600"
---

<objective>
Domkniecie BLOCKERA #2 z 04-UAT.md (Test 4): `assets/demo.gif` startuje z gotowej trasy NN (`inicjuj_nn!` PRZED `wizualizuj()` w `examples/eksport_mp4.jl:42-43`) i pokazuje SA-2-opt swapy jako nieczytelne "teleporty" (2-opt segment reversals wygladaja jak nagle przepiniecie 2 niesasiadujacych krawedzi). Rozdzielczosc docelowa 700x350 jest za mala dla README embed na FullHD/2K/4K.

Purpose: Hybryda 3-fazowa (decyzja uzytkownika 2026-05-04, opcja C — CONTEXT D-04):
- **Faza 1 (~5s, ~150 klatek @30fps):** NN-construction edge-by-edge od pustej sceny. Punkty widoczne, trasa rosnie po `chunk = ceil(N/150) ≈ 7` krawedzi/klatke dla N=1000.
- **Faza 2 (~0.5-1s, 15-30 klatek):** wizualny separator — czarna klatka + overlay text "Optymalizacja SA-2-opt".
- **Faza 3 (~5s, ~150 klatek @30fps):** SA-2-opt optimization (obecne zachowanie skryptu, T_zero=0.001 lock zachowany z plan 02-14, liczba_krokow zredukowana do polowy: 7500 zamiast 15000).

Plus: bump rozdzielczosci `Figure(size=(1920, 960))` zrodlo + `SZEROKOSC_GIF=1600` ffmpeg downscale.

Output:
1. Nowy prywatny helper `_animuj_nn_construction!` w `src/wizualizacja.jl` — reprodukuje algorytm `trasa_nn(D; start=1)` z incremental yieldem do Observable.
2. Nowy kwarg `figure_size=(1400, 700)` w `wizualizuj()` / `_wizualizuj_impl` / `_setup_figure` (backwards-compat default; `examples/eksport_mp4.jl` przekazuje `(1920, 960)`).
3. Refaktoryzowany `examples/eksport_mp4.jl` — 3 fazy zorkiestrowane na poziomie skryptu (3 osobne `record()` calls + ffmpeg concat), bumpniety `SZEROKOSC_GIF=1600`, pre-rm zachowany (Phase 4 D-04 LOCK).
4. Zregenerowany `assets/demo.gif` (binarny artefakt — `autonomous: false`, wymaga lokalnego GLMakie + commit z markerem "URGENT post-UAT 4.1").

**Decyzja planner'a (CONTEXT D-05/D-06 daje wybor):**
- **D-05 helper location:** TRZYMAMY w `src/wizualizacja.jl` (NIE nowy `src/animacja.jl`). Powod: helper uzywa GLMakie/Observable juz w scope tego pliku (VIZ-06 LOCKED — `using GLMakie` jest TYLKO w `wizualizacja.jl`). Nowy plik wymagalby explicite re-exportu Observable lub `using GLMakie` w drugim miejscu — naruszylby VIZ-06. Wszystkie istniejace helpery `_setup_figure`, `_init_observables`, `_live_loop`, `_export_loop`, `_dodaj_gotowe_overlay!`, `_wizualizuj_impl` siedza w tym pliku — `_animuj_nn_construction!` jest naturalnym kontynuowaniem tej konwencji.
- **D-06 entry point:** Skrypt-level orchestration w `examples/eksport_mp4.jl`, BEZ nowego publicznego `wizualizuj_hybrid()`. Powod: Phase 3 D-09 LOCKED (single API entry point `wizualizuj`). Nowy publiczny wrapper rozszerzylby powierzchnie API o specyficzny use-case demo. Skrypt-level orchestration: `_animuj_nn_construction!` zostanie wystawione przez `JuliaCity` namespace (NIE eksportowane, ale dostepne jako `JuliaCity._animuj_nn_construction!` — internal helper consumed by demo script). To powtarza pattern z `bench/historyczne/diagnostyka_test05*.jl` ktore tez referuja internals. Alternatywne: dodac pelny eksport — odrzucone, KISS dla v1.

Plan blokuje Wave 3 (04.1-03 testy) bo te testy waliduja rzeczywisty output GIFa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-demo-gif-hybrid-and-isopen-fix/04.1-CONTEXT.md
@.planning/phases/04-demo-benchmarks-documentation/04-UAT.md
@.planning/phases/04-demo-benchmarks-documentation/04-CONTEXT.md
@.planning/phases/04-demo-benchmarks-documentation/04-07-SUMMARY.md
@.planning/phases/03-visualization-export/03-CONTEXT.md
@src/wizualizacja.jl
@src/baselines.jl
@examples/eksport_mp4.jl
</context>

<interfaces>
<!-- Kluczowe kontrakty ktore wykonawca dostaje BEZ koniecznosci eksploracji kodu. -->

From src/typy.jl (z Phase 1+2 — LOCKED):
```julia
mutable struct StanSymulacji{R<:AbstractRNG}
    punkty::Vector{Punkt2D}    # Vector{Point2{Float64}}, dlugosc N
    trasa::Vector{Int}          # permutacja 1:N (cykl Hamiltona) PO inicjalizacji
    D::Matrix{Float64}          # NxN macierz dystansow euklidesowych
    energia::Float64
    iteracja::Int
    temperatura::Float64
    rng::R
end
```

From src/baselines.jl (NIE MODYFIKOWANE — Phase 2 LOCKED, helper reprodukuje algorytm):
```julia
function trasa_nn(D::Matrix{Float64}; start::Int=1)::Vector{Int}
    n = size(D, 1)
    odwiedzone = falses(n)
    trasa = Vector{Int}(undef, n)
    trasa[1] = start
    odwiedzone[start] = true
    @inbounds for k in 2:n
        biezacy = trasa[k - 1]
        najblizszy = 0
        min_dist = Inf
        for j in 1:n
            if !odwiedzone[j] && D[biezacy, j] < min_dist
                min_dist = D[biezacy, j]
                najblizszy = j
            end
        end
        trasa[k] = najblizszy
        odwiedzone[najblizszy] = true
    end
    return trasa
end

function inicjuj_nn!(stan::StanSymulacji)
    oblicz_macierz_dystans!(stan)
    stan.trasa = trasa_nn(stan.D; start=1)
    bufor = zeros(Float64, Threads.nthreads())
    stan.energia = oblicz_energie(stan.D, stan.trasa, bufor)
    stan.iteracja = 0
    return nothing
end
```

From src/wizualizacja.jl (po plan 04.1-01):
```julia
# Helpery internal z Phase 3:
function _trasa_do_punkty(stan::StanSymulacji)::Vector{Point2f}
function _setup_figure(stan::StanSymulacji, nn_trasa::Vector{Int})  # zwraca (fig, ax_trasa, ax_energia)
function _init_observables(stan::StanSymulacji, alg::Algorytm, ax_trasa::Axis, ax_energia::Axis)  # NamedTuple
function _is_window_open(fig)::Bool                                  # nowy z 04.1-01

# Public API (D-09 LOCKED):
function wizualizuj(stan::StanSymulacji, params::Parametry, alg::Algorytm;
                    liczba_krokow::Int=params.liczba_krokow,
                    fps::Int=30,
                    kroki_na_klatke::Int=50,
                    eksport::Union{Nothing,String}=nothing)::Nothing
```

From Makie 0.24 (re-eksportowane przez using GLMakie):
```julia
Makie.record(fig, sciezka, iter; framerate=fps) do frame_i
    # mutuj Observable, frame zostanie zapisany
end
# Zwraca nothing, zapisuje plik docelowy w sciezka.
# Format wykrywany z extensji (.gif/.mp4/.webm).
```

From FFMPEG_jll (transitive via Makie — uzywane juz w eksport_mp4.jl):
```julia
ffmpeg() do exe
    run(`$exe -y -hide_banner ...`)
end
# concat 3 GIFow: ffmpeg -f concat -safe 0 -i list.txt -c copy out.gif
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Dodaj kwarg figure_size do wizualizuj/_wizualizuj_impl/_setup_figure (D-08)</name>
  <read_first>
    - src/wizualizacja.jl (linie 91-122 — `_setup_figure`, w tym hardcoded `Figure(size=(1400, 700))` na linii 93)
    - src/wizualizacja.jl (linie 340-390 — `_wizualizuj_impl`, propagacja kwargow)
    - src/wizualizacja.jl (linie 432-465 — `wizualizuj()` public signature)
    - .planning/phases/04.1-demo-gif-hybrid-and-isopen-fix/04.1-CONTEXT.md (D-08 — preferowana parametryzacja przez kwarg z defaultem `(1400, 700)` zachowujacym backwards-compat)
    - .planning/phases/03-visualization-export/03-CONTEXT.md (D-09 LOCKED — single API entry point; D-01 dual-panel — aspect ratio 2:1 zachowac)
  </read_first>
  <action>
    Dodac NOWY kwarg `figure_size::Tuple{Int,Int}` w 3 funkcjach (od dol w gore — `_setup_figure` najpierw, potem przepuscic przez `_wizualizuj_impl`, na koniec `wizualizuj`).

    KROK 1.1 — `_setup_figure` (linie 91-122). BEFORE (linia 91-93):
    ```julia
    function _setup_figure(stan::StanSymulacji, nn_trasa::Vector{Int})
        n_punktow = length(stan.punkty)
        fig = Figure(size=(1400, 700))
    ```
    AFTER:
    ```julia
    function _setup_figure(stan::StanSymulacji, nn_trasa::Vector{Int};
                           figure_size::Tuple{Int,Int}=(1400, 700))
        n_punktow = length(stan.punkty)
        fig = Figure(size=figure_size)
    ```

    KROK 1.2 — `_wizualizuj_impl` (linie 340-388). BEFORE (sygnatura linie 340-342):
    ```julia
    function _wizualizuj_impl(stan::StanSymulacji, params::Parametry, alg::Algorytm;
                              liczba_krokow::Int, fps::Int, kroki_na_klatke::Int,
                              eksport::Union{Nothing,String})
    ```
    AFTER:
    ```julia
    function _wizualizuj_impl(stan::StanSymulacji, params::Parametry, alg::Algorytm;
                              liczba_krokow::Int, fps::Int, kroki_na_klatke::Int,
                              eksport::Union{Nothing,String},
                              figure_size::Tuple{Int,Int}=(1400, 700))
    ```

    Dodatkowo wewnatrz `_wizualizuj_impl`, w `with_theme(theme_dark()) do ... end` (linia 358-359), przekazac kwarg do `_setup_figure`. BEFORE (linia 359):
    ```julia
        fig, ax_trasa, ax_energia = _setup_figure(stan, nn_trasa)
    ```
    AFTER:
    ```julia
        fig, ax_trasa, ax_energia = _setup_figure(stan, nn_trasa; figure_size=figure_size)
    ```

    KROK 1.3 — `wizualizuj()` public (linie 432-465). BEFORE (sygnatura linie 432-436):
    ```julia
    function wizualizuj(stan::StanSymulacji, params::Parametry, alg::Algorytm;
                        liczba_krokow::Int=params.liczba_krokow,
                        fps::Int=30,
                        kroki_na_klatke::Int=50,
                        eksport::Union{Nothing,String}=nothing)::Nothing
    ```
    AFTER:
    ```julia
    function wizualizuj(stan::StanSymulacji, params::Parametry, alg::Algorytm;
                        liczba_krokow::Int=params.liczba_krokow,
                        fps::Int=30,
                        kroki_na_klatke::Int=50,
                        eksport::Union{Nothing,String}=nothing,
                        figure_size::Tuple{Int,Int}=(1400, 700))::Nothing
    ```

    Dodatkowo w call do `_wizualizuj_impl` (linie 446-448 — wewnatrz try) propagowac kwarg. BEFORE:
    ```julia
            _wizualizuj_impl(stan, params, alg;
                             liczba_krokow=liczba_krokow, fps=fps,
                             kroki_na_klatke=kroki_na_klatke, eksport=eksport)
    ```
    AFTER:
    ```julia
            _wizualizuj_impl(stan, params, alg;
                             liczba_krokow=liczba_krokow, fps=fps,
                             kroki_na_klatke=kroki_na_klatke, eksport=eksport,
                             figure_size=figure_size)
    ```

    KROK 1.4 — Zaktualizowac docstring `wizualizuj()` (linie 396-431) — dodac jedna linijke o nowym kwargu w sekcji `# Slowa kluczowe`. Po linijce z `eksport::Union{Nothing,String}=nothing` dodac:
    ```
    - `figure_size::Tuple{Int,Int}=(1400, 700)` — rozmiar zrodlowego okna Makie (Phase 4.1 D-08 — bumpowany do `(1920, 960)` przez `examples/eksport_mp4.jl` dla wyzszej rozdzielczosci eksportu GIF)
    ```

    KRYTYCZNE — D-08 backwards-compat:
    - Default `(1400, 700)` IDENTYCZNY z hardcoded wartoscia sprzed zmiany. Wszystkie istniejace callerzy (`examples/podstawowy.jl`, ewentualne testy) bez przekazywania `figure_size` dostana identyczne zachowanie.
    - Aspect ratio `1400:700 = 2:1` zachowany w defaultcie i zalecanej wartosci `(1920, 960) ≈ 2:1` (Phase 3 D-01 dual-panel layout sensible przy 2:1).
    - Brak modyfikacji w `_init_observables`, `_live_loop`, `_export_loop`, `_dodaj_gotowe_overlay!` — kwarg propagowany TYLKO do `_setup_figure`.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content src/wizualizacja.jl -Raw; if (-not ($c -match 'figure_size::Tuple\{Int,Int\}=\(1400, 700\)')) { Write-Error 'figure_size kwarg missing or wrong default'; exit 1 }; if (($c | Select-String -Pattern 'figure_size' -AllMatches).Matches.Count -lt 6) { Write-Error 'figure_size not propagated through all 3 funcs (need at least 6 occurrences: 3 sygnatury + 1 docstring + 2 call-sites)'; exit 2 }; if ($c -match 'Figure\(size=\(1400, 700\)\)') { Write-Error 'hardcoded Figure(size=(1400, 700)) still present — should be Figure(size=figure_size)'; exit 3 }; if (-not ($c -match 'Figure\(size=figure_size\)')) { Write-Error 'Figure(size=figure_size) missing'; exit 4 }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - `wizualizuj()` ma nowy kwarg `figure_size::Tuple{Int,Int}=(1400, 700)`.
    - `_wizualizuj_impl()` ma nowy kwarg `figure_size::Tuple{Int,Int}=(1400, 700)` i propaguje go do `_setup_figure`.
    - `_setup_figure()` ma nowy kwarg `figure_size::Tuple{Int,Int}=(1400, 700)` i uzywa go w `Figure(size=figure_size)`.
    - Stary hardcoded `Figure(size=(1400, 700))` ZASTAPIONY przez `Figure(size=figure_size)` (0 wystapien starego patternu).
    - `examples/podstawowy.jl` (NIE modyfikowany w tym tasku) nadal dziala bez zmian — default `(1400, 700)` zachowuje backwards-compat.
    - Docstring `wizualizuj()` rozszerzony o opis `figure_size`.
    - Phase 3 D-09 LOCKED zachowane: kwarg jest ADDITIVE (nowy kwarg z defaultem), nie zmienia semantyki istniejacych kwargow.
    - Plik nadal walidny UTF-8 NFC + LF.
  </acceptance_criteria>
  <done>Trzy funkcje (`wizualizuj`, `_wizualizuj_impl`, `_setup_figure`) maja nowy kwarg `figure_size::Tuple{Int,Int}=(1400, 700)` propagowany od public API w dol; default backwards-compat z `Figure(size=(1400, 700))`; docstring `wizualizuj()` zaktualizowany.</done>
</task>

<task type="auto">
  <name>Task 2: Dodaj helper _animuj_nn_construction! w src/wizualizacja.jl (D-05)</name>
  <read_first>
    - src/baselines.jl linie 50-73 (`trasa_nn` — algorytm referencyjny, helper MUSI reprodukowac sekwencje IDENTYCZNIE dla determinism)
    - src/wizualizacja.jl linie 165-247 (`_live_loop` — pattern uzycia Observable update + sleep yielding GLMakie event loopowi)
    - src/wizualizacja.jl linie 263-316 (`_export_loop` — pattern Makie.record + per-frame Observable mutation)
    - .planning/phases/04.1-demo-gif-hybrid-and-isopen-fix/04.1-CONTEXT.md (D-05 — sygnatura `animuj_nn_construction!(stan, fig, observable; chunk=7)`; D-04 — chunk = ceil(N/150) ≈ 7 dla N=1000)
  </read_first>
  <action>
    Dodac NOWY prywatny helper do `src/wizualizacja.jl` BEZPOSREDNIO PRZED `_wizualizuj_impl` (po `_dodaj_gotowe_overlay!`, linia ~334-336 — sekcja "Helpery finalizujace"). Helper bedzie konsumowany przez `examples/eksport_mp4.jl` (Task 3) jako `JuliaCity._animuj_nn_construction!` (internal namespace, brak eksportu z modulu).

    Tresc helpera:

    ```julia
    """
    Inkrementalne budowanie trasy Nearest-Neighbor z yieldem do Observable per `chunk`
    krawedzi (Phase 4.1 D-05 — hybryda GIF, faza 1 NN-construction edge-by-edge).

    Reprodukuje algorytm `trasa_nn(D; start=1)` z `src/baselines.jl` (LOCKED Phase 2)
    z dwoma roznicami:
    1. Buduje incremental Vector{Int} (`stan.trasa[1:k]`) zamiast batch zwrocenia n-elementowego.
    2. Po kazdym `chunk` krawedziach mutuje `obs_trasa[]` z aktualna prefix-trasa
       zamknieta tymczasowo cyklem `[trasa[1:k]..., trasa[1]]` — daje wizualizacje
       "trasa rosnie" w GIFie zamiast pelnego cyklu od pierwszej klatki.

    Po zakonczeniu `stan.trasa` to PELEN cykl Hamiltona identyczny z `trasa_nn(stan.D;
    start=1)` (assertion: `stan.trasa == trasa_nn(stan.D; start=1)` musi zachodzic).

    Wywolywane WEWNATRZ bloku `Makie.record(fig, sciezka, 1:n_klatek; ...) do frame_i`
    w `examples/eksport_mp4.jl` (faza 1) — kazda iteracja petli wewnetrznej tej funkcji
    odpowiada JEDNEJ klatce GIFa (po `chunk` krawedziach).

    # Argumenty
    - `stan::StanSymulacji` — z wypelnionym `stan.D` (przez `oblicz_macierz_dystans!`),
      ale `stan.trasa` PRZED wywolaniem ma byc resetowane do `[1]` (start=1, faza 0)
      przez callera. Helper zaklada: `length(stan.trasa) == 1` i `stan.trasa[1] == 1`.
    - `obs_trasa::Observable{Vector{Point2f}}` — Observable z `_init_observables` ktore
      bedzie mutowane co `chunk` krawedzi.
    - `n_klatek::Int` — zaplanowana liczba klatek fazy 1 (np. `ceil(N / chunk)`).
      Helper iteruje DOKLADNIE `n_klatek` razy — caller dba zeby `chunk * n_klatek >= N - 1`
      (dodanych krawedzi do pustej trasy).
    - `chunk::Int=7` — krawedzi per klatka (D-04: ceil(1000/150) = 7).

    Zwraca `Nothing`. Po zakonczeniu `stan.trasa` ma `length == n` (pelen cykl Hamiltona).
    """
    function _animuj_nn_construction!(stan::StanSymulacji,
                                       obs_trasa::Observable{Vector{Point2f}},
                                       n_klatek::Int;
                                       chunk::Int=7)
        n = length(stan.punkty)
        @assert n >= 2 "Wymaga co najmniej 2 punktow"
        @assert length(stan.trasa) == 1 && stan.trasa[1] == 1 "stan.trasa musi byc zresetowane do [1]"
        @assert n_klatek >= 1 "n_klatek must be positive"
        @assert chunk >= 1 "chunk must be positive"

        # Lokalna replika `odwiedzone` z `trasa_nn` — identyczna semantyka, zaczynamy od start=1.
        odwiedzone = falses(n)
        odwiedzone[1] = true
        # Pelny prealoc trasy (jak w `trasa_nn`) — wypelniamy in-place; stan.trasa rosnie.
        pelna_trasa = Vector{Int}(undef, n)
        pelna_trasa[1] = 1
        k = 1   # ile elementow trasy juz wybralismy (1, bo start=1 wstawiony)

        @inbounds for klatka in 1:n_klatek
            # Dodaj `chunk` krawedzi (lub mniej, jezeli zostalo < chunk)
            for _ in 1:chunk
                k >= n && break
                biezacy = pelna_trasa[k]
                najblizszy = 0
                min_dist = Inf
                for j in 1:n
                    if !odwiedzone[j] && stan.D[biezacy, j] < min_dist
                        min_dist = stan.D[biezacy, j]
                        najblizszy = j
                    end
                end
                k += 1
                pelna_trasa[k] = najblizszy
                odwiedzone[najblizszy] = true
            end

            # Zaktualizuj stan.trasa do prefix-trasa dlugosci k (wycinek)
            stan.trasa = pelna_trasa[1:k]

            # Zbuduj snapshot Point2f dla obs_trasa: prefix punkty + (jezeli k >= 2)
            # tymczasowe domkniecie "wracamy do startu" zeby cykl byl zawsze widoczny
            # jako linia closed (NIE break w wizualizacji srodek-fazy NN).
            # Dla k == 1: tylko jeden punkt (lines! nie narysuje krawedzi — co jest pozadane).
            pts = Vector{Point2f}(undef, k + 1)
            for i in 1:k
                pts[i] = Point2f(stan.punkty[stan.trasa[i]])
            end
            pts[k + 1] = pts[1]  # tymczasowe domkniecie — dla k=1 da 2 identyczne punkty (degenerate, lines! ignoruje)
            obs_trasa[] = pts
        end

        # Po zakonczeniu pelna trasa wpisana do stan.trasa (assertion zgodnosci z trasa_nn)
        @assert k == n "Helper nie ukonczyl pelnego cyklu Hamiltona — zwiększ n_klatek lub chunk"
        stan.trasa = pelna_trasa
        return nothing
    end
    ```

    KRYTYCZNE — zgodnosc algorytmiczna z `trasa_nn(D; start=1)`:
    - Identyczna inicjalizacja: `start=1`, `odwiedzone[1] = true`, `pelna_trasa[1] = 1`.
    - Identyczny argmin po `D[biezacy, j]` dla `j` nieodwiedzonego.
    - Po zakonczeniu: `stan.trasa == trasa_nn(stan.D; start=1)` MUSI zachodzic (deterministyczny output identyczny z `inicjuj_nn!`).
    - Test self-check w docstring (assertion `k == n`).

    Asercje wewnetrzne PO POLSKU per dotychczasowy styl pliku (linie 270 `error("Plik '$sciezka' już istnieje...")`, linie 276 `@assert n_klatek > 0 "n_klatek must be positive"` — mieszany; nowe asercje po polsku zeby pasowaly do user-facing failure mode jak w plan 04.1-03).

    Per LANG-04 dyrektywa "asercje wewnetrzne po angielsku", DWIE asercje pozostaja po angielsku zgodnie z istniejacym wzorcem `_export_loop:276`:
    - `"n_klatek must be positive"` — po angielsku (zgodne z _export_loop)
    - `"chunk must be positive"` — po angielsku (analogiczne)

    Pozostale po polsku (failure mode reprodukcji algorytmu — caller-facing):
    - `"Wymaga co najmniej 2 punktow"`
    - `"stan.trasa musi byc zresetowane do [1]"`
    - `"Helper nie ukonczyl pelnego cyklu Hamiltona — zwiększ n_klatek lub chunk"`

    Brak nowych deps — `Observable`, `Point2f`, `falses` wszystko juz w scope.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content src/wizualizacja.jl -Raw; if (-not ($c -match 'function _animuj_nn_construction!\(stan::StanSymulacji,')) { Write-Error 'helper missing'; exit 1 }; if (-not ($c -match 'chunk::Int=7')) { Write-Error 'chunk default missing'; exit 2 }; if (-not ($c -match 'argmin po D|argmin')) { Write-Output 'note: argmin pattern not in code (uses local min loop) — OK if min_dist + najblizszy + min_dist comparison present'; if (-not ($c -match 'min_dist')) { Write-Error 'NN inner loop missing'; exit 3 } }; if (-not ($c -match 'stan\.trasa = pelna_trasa\[1:k\]')) { Write-Error 'incremental prefix update missing'; exit 4 }; if (-not ($c -match 'obs_trasa\[\] = pts')) { Write-Error 'Observable mutation missing'; exit 5 }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - `src/wizualizacja.jl` zawiera definicje `function _animuj_nn_construction!(stan::StanSymulacji, obs_trasa::Observable{Vector{Point2f}}, n_klatek::Int; chunk::Int=7)`.
    - Helper znajduje sie PRZED `_wizualizuj_impl` (numer linii grep `_animuj_nn_construction!` definicji < numer linii grep `function _wizualizuj_impl`).
    - 4 asercje wstepne (`@assert n >= 2`, `@assert length(stan.trasa) == 1 && stan.trasa[1] == 1`, `@assert n_klatek >= 1`, `@assert chunk >= 1`).
    - Wewnetrzny pattern NN: `min_dist = Inf` + `for j in 1:n if !odwiedzone[j] && stan.D[biezacy, j] < min_dist ...`.
    - `stan.trasa` aktualizowane jako `pelna_trasa[1:k]` per klatka (incremental prefix).
    - `obs_trasa[] = pts` mutuje Observable per klatka (1 notify per klatka — Pitfall 5 zachowane).
    - Final assertion `@assert k == n "Helper nie ukonczyl pelnego cyklu Hamiltona ..."`.
    - Polish docstring + komentarze, ASCII identyfikator, NFC + LF.
    - Wlasciwosc deterministycznej zgodnosci z `trasa_nn(D; start=1)`: po helperze `stan.trasa[1:n] == trasa_nn(stan.D; start=1)` (waliduje plan 04.1-03 unit testem).
  </acceptance_criteria>
  <done>Prywatny helper `_animuj_nn_construction!` zdefiniowany w `src/wizualizacja.jl` PRZED `_wizualizuj_impl`; reprodukuje algorytm `trasa_nn` z incremental yield do Observable per chunk krawedzi; po zakonczeniu `stan.trasa` to pelen cykl Hamiltona identyczny z `trasa_nn(stan.D; start=1)`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Refaktoryzuj examples/eksport_mp4.jl na 3-fazowa hybryde + bump SZEROKOSC_GIF=1600 + regeneracja assets/demo.gif</name>
  <what-built>
    Skrypt `examples/eksport_mp4.jl` zostanie przepisany ze "single-record SA-from-NN" na "3-fazowa orchestracje hybrydy" zgodnie z CONTEXT D-04/D-06/D-09.

    Bedzie obejmowal:

    KROK 3.1 — Bump stalych:
    - `SZEROKOSC_GIF` z 700 → 1600 (linia ~26)
    - Nowy `LICZBA_KROKOW_SA = 7500` zamiast obecnego `LICZBA_KROKOW = 15_000` (polowa, by faza 3 trwala ~5s)
    - Nowy `CHUNK_NN = 7` (D-04: ceil(1000/150))
    - Nowy `N_KLATEK_NN = ceil(Int, N / CHUNK_NN) ≈ 143-150` (faza 1)
    - Nowy `N_KLATEK_SEPARATOR = 20` (D-04: 15-30 klatek dla 0.5-1s @30fps; preferowane 20)
    - Nowy `N_KLATEK_SA = LICZBA_KROKOW_SA ÷ KROKI_NA_KLATKE = 150` (faza 3)
    - Lacznie ~315 klatek = ~10.5s @30fps (zgodne z D-02 "10s GIF" + ~0.5s separator)
    - `FIGURE_SIZE = (1920, 960)` (CONTEXT D-08)

    KROK 3.2 — Refactor `main()` na orkiestracje 3 osobnych `record()` calls + ffmpeg concat (NIE jeden record() z roznymi modes — Makie.record() nie wspiera switching srodka petli):

    ```julia
    function main()
        # ... [stale + mkpath + pre-rm jak w obecnym skrypcie] ...

        @info "JuliaCity hybrid GIF — N=$N, seed=$SEED, threads=$(Threads.nthreads())"
        @info "Faza 1: NN-construction edge-by-edge (~5s)..."
        @info "Faza 2: Separator (0.5-1s)..."
        @info "Faza 3: SA-2-opt optimization (~5s)..."

        # === Setup wspolny dla faz 1 + 3 ===
        punkty = generuj_punkty(N; seed=SEED)
        stan = StanSymulacji(punkty; rng=Xoshiro(SEED))
        oblicz_macierz_dystans!(stan)  # MUSI byc PRZED _animuj_nn_construction! (helper czyta stan.D)
        stan.trasa = [1]                # reset do start=1 dla helpera (D-05 contract)
        stan.iteracja = 0

        # === FAZA 1: NN-construction edge-by-edge ===
        sciezka_faza1 = SCIEZKA_GIF * ".faza1.gif"
        with_theme(theme_dark()) do
            # _setup_figure wymaga gotowej trasy NN — przekazujemy juz policzona dla baseline overlay
            nn_trasa_pelna = trasa_nn(stan.D; start=1)  # baseline overlay (D-02 LOCKED)
            fig, ax_trasa, ax_energia = JuliaCity._setup_figure(stan, nn_trasa_pelna; figure_size=FIGURE_SIZE)
            alg_dummy = SimAnnealing(stan; T_zero=0.001)  # potrzebne dla _init_observables overlay
            obs = JuliaCity._init_observables(stan, alg_dummy, ax_trasa, ax_energia)

            # stan.trasa juz [1], pierwsza klatka pokaze pusty cykl
            Makie.record(fig, sciezka_faza1, 1:N_KLATEK_NN; framerate=FPS) do frame_i
                # Helper Phase 4.1 D-05 — wykonuje 1 batch chunk krawedzi per call
                # ALE: nasz helper iteruje `n_klatek` razy w jednym wywolaniu —
                # alternatywne: wywolac helper raz POZA record z hookem na obs.
                # Tu uzywamy prostszego patternu: w petli record manualnie 1 chunk per klatka.
                if frame_i == 1
                    # Klatka 1: tylko punkty + start (stan.trasa = [1]) — pusta linia
                else
                    _dodaj_jeden_chunk_nn!(stan, obs.obs_trasa; chunk=CHUNK_NN)
                end
            end
        end

        # === FAZA 2: Separator (czarna klatka + tekst) ===
        sciezka_faza2 = SCIEZKA_GIF * ".faza2.gif"
        with_theme(theme_dark()) do
            fig_sep = Figure(size=FIGURE_SIZE; backgroundcolor=:black)
            ax_sep = Axis(fig_sep[1, 1]; backgroundcolor=:black)
            hidedecorations!(ax_sep)
            hidespines!(ax_sep)
            text!(ax_sep, 0.5, 0.5;
                  text="Optymalizacja SA-2-opt",
                  align=(:center, :center),
                  fontsize=48,
                  color=:white,
                  space=:relative)
            xlims!(ax_sep, 0, 1); ylims!(ax_sep, 0, 1)

            Makie.record(fig_sep, sciezka_faza2, 1:N_KLATEK_SEPARATOR; framerate=FPS) do _
                # static frame — nic nie mutujemy, separator stoi
            end
        end

        # === FAZA 3: SA-2-opt optimization (PO NN-construction stan.trasa = pelen cykl NN) ===
        # Po fazie 1 stan.trasa to pelna trasa NN (helper konczy z pelnym cyklem).
        # Tutaj musimy upewnic sie, ze stan.energia jest zaktualizowana (wiele krokow NN
        # nie liczy energia per chunk).
        bufor_e = zeros(Float64, Threads.nthreads())
        stan.energia = oblicz_energie(stan.D, stan.trasa, bufor_e)
        energia_nn = stan.energia  # baseline po NN-construction (== inicjuj_nn! result)
        alg = SimAnnealing(stan; T_zero=0.001)  # T_zero LOCK z plan 02-14
        stan.temperatura = alg.T_zero
        params = Parametry(liczba_krokow=LICZBA_KROKOW_SA)

        sciezka_faza3 = SCIEZKA_GIF * ".faza3.gif"
        wizualizuj(stan, params, alg;
                   liczba_krokow=LICZBA_KROKOW_SA,
                   fps=FPS,
                   kroki_na_klatke=KROKI_NA_KLATKE,
                   eksport=sciezka_faza3,
                   figure_size=FIGURE_SIZE)

        # === CONCAT 3 GIFow przez ffmpeg ===
        sciezka_concat_list = SCIEZKA_GIF * ".concat.txt"
        open(sciezka_concat_list, "w") do io
            println(io, "file '$(basename(sciezka_faza1))'")
            println(io, "file '$(basename(sciezka_faza2))'")
            println(io, "file '$(basename(sciezka_faza3))'")
        end

        sciezka_raw = SCIEZKA_GIF * ".raw.gif"
        ffmpeg() do exe
            run(pipeline(`$exe -y -hide_banner -loglevel error -f concat -safe 0 -i $sciezka_concat_list -c copy $sciezka_raw`; dir=dirname(SCIEZKA_GIF)))
        end

        # === FFMPEG palette + downscale do SZEROKOSC_GIF=1600 (Lanczos) ===
        # [identyczny pipeline jak obecny eksport_mp4.jl linie 64-77, tylko SZEROKOSC_GIF=1600]
        sciezka_palette = SCIEZKA_GIF * ".palette.png"
        rozmiar_raw_mb = round(filesize(sciezka_raw) / 1024 / 1024; digits=2)
        @info "[ffmpeg] Optymalizuje palete + downscale do $(SZEROKOSC_GIF)px (input: $(rozmiar_raw_mb) MB)..."
        filtr_skala = "fps=$FPS,scale=$SZEROKOSC_GIF:-1:flags=lanczos"
        ffmpeg() do exe
            run(pipeline(`$exe -y -hide_banner -loglevel error -i $sciezka_raw -vf "$filtr_skala,palettegen=stats_mode=diff" $sciezka_palette`))
            run(pipeline(`$exe -y -hide_banner -loglevel error -i $sciezka_raw -i $sciezka_palette -lavfi "$filtr_skala [x]; [x][1:v] paletteuse=dither=bayer:bayer_scale=5:diff_mode=rectangle" $SCIEZKA_GIF`))
        end

        # Cleanup posrednich plikow
        for f in [sciezka_faza1, sciezka_faza2, sciezka_faza3, sciezka_concat_list, sciezka_raw, sciezka_palette]
            isfile(f) && rm(f)
        end

        rozmiar_mb = round(filesize(SCIEZKA_GIF) / 1024 / 1024; digits=2)
        ratio = round(stan.energia / energia_nn; digits=4)
        @info "GOTOWE hybryda: $SCIEZKA_GIF = $(rozmiar_mb) MB, ratio_SA/NN=$ratio"
        return (stan, energia_nn)  # D-11: return tuple dla testu (plan 04.1-03 wykorzysta)
    end

    main()
    ```

    DECYZJA ZAMKNIETA (plan-checker iteracja, 2026-05-04): **Wariant B — helper monolityczny + lokalny closure w skrypcie.**

    Powod: zgodnosc z istniejaca sygnatura Tasku 2 (`_animuj_nn_construction!(stan, obs, n_klatek; chunk)` z `for klatka in 1:n_klatek` wewnatrz). Lock Wariantu A wymagalby przepisania Tasku 2 (sygnatura, body, asercje, acceptance) — wiekszy blast radius bez korzysci. Plan 04.1-03 Test 1 (helper-vs-trasa_nn equivalence check) walidowal by helper monolityczny TAK SAMO jak A — robi `_animuj_nn_construction!(stan, obs, n_klatek=ceil(N/chunk); chunk=7)` raz na pustym stanie i sprawdza `stan.trasa == trasa_nn(stan.D; start=1)`. Brak konfliktu.

    KONTRAKT helpera (Task 2, jak juz napisany):
    ```julia
    function _animuj_nn_construction!(stan::StanSymulacji,
                                       obs_trasa::Observable{Vector{Point2f}},
                                       n_klatek::Int;
                                       chunk::Int=7)
        # iteruje n_klatek razy wewnetrznie, mutuje stan.trasa + obs_trasa po kazdym chunk
    end
    ```

    KONSUMPCJA w `examples/eksport_mp4.jl` (Wariant B layout):
    Skrypt definiuje LOKALNIE w `main()` zamykajacy closure `_jeden_chunk_nn!()` ktory robi 1 iteracje petli wewnetrznej Tasku 2 — inline copy-paste algorytmu (greedy nearest unvisited z `stan.trasa[end]`, mutacja `odwiedzone[next]=true`, `push!(stan.trasa, next)`, mutacja Observable). Closure zamyka nad `pelna_trasa, odwiedzone, k` jako captured locals w `main()`. Wywolywane raz per `record()` callback dla fazy 1.

    UWAGA dla wykonawcy: Wariant B duplikuje logike algorytmu (raz w helperze, raz w closure). Akceptowalne dla 4.1 — closure to ~10 linii Julia, dokumentowane komentarzem `# WARIANT B per 04.1-CONTEXT D-06: lokalny chunk-stepper ze wzgledu na monolityczna sygnature helpera`. Refactor do Wariantu A jest todo dla v1.2 (jezeli pojawi sie drugi konsument helpera).

    KROK 3.3 — Regeneracja `assets/demo.gif`:
    - Uruchomic `julia --project=. --threads=auto examples/eksport_mp4.jl`
    - Sprawdzic ze `assets/demo.gif` zostal nadpisany (mtime po regen, file --mime-type GIF89a)
    - Sprawdzic rozmiar: `filesize(assets/demo.gif) <= 1.5 * 1024 * 1024 = 1572864` bajtow
    - Recznie obejrzec GIF w przegladarce: czy widac 3 fazy (NN budowanie → separator z tekstem → SA optymalizacja)?
    - `git add assets/demo.gif` (binarny commit)

    Constraints zachowane (CONTEXT D-13/D-14/D-15/D-16/D-17):
    - BRAK modyfikacji w `src/{punkty,energia,baselines,algorytmy/,typy,JuliaCity}.jl`
    - ASCII identyfikatory + ASCII filenames
    - Polski docstring + @info + komentarze, asercje wewnetrzne po angielsku
    - NFC + BOM-free + LF + final newline
    - Phase 4 D-04 pre-rm istniejacego `assets/demo.gif` zachowany
    - Phase 4 D-05 .gitignore allowlist (`assets/*` + `!assets/demo.gif`) zachowany
  </what-built>
  <how-to-verify>
    Po implementacji skryptu i regeneracji GIFa, wykonaj recznie:

    1. **Walidacja struktury skryptu (automated, mozna uruchomic bez Julia toolchainu):**
       ```powershell
       Select-String -Path examples/eksport_mp4.jl -Pattern 'SZEROKOSC_GIF\s*=\s*1600'
       Select-String -Path examples/eksport_mp4.jl -Pattern 'FIGURE_SIZE\s*=\s*\(1920,\s*960\)'
       Select-String -Path examples/eksport_mp4.jl -Pattern 'figure_size\s*=\s*FIGURE_SIZE'
       Select-String -Path examples/eksport_mp4.jl -Pattern 'Faza 1: NN-construction'
       Select-String -Path examples/eksport_mp4.jl -Pattern 'Faza 2: Separator'
       Select-String -Path examples/eksport_mp4.jl -Pattern 'Faza 3: SA-2-opt'
       Select-String -Path examples/eksport_mp4.jl -Pattern 'concat'
       Select-String -Path examples/eksport_mp4.jl -Pattern 'return\s*\(stan,\s*energia_nn\)'
       ```
       Wszystkie powinny zwrocic 1+ dopasowan.

    2. **Uruchomienie regeneracji (wymaga Julia + GLMakie + display + ffmpeg):**
       ```
       julia --project=. --threads=auto examples/eksport_mp4.jl
       ```
       Spodziewany output (~3-5 min):
       - `[ Info: JuliaCity hybrid GIF — N=1000, seed=42, threads=...`
       - `[ Info: Faza 1: NN-construction edge-by-edge (~5s)...`
       - `[ Info: Faza 2: Separator (0.5-1s)...`
       - `[ Info: Faza 3: SA-2-opt optimization (~5s)...`
       - `[ Info: [ffmpeg] Optymalizuje palete + downscale do 1600px (input: ... MB)...`
       - `[ Info: GOTOWE hybryda: assets/demo.gif = ... MB, ratio_SA/NN=0.9...`

    3. **Walidacja artefaktu binarnego:**
       ```powershell
       # Plik istnieje i ma format GIF89a
       Get-Item assets/demo.gif | Select-Object Name, Length, LastWriteTime
       # Powinien byc <= 1.5 MB:
       (Get-Item assets/demo.gif).Length -le 1572864

       # Sprawdz format (jezeli `file` dostepne — w PowerShell mozna sprawdzic naglowek):
       $bytes = [System.IO.File]::ReadAllBytes("assets/demo.gif")[0..5]
       [System.Text.Encoding]::ASCII.GetString($bytes)
       # Spodziewane: "GIF89a"
       ```

    4. **Wizualna walidacja (CRITICAL — tylko czlowiek mozna zweryfikowac):**
       Otworz `assets/demo.gif` w przegladarce (Windows: ShareX/Chrome, otwórz plik bezposrednio) lub VS Code preview.

       Czy widzisz:
       - **Faza 1 (~5s):** Punkty pojawiaja sie od poczatku, trasa zaczyna byc pusta i ROSNIE krawedz po krawedzi (~7 krawedzi/klatka, plynnie). Po ~5s pelny cykl NN widoczny.
       - **Faza 2 (~0.5-1s):** Czarne tlo + bialy tekst "Optymalizacja SA-2-opt" w srodku, statyczne przez ~20 klatek.
       - **Faza 3 (~5s):** Powrot do dual-panel, trasa NN widoczna, SA-2-opt swap'y zaciskaja trase. Overlay 7 pol + krzywa energii spadajaca po prawej. GOTOWE — ratio ~0.94 widoczne na koncu.

       Jezeli ktorakolwiek faza nie dziala, opisz problem.

    5. **Walidacja git (przed commitem):**
       ```powershell
       git status assets/demo.gif    # powinien byc M lub ?? (nowy / zmodyfikowany)
       git diff --stat assets/demo.gif  # binary diff: "Bin XXX -> YYY bytes"
       ```
  </how-to-verify>
  <resume-signal>
    Wpisz "approved" jezeli:
    - GIF wizualnie pokazuje 3 fazy (NN-budowanie → separator → SA optymalizacja)
    - Rozmiar pliku <= 1.5 MB
    - Skrypt eksportu uruchomil sie bez bledu
    - Format GIF89a 1600x... potwierdzony

    Wpisz "issues: [opis]" jezeli:
    - Ktorakolwiek faza nie dziala wizualnie (NN nie rosnie, separator brak tekstu, SA nie startuje od pelnej trasy NN)
    - Rozmiar > 1.5 MB (potrzebny dalszy downscale lub palette tweak)
    - ffmpeg concat nie dziala (3 GIFy roznia sie palette → trzeba paletegen JOINT zamiast per-faza)
    - Helper `_animuj_nn_construction!` nie produkuje cyklu identycznego z `trasa_nn(D; start=1)` — fallback na `inicjuj_nn!` przed faza 3 (dziala niezaleznie ale traci D-05 pure incremental property)
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ffmpeg subprocess | Trzy `run(pipeline(`$exe ... `))` calls do FFMPEG_jll — zewnetrzne procesy z parametrami z naszego kodu. Sciezki sa lokalne (pod `assets/`), brak user input z sieci. |
| GLMakie native renderer | Trzy `Makie.record()` blocki spawnuja okno GLMakie (lub uzywaja off-screen — Makie automatycznie wybiera). Plik wynikowy zapisywany do lokalnej sciezki. |
| Binarny commit assets/demo.gif | Plik binarny (~500-1500 KB) wchodzi do git history. Commit message zawiera marker "URGENT post-UAT 4.1" per CONTEXT D-17. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-02-01 | Tampering | ffmpeg concat z `-safe 0` flag | accept | `-safe 0` jest wymagane dla wzglednych sciezek w concat list. Sciezki sa generowane przez nasz kod (basename SCIEZKA_GIF) — nie ma user input. ASVS L1 nie wymaga sandbox dla local-only file processing. |
| T-04.1-02-02 | Information Disclosure | Posrednie pliki .faza1.gif/.faza2.gif/.faza3.gif/.raw.gif/.palette.png/.concat.txt | mitigate | Skrypt usuwa wszystkie posrednie pliki w `cleanup` block (`for f in [...] isfile(f) && rm(f) end`). Phase 4 D-05 .gitignore (`assets/*` + `!assets/demo.gif`) chroni przed accidental commit jezeli cleanup zawiedzie. |
| T-04.1-02-03 | Denial of Service | 3 osobne `Makie.record()` calls + 2 ffmpeg subprocess calls | accept | Skrypt jest jednorazowym narzedziem dewelopera (NIE runtime API). Faza 1 + 2 + 3 = ~3-5 min na laptopie z GLMakie. Brak DoS attack vector. |
| T-04.1-02-04 | Tampering | Helper `_animuj_nn_construction!` musi reprodukowac `trasa_nn` deterministycznie | mitigate | Asercja koncowa `@assert k == n` waliduje pelny cykl. Plan 04.1-03 dodaje unit test sprawdzajacy `stan.trasa == trasa_nn(stan.D; start=1)` po helperze. |

Brak ASVS L1 controls naruszonych — local development tooling, brak network/auth/persistence beyond git binary commit.
</threat_model>

<verification>
- Helper `_animuj_nn_construction!` istnieje w src/wizualizacja.jl, zdefiniowany przed `_wizualizuj_impl`.
- `wizualizuj()`, `_wizualizuj_impl()`, `_setup_figure()` maja nowy kwarg `figure_size::Tuple{Int,Int}=(1400, 700)`.
- `examples/eksport_mp4.jl` ma `SZEROKOSC_GIF = 1600`, `FIGURE_SIZE = (1920, 960)`, 3 osobne `Makie.record()` calls (Faza 1/2/3) + ffmpeg concat + ffmpeg downscale Lanczos.
- `examples/eksport_mp4.jl::main()` zwraca `(stan, energia_nn)` (D-11 contract dla planu 04.1-03 testu).
- `assets/demo.gif` zregenerowany, format GIF89a, rozmiar <= 1.5 MB, 3 fazy widoczne wizualnie (human verify).
- Phase 4 D-04 pre-rm zachowany, Phase 4 D-05 .gitignore allowlist zachowany.
- BRAK modyfikacji w src/{punkty.jl, energia.jl, baselines.jl, algorytmy/, typy.jl, JuliaCity.jl} (CONTEXT D-13).
- BRAK modyfikacji w examples/podstawowy.jl (backwards-compat — default figure_size=(1400, 700) zachowuje stary rozmiar).
- Pkg.test() jezeli uruchomione: nadal 230/230 (Phase 4 nie modyfikuje testow Phase 1/2/3, plan 04.1-03 dodaje nowe).
</verification>

<success_criteria>
- 3-fazowa hybryda widoczna w `assets/demo.gif` (NN-construction → separator → SA-2-opt)
- Pierwsza klatka GIFa: pusty cykl (tylko punkty + start, brak krawedzi widocznej — `stan.trasa = [1]` przed faza 1)
- Ostatnia klatka GIFa fazy SA: trasa z `energia < energia_nn` (SA bil baseline, ratio ~0.94 z plan 02-14 LOCK)
- Rozdzielczosc finalna: 1600 x ~800 (Lanczos downscale z 1920x960)
- Rozmiar `assets/demo.gif` <= 1.5 MB
- Phase 3 D-09 LOCKED: `wizualizuj()` public API rozszerzony tylko o jeden ADDITIVE kwarg z backwards-compat defaultem
- VIZ-06 LOCKED: `using GLMakie` nadal TYLKO w `src/wizualizacja.jl` (helper trzymany w tym pliku, NIE nowy `src/animacja.jl`)
- Phase 1+2 src/* niezmienione (D-13)
- Pkg.test() exit 0 — nadal 230/230 (testy nowe dopiero w 04.1-03)
- Commit `assets/demo.gif` z markerem "URGENT post-UAT 4.1" (CONTEXT D-17)
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-demo-gif-hybrid-and-isopen-fix/04.1-02-SUMMARY.md` with:
- Tasks executed (3) + commit hashes (3 commits sugerowane: helper add + figure_size kwarg, eksport_mp4 refactor + bump, assets/demo.gif binary regen)
- Coverage matrix dla CONTEXT D-04, D-05, D-06, D-07, D-08, D-09, D-17 (DONE per task)
- Files modified: src/wizualizacja.jl (+helper, +figure_size kwarg w 3 funkcjach), examples/eksport_mp4.jl (full rewrite main()), assets/demo.gif (binarny regen)
- WYBOR Wariantu A vs B dla `_animuj_nn_construction!` consumption pattern (z Tasku 3 decyzja wykonawcy)
- Wizualne walidacje (screenshot + opis 3 faz, jezeli mozliwe)
- Rozmiar pliku przed/po + ratio_SA/NN z run output
- Confirmed unchanged: Phase 1+2 src/, examples/podstawowy.jl, Phase 3 wizualizuj() backwards-compat
- Next-wave readiness: 04.1-03 (smoke testy) odblokowany — `_animuj_nn_construction!` istnieje, `examples/eksport_mp4.jl::main()` zwraca `(stan, energia_nn)` dla testu D-11
</output>
