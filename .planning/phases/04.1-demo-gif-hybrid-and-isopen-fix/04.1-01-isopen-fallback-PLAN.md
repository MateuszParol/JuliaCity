---
phase: 04.1-demo-gif-hybrid-and-isopen-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/wizualizacja.jl
autonomous: true
requirements:
  - VIZ-06
must_haves:
  truths:
    - "examples/podstawowy.jl nie crashuje juz na MethodError: no method matching isopen(::Makie.Figure)"
    - "Wszystkie 3 historyczne call-sites isopen(fig) (linie 202, 373, 377 src/wizualizacja.jl) korzystaja z fallback events(fig).window_open[] przez prywatny helper _is_window_open"
    - "Outer try/catch w wizualizuj() (linie 449-462) NIE reklasyfikuje juz MethodError jako 'GLMakie display issue' — propaguje runtime errors unwrapped"
    - "Init-time blokady GLMakie (LoadError z OpenGL/X11/display, InitError, GLFW.GLFWError) nadal sa lapane i tlumaczone na polski D-13 verbatim"
  artifacts:
    - path: "src/wizualizacja.jl"
      provides: "Helper _is_window_open(fig)::Bool + 3 podmienione call-sites isopen + zawezony outer catch (D-01..D-03)"
      contains: "_is_window_open"
  key_links:
    - from: "src/wizualizacja.jl::_live_loop"
      to: "events(fig).window_open[]"
      via: "_is_window_open(fig) helper"
      pattern: "_is_window_open\\(fig\\)"
    - from: "src/wizualizacja.jl::_wizualizuj_impl (live mode po SA stop)"
      to: "events(fig).window_open[]"
      via: "_is_window_open(fig) helper"
      pattern: "_is_window_open\\(fig\\)"
    - from: "src/wizualizacja.jl::wizualizuj outer catch (linie 449-462)"
      to: "rethrow(e) dla MethodError"
      via: "narrowed catch (only LoadError/InitError/GLFW + komunikat OpenGL/X11/display)"
      pattern: "isa\\(e, MethodError\\) \\|\\| isa\\(e, BoundsError\\).*rethrow"
---

<objective>
Domkniecie BLOCKERA #1 z 04-UAT.md (Test 2): `examples/podstawowy.jl` rzuca natychmiast `MethodError: no method matching isopen(::Makie.Figure)` w `src/wizualizacja.jl:202` (`_live_loop`), bo Makie 0.24+ wycofalo metode `isopen(::Figure)`. Dodatkowo outer `try/catch` (linie 449-462) reklasyfikuje ten MethodError jako falszywy "GLMakie display issue" przez string-match na `"GLMakie"` w komunikacie bledu — maskuje prawdziwa przyczyne.

Purpose: Phase 3 plan 03-05 PLANOWAL ten fallback (komentarz nad linia 202: `"jezeli MethodError, plan 03-05 doda try/catch z fallback events(fig).window_open[]"`), ale zmiana NIE weszla do kodu — 03-05-SUMMARY potwierdza, ze plan 03-05 dotyczyl D-06/D-08/D-13, a fallback isopen pozostal TODO. Plan 04.1-01 zamyka te luke punktowo: 1 prywatny helper `_is_window_open(fig)::Bool` per CONTEXT D-02 + podmiana 3 call-sites per CONTEXT D-01 + zawezenie outer catch per CONTEXT D-03 (MethodError MUSI propagowac unwrapped).

Output: Zmodyfikowany `src/wizualizacja.jl` (jeden plik, ~10-15 linii netto zmian) z dzialajacym live mode na Makie 0.24+, bez modyfikacji publicznego API `wizualizuj()` (Phase 3 D-09 LOCKED). Dwa pozostale plany (04.1-02, 04.1-03) blokowane dopoki ten plan nie wladzie — `record()` w eksporcie potrzebuje stabilnego live mode dla nested rendering w 04.1-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-demo-gif-hybrid-and-isopen-fix/04.1-CONTEXT.md
@.planning/phases/04-demo-benchmarks-documentation/04-UAT.md
@.planning/phases/03-visualization-export/03-05-SUMMARY.md
@src/wizualizacja.jl
@examples/podstawowy.jl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dodaj prywatny helper _is_window_open(fig)::Bool (D-02)</name>
  <read_first>
    - src/wizualizacja.jl (linie 165-180 — sekcja "Live renderloop", const _ACC_WIN i komentarze nad _live_loop; helper musi byc PRZED pierwszym call-site na linii 202)
    - .planning/phases/04.1-demo-gif-hybrid-and-isopen-fix/04.1-CONTEXT.md (D-01 — fallback przez `events(fig).window_open[]`; D-02 — defensywna logika true on throw)
  </read_first>
  <action>
    Wstawic NOWY prywatny helper do `src/wizualizacja.jl` BEZPOSREDNIO PRZED komentarzem `# Live renderloop (D-09 branch eksport === nothing)` (obecnie linie 165-167). Helper trafia w sekcje "Helpery wewnetrzne" (prefiks `_` per Phase 3 PATTERNS), tuz po istniejacym `_init_observables` (~linia 163), PRZED separatorem komentarzowym i `const _ACC_WIN = 1000`.

    Tresc helpera (polski docstring per LANG-01, ASCII identyfikator per BOOT-04, asercja po angielsku per LANG-04 NIE wymagana — funkcja czysto zwraca Bool):

    ```julia
    """
    Sprawdza czy okno GLMakie powiazane z `fig` jest nadal otwarte.

    Phase 4.1 D-01/D-02 fallback po deprecacji `Base.isopen(::Makie.Figure)` w Makie
    0.24+. Korzystamy z udokumentowanego publicznego API Makie: `events(fig).window_open`
    to `Observable{Bool}` ustawiany przez backend (GLMakie ↔ GLFW callbacks). Zaleta nad
    konwersja do `GLFW.Window`: nie siegamy do GLMakie internals (lzejszy fallback dla
    przyszlej zmiany backendu na CairoMakie/WGLMakie).

    Defensywnie zwracamy `true` gdy odczyt rzuca (np. `fig` nie zostal jeszcze
    wyswietlony przez `display()` — `events(fig).window_open` moze nie byc zainicjowany).
    Alternatywa byloby falszywe stop przed renderem pierwszej klatki — gorsza UX.
    """
    function _is_window_open(fig)::Bool
        try
            return events(fig).window_open[]
        catch
            return true
        end
    end
    ```

    KRYTYCZNE:
    - Funkcja MUSI byc zdefiniowana PRZED `_live_loop` (kolejnosc include w Julii — wszystkie 3 call-sites kompiluja sie na linii 202+, helper musi byc widoczny).
    - NIE eksportujemy z modulu (prefiks `_`, internal usage).
    - Brak nowych deps — `events(...)` jest re-eksportowane przez `using GLMakie` (linia 27 src/wizualizacja.jl), tak samo jak `Observable`, `Figure`, `Axis` (`# Observable jest re-eksportowane przez using GLMakie` — komentarz w linii 30 potwierdza).
    - Komentarz polski + diakrytyki ("powiazane", "wyswietlony", "zainicjowany", "Defensywnie") — Phase 1 D-21 NFC waliduje.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path src/wizualizacja.jl -Pattern '^function _is_window_open\(fig\)::Bool$' -SimpleMatch:$false"</automated>
  </verify>
  <acceptance_criteria>
    - `src/wizualizacja.jl` zawiera linie `function _is_window_open(fig)::Bool` (dokladne dopasowanie sygnatury, ASCII identyfikator).
    - Helper znajduje sie PRZED definicja `_live_loop` (numer linii grep `_is_window_open` < numer linii grep `function _live_loop`).
    - `try ... return events(fig).window_open[] ... catch ... return true ... end` obecny w ciele.
    - Polski docstring z odwolaniem do D-01/D-02 + Phase 4.1.
    - Brak modyfikacji w innych funkcjach (helper jest pure additive).
    - Plik nadal walidny UTF-8 NFC + LF + final newline (Phase 1 D-21 — encoding hygiene testset to wykryje).
  </acceptance_criteria>
  <done>Helper `_is_window_open(fig)::Bool` zdefiniowany w `src/wizualizacja.jl` przed `_live_loop`, ASCII nazwa, polski docstring, defensywny try/catch zwracajacy `true` na throw.</done>
</task>

<task type="auto">
  <name>Task 2: Podmien 3 call-sites isopen(fig) na _is_window_open(fig) (D-01)</name>
  <read_first>
    - src/wizualizacja.jl (linie 200-205, 370-380 — kontekst kazdego z 3 call-sites)
    - .planning/phases/04-demo-benchmarks-documentation/04-UAT.md (sekcja Test 2 reported — dokladne sygnatury 3 call-sites z UAT artifacts)
    - .planning/phases/04.1-demo-gif-hybrid-and-isopen-fix/04.1-CONTEXT.md (D-01 — wszystkie 3 podmienione)
  </read_first>
  <action>
    Wykonac DOKLADNIE 3 podmiany w `src/wizualizacja.jl`. Komentarze obok kazdej zmiany NIE sa wymagane (audit trail siedzi w SUMMARY); ale komentarz nad linia 199-201 ("isopen(fig) z Makie events; jezeli MethodError, plan 03-05 doda try/catch z fallback events(fig).window_open[].") ZAKTUALIZOWAC zeby odzwierciedlal rzeczywistosc po fixie (plan 04.1-01, NIE 03-05).

    PODMIANA 1 — `_live_loop` warunek glownej petli (linia 202):
    BEFORE:
    ```julia
        # Stop conditions: window zamkniete (A2 — isopen dla Figure) LUB SA osiagnal limit.
        # `isopen(fig)` z Makie events; jezeli MethodError, plan 03-05 doda try/catch
        # z fallback `events(fig).window_open[]`.
        while isopen(fig) && stan.iteracja < liczba_krokow
    ```
    AFTER (komentarz zaktualizowany — fallback JEST, nie BEDZIE):
    ```julia
        # Stop conditions: window zamkniete (Phase 4.1 D-01 fallback przez
        # `events(fig).window_open[]` zamiast deprecated `isopen(::Makie.Figure)`)
        # LUB SA osiagnal limit. Helper `_is_window_open` defensywnie zwraca true
        # gdy fig nie zostal jeszcze wyswietlony (D-02).
        while _is_window_open(fig) && stan.iteracja < liczba_krokow
    ```

    PODMIANA 2 — `_wizualizuj_impl` warunek po `_live_loop` (linia 373, branch live mode po SA stop, GOTOWE overlay guard):
    BEFORE:
    ```julia
            # D-06: GOTOWE overlay — tylko gdy SA dobiegl konca I okno jest jeszcze otwarte.
            # Gdy user zamknie okno przed SA stop, isopen(fig) == false — pomijamy overlay.
            if isopen(fig) && stan.iteracja >= liczba_krokow
    ```
    AFTER:
    ```julia
            # D-06: GOTOWE overlay — tylko gdy SA dobiegl konca I okno jest jeszcze otwarte.
            # Gdy user zamknie okno przed SA stop, _is_window_open(fig) == false — pomijamy overlay.
            # Phase 4.1 D-01 fallback (Makie 0.24+ deprecate isopen(::Figure)).
            if _is_window_open(fig) && stan.iteracja >= liczba_krokow
    ```

    PODMIANA 3 — `_wizualizuj_impl` pasywny event loop trzymajacy okno (linia 377):
    BEFORE:
    ```julia
                # Pasywny event loop — czekamy az user zamknie okno recznie (D-06: brak auto-close).
                # sleep(1/fps) yielding GLMakie event loopowi (RESEARCH Q2).
                while isopen(fig)
                    sleep(1 / fps)
                end
    ```
    AFTER:
    ```julia
                # Pasywny event loop — czekamy az user zamknie okno recznie (D-06: brak auto-close).
                # sleep(1/fps) yielding GLMakie event loopowi (RESEARCH Q2).
                # Phase 4.1 D-01 fallback (Makie 0.24+ deprecate isopen(::Figure)).
                while _is_window_open(fig)
                    sleep(1 / fps)
                end
    ```

    KRYTYCZNE:
    - Po wykonaniu Tasku 2 w `src/wizualizacja.jl` MUSI byc DOKLADNIE 0 wystapien `isopen(fig)` (gdzie `fig` to lokalna zmienna typu `Makie.Figure`). Sprawdzenie: `Select-String -Pattern 'isopen\(fig\)'` MUSI zwrocic 0 dopasowan.
    - Zachowac semantyke krotkiego spiecia: `_is_window_open(fig) && stan.iteracja < liczba_krokow` (lewa strona pierwsza, identyczna kolejnosc do oryginalu — defensive `true` z helpera nie zmienia logiki gdy SA dobiegl konca).
    - Komentarze polskie + diakrytyki — Phase 1 D-21 NFC waliduje.
    - NIE dotykac `_export_loop` (Makie.record() nie uzywa isopen — branch eksportu nie ma tego problemu).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$txt = Get-Content src/wizualizacja.jl -Raw; if ((Select-String -Path src/wizualizacja.jl -Pattern '_is_window_open\(fig\)' -AllMatches).Matches.Count -ne 3) { exit 1 }; if ((Select-String -Path src/wizualizacja.jl -Pattern 'isopen\(fig\)' -AllMatches).Matches.Count -ne 0) { exit 2 }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Pattern '_is_window_open\(fig\)' src/wizualizacja.jl` zwraca DOKLADNIE 3 dopasowania (3 call-sites podmienione).
    - `Select-String -Pattern 'isopen\(fig\)' src/wizualizacja.jl` zwraca DOKLADNIE 0 dopasowan (zaden ze starych call-sites nie zostal pominiety).
    - Komentarz nad linia z `while _is_window_open(fig) && stan.iteracja` (pierwszy call-site) odwoluje sie do "Phase 4.1 D-01" (audit trail), NIE do "plan 03-05".
    - Wszystkie 3 podmiany sa wewnatrz funkcji zwracajacych nothing (nie wprowadzono zmian sygnatur, brak side-effectu na publiczny API).
    - Plik nadal walidny UTF-8 NFC + LF (Phase 1 D-21 testset to wykryje).
  </acceptance_criteria>
  <done>Trzy stare call-sites `isopen(fig)` (linie 202, 373, 377 sprzed zmiany) zastapione przez `_is_window_open(fig)`; komentarze zaktualizowane do Phase 4.1 D-01; brak pozostawionych wystapien starego patternu.</done>
</task>

<task type="auto">
  <name>Task 3: Zawezic outer try/catch w wizualizuj() — MethodError MUSI propagowac (D-03)</name>
  <read_first>
    - src/wizualizacja.jl (linie 442-465 — pelna definicja wizualizuj() public + outer try/catch z D-13 hard-fail)
    - .planning/phases/04-demo-benchmarks-documentation/04-UAT.md (sekcja Test 2 — UAT cytuje obecny "if contains(msg, 'GLMakie')" jako maskujacy MethodError)
    - .planning/phases/03-visualization-export/03-CONTEXT.md (D-13 — verbatim polski error string MUSI byc zachowany dla init-time GLMakie/OpenGL/X11/display blokad)
    - .planning/phases/04.1-demo-gif-hybrid-and-isopen-fix/04.1-CONTEXT.md (D-03 — kryterium reklasyfikacji: "Tylko LoadError z initialization-time loadu GLMakie LUB ErrorException zawierajacy w message OpenGL/X11/display (NIE GLMakie sam w sobie). MethodError MUSI propagowac unwrapped.")
  </read_first>
  <action>
    Zmodyfikowac CIALO bloku `catch e ... end` (obecnie linie 449-462) w `src/wizualizacja.jl`. Sygnatura `wizualizuj(...)` (linie 432-436) i `try ... _wizualizuj_impl(...) ... catch e` (linie 445-449) NIEZMIENIONE.

    BEFORE (linie 449-462):
    ```julia
        catch e
            # Q10: GLMakie rzuca GLFW.GLFWError lub InitError przy braku OpenGL/displayu.
            # Sprawdzamy po stringu (GLFW nie eksportowane do scope'u) ORAZ typie InitError.
            msg = sprint(showerror, e)
            if contains(msg, "GLFW") || contains(msg, "OpenGL") || contains(msg, "display") ||
               contains(msg, "X11") || contains(msg, "GLMakie") || isa(e, InitError)
                # D-13: doslowny polish error (CONTEXT.md). Diakrytyki: "Spróbuj", "Linuksie".
                error("GLMakie wymaga aktywnego kontekstu OpenGL. Brak displayu? " *
                      "Spróbuj `xvfb-run -a julia ...` na Linuksie albo uruchom lokalnie z GUI. " *
                      "Headless cloud (CI, Docker bez X) NIE jest wspierany w wersji v1.")
            else
                # Inny blad (np. ArgumentError z @assert, BoundsError) — propagujemy bez zmian.
                rethrow(e)
            end
        end
    ```

    AFTER (Phase 4.1 D-03 — usuniete `contains(msg, "GLMakie")` jako trigger; runtime errors `MethodError`/`BoundsError`/`ArgumentError` propaguja unwrapped przez wczesne `rethrow(e)`; init-time GLMakie blokady nadal lapane przez kombinacje `LoadError`/`InitError` PLUS message OpenGL/X11/display/GLFW):
    ```julia
        catch e
            # Phase 4.1 D-03: zawezone do PRAWDZIWYCH init-time GLMakie blokad. Stara
            # logika "if contains(msg, 'GLMakie')" reklasyfikowala runtime MethodError
            # (np. isopen(::Makie.Figure) na Makie 0.24+) jako falszywy "display issue" —
            # 04-UAT.md Test 2 BLOCKER. Teraz: runtime errors (MethodError, BoundsError,
            # ArgumentError z @assert) propaguja unwrapped przez wczesne rethrow.
            if isa(e, MethodError) || isa(e, BoundsError) || isa(e, ArgumentError) ||
               isa(e, AssertionError) || isa(e, DomainError)
                rethrow(e)
            end

            # Init-time GLMakie/OpenGL/X11/display blokady: LoadError/InitError + komunikat
            # technologii. Slowo "GLMakie" same w sobie NIE wystarcza (zbyt szerokie —
            # MethodError z stack trace zawiera nazwy modulow). Wymagane konkretne
            # wskazniki braku kontekstu OpenGL.
            msg = sprint(showerror, e)
            init_time_glmakie = (isa(e, InitError) || isa(e, LoadError)) &&
                                (contains(msg, "GLFW") || contains(msg, "OpenGL") ||
                                 contains(msg, "X11") || contains(msg, "display"))
            runtime_opengl = !isa(e, MethodError) &&
                             (contains(msg, "GLFW") || contains(msg, "OpenGL") ||
                              contains(msg, "X11") || contains(msg, "no display"))

            if init_time_glmakie || runtime_opengl
                # D-13 verbatim polish error (Phase 3 03-CONTEXT D-13 LOCKED).
                # Diakrytyki: "Spróbuj", "Linuksie".
                error("GLMakie wymaga aktywnego kontekstu OpenGL. Brak displayu? " *
                      "Spróbuj `xvfb-run -a julia ...` na Linuksie albo uruchom lokalnie z GUI. " *
                      "Headless cloud (CI, Docker bez X) NIE jest wspierany w wersji v1.")
            else
                # Wszystko inne (nieoczekiwane runtime errors) — propagujemy bez zmian
                # zeby uzytkownik widzial prawdziwy stack trace zamiast falszywego komunikatu.
                rethrow(e)
            end
        end
    ```

    KRYTYCZNE — co MUSI dzialac po Tasku 3:
    1. `MethodError` rzucony z `_live_loop` (np. gdyby _is_window_open mial bug i sam rzucil) — propaguje TAKI JAKI JEST, z prawdziwym stack trace.
    2. `BoundsError`, `ArgumentError`, `AssertionError`, `DomainError` (np. z `@assert liczba_krokow > 0` w `_wizualizuj_impl`) — propaguje unwrapped.
    3. Brak DISPLAY w Linuksie → `LoadError` z stack trace zawierajacym "OpenGL" / "X11" / "display" / "GLFW" → tlumaczone na polski D-13 verbatim.
    4. Headless Docker bez X → `InitError` z message "X11" → tlumaczone na polski.
    5. Verbatim polski error string IDENTYCZNY z 03-05-SUMMARY (D-13 LOCKED — Phase 3 lock zachowany, Phase 4.1 nie zmienia komunikatu, tylko logike triggerowania).

    Test ad-hoc dla samego Taska 3 (BEZ uruchamiania GLMakie):
    ```julia
    julia --project=. -e 'using JuliaCity; try; throw(MethodError(isopen, (1,))); catch e; @assert isa(e, MethodError); println("MethodError type ok"); end'
    ```
    (To NIE testuje patcha bezposrednio — testuje ze MethodError jest unikalnym typem; pelna walidacja w Tasku acceptance ponizej.)
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$content = Get-Content src/wizualizacja.jl -Raw; if (-not ($content -match 'isa\(e, MethodError\)')) { Write-Error 'MethodError early rethrow missing'; exit 1 }; if (-not ($content -match 'init_time_glmakie')) { Write-Error 'narrowed init-time check missing'; exit 2 }; if ($content -match 'contains\(msg, \"GLMakie\"\)') { Write-Error 'old broad GLMakie string-match still present'; exit 3 }; if (-not ($content -match 'GLMakie wymaga aktywnego kontekstu OpenGL\. Brak displayu\?')) { Write-Error 'D-13 verbatim polish error string missing'; exit 4 }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - `src/wizualizacja.jl` zawiera `isa(e, MethodError)` w outer catch (early rethrow guard).
    - `src/wizualizacja.jl` zawiera identyfikator `init_time_glmakie` (lokalna zmienna z zawezona logika).
    - `src/wizualizacja.jl` NIE zawiera juz patternu `contains(msg, "GLMakie")` (stary szeroki trigger usuniety — D-03 wymaga tego explicite).
    - Verbatim polski error string D-13 (`"GLMakie wymaga aktywnego kontekstu OpenGL. Brak displayu? Spróbuj \`xvfb-run -a julia ...\` na Linuksie albo uruchom lokalnie z GUI. Headless cloud (CI, Docker bez X) NIE jest wspierany w wersji v1."`) ZACHOWANY (Phase 3 D-13 LOCKED).
    - `BoundsError`, `ArgumentError`, `AssertionError`, `DomainError` rowniez sa we wczesnym `rethrow` guard (lista 5 typow runtime).
    - Plik nadal walidny UTF-8 NFC + LF + final newline.
    - Brak modyfikacji sygnatury `wizualizuj(...)` ani `_wizualizuj_impl(...)` (Phase 3 D-09 LOCKED).
  </acceptance_criteria>
  <done>Outer catch w `wizualizuj()` zawezony do PRAWDZIWYCH init-time GLMakie/OpenGL/X11/display blokad; runtime errors (MethodError, BoundsError, ArgumentError, AssertionError, DomainError) propaguja unwrapped przez wczesne rethrow; D-13 verbatim polski error string zachowany; logika string-matchu na samym slowie "GLMakie" usunieta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GLMakie native window <-> Julia process | Okno GLMakie jest sterowane przez backend GLFW (native code). `events(fig).window_open` to publiczne Observable Makie ktore backend mutuje przez callbacki. Brak wejscia od uzytkownika sieci/dyskowego — pure GUI state. |
| Exception flow w wizualizuj() outer catch | Granica miedzy library code (Makie/GLMakie) a user code (`wizualizuj()` caller). Reklasyfikacja exception traci informacje — D-03 zawezenie zmniejsza powierzchnie tego "translation layer". |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-01-01 | Tampering | `_is_window_open` defensive `try/catch` zwraca `true` na throw | accept | Defensywne `true` celowe (D-02) — alternatywa byloby falszywe stop przed pierwsza klatka. Caller (`_live_loop`) tak czy inaczej koncowy stop wpadnie na `stan.iteracja >= liczba_krokow`. |
| T-04.1-01-02 | Information Disclosure | Outer catch w `wizualizuj()` reformatuje stack trace na polski blad | mitigate | D-03 zawezenie — runtime errors (MethodError, BoundsError, ArgumentError, AssertionError, DomainError) NIE sa juz reformatowane, propaguja prawdziwy stack trace. Jedyne co reformatujemy to PRAWDZIWE init-time GLMakie/OpenGL/X11/display blokady (LoadError/InitError + dopasowanie komunikatu). |
| T-04.1-01-03 | Denial of Service | Pasywny event loop `while _is_window_open(fig) sleep(1/fps) end` po GOTOWE overlay | accept | Zachowanie identyczne jak przed fixem (Phase 3 D-06 lock). Helper defensywny — jezeli `events(fig).window_open` rzuci, wracamy `true` i petla kontynuuje. User zamyka okno recznie (D-06 brak auto-close). |

Brak ASVS L1 controls naruszonych — czysta refaktoryzacja interna, brak network/auth/input/persistence.
</threat_model>

<verification>
- `Select-String -Path src/wizualizacja.jl -Pattern '^function _is_window_open\(fig\)::Bool$'` zwraca 1 dopasowanie.
- `(Select-String -Path src/wizualizacja.jl -Pattern '_is_window_open\(fig\)' -AllMatches).Matches.Count` zwraca 4 (1 definicja + 3 call-sites).
- `(Select-String -Path src/wizualizacja.jl -Pattern 'isopen\(fig\)' -AllMatches).Matches.Count` zwraca 0 (zaden stary call-site nie pozostal).
- `Select-String -Path src/wizualizacja.jl -Pattern 'isa\(e, MethodError\)'` zwraca 1 dopasowanie (early rethrow w outer catch).
- `Select-String -Path src/wizualizacja.jl -Pattern 'contains\(msg, "GLMakie"\)'` zwraca 0 dopasowan (stary szeroki trigger usuniety per D-03).
- `Select-String -Path src/wizualizacja.jl -Pattern 'GLMakie wymaga aktywnego kontekstu OpenGL'` zwraca 1 dopasowanie (D-13 verbatim zachowany).
- Smoke walidacja koncowa (po wszystkich 3 taskach, jezeli toolchain Julia + GLMakie dostepny):
  ```
  julia --project=. --threads=auto -e 'using JuliaCity; using Random: Xoshiro; punkty=generuj_punkty(10;seed=42); stan=StanSymulacji(punkty;rng=Xoshiro(42)); inicjuj_nn!(stan); alg=SimAnnealing(stan;T_zero=0.001); stan.temperatura=alg.T_zero; params=Parametry(liczba_krokow=5); try; wizualizuj(stan, params, alg; liczba_krokow=5, kroki_na_klatke=1); catch e; if isa(e, MethodError); println("STILL BROKEN: MethodError leak"); exit 1; else; println("OK: no MethodError; exception: ", typeof(e)); end; end'
  ```
  Spodziewany rezultat (1 z dwoch — oba akceptowalne dla scope tego planu):
  - Sukces: okno otwiera sie i zamyka, exit 0 (toolchain z displayem).
  - Komunikat polski D-13: "GLMakie wymaga aktywnego kontekstu OpenGL..." (toolchain bez displayu — error oczekiwany na D-13).
  - NIE ZAAKCEPTOWANE: `MethodError: no method matching isopen(::Makie.Figure)` lub jakikolwiek `MethodError` w stderr → fix nie zadzialal.

  Pelny live-mode smoke test (z headless guard) jest scope planu 04.1-03; tu walidujemy tylko ze MethodError nie ucieka.
</verification>

<success_criteria>
- Trzy podmiany call-sites + jeden helper + jedno zawezenie outer catch — wszystko w `src/wizualizacja.jl` (single-file change, ~30-40 linii netto).
- Brak modyfikacji w `src/{punkty.jl, energia.jl, baselines.jl, algorytmy/, typy.jl, JuliaCity.jl}` (Phase 1+2 LOCKED per CONTEXT D-13).
- Brak nowych deps (`events()` re-eksportowane przez `using GLMakie`).
- Brak modyfikacji sygnatury `wizualizuj(...)` (Phase 3 D-09 LOCKED, CONTEXT D-06 explicit constraint).
- D-13 verbatim polski error string zachowany (Phase 3 LOCKED).
- Phase 1 D-21 NFC + LF + BOM-free + final newline + ASCII identyfikatory (BOOT-04) zachowane.
- Brak nowych testow w tym planie — testy live-mode smoke + content-level w planie 04.1-03 (po wladzaniu Wave 2).
- Plan blokuje Wave 2 (04.1-02): hybrid GIF rewrite uzywa `record()` w eksporcie ktory NIE wymaga _is_window_open, ale stabilna semantyka outer catch jest potrzebna do wykrycia bledow w nested `record()` calls.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-demo-gif-hybrid-and-isopen-fix/04.1-01-SUMMARY.md` with:
- Tasks executed (3) + commit hashes
- Coverage matrix dla CONTEXT D-01, D-02, D-03 (DONE per task)
- Files modified: src/wizualizacja.jl (single file, ~30-40 net lines)
- Smoke validation result (jezeli julia/GLMakie dostepne) — output of verify command
- Confirmed unchanged: Phase 1+2 src/, public API wizualizuj(), D-13 verbatim error string
- Next-wave readiness: 04.1-02 (hybrid GIF) odblokowany — live mode dziala, outer catch propaguje runtime errors zamiast je maskowac
</output>
