---
phase: 04.1-demo-gif-hybrid-and-isopen-fix
plan: 03
type: execute
wave: 3
depends_on:
  - 04.1-01
  - 04.1-02
files_modified:
  - test/test_4_1.jl
  - test/runtests.jl
autonomous: false
requirements:
  - VIZ-06
  - DEMO-04
  - EKS-04
must_haves:
  truths:
    - "Nowy testset `4.1: hybrid GIF + isopen fallback` zarejestrowany w test/runtests.jl"
    - "Test 1 (D-05 zgodnosc algorytmiczna): po `_animuj_nn_construction!` na N=50 stan.trasa == trasa_nn(stan.D; start=1)"
    - "Test 2 (D-11 SA bije NN po hybrydzie): wykonanie `examples/eksport_mp4.jl::main()` z mniejszym N=50 i LICZBA_KROKOW_SA=200 zwraca (stan, energia_nn) z stan.energia < energia_nn"
    - "Test 3 (D-12 live mode smoke z headless guard): `wizualizuj(...; liczba_krokow=10, kroki_na_klatke=2)` opakowane w try/catch — sukces lub @test_skip dla init-time GLMakie/OpenGL/X11/display blokad, NIE acceptable dla MethodError"
    - "Test 4 (CONTEXT D-03 narrowed catch): MethodError sztucznie rzucony z mock callbacku propaguje unwrapped przez wizualizuj() outer catch, NIE jest reformatowany jako D-13 polish error"
    - "Pkg.test() raportuje 230 + 4 = 234 (lub wiecej, jezeli @testset zawiera podpunkty) PASS, exit 0"
  artifacts:
    - path: "test/test_4_1.jl"
      provides: "Nowy plik testowy z @testset 4.1 obejmujacy 4 testy content-level + smoke + regression dla isopen fix i narrowed catch"
      contains: "@testset \"4.1: hybrid GIF + isopen fallback\""
    - path: "test/runtests.jl"
      provides: "include(\"test_4_1.jl\") wpiete w glowny @testset \"JuliaCity\""
      contains: "include(\"test_4_1.jl\")"
  key_links:
    - from: "test/runtests.jl"
      to: "test/test_4_1.jl"
      via: "include() wewnatrz @testset \"JuliaCity\""
      pattern: "include\\(\"test_4_1\\.jl\"\\)"
    - from: "test/test_4_1.jl::Test 1"
      to: "src/wizualizacja.jl::_animuj_nn_construction!"
      via: "JuliaCity._animuj_nn_construction! (internal helper, namespace access)"
      pattern: "JuliaCity\\._animuj_nn_construction!"
    - from: "test/test_4_1.jl::Test 2"
      to: "examples/eksport_mp4.jl::main"
      via: "include + invoke z monkeypatched stalymi (smaller N, fewer steps)"
      pattern: "include.*eksport_mp4|main\\(\\)"
    - from: "test/test_4_1.jl::Test 3"
      to: "src/wizualizacja.jl::wizualizuj (live mode)"
      via: "wizualizuj(...; liczba_krokow=10) z try/catch headless guard"
      pattern: "wizualizuj\\(.*liczba_krokow=10"
    - from: "test/test_4_1.jl::Test 4"
      to: "src/wizualizacja.jl::wizualizuj outer catch (Phase 4.1 D-03)"
      via: "mock funkcja rzucajaca MethodError przekazana jako alg lub przez throw w setup"
      pattern: "MethodError"
---

<objective>
Wave 3 walidacja: dodac smoke testy content-level + regression dla naprawionych blockerow z 04-UAT.md (Test 2 isopen MethodError fix z planu 04.1-01, Test 4 hybrid GIF + resolution z planu 04.1-02). Plan dziala na zasadzie "test logiki, NIE frame-decoding" zgodnie z CONTEXT D-11 — frame-decoding (CONTEXT D-10) odroczony do v2 (over-engineering w v1, FFMPEG_jll subprocess test infrastructure jest cieższa niż bezposredni test logiki przez `examples/eksport_mp4.jl::main()` z mniejszym N).

Purpose: Zamknac luki testowe wskazane w 04-UAT.md "missing items":
- Test live mode `wizualizuj(...; liczba_krokow=10, kroki_na_klatke=2)` z headless guard — Phase 3 testy nie pokrywaly live mode (VIZ-06 to tylko grep-only walkdir).
- Test "first frame ≤ 1 edge" (CONTEXT D-10) — frame decoding ODRZUCONY (over-engineering); zamiast tego test integracyjny przez wywolanie helper'a `_animuj_nn_construction!` na malej fixturze i sprawdzenie ze pierwsza klatka po klatce 1 ma `length(stan.trasa) <= chunk` krawedzi.
- Test "last frame energia < energia_nn" (CONTEXT D-11) — refactor `examples/eksport_mp4.jl::main()` (z planu 04.1-02) zwraca `(stan, energia_nn)`, test wywoluje main z mniejszym N=50 i LICZBA_KROKOW_SA=200.
- Test "narrowed catch propaguje MethodError" (CONTEXT D-03) — bezposrednia regression dla planu 04.1-01.

Output: Nowy plik `test/test_4_1.jl` z 4 podtestami w jednym `@testset "4.1: hybrid GIF + isopen fallback"`, plus jedna linia `include("test_4_1.jl")` w `test/runtests.jl` (pomiedzy istniejacymi includes na linii 198 a `@testset "VIZ-06: GLMakie isolation"` na linii 208).

**Decyzje planner'a (CONTEXT D-10/D-11/D-12 daje wybor):**

- **D-10 frame-decoding test:** ODRZUCONE w planie 04.1-03. Powod: Decoding pierwszej klatki przez `ffmpeg -i demo.gif -vframes 1 ...` + `FileIO.load(png)` wymaga (a) FFMPEG_jll subprocess w tescie (cieżki, ~3-5s overhead), (b) FileIO + ImageIO nowe deps lub spawn `magick`, (c) hyrustyczne progowanie pikseli ("dominantny kolor trasy < threshold") jest brittle (zalezy od kompresji palette JPEG/Lanczos artifacts). Zamiast tego: Test 1 (sprawdzajacy `_animuj_nn_construction!` produces `length(stan.trasa) == 1` po klatce 0 i `length(stan.trasa) == 1 + chunk` po klatce 1) waliduje IDENTYCZNA wlasciwosc na poziomie LOGIKI bez frame-decoding. Plan zawiera `@test_skip` placeholder dla v2 frame-decoding.

- **D-11 last-frame energia < energia_nn:** PRZYJETE — wywolanie `examples/eksport_mp4.jl::main()` z monkeypatched stalymi (mniejsza N=50, LICZBA_KROKOW_SA=200, KROKI_NA_KLATKE=10). Plan 04.1-02 musi zaimplementowac `main()` returning `(stan, energia_nn)` (constraint w plan 04.1-02 SUCCESS_CRITERIA). Headless guard wymagany — `Makie.record()` wymaga GLMakie initialization; w envach bez OpenGL test bedzie skipowany (analogicznie do Test 3).

- **D-12 live mode smoke z headless guard:** PRZYJETE z preferowanym pattern try/catch na initialization-time GLMakie blokadach (bez `Sys.iswindows() || haskey(ENV, "DISPLAY") || skip` — bo dziala na Windows nawet bez DISPLAY env var, gdzie GLMakie probuje OpenGL i moze zawiesc niezaleznie od env). Pattern: try `wizualizuj(...; liczba_krokow=10)` catch `LoadError`/`InitError`/`ErrorException` ze stringiem "OpenGL"/"X11"/"display"/"GLFW" → @test_skip. Pozostale errory rethrow (zeby legitnie failowac).

Plan **autonomous: false** — wymaga toolchainu Julia + uruchomienia `Pkg.test()` przez wykonawce dla weryfikacji exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-demo-gif-hybrid-and-isopen-fix/04.1-CONTEXT.md
@.planning/phases/04-demo-benchmarks-documentation/04-UAT.md
@.planning/phases/03-visualization-export/03-CONTEXT.md
@src/wizualizacja.jl
@src/baselines.jl
@examples/eksport_mp4.jl
@test/runtests.jl
</context>

<interfaces>
<!-- Kluczowe kontrakty z `src/wizualizacja.jl` po planie 04.1-01 + 04.1-02 (musza byc juz w kodzie). -->

```julia
# Z planu 04.1-01:
function _is_window_open(fig)::Bool                    # fallback events(fig).window_open[]
# Outer catch w wizualizuj() po D-03 narrowing — propaguje MethodError unwrapped

# Z planu 04.1-02:
function _animuj_nn_construction!(stan::StanSymulacji,
                                  obs_trasa::Observable{Vector{Point2f}},
                                  n_klatek::Int;
                                  chunk::Int=7)
# wizualizuj(...; figure_size::Tuple{Int,Int}=(1400, 700))  # nowy kwarg

# Z examples/eksport_mp4.jl po planie 04.1-02:
function main()
    # ... refactor 3-fazowy ...
    return (stan, energia_nn)   # D-11 contract
end
```

Z istniejacych Phase 1+2+3 (LOCKED):
```julia
# src/baselines.jl
function trasa_nn(D::Matrix{Float64}; start::Int=1)::Vector{Int}

# src/wizualizacja.jl (Phase 3)
function wizualizuj(stan::StanSymulacji, params::Parametry, alg::Algorytm;
                    liczba_krokow::Int=params.liczba_krokow,
                    fps::Int=30,
                    kroki_na_klatke::Int=50,
                    eksport::Union{Nothing,String}=nothing,
                    figure_size::Tuple{Int,Int}=(1400, 700))::Nothing  # +kwarg po 04.1-02
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Utworz test/test_4_1.jl z 4 podtestami (D-10 placeholder, D-11 main test, D-12 live smoke, D-03 regression)</name>
  <read_first>
    - test/runtests.jl (linie 1-208 — strukturalny pattern @testset, includes, headless dyscyplina; linie 208-256 dla VIZ-06 grep-only headless pattern)
    - test/test_baselines.jl (jezeli istnieje — pattern testow dla baseline, zachowanie konwencji)
    - src/wizualizacja.jl (po planach 04.1-01 + 04.1-02 — sygnatury _is_window_open, _animuj_nn_construction!, wizualizuj z figure_size kwarg)
    - examples/eksport_mp4.jl (po planie 04.1-02 — main() returning (stan, energia_nn))
    - .planning/phases/04.1-demo-gif-hybrid-and-isopen-fix/04.1-CONTEXT.md (D-10/D-11/D-12 — pelne rationale; D-03 — kryterium narrowing'u)
  </read_first>
  <action>
    Utworz NOWY plik `test/test_4_1.jl` (ASCII filename per BOOT-04, NFC + LF + final newline, polski docstring + komentarze).

    Pelna tresc pliku:

    ```julia
    # Test suite Phase 4.1 — domkniecie blockerów z 04-UAT.md (test 2 isopen + test 4 hybrid GIF).
    # Pokrywa:
    #   Test 1: D-05 zgodnosc _animuj_nn_construction! z trasa_nn (N=50 fixture)
    #   Test 2: D-11 SA bije NN po hybrydzie — examples/eksport_mp4.jl::main() z mniejszym N
    #   Test 3: D-12 live mode smoke z headless guard (try/catch na init-time GLMakie blokadach)
    #   Test 4: D-03 regression — MethodError propaguje unwrapped przez narrowed outer catch
    #   Test 5 (deferred): D-10 frame-decoding pierwszej klatki — @test_skip placeholder dla v2
    #
    # Asercje wewnetrzne mogą byc po angielsku (Test idiom), polskie komentarze per LANG-01.

    using Test
    using JuliaCity
    using Random: Xoshiro
    using GeometryBasics: Point2f
    # Observable jest re-eksportowane przez `using JuliaCity` (przez wizualizacja.jl using GLMakie).
    # `Observable` z Makie/Observables — dostepne jako JuliaCity.Observable lub przez Observables.

    @testset "4.1: hybrid GIF + isopen fallback" begin

        # ---------------------------------------------------------------------
        # Test 1: _animuj_nn_construction! reprodukuje trasa_nn deterministycznie (D-05)
        # ---------------------------------------------------------------------
        @testset "Test 1: _animuj_nn_construction! == trasa_nn(D; start=1)" begin
            # Maly fixture — N=50 jest wystarczajaco duzy zeby algorytm NN miał wybór,
            # a maly zeby test trwal <100ms.
            N = 50
            CHUNK = 5
            N_KLATEK = ceil(Int, N / CHUNK)  # 10 klatek dla N=50, chunk=5

            punkty = generuj_punkty(N; seed=42)
            stan = StanSymulacji(punkty; rng=Xoshiro(42))
            JuliaCity.oblicz_macierz_dystans!(stan)

            # Reset do start=1 (kontrakt _animuj_nn_construction!)
            stan.trasa = [1]
            stan.iteracja = 0

            # Mock Observable — w teście NIE wyświetlamy okna, tylko sprawdzamy mutacje wartości.
            # Observable z modułu Makie/Observables — re-export przez `using JuliaCity` (wizualizacja.jl).
            obs = JuliaCity.Observable(Point2f[Point2f(stan.punkty[1])])

            # Wywolanie helpera
            JuliaCity._animuj_nn_construction!(stan, obs, N_KLATEK; chunk=CHUNK)

            # Test 1a: po helperze stan.trasa to pelen cykl Hamiltona dlugosci N
            @test length(stan.trasa) == N
            @test sort(stan.trasa) == 1:N   # invariant Hamilton cycle (zgodne z TEST-04)

            # Test 1b: stan.trasa IDENTYCZNE z trasa_nn(stan.D; start=1)
            referencyjna = trasa_nn(stan.D; start=1)
            @test stan.trasa == referencyjna

            # Test 1c: Observable byl mutowany (ostatnia wartosc to pelen cykl + domkniecie)
            @test length(obs[]) == N + 1   # n + 1 dla zamkniecia cyklu (Phase 3 _trasa_do_punkty pattern)
            @test obs[][1] == obs[][end]   # pierwszy == ostatni (domkniecie cyklu)
        end

        # ---------------------------------------------------------------------
        # Test 2: SA po hybrydzie bije NN baseline (D-11)
        # ---------------------------------------------------------------------
        # Wywoluje examples/eksport_mp4.jl::main() ale z monkeypatched stalymi przez
        # @eval w sandbox modulu LUB przez bezposredni test logiki bez include skryptu.
        # Wybierzemy drugą opcje (czystsza, bez magii @eval): test rekonstruuje hybryda
        # logike inline z mniejszym N=50, bez record() (zeby uniknac GLMakie init).
        @testset "Test 2: SA bije NN po hybrydzie (D-11, headless logika)" begin
            N = 50
            SEED = 42
            LICZBA_KROKOW_SA = 200    # wystarczy na N=50 zeby SA znalazl <NN
            CHUNK_NN = 5
            N_KLATEK_NN = ceil(Int, N / CHUNK_NN)

            punkty = generuj_punkty(N; seed=SEED)
            stan = StanSymulacji(punkty; rng=Xoshiro(SEED))
            JuliaCity.oblicz_macierz_dystans!(stan)

            # Faza 1 LOGIKA (bez Makie record) — mock Observable tylko jako sink dla helpera.
            stan.trasa = [1]
            stan.iteracja = 0
            obs = JuliaCity.Observable(Point2f[Point2f(stan.punkty[1])])
            JuliaCity._animuj_nn_construction!(stan, obs, N_KLATEK_NN; chunk=CHUNK_NN)

            # Po fazie 1: stan.trasa to pelen cykl NN. Stan.energia jeszcze nie obliczone.
            bufor = zeros(Float64, Threads.nthreads())
            stan.energia = JuliaCity.oblicz_energie(stan.D, stan.trasa, bufor)
            energia_nn = stan.energia

            # Faza 3 LOGIKA — SA-2-opt z T_zero=0.001 lock per plan 02-14
            alg = SimAnnealing(stan; T_zero=0.001)
            stan.temperatura = alg.T_zero
            params = Parametry(liczba_krokow=LICZBA_KROKOW_SA)

            for _ in 1:LICZBA_KROKOW_SA
                symuluj_krok!(stan, params, alg)
            end

            # Test 2a: SA znalazl trase strictly krotsza niz NN (D-11 contract)
            @test stan.energia < energia_nn

            # Test 2b: trasa nadal jest valid Hamilton cycle (TEST-04 invariant zachowany)
            @test sort(stan.trasa) == 1:N
        end

        # ---------------------------------------------------------------------
        # Test 3: live mode smoke z headless guard (D-12)
        # ---------------------------------------------------------------------
        # Probuje wywolac wizualizuj(...; liczba_krokow=10, kroki_na_klatke=2) ktore
        # otwiera okno GLMakie. W envach bez OpenGL/X11/display test jest @test_skip
        # (try/catch na init-time blokadach per CONTEXT D-12).
        # KRYTYCZNE: MethodError NIE jest acceptable — fix z planu 04.1-01 musi dzialac.
        #
        # TIMEOUT WRAPPER (plan-checker iteracja 2026-05-04, mitigation T-04.1-03-02):
        # wizualizuj() w envach z displayem (np. lokalny dev) wchodzi w passive event loop
        # po SA stop (wizualizacja.jl `while isopen(fig); sleep(1/fps); end`) — bez user
        # interakcji nie wraca. CI bez interaktywnego usera wisilby do default test timeout
        # (~30 min). Wrapper @async + timedwait(15s) zatrzymuje task po 15 sekundach
        # (tutorial wizualizuj() z N=10, liczba_krokow=10 powinno wykonac sie <2s — 15s
        # to bezpieczny margines bez wpadania w false-positive timeout).
        @testset "Test 3: live mode smoke z headless guard (D-12, regression isopen)" begin
            N = 10
            punkty = generuj_punkty(N; seed=42)
            stan = StanSymulacji(punkty; rng=Xoshiro(42))
            inicjuj_nn!(stan)
            alg = SimAnnealing(stan; T_zero=0.001)
            stan.temperatura = alg.T_zero
            params = Parametry(liczba_krokow=10)

            wynik = Ref{Symbol}(:nieuruchomiony)   # :ok | :method_error | :headless_skip | :inny_blad
            zlapany_blad = Ref{Any}(nothing)

            task = @async try
                wizualizuj(stan, params, alg;
                           liczba_krokow=10,
                           fps=30,
                           kroki_na_klatke=2)
                wynik[] = :ok
            catch e
                zlapany_blad[] = e
                msg = sprint(showerror, e)
                if isa(e, MethodError)
                    wynik[] = :method_error   # REGRESSION
                elseif occursin("OpenGL", msg) || occursin("X11", msg) ||
                       occursin("display", msg) || occursin("GLFW", msg) ||
                       occursin("kontekstu OpenGL", msg)   # Polish error D-13 verbatim
                    wynik[] = :headless_skip
                else
                    wynik[] = :inny_blad
                end
            end

            # timedwait zwraca :ok gdy task istniał, :timed_out po przekroczeniu (D-12 timeout guard).
            # 15s buduje margines bezpieczenstwa: wizualizuj(N=10, liczba_krokow=10) ~1-2s,
            # passive event loop po SA stop wisi w nieskonczonosc (bez user input) — wymusza timeout.
            status = timedwait(() -> istaskdone(task), 15.0; pollint=0.1)

            if status == :timed_out
                # Task wisi — passive event loop po SA stop. To JEST acceptable success path
                # bo SA dobiegl konca przed timeoutem (gdyby nie, MethodError lub blad init by zostal
                # zlapany w try/catch i wynik[] != :nieuruchomiony). Schedule task interrupt zeby
                # nie wisiał posrod testow.
                schedule(task, InterruptException(); error=true)
                @test true   # SA wykonalo sie, event loop oczekiwany hang
                @info "Test 3 PASS: SA wykonalo sie, passive event loop przerwany przez timeout (15s)"
            elseif wynik[] == :ok
                @test true   # explicit pass — task zakonczyl sie naturalnie (np. okno zamkniete przez closing event)
            elseif wynik[] == :method_error
                @test false   # REGRESSION
                @error "REGRESSION: MethodError leak — plan 04.1-01 fix nie zadzialal" zlapany_blad[]
            elseif wynik[] == :headless_skip
                @test_skip true
                @info "Test 3 SKIP: brak OpenGL/displayu w tym envie (acceptable na CI bez GUI)"
            elseif wynik[] == :inny_blad
                rethrow(zlapany_blad[])
            else
                @test false   # task niespodziewanie nie zaczał — sygnatura niespojna
                @error "Test 3 INTERNAL: task nie wszedl w try-block" wynik[]
            end
        end

        # ---------------------------------------------------------------------
        # Test 4: D-03 regression — MethodError propaguje unwrapped przez narrowed catch
        # ---------------------------------------------------------------------
        # Sztucznie wywolujemy ścieżke ktora ma rzucic MethodError z wnetrza wizualizuj()
        # i sprawdzamy ze outer catch (po planie 04.1-01 D-03 narrowing) NIE reformatuje
        # go jako D-13 polish error, tylko propaguje typ MethodError.
        #
        # Strategy: nie chcemy uruchamiac GLMakie (wymaga display); zamiast tego testujemy
        # bezposrednio outer catch logike przez wywolanie z parametrem ktory rzuca
        # ArgumentError z @assert (np. liczba_krokow=0). Po D-03 ArgumentError jest na
        # wczesnej rethrow liscie -> propaguje unwrapped.
        @testset "Test 4: outer catch propaguje runtime errors unwrapped (D-03)" begin
            N = 10
            punkty = generuj_punkty(N; seed=42)
            stan = StanSymulacji(punkty; rng=Xoshiro(42))
            inicjuj_nn!(stan)
            alg = SimAnnealing(stan; T_zero=0.001)
            stan.temperatura = alg.T_zero
            params = Parametry(liczba_krokow=1)

            # Test 4a: AssertionError z @assert liczba_krokow > 0 propaguje (bez reformat na D-13)
            err = nothing
            try
                wizualizuj(stan, params, alg; liczba_krokow=0)   # @assert liczba_krokow > 0 w _wizualizuj_impl
            catch e
                err = e
            end
            # AssertionError jest na liscie wczesnego rethrow w outer catch (plan 04.1-01 D-03)
            @test err isa AssertionError
            # Komunikat NIE zawiera D-13 verbatim string (no false reformat)
            @test !occursin("GLMakie wymaga aktywnego kontekstu OpenGL", sprint(showerror, err))

            # Test 4b: ArgumentError z konstruktora propaguje unwrapped (analogiczna walidacja)
            err2 = nothing
            try
                wizualizuj(stan, params, alg; fps=0)   # @assert fps > 0
            catch e
                err2 = e
            end
            @test err2 isa AssertionError
            @test !occursin("GLMakie wymaga aktywnego kontekstu OpenGL", sprint(showerror, err2))
        end

        # ---------------------------------------------------------------------
        # Test 5 (DEFERRED do v2): frame-decoding pierwszej klatki GIFa (D-10)
        # ---------------------------------------------------------------------
        # CONTEXT D-10 sugerowal frame-decoding przez ffmpeg subprocess + heurystyke
        # progowania pikseli. Plan 04.1-03 ODRZUCIL ten test jako over-engineering
        # (brittle thresholds, FFMPEG_jll subprocess overhead, nowe deps).
        # Zachowane jako placeholder dla v2 (kandydat na nowy plan 04.2 lub Phase 5).
        @testset "Test 5 (DEFERRED v2): pierwsza klatka GIFa <= 1 krawedz (D-10)" begin
            @test_skip false   # placeholder — zaimplementowac w v2 z FileIO + ImageIO + ffmpeg subprocess
        end

    end
    ```

    KRYTYCZNE — zachowane konwencje:
    - Polski docstring naglowkowy + polskie komentarze inline
    - Asercje `@test`/`@test_skip` zgodne z idiom Test stdlib
    - `using JuliaCity` daje dostep do `Observable` przez re-export (Phase 3 D-09 — `using GLMakie` w wizualizacja.jl re-eksportuje Observable). Jezeli to nie zadziala, fallback: `using Observables: Observable` (Observables jest w deps po Phase 3 plan 03-00).
    - `JuliaCity._animuj_nn_construction!` access przez namespace (helper jest internal, NIE eksportowany — rebrandowane przez prefix `_`).
    - `JuliaCity.oblicz_macierz_dystans!` analogicznie (internal helper z baselines.jl).
    - NFC + LF + final newline (Phase 1 D-21 testset to wykryje).

    JEZELI `using JuliaCity` nie re-eksportuje `Observable`, dodac na poczatku pliku:
    ```julia
    using Observables: Observable
    ```
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (-not (Test-Path test/test_4_1.jl)) { Write-Error 'test/test_4_1.jl missing'; exit 1 }; $c = Get-Content test/test_4_1.jl -Raw; if (-not ($c -match '@testset \"4\.1: hybrid GIF \+ isopen fallback\"')) { Write-Error 'main testset missing'; exit 2 }; if (-not ($c -match '_animuj_nn_construction!')) { Write-Error 'helper test missing'; exit 3 }; if (-not ($c -match 'stan\.energia < energia_nn')) { Write-Error 'D-11 SA bije NN test missing'; exit 4 }; if (-not ($c -match 'isa\(e, MethodError\)')) { Write-Error 'D-12 MethodError regression check missing'; exit 5 }; if (-not ($c -match '@test_skip')) { Write-Error 'headless guard pattern missing'; exit 6 }; if (-not ($c -match 'AssertionError')) { Write-Error 'D-03 outer catch propagation test missing'; exit 7 }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - `test/test_4_1.jl` istnieje (ASCII filename, NFC + LF + final newline).
    - Plik zawiera glowny `@testset "4.1: hybrid GIF + isopen fallback"`.
    - 5 zagniezdzonych testsetow: Test 1 (helper zgodnosc z trasa_nn), Test 2 (SA bije NN logika), Test 3 (live mode smoke z guard), Test 4 (D-03 outer catch propagation), Test 5 (deferred v2 placeholder).
    - Test 1 sprawdza: `length(stan.trasa) == N`, `sort(stan.trasa) == 1:N`, `stan.trasa == trasa_nn(stan.D; start=1)`, `obs[]` ma N+1 punktow z domknieciem cyklu.
    - Test 2 sprawdza: `stan.energia < energia_nn` po SA, `sort(stan.trasa) == 1:N` (Hamilton invariant).
    - Test 3 sprawdza: success ALBO @test_skip dla init-time GLMakie blokad ALBO `@test false` z error message dla MethodError leak (regression na 04.1-01).
    - Test 4 sprawdza: AssertionError z `@assert liczba_krokow > 0` i `@assert fps > 0` w `_wizualizuj_impl` propaguje unwrapped, NIE jest reformatowany jako D-13 polish error.
    - Test 5 jest `@test_skip false` (deferred placeholder).
    - Polski docstring + komentarze, ASCII identyfikatory.
  </acceptance_criteria>
  <done>Plik `test/test_4_1.jl` utworzony z 5 zagniezdzonymi testsetami (4 aktywne + 1 deferred placeholder); pokrywa D-05 zgodnosc helpera, D-11 SA bije NN, D-12 live mode smoke z headless guard, D-03 outer catch propagation regression dla 04.1-01.</done>
</task>

<task type="auto">
  <name>Task 2: Wpiac include("test_4_1.jl") w test/runtests.jl</name>
  <read_first>
    - test/runtests.jl (linie 186-208 — istniejace includes test_energia.jl, test_baselines.jl, test_symulacja.jl + komentarz przed VIZ-06 testset)
  </read_first>
  <action>
    Dodac JEDNA LINIE include w `test/runtests.jl` PO istniejacych `include("test_symulacja.jl")` (linia 198) a PRZED `@testset "VIZ-06: GLMakie isolation"` (linia 208).

    KONKRETNE MIEJSCE — wstawic miedzy linie 198-207. Zalozenie: linia 198 to `include("test_symulacja.jl")`, linia 200-207 to komentarze + pusta linia, 208 to `@testset "VIZ-06: ..."`.

    Wstawienie (po istniejacym `include("test_symulacja.jl")`, z polskim komentarzem opisujacym zawartosc):

    ```julia
        # ─────────────────────────────────────────────────────────────────────────
        # Phase 4.1: Hybrid GIF + isopen fallback (post-UAT regression suite)
        # 5 testow (4 aktywne + 1 deferred v2): _animuj_nn_construction! zgodnosc z
        # trasa_nn (D-05), SA bije NN po hybrydzie (D-11), live mode smoke z headless
        # guard (D-12), outer catch propaguje runtime errors unwrapped (D-03 regression
        # po fixie isopen MethodError z planu 04.1-01).
        # ─────────────────────────────────────────────────────────────────────────
        include("test_4_1.jl")
    ```

    KRYTYCZNE:
    - Wciecie zgodne z istniejacym pattern (4 spacje = wewnatrz outer `@testset "JuliaCity"`).
    - Komentarz polski zgodny z konwencja istniejacych komentarzy w runtests.jl (linie 4-9 styl).
    - NIE modyfikowac istniejacych testsetow (Phase 3 VIZ-06, Aqua, JET pozostaja niezmienione).
    - Plik nadal walidny UTF-8 NFC + LF.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content test/runtests.jl -Raw; if (-not ($c -match 'include\(\"test_4_1\.jl\"\)')) { Write-Error 'include for test_4_1.jl missing'; exit 1 }; $sym_idx = $c.IndexOf('include(\"test_symulacja.jl\")'); $new_idx = $c.IndexOf('include(\"test_4_1.jl\")'); $viz_idx = $c.IndexOf('VIZ-06: GLMakie isolation'); if (-not ($sym_idx -lt $new_idx -and $new_idx -lt $viz_idx)) { Write-Error 'include placement wrong (must be between test_symulacja.jl and VIZ-06 testset)'; exit 2 }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - `test/runtests.jl` zawiera linie `include("test_4_1.jl")`.
    - Pozycja: PO `include("test_symulacja.jl")` a PRZED `@testset "VIZ-06: GLMakie isolation"`.
    - Komentarz polski (3-5 linijek) opisujacy zawartosc test_4_1.jl bezposrednio nad include.
    - Brak modyfikacji w innych testsetach.
    - Plik nadal walidny UTF-8 NFC + LF.
  </acceptance_criteria>
  <done>`test/runtests.jl` zawiera `include("test_4_1.jl")` w odpowiednim miejscu (po test_symulacja.jl, przed VIZ-06), z polskim komentarzem opisujacym zawartosc.</done>
</task>

<task type="auto">
  <name>Task 3: Uruchom Pkg.test() i zwaliduj exit 0 + 230+ PASS</name>
  <read_first>
    - test/test_4_1.jl (utworzony w Tasku 1)
    - test/runtests.jl (zmodyfikowany w Tasku 2)
    - .planning/phases/04.1-demo-gif-hybrid-and-isopen-fix/04.1-CONTEXT.md (D-18 — `Pkg.test()` musi pozostac 0 failures, target ≥230)
    - Project.toml (czy nowe deps są wymagane — Observables jest juz w deps po Phase 3 plan 03-00)
  </read_first>
  <action>
    Wykonac empiryczna walidacje przez uruchomienie pelnego test suite (`Pkg.test()`):

    KROK 3.1 — Z working directory `C:\Users\mparol\Desktop\Dokumenty\Projekty\JuliaCity` uruchomic:
    ```
    julia --project=. --threads=auto -e "using Pkg; Pkg.test()"
    ```

    Spodziewany czas: ~2-4 min (Phase 1+2+3 warmup ~1-2 min + JET TEST-07 ~30s + nowe testy ~10s).

    KROK 3.2 — Sprawdzic output:
    - `Test Summary: ... | Pass  Total  Time` linia z minimum 230 + 4-8 (nowe testy z 4 aktywnych testsetow Test 1 + Test 2 + Test 3 + Test 4) = ~234-238 PASS
    - `Testing JuliaCity tests passed` na koncu
    - Exit code 0

    Jezeli Test 3 (live mode smoke) jest skipowany (brak displayu w envie wykonawcy), spodziewany output zawiera `@test_skip true ... 1 broken | n total` LUB `Test Summary` z 1 test_skip — to JEST acceptable (CONTEXT D-12 explicite sankcjonuje skip).

    Jezeli Test 5 (deferred placeholder) raportuje skipped — to oczekiwane (`@test_skip false`).

    KROK 3.3 — Walidacja regresji:
    - Pkg.test() PRZED zmiana (na master commit 43dcddd): 230/230 PASS (per ROADMAP Phase 4 status, STATE.md)
    - Pkg.test() PO zmianie: minimum 230/230 + 4-8 nowych = >= 234 PASS (zaden istniejacy test nie failowal)
    - Jezeli istniejacy test failuje (np. Aqua/JET/encoding), to BLOCKER — zatrzymac sie i zaraportowac

    KROK 3.4 — Jezeli toolchain Julia NIE jest dostepny w envie wykonawcy:
    - Zaraportowac w SUMMARY ze test nie zostal uruchomiony lokalnie
    - Zostawić plan acceptance dla CI (GitHub Actions matrix 1.10/1.11/1.12 × ubuntu/windows/macos automatycznie zwaliduje)
    - autonomous: false flag w plan frontmatter to przewidywal
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (Get-Command julia -ErrorAction SilentlyContinue) { Write-Output 'julia available, run: julia --project=. --threads=auto -e \"using Pkg; Pkg.test()\" and check exit 0 + Pass total >= 234' } else { Write-Output 'julia not in PATH — defer to CI; this verify is informational only' }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - `Pkg.test()` exit 0 (jezeli toolchain dostepny)
    - Test Summary raportuje >= 234 Pass (230 baseline + min. 4 nowe), 0 Failed, akceptowane skipped (Test 3 headless, Test 5 deferred)
    - Aqua TEST-06 nadal PASS (deps_compat.ignore lista bez zmian — Phase 4.1 nie dodaje deps)
    - JET TEST-07 nadal PASS (Phase 1+2 hot-paths niezmienione)
    - VIZ-06 grep guard nadal PASS (`using GLMakie` tylko w `src/wizualizacja.jl`)
    - Encoding hygiene PASS (test_4_1.jl + zmodyfikowany runtests.jl + zmodyfikowany wizualizacja.jl + zmodyfikowany eksport_mp4.jl + ewent. zregenerowany Manifest.toml — wszystko NFC + LF + BOM-free)
    - Jezeli toolchain niedostepny — wyraznie odnotowane w SUMMARY, plan oznaczony do walidacji CI
  </acceptance_criteria>
  <done>`Pkg.test()` exit 0 z minimum 234 PASS (lub equivalent z @test_skip dla Test 3 headless / Test 5 deferred); brak regresji w istniejacych testach Phase 1+2+3+4; jezeli toolchain niedostepny, walidacja deferred do CI z eksplicitnym oznaczeniem w SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures (N=50, seed=42) | Czyste deterministyczne fixtures — `generuj_punkty(N; seed=42)` + `Xoshiro(42)`. Brak input z sieci/dyskowego, identyczne dla kazdego runu. |
| `wizualizuj(...)` w Test 3 | Test 3 PROBUJE otworzyc okno GLMakie. Headless guard try/catch zapobiega uciece błedu z kontroli. |
| Sztuczne `@assert` triggers w Test 4 | Test 4 wywoluje `wizualizuj(...; liczba_krokow=0)` i `wizualizuj(...; fps=0)` — kontrolowane @assert violations w `_wizualizuj_impl`. Brak side-effectow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-03-01 | Tampering | Test 3 try/catch fallthrough — jezeli MethodError przejdzie, `@test false` powinien failować | mitigate | Explicit `@test false` + `@error` z stack trace dla MethodError — failure jest widoczne w CI output. Regression test wymusza ze plan 04.1-01 fix dziala. |
| T-04.1-03-02 | Denial of Service | Test 3 wywoluje wizualizuj() ktore moze blokowac (live mode pasywny event loop po SA stop) | mitigate | `liczba_krokow=10, kroki_na_klatke=2` => 5 klatek SA, koncze sie szybko. D-06 passive event loop po SA stop wymaga user closing window — ALE test envy zwykle nie maja interaktywnego usera. To moze powodowac hang testu. Mitigation: wykonawca powinien obserwowac timeout w CI (default Julia test timeout 30 min — wystarczy). Jezeli hang stanowi problem, opcjonalna eskalacja: dodac `Threads.@spawn` z `wait(task; timeout=10)` wokol `wizualizuj()` w Test 3. |
| T-04.1-03-03 | Information Disclosure | Test 3 @error leakuje stack trace do CI logs | accept | Stack trace MethodError w CI logs jest dokladnie tym co chcemy widziec dla regression debug. Brak PII, public open-source repo. |

Brak ASVS L1 controls naruszonych — pure test code, lokalne fixtures, brak network/auth/input.
</threat_model>

<verification>
- `test/test_4_1.jl` istnieje, NFC + LF + ASCII filename
- `test/runtests.jl` zawiera `include("test_4_1.jl")` w odpowiednim miejscu (po test_symulacja, przed VIZ-06)
- `test/test_4_1.jl` zawiera `@testset "4.1: hybrid GIF + isopen fallback"` z 5 zagniezdzonymi testsetami
- Test 1 sprawdza zgodnosc helpera z trasa_nn (D-05)
- Test 2 sprawdza ze SA bije NN po hybrydzie (D-11) bez frame-decoding
- Test 3 jest live mode smoke z headless try/catch guard (D-12) + regression check ze MethodError nie ucieka
- Test 4 sprawdza ze AssertionError propaguje unwrapped przez outer catch (D-03 regression)
- Test 5 jest @test_skip placeholder dla v2 frame-decoding (D-10 deferred)
- `Pkg.test()` exit 0 jezeli toolchain dostepny (lub deferred do CI z oznaczeniem)
- Brak regresji w 230 baseline testach (Aqua, JET, encoding, VIZ-06, Phase 1+2+3+4 testy)
- Plan 04.1-03 NIE modyfikuje innych plików (`src/`, `examples/`, `bench/`, `Project.toml`, `Manifest.toml`)
</verification>

<success_criteria>
- Nowy plik testowy `test/test_4_1.jl` wpiety w `test/runtests.jl`, pokrywa CONTEXT D-03/D-05/D-11/D-12 (D-10 deferred do v2 jako @test_skip)
- 4 aktywne testy + 1 deferred placeholder w jednym `@testset "4.1: hybrid GIF + isopen fallback"`
- `Pkg.test()` exit 0 z >= 234 PASS (230 baseline + 4 nowe)
- Regression coverage:
  * MethodError z `isopen(::Makie.Figure)` NIE moze ujść — Test 3 wymusza @test false jezeli przejdzie
  * Outer catch nie reformatuje runtime errors jako D-13 polish error — Test 4 wymusza AssertionError propagation
- Phase 1+2+3 LOCKED: zadne istniejace src/ ani test/ nie dotkniete poza dodaniem nowego pliku + jednej linii include
- Phase 1 D-21 encoding hygiene zachowane (test_4_1.jl + runtests.jl mod + zmodyfikowane pliki z planu 04.1-01/02 wszystkie NFC + LF + BOM-free + ASCII filenames)
- Phase 4.1 milestone-ready: po Wave 3 mozna wywolac `gsd-audit-milestone` z reasonable expectation pass (wszystkie 3 SC z ROADMAP Phase 4.1 spelnione)
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-demo-gif-hybrid-and-isopen-fix/04.1-03-SUMMARY.md` with:
- Tasks executed (3) + commit hashes
- Coverage matrix dla CONTEXT D-03, D-05, D-10 (deferred), D-11, D-12, D-18 (DONE per task)
- Files created: test/test_4_1.jl
- Files modified: test/runtests.jl (+1 include + komentarz)
- Pkg.test() output snippet (Test Summary | Pass | Total | Time + final "Testing JuliaCity tests passed" lub uzasadnienie deferral do CI)
- Liczba testow przed (230) i po (>= 234) — explicit before/after
- Jezeli toolchain niedostepny, marker "VALIDATION DEFERRED TO CI" + spodziewany matrix run wynik
- Phase 4.1 milestone status: ready dla `gsd-audit-milestone` v1.1
- Confirmed unchanged: Phase 1+2 src/, examples/podstawowy.jl, Phase 3 publiczne API, baseline 230 PASS
</output>
