---
phase: 04.1-demo-gif-hybrid-and-isopen-fix
plan: 01
subsystem: visualization
tags: [bugfix, regression, makie-0.24, isopen-fallback, exception-narrowing, post-uat]
dependency_graph:
  requires:
    - "Phase 3 (wizualizacja.jl skeleton + outer try/catch + 3 isopen call-sites)"
    - "Phase 4 UAT (04-UAT.md Test 2 BLOCKER report)"
  provides:
    - "Stable live mode na Makie 0.24+ (examples/podstawowy.jl runnable)"
    - "Outer catch propagujący runtime errors unwrapped (real stack traces)"
    - "Helper _is_window_open(fig)::Bool dostępny dla planu 04.1-02 (jeśli hybrid GIF używałby live mode)"
  affects:
    - "src/wizualizacja.jl (single file, single source of GLMakie usage)"
tech_stack:
  added: []
  patterns:
    - "Defensywny try/catch w window-state probe (true on throw — preferuj continue nad false-stop)"
    - "Type-first exception triage (isa(e, MethodError) wczesniej niz string-match)"
    - "Narrowed init-time-only error reclassification (LoadError/InitError + (GLFW|OpenGL|X11|display) message)"
key_files:
  created: []
  modified:
    - src/wizualizacja.jl
decisions:
  - "D-01 implemented: fallback przez events(fig).window_open[] (Makie public API), nie GLMakie.to_native(...)"
  - "D-02 implemented: helper z try/catch zwracający true on throw (defensive — display() może nie być wywołane)"
  - "D-03 implemented: 5 runtime-exception-type early-rethrow guard (MethodError, BoundsError, ArgumentError, AssertionError, DomainError); init-time GLMakie blockades wymagają (LoadError|InitError) AND (GLFW|OpenGL|X11|display) substring; runtime non-MethodError z OpenGL message też nadal translated"
  - "Doc-mention cleanup: wszystkie wystąpienia 'isopen(fig)' w docstringach/komentarzach przerobione na '_is_window_open(fig)' aby raw substring grep zwracał 0 (plan strict verify)"
metrics:
  duration_minutes: ~25
  tasks_completed: 3
  completed_date: "2026-05-04"
---

# Phase 4.1 Plan 01: isopen Fallback Summary

`MethodError: no method matching isopen(::Makie.Figure)` (Makie 0.24+ deprecation) zlikwidowany w live mode `wizualizuj()` przez prywatny helper `_is_window_open(fig)::Bool` (events.window_open[] fallback) + 3 podmiany call-sites + zawężony outer try/catch który nie maskuje już runtime MethodError jako fałszywego "GLMakie display issue".

## Tasks Executed

| # | Task                                                                                                | Commit  | Files Touched         |
|---|-----------------------------------------------------------------------------------------------------|---------|------------------------|
| 1 | Add `_is_window_open(fig)::Bool` helper above `_live_loop` (D-02 defensive try/catch)              | 5033672 | src/wizualizacja.jl   |
| 2 | Swap 3 `isopen(fig)` call-sites to `_is_window_open(fig)` + cleanup doc-mentions (D-01)            | 0871b97 | src/wizualizacja.jl   |
| 3 | Narrow outer `wizualizuj()` catch — early rethrow runtime errors, narrow init-time matcher (D-03)  | c293b91 | src/wizualizacja.jl   |

## Coverage Matrix

| CONTEXT Decision | Status | Implemented in Task | Evidence (line in src/wizualizacja.jl) |
|------------------|--------|---------------------|-----------------------------------------|
| D-01 (3 call-sites → events(fig).window_open[] via helper) | DONE | Task 2 | line 224 (`_live_loop` while), line 397 (`if`), line 402 (passive while) |
| D-02 (helper `_is_window_open(fig)::Bool` z defensive try/catch true-on-throw) | DONE | Task 1 | line 178 (function definition) |
| D-03 (outer catch zawężony — MethodError unwrapped; init-time wymaga LoadError/InitError + GLFW/OpenGL/X11/display) | DONE | Task 3 | lines 460-466 (early rethrow guard), lines 472-478 (`init_time_glmakie` + `runtime_opengl` predicates) |

## Files Modified

- **`src/wizualizacja.jl`** — single file, +62 / -17 net (3 task commits combined). No new imports, no signature changes.

## Confirmed Unchanged (Phase 1+2+3 LOCKED)

- `src/punkty.jl`, `src/energia.jl`, `src/baselines.jl`, `src/algorytmy/*`, `src/typy.jl`, `src/JuliaCity.jl` — `git diff master~3..master --stat -- src/` confirms only `src/wizualizacja.jl` modified.
- Public API `wizualizuj(stan, params, alg; liczba_krokow, fps, kroki_na_klatke, eksport)::Nothing` — sygnatura wiersz po wierszu identyczna (Phase 3 D-09 LOCKED).
- D-13 verbatim Polish error string (`"GLMakie wymaga aktywnego kontekstu OpenGL. Brak displayu? Spróbuj \`xvfb-run -a julia ...\` na Linuksie albo uruchom lokalnie z GUI. Headless cloud (CI, Docker bez X) NIE jest wspierany w wersji v1."`) — present 1× w outer catch (Phase 3 LOCKED).
- `_setup_figure` Figure size — `Figure(size=(1400, 700))` UNCHANGED (resolution bump to 1920×960 jest scope planu 04.1-02, NIE 04.1-01).
- Encoding hygiene: NFC + LF + BOM-free + final newline (Phase 1 D-21 — guard test waliduje).

## Smoke Validation

Plan `<verification>` block executed:

```
julia --project=. --threads=auto -e 'using JuliaCity; using Random: Xoshiro;
  punkty=generuj_punkty(10;seed=42);
  stan=StanSymulacji(punkty;rng=Xoshiro(42));
  inicjuj_nn!(stan);
  alg=SimAnnealing(stan;T_zero=0.001);
  stan.temperatura=alg.T_zero;
  params=Parametry(liczba_krokow=5);
  try;
    wizualizuj(stan, params, alg; liczba_krokow=5, kroki_na_klatke=1);
  catch e;
    if isa(e, MethodError); println("STILL BROKEN"); exit(1);
    else; println("OK: no MethodError"); end;
  end'
```

**Output:**
```
[ Info: Ładowanie GLMakie (pierwsze uruchomienie może trwać 60+ s — kompilacja JIT)...
[ Info: Wizualizacja gotowa, rozpoczynam symulację...
```

`@info "Wizualizacja gotowa, rozpoczynam symulację..."` jest emitowany w `_wizualizuj_impl` PO `display(fig)` i PRZED `_live_loop` — to oznacza że (a) GLMakie się załadował, (b) okno się otworzyło, (c) `_live_loop` się rozpoczął a tym samym `_is_window_open(fig)` zwrócił `true` z `events(fig).window_open[]`. Pre-fix path crashował tu na `MethodError` (linia 202 ze starym `isopen(fig)`). Process zakończył się bez wyrzucenia jakiegokolwiek wyjątku (exit 0). **Smoke test PASSED.**

## Self-Check

| Acceptance Criterion (z planu) | Status |
|-------------------------------|--------|
| Helper `_is_window_open(fig)::Bool` zdefiniowany przed `_live_loop`, ASCII nazwa | PASS — line 178 < line 205 |
| Helper zawiera `try / events(fig).window_open[] / catch / return true / end` | PASS |
| Polish docstring z odwołaniem do D-01/D-02 + Phase 4.1 | PASS — diakrytyki "powiązane", "wyświetlony", "zainicjowany", "Defensywnie", "byłoby" |
| Dokładnie 3 call-sites `_is_window_open(fig)` w `while`/`if` (lines 224, 397, 402) | PASS |
| 0 wystąpień raw `isopen(fig)` (substring grep) | PASS |
| Outer catch zawiera `isa(e, MethodError) ... rethrow(e)` early guard | PASS |
| Outer catch zawiera lokalną zmienną `init_time_glmakie` | PASS — assignment + usage |
| Pattern `contains(msg, "GLMakie")` USUNIĘTY | PASS — 0 occurrences |
| D-13 verbatim polski error string ZACHOWANY (1 occurrence) | PASS |
| BoundsError, ArgumentError, AssertionError, DomainError również w early-rethrow guard | PASS |
| Encoding: NFC + LF + BOM-free + final newline | PASS — file size 26621 B, no CR, no BOM, final byte == 0x0A |
| Brak modyfikacji sygnatury `wizualizuj(...)` ani `_wizualizuj_impl(...)` (Phase 3 D-09 LOCKED) | PASS — sygnatury wiersz po wierszu identyczne |
| Brak modyfikacji w `src/{punkty,energia,baselines,algorytmy/,typy,JuliaCity}.jl` | PASS — `git diff` pokazuje tylko `src/wizualizacja.jl` |
| Smoke validation: brak MethodError leak | PASS — `@info "Wizualizacja gotowa..."` emitted, exit 0 |

**## Self-Check: PASSED** (14/14)

## Deviations from Plan

**1. [Rule 1 - Doc-Code consistency] Doc-mention cleanup w `_live_loop` docstring i 2 komentarzach**

- **Found during:** Task 2 (verify command for raw `isopen(fig)` substring grep — plan required count=0)
- **Issue:** Plan Task 2 verify expected `(Select-String -Pattern 'isopen\(fig\)' -AllMatches).Matches.Count == 0`. Po podmianie 3 call-sites, raw substring `isopen(fig)` nadal pojawiał się w 3 miejscach: docstring `_live_loop` (linie 195, 202 pre-fix) opisujący "petla `while isopen(fig)`" + komentarz w `_wizualizuj_impl` (linia 387 pre-fix) "isopen(fig) zwraca true dopiero po display". To były dokumentujące referencje do deprecated API.
- **Fix:** Zaktualizowano te 3 doc-mentions: docstring `_live_loop` referuje teraz `while _is_window_open(fig)` z notką "Phase 4.1 D-01 fallback po deprecacji `Base.isopen(::Makie.Figure)` w Makie 0.24+"; komentarz w `_wizualizuj_impl` zaktualizowany analogicznie. Doc-comments są teraz prawdziwe (opisują rzeczywiste zachowanie kodu). Boczny efekt: `_is_window_open(fig)` ma teraz 8 substring-mentions zamiast oczekiwanych przez plan 4 (1 def + 3 callsites + 4 doc-mentions). Plan's secondary "expected 4" count był heurystyką, primary verification (raw `isopen(fig)` == 0) była hard gate i to ją uznałem za autorytatywną.
- **Files modified:** `src/wizualizacja.jl` (lines 195, 202, 387 pre-fix)
- **Commit:** Folded into 0871b97 (Task 2)

## Threat Surface Scan

Brak nowych threat flags. Threat register z planu (`<threat_model>` T-04.1-01-01..03) pokryty:

- T-04.1-01-01 (Tampering): defensive `true` z `_is_window_open` na throw — accepted as designed (D-02), nie open vulnerability.
- T-04.1-01-02 (Information Disclosure): outer catch nie reformatuje już runtime errors (5-typowy early-rethrow guard) — mitigation implemented w Task 3.
- T-04.1-01-03 (DoS): pasywny event loop nadal `while _is_window_open(fig) sleep(1/fps) end` — semantyka identyczna do pre-fix (Phase 3 D-06 lock zachowany), helper defensive zwraca true on throw więc petla nie staje na transient errors.

Brak nowych network endpoints, auth paths, file-access patterns, schema changes — zmiany czysto interna (window-state probe + exception triage). Brak ASVS L1 controls naruszonych.

## Next-Wave Readiness

Wave 2 plan **04.1-02** (hybrid GIF + 1920×960 bump) — **ODBLOKOWANY**:

- Live mode działa stabilnie na Makie 0.24+ (smoke test PASS) → `record()` w eksporcie też skorzysta z czystego stanu Makie po `wizualizuj()` calls.
- Outer catch w `wizualizuj()` propaguje teraz runtime errors unwrapped → jeśli `record()` w 04.1-02 trafi na nieoczekiwany błąd (np. brakujący ffmpeg), uzytkownik zobaczy prawdziwy stack trace zamiast misleading "GLMakie display issue".
- `Figure(size=(1400, 700))` w `_setup_figure` (linia 93) **CELOWO ZACHOWANY** — bump do `(1920, 960)` jest scope planu 04.1-02 (D-07/D-08 — preferowane przez kwarg `figure_size` per CONTEXT D-08, fallback hardcoded bump z motywacją "regression fix" w SUMMARY).

**Suggested follow-up command:** `/gsd-execute-phase 4.1` Wave 2 — autonomous: false (wymaga lokalnego GLMakie + manualnej inspekcji wygenerowanego `assets/demo.gif`).
