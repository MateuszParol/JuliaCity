---
phase: 01-bootstrap-core-types-points
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - Project.toml
  - Manifest.toml
  - test/runtests.jl
autonomous: true
requirements: [BOOT-01, BOOT-02]
tags: [project-toml, manifest, deps, compat, extras, targets]
must_haves:
  truths:
    - "`Project.toml` istnieje, ma pola `name = \"JuliaCity\"`, `uuid = \"<UUID v4>\"`, `version = \"0.1.0\"`"
    - "`Project.toml [deps]` zawiera DOKŁADNIE: `GeometryBasics`, `Random` — żadnych innych runtime deps w Phase 1 (D-02: GeometryBasics jako bezpośredni dep)"
    - "`Project.toml [compat]` zawiera Wariant a (Project.toml [compat] zawiera julia, GLMakie, Makie, GeometryBasics, Observables, StableRNGs, Aqua, JET, BenchmarkTools) — literal compliance z ROADMAP SC2 (D-17)"
    - "`Project.toml [compat]` zawiera `julia = \"1.10\"`, `GLMakie = \"0.24\"`, `Makie = \"0.24\"` (literal text z ROADMAP Phase 1 SC2; D-17)"
    - "PkgTemplates użyty jednorazowo lokalnie (REPL) do scaffolding lub manual scaffold — `PkgTemplates` NIE jest w `[deps]` ani `[extras]` Project.toml (D-16)"
    - "`Project.toml [extras]` zawiera `Test`, `Aqua`, `JET`, `StableRNGs` (test-only deps)"
    - "`Project.toml [targets]` zawiera `test = [\"Test\", \"Aqua\", \"JET\", \"StableRNGs\"]`"
    - "`Manifest.toml` istnieje, jest commitowany (D-25), zawiera resolved versions wszystkich deps + extras"
    - "`Pkg.instantiate()` w czystym REPL kończy się bez błędu — wszystkie zależności da się zainstalować"
    - "`test/runtests.jl` istnieje jako stub `using Test; @testset \"JuliaCity\" begin; @test true; end` — pełna treść w plan 06"
  artifacts:
    - path: "Project.toml"
      provides: "Manifest pakietu Julia — name, uuid, version, [deps], [compat], [extras], [targets]"
      contains: "name = \"JuliaCity\""
      min_lines: 30
    - path: "Manifest.toml"
      provides: "Pinned dependency tree dla reproducibility (D-25)"
      contains: "GeometryBasics"
    - path: "test/runtests.jl"
      provides: "Stub test runner — będzie rozbudowany w plan 06"
      contains: "@testset"
  key_links:
    - from: "Project.toml [deps]"
      to: "src/JuliaCity.jl using statements"
      via: "Pkg dependency resolution + Julia import"
      pattern: "GeometryBasics"
    - from: "Project.toml [extras] + [targets]"
      to: "test/runtests.jl using statements"
      via: "Pkg test environment activation"
      pattern: "Aqua"
    - from: "Project.toml [compat]"
      to: "Pkg resolver"
      via: "version constraints"
      pattern: "julia = \"1.10\""
---

<objective>
Stworzyć `Project.toml` z runtime deps (`GeometryBasics`, `Random`), pełną sekcją `[compat]`
(Wariant a — literal ROADMAP SC2 compliance: julia, GLMakie, Makie, GeometryBasics, Observables,
StableRNGs, Aqua, JET, BenchmarkTools), test-only deps przez `[extras] + [targets]`, oraz committed
`Manifest.toml` (D-25). Stub `test/runtests.jl` żeby `Pkg.test()` nie rzucał błędu o braku entry pointa.

Purpose: BOOT-02 (Project.toml [compat] z julia=1.10), BOOT-01 (Manifest.toml). Wariant a z D-17
zapewnia dosłowną zgodność z literal text ROADMAP Phase 1 SC2 ("`Project.toml` zawiera sekcję
`[compat]` z `julia = \"1.10\"`, `GLMakie = \"0.24\"`, `Makie = \"0.24\"` plus pozostałymi
twardymi zależnościami z STACK.md"). Trade-off: Aqua zgłosi "stale [compat]" dla GLMakie/Makie/
Observables/BenchmarkTools — mitigowane w Plan 06 przez `Aqua.test_all(JuliaCity; stale_deps=false)`
z TODO Phase 4.

Plan 04 i 05 będą używać `Pkg.activate(".")` więc Project.toml musi być gotowy.

Output: Działający Pkg environment, `Pkg.instantiate()` clean, gotowy na pierwsze `using JuliaCity`
w plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-bootstrap-core-types-points/01-CONTEXT.md
@.planning/phases/01-bootstrap-core-types-points/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inicjalizacja Project.toml + dodanie runtime deps przez Pkg.add</name>
  <files>Project.toml, Manifest.toml</files>
  <read_first>
    - .planning/phases/01-bootstrap-core-types-points/01-CONTEXT.md (D-02: GeometryBasics ≥ 0.5; D-12: seed=42; D-25: Manifest commit)
    - .planning/phases/01-bootstrap-core-types-points/01-RESEARCH.md (Pattern 1: [compat] semantics; Pattern 2: [extras]+[targets]; Pitfall 1: bumpować [compat] PO Pkg.add; Pitfall 5: Manifest.toml in .gitignore)
    - .gitignore (potwierdź — Manifest.toml NIE jest tam, plan 02 task 2)
  </read_first>
  <action>
    Stwórz wstępny `Project.toml` ręcznie (NIE używamy PkgTemplates `Tests()` plugin'a do edycji
    rootowego Project.toml — A2 z RESEARCH.md zakłada, że plugin tego nie robi, plus PkgTemplates
    odsuwamy do Phase 2/3 jeśli kiedykolwiek). Wpisz dokładnie tę treść jako PIERWSZĄ wersję
    `Project.toml` (UUID wygeneruj nowy v4 — komenda `julia -e 'using UUIDs; println(uuid4())'`):

    ```toml
    name = "JuliaCity"
    uuid = "<UUID-V4-WYGENEROWANY-PRZEZ-julia-uuid4>"
    authors = ["Mateusz Parol <matimuzykant@gmail.com>"]
    version = "0.1.0"

    [deps]
    # Wypełnione w kolejnym kroku przez Pkg.add — UUIDs muszą być z rejestru
    ```

    KRYTYCZNE: NIE wpisuj sekcji `[compat]`, `[extras]`, `[targets]` jeszcze. Zgodnie z
    Pitfall 1: bumpujemy `[compat]` PO `Pkg.add` (resolver wpisze nam aktualne wersje do
    Manifestu, my je odczytamy i wpiszemy do `[compat]`).

    Następnie w terminalu (z poziomu repo root):

    ```bash
    # Wygeneruj UUID v4 i wpisz do Project.toml
    UUID=$(julia -e 'using UUIDs; println(uuid4())')
    sed -i.bak "s|<UUID-V4-WYGENEROWANY-PRZEZ-julia-uuid4>|$UUID|" Project.toml
    rm -f Project.toml.bak

    # Aktywuj environment i dodaj runtime deps
    julia --project=. -e '
        using Pkg
        Pkg.add("GeometryBasics")
        # Random jest stdlib — Pkg.add zarejestruje w Project.toml [deps]
        Pkg.add("Random")
        Pkg.status()
    '
    ```

    Po `Pkg.add`, `Project.toml [deps]` powinien zawierać:

    ```toml
    [deps]
    GeometryBasics = "5c1252a2-5f33-56bf-86c9-59e7332b4326"
    Random = "9a3f8284-a2c9-5f02-9a11-845980a1fd5c"
    ```

    Manifest.toml zostanie utworzony automatycznie i będzie zawierał resolved versions.

    KRYTYCZNE — sprawdź wynik `Pkg.status()`:
    - `GeometryBasics` powinien być `0.5.x` (jeśli rejestr ma starszą wersję, np. 0.4.x — research
      flagged GeometryBasics 0.5 jako compat target; jeśli rejestr Julia 1.10 ma tylko 0.4.x,
      ZGŁOŚ checkpoint zamiast obniżać version floor)
    - Wersje pod Julia 1.10 mogą się różnić od Julia 1.11/1.12 — to OK (Manifest pinuje
      konkretną wersję, [compat] określa range).
  </action>
  <verify>
    <automated>test -f Project.toml && test -f Manifest.toml && grep -q "name = \"JuliaCity\"" Project.toml && grep -q "uuid = " Project.toml && grep -q "GeometryBasics = " Project.toml && grep -q "Random = " Project.toml && julia --project=. -e 'using Pkg; Pkg.status()' 2>&1 | grep -q "GeometryBasics"</automated>
  </verify>
  <acceptance_criteria>
    - `Project.toml` istnieje
    - `Project.toml` zawiera: `name = "JuliaCity"`, `uuid = "..."` (UUID v4 format: 8-4-4-4-12 hex digits with dashes), `version = "0.1.0"`
    - `Project.toml [deps]` zawiera literal `GeometryBasics = "5c1252a2-5f33-56bf-86c9-59e7332b4326"` (UUID GeometryBasics)
    - `Project.toml [deps]` zawiera literal `Random = "9a3f8284-a2c9-5f02-9a11-845980a1fd5c"` (UUID Random stdlib)
    - `Manifest.toml` istnieje (utworzony przez `Pkg.add`)
    - `julia --project=. -e 'using Pkg; Pkg.status()'` exit code 0, wypisuje `GeometryBasics v0.5.x` (lub późniejsza, ale ≥ 0.5)
    - `git status` pokazuje `Project.toml` i `Manifest.toml` jako new files (NIE jako ignored)
  </acceptance_criteria>
  <done>
    - Project.toml + Manifest.toml utworzone, deps dodane przez Pkg.add (nie ręcznie)
    - GeometryBasics ≥ 0.5.0 i Random (stdlib) zarejestrowane
    - Pkg.status() działa bez błędu
  </done>
</task>

<task type="auto">
  <name>Task 2: Dodaj test deps przez Pkg.activate("test/Project.toml") → przenieś do `[extras]`+`[targets]`</name>
  <files>Project.toml, Manifest.toml</files>
  <read_first>
    - .planning/phases/01-bootstrap-core-types-points/01-RESEARCH.md (Pattern 2: `[extras]+[targets]`; Pitfall 9: UUID stamp; Open Question 1: PkgTemplates Tests() — zakładamy, że NIE)
    - .planning/phases/01-bootstrap-core-types-points/01-CONTEXT.md (D-17: Aqua, JET, StableRNGs w `[compat]`)
  </read_first>
  <action>
    Najczystszy sposób (per Pitfall 9 — żeby uniknąć ręcznych UUID-ów): użyj `Pkg.add` w
    test environmentcie, a potem ręcznie przenieś do `[extras]`+`[targets]` rootowego Project.toml.

    Krok 1 — zainstaluj test deps tymczasowo do globalnego cache i odczytaj UUID-y:

    ```bash
    julia --project=. -e '
        using Pkg
        # Aktywujemy temp env do rezolucji UUID-ów (NIE chcemy zaśmiecać [deps])
        Pkg.activate(temp=true)
        Pkg.add(["Test", "Aqua", "JET", "StableRNGs", "Unicode"])
        # Pkg automatycznie odczytuje UUID-y; wypisz je
        for pkg in ["Test", "Aqua", "JET", "StableRNGs", "Unicode"]
            uuid = Pkg.dependencies()[Base.UUID(Pkg.project().dependencies[pkg])].uuid
            println(pkg, " = \"", uuid, "\"")
        end
    '
    ```

    Powyższe wypisze UUID-y w formie: `Aqua = "4c88cf16-eb10-579e-8560-4a9242c79595"` itp.

    UUID-y do skopiowania (zweryfikowane z RESEARCH.md Pattern 2 + Pkg General Registry):

    - `Test` = `"8dfed614-e22c-5e08-85e1-65c5234f0b40"` (stdlib)
    - `Unicode` = `"4ec0a83e-493e-50e2-b9ac-8f72acf5a8f5"` (stdlib)
    - `Aqua` = `"4c88cf16-eb10-579e-8560-4a9242c79595"`
    - `JET` = `"c3a54625-cd67-489e-a8e7-0a5a0ff4e31b"`
    - `StableRNGs` = `"860ef19b-820b-49d6-a774-d7a799459cd3"`

    Krok 2 — ręcznie edytuj `Project.toml`, dodaj sekcje `[extras]` i `[targets]` po `[deps]`:

    ```toml
    [extras]
    Aqua = "4c88cf16-eb10-579e-8560-4a9242c79595"
    JET = "c3a54625-cd67-489e-a8e7-0a5a0ff4e31b"
    StableRNGs = "860ef19b-820b-49d6-a774-d7a799459cd3"
    Test = "8dfed614-e22c-5e08-85e1-65c5234f0b40"
    Unicode = "4ec0a83e-493e-50e2-b9ac-8f72acf5a8f5"

    [targets]
    test = ["Aqua", "JET", "StableRNGs", "Test", "Unicode"]
    ```

    UWAGA: `Unicode` jest stdlib ale wymagana w `runtests.jl` (encoding guard używa `Unicode.normalize`)
    — patrz Pattern 6 z RESEARCH.md. Trzeba ją deklarować w `[extras]+[targets]` żeby `Pkg.test()`
    aktywował ją w test env.

    Krok 3 — zweryfikuj `Pkg.test()` rezolwuje:

    ```bash
    julia --project=. -e '
        using Pkg
        Pkg.activate(".")
        Pkg.resolve()       # weryfikuje że Manifest jest spójny z Project
        Pkg.test()          # uruchomi test/runtests.jl jeśli istnieje; jeśli nie — fail OK
    '
    ```

    Jeśli `Pkg.test()` zwraca błąd "no test/runtests.jl found" — IGNORUJ, plan dalej tworzy stub
    w Task 4. Sprawdzamy tylko, czy resolver akceptuje [extras]+[targets].
  </action>
  <verify>
    <automated>grep -q "^\[extras\]" Project.toml && grep -q "^\[targets\]" Project.toml && grep -q "^Aqua = \"" Project.toml && grep -q "^JET = \"" Project.toml && grep -q "^StableRNGs = \"" Project.toml && grep -q "^Test = \"" Project.toml && grep -q "^Unicode = \"" Project.toml && grep -q 'test = \[.*"Aqua".*"JET".*"StableRNGs".*"Test".*"Unicode".*\]' Project.toml && julia --project=. -e 'using Pkg; Pkg.resolve()'</automated>
  </verify>
  <acceptance_criteria>
    - `Project.toml` zawiera sekcję `[extras]` z dokładnie 5 wpisami: `Aqua`, `JET`, `StableRNGs`, `Test`, `Unicode`
    - Każdy z 5 wpisów ma poprawny UUID (zgodny z listą wyżej, w cudzysłowach)
    - `Project.toml` zawiera sekcję `[targets]` z literal `test = ["Aqua", "JET", "StableRNGs", "Test", "Unicode"]` (kolejność w liście dowolna, ale wszystkie 5 obecne)
    - `julia --project=. -e 'using Pkg; Pkg.resolve()'` exit code 0
    - `Manifest.toml` jest aktualne (po `Pkg.resolve` może być modified)
  </acceptance_criteria>
  <done>
    - 5 test-only deps w `[extras]` z poprawnymi UUID-ami
    - `[targets].test` referuje wszystkie 5
    - Pkg resolver akceptuje konfigurację
  </done>
</task>

<task type="auto">
  <name>Task 3: Dodaj sekcję `[compat]` (Wariant a — literal compliance z ROADMAP SC2)</name>
  <files>Project.toml</files>
  <read_first>
    - .planning/phases/01-bootstrap-core-types-points/01-CONTEXT.md (D-17: lista compat entries; planner-decyzja: Wariant a vs b)
    - .planning/phases/01-bootstrap-core-types-points/01-RESEARCH.md (sekcja "Phase 1 — co NIE wchodzi w [deps] jeszcze"; Open Question 3 — Aqua "stale deps" warning)
    - .planning/ROADMAP.md (Phase 1 SC2 literal text — wymaga GLMakie/Makie/Observables/BenchmarkTools w [compat])
    - critical_research_findings #3 (revision): WYBRANO Wariant a — literal SC2 compliance; trade-off (Aqua stale_deps) mitigowany w Plan 06
  </read_first>
  <action>
    Decyzja zlockowana: **Wariant a (per ROADMAP SC2 literal compliance)** z D-17.

    ROADMAP Phase 1 Success Criterion #2 wymaga literal text:
    > `Project.toml` zawiera sekcję `[compat]` z `julia = "1.10"`, `GLMakie = "0.24"`, `Makie = "0.24"`
    > plus pozostałymi twardymi zależnościami z STACK.md.

    Dlatego `[compat]` w Phase 1 zawiera PEŁNĄ listę pakietów ze STACK.md, nawet tych które wejdą do
    `[deps]` dopiero w Phase 3 (GLMakie, Makie, Observables) lub Phase 4 (BenchmarkTools).

    Trade-off: Aqua zgłosi "stale [compat]" dla GLMakie/Makie/Observables/BenchmarkTools (są w
    `[compat]` ale nie w `[deps]` jeszcze). Mitigacja: Plan 06 Task 1 wywoła
    `Aqua.test_all(JuliaCity; stale_deps=false)` z TODO marker do re-enable w Phase 4.

    Edytuj `Project.toml` — dodaj sekcję `[compat]` POMIĘDZY `[deps]` a `[extras]` z DOKŁADNIE
    tą treścią (kolejność per D-17, alfabetycznie po `julia`):

    ```toml
    [compat]
    julia = "1.10"
    GLMakie = "0.24"
    Makie = "0.24"
    GeometryBasics = "0.5"
    Observables = "0.5"
    StableRNGs = "1.0"
    Aqua = "0.8.14"
    JET = "0.11"
    BenchmarkTools = "1.6"
    ```

    KRYTYCZNE — co WCHODZI do `[compat]` w Phase 1 (Wariant a, literal SC2 compliance):
    - `julia = "1.10"` — BOOT-02, ROADMAP SC2 literal
    - `GLMakie = "0.24"` — ROADMAP SC2 literal (deps wejdą w Phase 3)
    - `Makie = "0.24"` — ROADMAP SC2 literal (deps wejdą w Phase 3)
    - `GeometryBasics = "0.5"` — D-02, w `[deps]` od Phase 1
    - `Observables = "0.5"` — STACK.md (deps wejdą w Phase 3)
    - `StableRNGs = "1.0"` — w `[extras]` od Phase 1
    - `Aqua = "0.8.14"` — w `[extras]` od Phase 1
    - `JET = "0.11"` — w `[extras]` od Phase 1
    - `BenchmarkTools = "1.6"` — STACK.md (deps wejdą w Phase 4)

    Co NIE wchodzi do `[compat]`:
    - `Random`, `Test`, `Unicode` (stdlib) — Pkg ekosystem-konwencja: dla stdlib NIE wpisuje się
      `[compat]` (są bundled z Julia, sterowane przez `julia = "1.10"`)

    Sprawdź wersje aktualnie zainstalowanych deps przed wpisaniem do `[compat]`:

    ```bash
    julia --project=. -e '
        using Pkg
        Pkg.status()
    '
    ```

    Jeśli `Pkg.status()` pokazuje wersję starszą niż wpisana w `[compat]` floor (np. `Aqua v0.8.13`
    zamiast 0.8.14):
    - Opcja A: bumpuj `Pkg.add("Aqua")` (jeśli rejestr ma 0.8.14+)
    - Opcja B: obniż floor w `[compat]` do aktualnie dostępnej wersji (np. `Aqua = "0.8"`)

    Po edycji uruchom resolver żeby potwierdzić spójność:

    ```bash
    julia --project=. -e '
        using Pkg
        Pkg.resolve()
        Pkg.instantiate()
        Pkg.status()
    '
    ```

    Wszystkie trzy komendy MUSZĄ przejść bez błędu. Resolver akceptuje wpisy `[compat]` dla
    pakietów które nie są w `[deps]` ani `[extras]` — to legal Pkg behavior, ale Aqua to flaguje
    jako "stale" (stąd `stale_deps=false` w Plan 06).
  </action>
  <verify>
    <automated>grep -q "^\[compat\]" Project.toml && grep -E '^julia\s*=\s*"1\.10"' Project.toml && grep -E '^GLMakie\s*=\s*"0\.24"' Project.toml && grep -E '^Makie\s*=\s*"0\.24"' Project.toml && grep -E '^GeometryBasics\s*=\s*"0\.5"' Project.toml && grep -E '^Observables\s*=\s*"0\.5"' Project.toml && grep -E '^StableRNGs\s*=\s*"1\.0"' Project.toml && grep -E '^Aqua\s*=\s*"0\.8' Project.toml && grep -E '^JET\s*=\s*"0\.11"' Project.toml && grep -E '^BenchmarkTools\s*=\s*"1\.6"' Project.toml && julia --project=. -e 'using Pkg; Pkg.resolve(); Pkg.instantiate()'</automated>
  </verify>
  <acceptance_criteria>
    - `Project.toml [compat]` zawiera DOKŁADNIE 9 wpisów (Wariant a — literal SC2 compliance):
      `julia`, `GLMakie`, `Makie`, `GeometryBasics`, `Observables`, `StableRNGs`, `Aqua`, `JET`, `BenchmarkTools`
    - `julia = "1.10"` (BOOT-02, ROADMAP SC2 literal)
    - `GLMakie = "0.24"` (ROADMAP SC2 literal)
    - `Makie = "0.24"` (ROADMAP SC2 literal)
    - `GeometryBasics = "0.5"` (D-02)
    - `Observables = "0.5"` (STACK.md)
    - `StableRNGs = "1.0"`
    - `Aqua = "0.8.14"` lub `"0.8"` (zależy od rejestru — patch level OK)
    - `JET = "0.11"`
    - `BenchmarkTools = "1.6"`
    - `[compat]` NIE zawiera stdlib (`Random`, `Test`, `Unicode`)
    - `julia --project=. -e 'using Pkg; Pkg.resolve(); Pkg.instantiate()'` exit code 0
  </acceptance_criteria>
  <done>
    - [compat] zawiera 9 wpisów (Wariant a, literal ROADMAP SC2 compliance)
    - GLMakie/Makie/Observables/BenchmarkTools obecne (Aqua stale_deps suppression w Plan 06)
    - Pkg resolver i instantiate pass clean
  </done>
</task>

<task type="auto">
  <name>Task 4: Stub `test/runtests.jl` (pełna treść w plan 06)</name>
  <files>test/runtests.jl</files>
  <read_first>
    - .planning/phases/01-bootstrap-core-types-points/01-RESEARCH.md (Pełen `test/runtests.jl` w sekcji "Code Examples" — to jest TARGET dla plan 06; teraz tylko stub)
  </read_first>
  <action>
    Utwórz katalog `test/` jeśli nie istnieje, i wpisz minimalny stub `test/runtests.jl`:

    ```julia
    # Test runner pakietu JuliaCity
    # Stub utworzony w Phase 1 plan 03 — pełna treść (encoding guard, generuj_punkty tests,
    # StanSymulacji tests, Aqua, JET smoke) dochodzi w Phase 1 plan 06.

    using Test

    @testset "JuliaCity (stub)" begin
        @test true  # placeholder — pełne testy w plan 06
    end
    ```

    Komentarz po polsku (LANG-01), asercja po angielsku w `@test true` (idiomy Julia, LANG-04 OK).

    Uruchom testy żeby potwierdzić że stub działa:

    ```bash
    julia --project=. -e '
        using Pkg
        Pkg.test()
    '
    ```

    Oczekiwany output: `Test Summary: | Pass  Total  Time` z `1 Pass`. To potwierdza że
    `[extras]+[targets]` jest poprawnie skonfigurowany — Pkg.test() znalazł test/runtests.jl,
    aktywował test env (z Test/Aqua/JET/StableRNGs/Unicode) i uruchomił go.

    Jeśli `Pkg.test()` zwraca błąd "Aqua not found" lub podobny — odebierz, że `[extras]+[targets]`
    z Task 2 ma błąd. Verify UUID-y, kolejność sekcji w Project.toml.
  </action>
  <verify>
    <automated>test -f test/runtests.jl && grep -q "using Test" test/runtests.jl && grep -q "@testset" test/runtests.jl && julia --project=. -e 'using Pkg; Pkg.test()' 2>&1 | grep -q "Pass"</automated>
  </verify>
  <acceptance_criteria>
    - `test/runtests.jl` istnieje
    - Plik jest UTF-8 bez BOM (`head -c 3 test/runtests.jl | xxd | grep -q "ef bb bf"` exit 1)
    - Plik jest po polsku w komentarzach (zawiera literal "Phase 1" lub "stub" — komentarz wyjaśniający)
    - Plik zawiera `using Test` i `@testset`
    - `julia --project=. -e 'using Pkg; Pkg.test()'` zwraca exit 0
    - Output `Pkg.test()` zawiera string `Pass` (stub `@test true` przechodzi)
  </acceptance_criteria>
  <done>
    - test/runtests.jl stub utworzony
    - Pkg.test() działa — confirms [extras]+[targets] configuration
    - Gotowe na rozszerzenie w plan 06
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Julia Pkg registry | Pkg.add pobiera pakiety z General Registry przez HTTPS |
| Project.toml → Pkg resolver | Resolver respektuje `[compat]` constraints |
| Manifest.toml → CI | CI uruchamia `Pkg.instantiate` z committed Manifest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Tampering | Manifest.toml ujawnia path-dependent paths (`path = "/Users/..."`) | mitigate | Plan 03 NIE używa `Pkg.develop("/local/path")` — tylko `Pkg.add("Name")` z rejestru. Pre-commit verify: `git diff Manifest.toml` przed commitem nie zawiera `path = "/`. |
| T-01-10 | Supply Chain Tampering | Pkg.add pobiera pakiet z złośliwego mirrora | accept | Domyślny rejestr to oficjalny `General` (z `pkg.julialang.org`). Bez private mirrors. Manifest.toml pinuje konkretną wersję — kolejne resolve nie eskalują. |
| T-01-11 | Tampering | Ręcznie wpisany UUID w `[extras]` jest błędny | mitigate | Task 2 instruuje `Pkg.add` w temp env do odczytu UUID-ów; UUID-y dodatkowo zlistowane w action (cross-check z RESEARCH.md Pattern 2). |
| T-01-12 | Information Disclosure | `authors` field zawiera email użytkownika (publiczne repo) | accept | Standardowa praktyka Julia ekosystemu (każdy Project.toml na General Registry ma authors). Email jest już publiczny w git commits. |
</threat_model>

<verification>
Pełna walidacja planu 03:

```bash
# Pliki istnieją
test -f Project.toml && test -f Manifest.toml && test -f test/runtests.jl

# Project.toml ma poprawne sekcje
for sec in '\[deps\]' '\[compat\]' '\[extras\]' '\[targets\]'; do
  grep -q "^$sec" Project.toml || { echo "FAIL: brak sekcji $sec"; exit 1; }
done

# [compat] honoruje Wariant a — zawiera literal ROADMAP SC2 entries
for pkg in 'julia' 'GLMakie' 'Makie' 'GeometryBasics' 'Observables' 'StableRNGs' 'Aqua' 'JET' 'BenchmarkTools'; do
  if ! grep -qE "^$pkg\s*=\s*\"" Project.toml; then
    echo "FAIL: brak $pkg w [compat] (Wariant a wymaga literal SC2 compliance)"; exit 1
  fi
done

# Resolver działa
julia --project=. -e 'using Pkg; Pkg.resolve(); Pkg.instantiate()' || { echo "FAIL: resolver"; exit 1; }

# Test stub działa
julia --project=. -e 'using Pkg; Pkg.test()' || { echo "FAIL: Pkg.test"; exit 1; }

# Manifest.toml committable (nie ma path = "/" entries z dev'a)
! grep -q 'path = "' Manifest.toml || { echo "FAIL: Manifest ma path entries"; exit 1; }

echo "OK: plan 03 zakończony"
```
</verification>

<success_criteria>
- `Project.toml` istnieje z sekcjami `[deps]` (2 wpisy), `[compat]` (9 wpisów — Wariant a literal SC2), `[extras]` (5 wpisów), `[targets]` (test = lista 5)
- `Manifest.toml` istnieje, commitowany (D-25)
- `test/runtests.jl` stub działa
- `julia --project=. -e 'using Pkg; Pkg.test()'` zwraca exit 0
- BOOT-01 (struktura plików) i BOOT-02 ([compat] z julia=1.10) pokryte
- ROADMAP Phase 1 SC2 literal text spełniony (`julia = "1.10"`, `GLMakie = "0.24"`, `Makie = "0.24"` w [compat])
</success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap-core-types-points/01-03-SUMMARY.md`.
</output>
</content>
</invoke>