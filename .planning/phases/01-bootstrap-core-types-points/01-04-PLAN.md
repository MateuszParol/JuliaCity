---
phase: 01-bootstrap-core-types-points
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - src/JuliaCity.jl
  - src/typy.jl
autonomous: true
requirements: [BOOT-04, LANG-01]
tags: [types, struct, parametric, mutable, const-fields, abstract-type]
must_haves:
  truths:
    - "`using JuliaCity` działa bez błędu w `julia --project=.` po `Pkg.instantiate()`"
    - "`Punkt2D` jest aliasem `const Punkt2D = Point2{Float64}` z GeometryBasics i jest eksportowany (D-01: alias; D-03: export Punkt2D)"
    - "Brak custom akcesorów (`wsp_x`, `wsp_y`) — używamy surowych `.x` / `.y` / `p[1]` / `p[2]` z `GeometryBasics.Point2` (D-04)"
    - "`mutable struct StanSymulacji{R<:AbstractRNG}` istnieje z `const` polami `punkty`, `D`, `rng` (Julia ≥ 1.8 syntax — D-05: const-on-set-once fields)"
    - "`StanSymulacji` ma mutable pola: `trasa::Vector{Int}`, `energia::Float64`, `temperatura::Float64`, `iteracja::Int` (D-06: pełen komplet pól pod SA już w Phase 1)"
    - "Konstruktor `StanSymulacji(punkty; rng=Xoshiro(42))` pre-alokuje `D = Matrix{Float64}(undef, n, n)` i `trasa = collect(1:n)` (D-07: zero-state, NIE liczy NN ani dystansów; D-08: D::Matrix precompute n×n)"
    - "`abstract type Algorytm end` jest zadeklarowany w `src/typy.jl` (extension point dla Holy-traits w Phase 2; D-09)"
    - "Próba reassignmentu `const` pola (`stan.punkty = Punkt2D[]`) rzuca `ErrorException` (\"setfield!: const field cannot be changed\")"
    - "Wszystkie komentarze w `src/typy.jl` i `src/JuliaCity.jl` są po polsku (LANG-01)"
    - "Asercje wewnętrzne (`throw(ArgumentError)`) są po angielsku (LANG-04, D-23)"
  artifacts:
    - path: "src/JuliaCity.jl"
      provides: "Module entry point — using GeometryBasics, Random; include(typy.jl); export Punkt2D, StanSymulacji, Algorytm"
      contains: "module JuliaCity"
      min_lines: 10
    - path: "src/typy.jl"
      provides: "Typy domenowe: Punkt2D, Algorytm (abstract), StanSymulacji{R} (mutable struct + const fields), zewnętrzny konstruktor"
      contains: "mutable struct StanSymulacji"
      min_lines: 40
  key_links:
    - from: "src/JuliaCity.jl"
      to: "src/typy.jl"
      via: "include() na top-level w module"
      pattern: "include\\(\"typy\\.jl\"\\)"
    - from: "src/typy.jl"
      to: "GeometryBasics.Point2"
      via: "using GeometryBasics: Point2"
      pattern: "using GeometryBasics"
    - from: "src/typy.jl"
      to: "Random.AbstractRNG, Random.Xoshiro"
      via: "using Random"
      pattern: "using Random"
    - from: "src/JuliaCity.jl exports"
      to: "user code (Phase 2+ algorytmy/, Phase 3 wizualizacja)"
      via: "export Punkt2D, StanSymulacji, Algorytm"
      pattern: "export.*Punkt2D.*StanSymulacji.*Algorytm"
---

<objective>
Stworzyć module entry point `src/JuliaCity.jl` i `src/typy.jl` z trzema typami domenowymi:
`Punkt2D`, `abstract type Algorytm`, oraz `mutable struct StanSymulacji{R<:AbstractRNG}` z
`const` polami i zewnętrznym konstruktorem zero-state.

Purpose: BOOT-04 (struktura kodu Julia w `src/`), LANG-01 (komentarze po polsku). D-01 do D-10
implementowane jednorazowo — Phase 2 wypełni `D::Matrix{Float64}` przez `oblicz_macierz_dystans!`,
ale konstruktor już istnieje i jest stabilny. `Punkt2D` flowuje zero-cost do GLMakie scatter
w Phase 3.

Output: `using JuliaCity` w REPL działa, `StanSymulacji(punkty)` konstruuje zero-state,
`Algorytm` jest extension point ready dla `struct SimAnnealing <: Algorytm` w Phase 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-bootstrap-core-types-points/01-CONTEXT.md
@.planning/phases/01-bootstrap-core-types-points/01-RESEARCH.md
@CLAUDE.md
@CONTRIBUTING.md

<interfaces>
<!-- Kluczowe typy z GeometryBasics 0.5.x (z RESEARCH.md):
     Point2{T} = Point{2,T} <: AbstractPoint{2,T} <: StaticVector{2,T}
     Point2(0.5, 0.5)::Point2{Float64} — konstruktor wartości
     p.x / p.y / p[1] / p[2] — akcesory (D-04: NIE piszemy własnych)

     Random stdlib (z Julii):
     abstract type AbstractRNG end
     mutable struct Xoshiro <: AbstractRNG
       — Xoshiro(seed::Integer) konstruktor
       — domyślny RNG od Julia 1.7

     Julia ≥ 1.8 syntax:
       mutable struct Foo{T}
           const x::T          # ustawiane raz w new(), niezmienne potem
           y::T                # mutable
       end
     — przypisanie do const polu po new() rzuca ErrorException -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stwórz `src/JuliaCity.jl` (module entry point + exports + include order)</name>
  <files>src/JuliaCity.jl</files>
  <read_first>
    - .planning/phases/01-bootstrap-core-types-points/01-CONTEXT.md (D-03: export Punkt2D; D-09: abstract Algorytm w Phase 1)
    - .planning/phases/01-bootstrap-core-types-points/01-RESEARCH.md (sekcja "Pełen `src/JuliaCity.jl`" w Code Examples — to jest target struktury)
    - CONTRIBUTING.md (sekcja 3 — komentarze po polsku, asercje po angielsku)
  </read_first>
  <action>
    Utwórz katalog `src/` jeśli nie istnieje (powinien już istnieć po plan 02 task 2 — `src/algorytmy/.gitkeep`).

    Stwórz `src/JuliaCity.jl` z DOKŁADNIE TĄ TREŚCIĄ:

    ```julia
    """
        JuliaCity

    Pakiet rozwiązujący problem komiwojażera (TSP) heurystyką inspirowaną fizyką
    błony mydlanej. Publiczne API:

    - `generuj_punkty(n; seed)` — losowe punkty w `[0,1]²` (Phase 1)
    - `Punkt2D` — alias na `Point2{Float64}` (Phase 1)
    - `StanSymulacji(punkty; rng)` — stan symulacji (Phase 1, zero-state)
    - `Algorytm` — abstract type, extension point dla wariantów (Phase 1)
    - `oblicz_energie(punkty, trasa)` — długość cyklu (Phase 2)
    - `symuluj_krok!(stan, params, alg)` — krok SA (Phase 2)
    - `wizualizuj(stan, params, alg; ...)` — animacja GLMakie (Phase 3)

    Phase 1 dostarcza tylko typy + `generuj_punkty`. Reszta dochodzi w fazach 2/3.
    """
    module JuliaCity

    # Zewnętrzne zależności runtime
    using GeometryBasics: Point2
    using Random

    # Typy domenowe (Punkt2D, Algorytm, StanSymulacji)
    include("typy.jl")

    # Eksport publicznego API
    # generuj_punkty będzie dodany w plan 05 (po stworzeniu src/punkty.jl)
    export Punkt2D, StanSymulacji, Algorytm

    end # module
    ```

    KRYTYCZNE — kolejność elementów:
    1. Module-level docstring (`"""..."""`) PRZED `module JuliaCity`
    2. `module JuliaCity` otwiera scope
    3. `using GeometryBasics: Point2` — selektywny import (mniej namespace pollution)
    4. `using Random` — pełny import (potrzebujemy `AbstractRNG`, `Xoshiro`)
    5. `include("typy.jl")` na top-level (NIE w funkcji — Pitfall: `include` w body funkcji jest anti-pattern)
    6. `export Punkt2D, StanSymulacji, Algorytm` — generuj_punkty będzie dodane w plan 05
    7. `end # module` zamyka scope

    Komentarze po polsku (LANG-01, D-22), docstring po polsku (LANG-01).

    Zweryfikuj plik jest UTF-8 bez BOM:

    ```bash
    head -c 3 src/JuliaCity.jl | xxd | grep -q "ef bb bf" && echo "FAIL: BOM" || echo "OK: no BOM"
    ```

    Zweryfikuj kompilację (po stworzeniu `src/typy.jl` w Task 2):

    ```bash
    # NIE uruchamiaj jeszcze — typy.jl nie istnieje, fail oczekiwany.
    # Po Task 2: julia --project=. -e 'using JuliaCity'
    ```
  </action>
  <verify>
    <automated>test -f src/JuliaCity.jl && grep -q "module JuliaCity" src/JuliaCity.jl && grep -q "using GeometryBasics: Point2" src/JuliaCity.jl && grep -q "using Random" src/JuliaCity.jl && grep -q 'include("typy.jl")' src/JuliaCity.jl && grep -q "export Punkt2D, StanSymulacji, Algorytm" src/JuliaCity.jl && grep -q "end # module" src/JuliaCity.jl && ! head -c 3 src/JuliaCity.jl | xxd | grep -q "ef bb bf"</automated>
  </verify>
  <acceptance_criteria>
    - `src/JuliaCity.jl` istnieje
    - Pierwsze trzy bajty NIE są BOM (0xEF 0xBB 0xBF)
    - Plik kończy się `\n`
    - Plik zawiera `module JuliaCity` (literal)
    - Plik zawiera `using GeometryBasics: Point2` i `using Random`
    - Plik zawiera `include("typy.jl")` (na top-level w module, nie w funkcji)
    - Plik zawiera `export Punkt2D, StanSymulacji, Algorytm` (generuj_punkty dodany w plan 05)
    - Plik zawiera komentarz lub docstring po polsku (zawiera literal "Pakiet" albo "Phase" — wystarczy jeden polski-context word)
    - Plik zawiera `end # module` jako ostatnią lub przedostatnią linię
  </acceptance_criteria>
  <done>
    - src/JuliaCity.jl utworzony, zawiera module skeleton z exports
    - Komentarze/docstring po polsku
    - Plik UTF-8 bez BOM
    - Gotowy na include("typy.jl") z Task 2
  </done>
</task>

<task type="auto">
  <name>Task 2: Stwórz `src/typy.jl` (Punkt2D + Algorytm + StanSymulacji + zewnętrzny konstruktor)</name>
  <files>src/typy.jl</files>
  <read_first>
    - .planning/phases/01-bootstrap-core-types-points/01-CONTEXT.md (D-01: const Punkt2D = Point2{Float64}; D-04: brak custom akcesorów; D-05/D-06/D-07: StanSymulacji shape; D-08: D::Matrix precompute; D-09: abstract Algorytm; D-23: asercje po angielsku)
    - .planning/phases/01-bootstrap-core-types-points/01-RESEARCH.md (Pattern 3: mutable struct ... const + zewnętrzny konstruktor; Pełen `src/typy.jl` w Code Examples — to jest target; Pitfall 2: const fields + new())
    - CONTRIBUTING.md (komentarze po polsku, asercje po angielsku)
  </read_first>
  <action>
    Stwórz `src/typy.jl` z DOKŁADNIE TĄ TREŚCIĄ (kopiowaną prosto z RESEARCH.md "Pełen `src/typy.jl`"
    z drobnymi adjustments dla LANG-01/LANG-04 spójności):

    ```julia
    # Typy domenowe pakietu JuliaCity
    # - Punkt2D: alias na Point2{Float64} z GeometryBasics
    # - Algorytm: abstract type — extension point dla Holy-traits dispatch (Phase 2+)
    # - StanSymulacji: parametryczny mutable struct ze stanem SA

    # Zależności są importowane raz w src/JuliaCity.jl (using GeometryBasics: Point2; using Random).
    # Tutaj zakładamy ich dostępność w scope'ie modułu.

    """
        Punkt2D

    Alias na `Point2{Float64}` z `GeometryBasics`. Float64 dla precyzji sumy
    ~1000 odległości euklidesowych w `oblicz_energie` (Phase 2). Bezpośrednio
    konsumowany przez Makie scatter w Phase 3 — zero-cost konwersja.

    Akcesory: `p.x`, `p.y`, `p[1]`, `p[2]` (z GeometryBasics — własnych nie definiujemy).
    """
    const Punkt2D = Point2{Float64}

    """
        Algorytm

    Abstract type — wszystkie konkretne algorytmy (`SimAnnealing`, `ForceDirected`,
    `Hybryda`) są podtypami dodawanymi w `src/algorytmy/`. Extension point dla
    Holy-traits dispatch w `symuluj_krok!`. Phase 1 deklaruje typ; Phase 2 wprowadza
    `struct SimAnnealing <: Algorytm` (REQ ALG-01).
    """
    abstract type Algorytm end

    """
        StanSymulacji{R<:AbstractRNG}

    Stan symulacji TSP. Parametryzacja po typie RNG umożliwia type-stable dispatch
    na operacjach próbkowania (Pitfall 1 z PITFALLS — abstract field type powoduje
    type instability; konkret w polu `R<:AbstractRNG` ją eliminuje).

    # Pola const (ustawiane raz w konstruktorze, niezmienne potem)
    - `punkty::Vector{Punkt2D}` — punkty 2D w `[0,1]²`
    - `D::Matrix{Float64}` — n×n macierz dystansów (pre-alokowana, wypełniana przez Phase 2)
    - `rng::R` — lokalny RNG dla deterministycznej akceptacji Metropolisa

    # Pola mutable (aktualizowane przez `symuluj_krok!` w Phase 2)
    - `trasa::Vector{Int}` — permutacja `1:n` reprezentująca cykl Hamiltona
    - `energia::Float64` — bieżąca długość trasy (cache; Phase 2 utrzymuje invariant)
    - `temperatura::Float64` — bieżąca temperatura SA
    - `iteracja::Int` — licznik kroków
    """
    mutable struct StanSymulacji{R<:AbstractRNG}
        const punkty::Vector{Punkt2D}
        const D::Matrix{Float64}
        const rng::R
        trasa::Vector{Int}
        energia::Float64
        temperatura::Float64
        iteracja::Int
    end

    """
        StanSymulacji(punkty; rng=Xoshiro(42))

    Konstruktor zewnętrzny — pre-alokuje `D` (n×n, niewypełnione przez `undef`) oraz
    `trasa = collect(1:n)`. Wartości pozostałych pól zerowe.

    Phase 2 doda funkcje `oblicz_macierz_dystans!(stan)` i `inicjuj_nn!(stan)` które
    wypełnią `D` i przepiszą `trasa` na NN-tour — ten konstruktor pozostaje
    niezmieniony (D-07: zero-state).

    Argumenty:
    - `punkty::Vector{Punkt2D}` — wymagane, `length(punkty) > 0`
    - `rng::R` (kwarg) — domyślnie `Xoshiro(42)` (deterministyczne testy)
    """
    function StanSymulacji(punkty::Vector{Punkt2D}; rng::R=Xoshiro(42)) where {R<:AbstractRNG}
        n = length(punkty)
        n > 0 || throw(ArgumentError("punkty must be non-empty"))   # asercja po angielsku (D-23, LANG-04)
        D = Matrix{Float64}(undef, n, n)
        trasa = collect(1:n)
        return StanSymulacji{R}(punkty, D, rng, trasa, 0.0, 0.0, 0)
    end
    ```

    KRYTYCZNE — Pattern 3 z RESEARCH.md, Pitfall 2:
    - Konstruktor zewnętrzny (poza `mutable struct ... end`) wywołuje `new{R}(...)` JEDEN RAZ
      z WSZYSTKIMI polami w kolejności declared.
    - NIE rób `obj.const_field = value` po `new()` — fail z `ErrorException("setfield!: const field cannot be changed")`.
    - Parametr `R` dedukowany z RNG argumentu — `where {R<:AbstractRNG}` w sygnaturze.

    Komentarze po polsku, docstringi po polsku, asercja `throw(ArgumentError("punkty must be non-empty"))` PO ANGIELSKU per D-23/LANG-04.

    Zweryfikuj UTF-8 bez BOM:

    ```bash
    head -c 3 src/typy.jl | xxd | grep -q "ef bb bf" && echo "FAIL: BOM" || echo "OK: no BOM"
    ```

    Zweryfikuj NFC normalization (D-22 — polskie diakrytyki w komentarzach):

    ```bash
    julia --project=. -e '
        using Unicode
        tresc = read("src/typy.jl", String)
        if Unicode.normalize(tresc, :NFC) != tresc
            error("FAIL: src/typy.jl is not NFC-normalized")
        end
        println("OK: NFC")
    '
    ```

    KRYTYCZNE — końcowy smoke test (compile + construct):

    ```bash
    julia --project=. -e '
        using JuliaCity
        # 1. Punkt2D istnieje i jest aliasem
        @assert Punkt2D == GeometryBasics.Point2{Float64} "Punkt2D nie jest aliasem Point2{Float64}"

        # 2. Algorytm jest abstract type
        @assert Algorytm isa Type "Algorytm should be a Type"
        @assert isabstracttype(Algorytm) "Algorytm should be abstract"

        # 3. StanSymulacji konstruuje
        punkty = [Punkt2D(0.0, 0.0), Punkt2D(1.0, 1.0), Punkt2D(0.5, 0.5)]
        stan = StanSymulacji(punkty)
        @assert size(stan.D) == (3, 3) "D should be 3×3"
        @assert stan.trasa == [1, 2, 3]
        @assert stan.energia == 0.0
        @assert stan.iteracja == 0

        # 4. const field reassignment fails
        try
            stan.punkty = Punkt2D[]
            error("FAIL: const field reassignment should have thrown")
        catch e
            e isa ErrorException || rethrow()
            occursin("const field", e.msg) || error("Unexpected error: $(e.msg)")
        end

        # 5. ArgumentError dla pustego wektora
        try
            StanSymulacji(Punkt2D[])
            error("FAIL: empty punkty should have thrown")
        catch e
            e isa ArgumentError || rethrow()
        end

        println("OK: all smoke tests passed")
    '
    ```

    Wszystkie 5 asercji muszą przejść.
  </action>
  <verify>
    <automated>test -f src/typy.jl && grep -q "const Punkt2D = Point2{Float64}" src/typy.jl && grep -q "abstract type Algorytm end" src/typy.jl && grep -q "mutable struct StanSymulacji{R<:AbstractRNG}" src/typy.jl && grep -q "const punkty::Vector{Punkt2D}" src/typy.jl && grep -q "const D::Matrix{Float64}" src/typy.jl && grep -q "const rng::R" src/typy.jl && grep -q "function StanSymulacji" src/typy.jl && grep -q 'throw(ArgumentError("punkty must be non-empty"))' src/typy.jl && ! head -c 3 src/typy.jl | xxd | grep -q "ef bb bf" && julia --project=. -e 'using JuliaCity; punkty = [Punkt2D(0.0, 0.0), Punkt2D(1.0, 1.0)]; stan = StanSymulacji(punkty); @assert size(stan.D) == (2,2); @assert stan.trasa == [1,2]; @assert stan.energia == 0.0; println("OK")'</automated>
  </verify>
  <acceptance_criteria>
    - `src/typy.jl` istnieje, UTF-8 bez BOM, kończy się `\n`
    - Plik zawiera literal `const Punkt2D = Point2{Float64}` (D-01)
    - Plik zawiera literal `abstract type Algorytm end` (D-09)
    - Plik zawiera literal `mutable struct StanSymulacji{R<:AbstractRNG}` (D-05)
    - Plik zawiera DOKŁADNIE 3 pola `const` w struct: `const punkty::Vector{Punkt2D}`, `const D::Matrix{Float64}`, `const rng::R` (D-06)
    - Plik zawiera 4 mutable pola w struct: `trasa::Vector{Int}`, `energia::Float64`, `temperatura::Float64`, `iteracja::Int` (D-06)
    - Plik zawiera zewnętrzny konstruktor z sygnaturą `function StanSymulacji(punkty::Vector{Punkt2D}; rng::R=Xoshiro(42)) where {R<:AbstractRNG}`
    - Konstruktor pre-alokuje `D = Matrix{Float64}(undef, n, n)` (D-07: zero-state, NIE liczy)
    - Konstruktor pre-alokuje `trasa = collect(1:n)` (D-07)
    - Konstruktor wywołuje `StanSymulacji{R}(...)` JEDEN RAZ z 7 argumentami (Pitfall 2)
    - Asercja `throw(ArgumentError("punkty must be non-empty"))` jest po angielsku (D-23, LANG-04)
    - Komentarze i docstringi po polsku (LANG-01, D-22) — zawierają polskie słowa np. "punkty", "macierz", "Phase"
    - NFC-normalized (`julia -e 'using Unicode; @assert Unicode.normalize(read("src/typy.jl", String), :NFC) == read("src/typy.jl", String)'`)
    - Smoke test compile+construct przechodzi: `using JuliaCity` + `StanSymulacji([Punkt2D(0,0), Punkt2D(1,1)])` zwraca poprawny obiekt
    - `stan.punkty = Punkt2D[]` rzuca `ErrorException` z message zawierającym "const field"
    - `StanSymulacji(Punkt2D[])` rzuca `ArgumentError`
  </acceptance_criteria>
  <done>
    - src/typy.jl utworzony, kompiluje się przez `using JuliaCity`
    - Wszystkie 3 typy (Punkt2D, Algorytm, StanSymulacji) działają per smoke test
    - Komentarze po polsku, asercje po angielsku — LANG-01/LANG-04 OK
    - const fields chronione przed reassignment (Julia 1.8+)
    - Phase 2 może bezpiecznie dodać `oblicz_macierz_dystans!(stan)` bez zmiany konstruktora
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User code → `using JuliaCity` | Module load wykonuje top-level statements w `JuliaCity.jl` (using, include) |
| `StanSymulacji` constructor → user input | `length(punkty)` może być 0 — DoS-style memory issues |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | Denial of Service | `StanSymulacji(punkty)` z `punkty=[]` allocate'uje 0×0 macierz, OK; ale negatywne lub zbyt duże N | mitigate | `n > 0 || throw(ArgumentError(...))` w konstruktorze. N-very-large nie jest zagrożeniem dla pakietu lokalnego — user knows scope (PROJECT.md mówi 1000 punktów). |
| T-01-14 | Tampering | NFC normalization regression — edytor zapisuje polskie diakrytyki w NFD | mitigate | Encoding guard test w plan 06 łapie. NFC verification w smoke test tego planu jako pre-flight check. |
| T-01-15 | Information Disclosure | Docstringi mogą zawierać comment-jak ścieżki/sekrety | accept | Docstringi w plan 04 zawierają tylko nazwy funkcji + opis domenowy. Zero PII. |
</threat_model>

<verification>
Pełna walidacja planu 04:

```bash
# Pliki istnieją
test -f src/JuliaCity.jl && test -f src/typy.jl

# Module compile + 3 typy work
julia --project=. -e '
    using JuliaCity
    using GeometryBasics

    # Punkt2D
    @assert Punkt2D == Point2{Float64} "Punkt2D alias"

    # Algorytm abstract
    @assert isabstracttype(Algorytm) "Algorytm abstract"

    # StanSymulacji construct
    punkty = [Punkt2D(0.5, 0.5), Punkt2D(0.7, 0.3), Punkt2D(0.2, 0.9)]
    stan = StanSymulacji(punkty)
    @assert size(stan.D) == (3,3)
    @assert stan.trasa == [1,2,3]
    @assert stan.energia == 0.0 && stan.temperatura == 0.0 && stan.iteracja == 0

    # const field protection
    try
        stan.punkty = Punkt2D[]
        error("FAIL")
    catch e
        @assert e isa ErrorException && occursin("const field", e.msg)
    end

    # ArgumentError
    try
        StanSymulacji(Punkt2D[])
        error("FAIL")
    catch e
        @assert e isa ArgumentError
    end

    println("OK: plan 04 verified")
'

# Komentarze po polsku
grep -q "Phase\|punkty\|symulacji\|Pakiet" src/JuliaCity.jl && grep -q "Punkt\|symulacji\|macierz" src/typy.jl && echo "OK: Polish content"

# NFC
julia --project=. -e '
    using Unicode
    for f in ["src/JuliaCity.jl", "src/typy.jl"]
        tresc = read(f, String)
        @assert Unicode.normalize(tresc, :NFC) == tresc "$f not NFC"
    end
    println("OK: NFC")
'
```
</verification>

<success_criteria>
- `src/JuliaCity.jl` zawiera module skeleton z `using GeometryBasics: Point2`, `using Random`, `include("typy.jl")`, `export Punkt2D, StanSymulacji, Algorytm`
- `src/typy.jl` zawiera 3 typy: `Punkt2D` (alias), `Algorytm` (abstract), `StanSymulacji{R}` (parametric mutable struct z const fields)
- `using JuliaCity` w REPL działa bez błędu
- `StanSymulacji(punkty)` konstruuje zero-state per D-07
- const field reassignment rzuca `ErrorException` (Julia 1.8+ `const` w `mutable struct`)
- LANG-01 (komentarze polski), LANG-04 (asercje angielski) — pokryte
- Wszystkie pliki UTF-8 bez BOM, NFC-normalized
</success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap-core-types-points/01-04-SUMMARY.md`.
</output>
