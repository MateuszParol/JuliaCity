---
phase: 01-bootstrap-core-types-points
plan: 05
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - src/punkty.jl
  - src/JuliaCity.jl
autonomous: true
requirements: [PKT-01, PKT-02, PKT-03, PKT-04, LANG-01]
tags: [generuj_punkty, Xoshiro, deterministic, no-global-rng]
must_haves:
  truths:
    - "`generuj_punkty()` (bez argumentów) zwraca `Vector{Punkt2D}` o długości 1000 (PKT-02; D-12: default n=1000, seed=42)"
    - "`generuj_punkty(n; seed=42)` zwraca `Vector{Punkt2D}` o długości `n` z punktami w `[0,1]²` (PKT-01, PKT-03; D-12: defaults)"
    - "`generuj_punkty(100; seed=42) == generuj_punkty(100; seed=42)` (determinizm — PKT-01)"
    - "`generuj_punkty(100; seed=42) != generuj_punkty(100; seed=43)` (różne seedy → różne wyniki)"
    - "`generuj_punkty(n, rng::AbstractRNG)` (drugi method, composable per D-11) działa z `Xoshiro`/`StableRNG`"
    - "Implementacja używa `rand(rng, Punkt2D, n)` z GeometryBasics — bez ręcznych comprehensions ani promotion (D-13)"
    - "Brak convenience constructora `StanSymulacji(n::Int; seed)` w v1 — `generuj_punkty` i `StanSymulacji` są osobnymi funkcjami, składanymi jawnie przez użytkownika (D-15)"
    - "Wywołanie `generuj_punkty(...)` NIE modyfikuje `Random.default_rng()` — `before == after` (PKT-04, D-14)"
    - "`eltype(generuj_punkty(10)) == Punkt2D` (smoke test dla A1 z RESEARCH.md — Pitfall 6 ochrona)"
    - "`generuj_punkty(0)` lub `generuj_punkty(-5)` rzuca `ArgumentError` (\"n must be positive\")"
  artifacts:
    - path: "src/punkty.jl"
      provides: "Dwie metody `generuj_punkty`: friendly default (n; seed) i composable (n, rng) — D-11"
      contains: "function generuj_punkty"
      min_lines: 25
  key_links:
    - from: "src/punkty.jl `generuj_punkty(n; seed)`"
      to: "src/punkty.jl `generuj_punkty(n, rng)`"
      via: "delegacja po lokalnym `Xoshiro(seed)`"
      pattern: "Xoshiro\\(seed\\)"
    - from: "src/punkty.jl `generuj_punkty(n, rng)`"
      to: "GeometryBasics.Point2 + StaticArrays rand"
      via: "rand(rng, Punkt2D, n) — A1 assumption z RESEARCH.md, smoke test broni"
      pattern: "rand\\(rng, Punkt2D, n\\)"
    - from: "src/JuliaCity.jl"
      to: "src/punkty.jl"
      via: "include + export generuj_punkty"
      pattern: "include\\(\"punkty\\.jl\"\\)"
---

<objective>
Zaimplementować `generuj_punkty` z dwiema metodami (D-11):
1. `generuj_punkty(n::Int=1000; seed::Integer=42)` — friendly default, lokalny `Xoshiro(seed)`
2. `generuj_punkty(n::Int, rng::AbstractRNG)` — composable, dla testów ze `StableRNG`

Brak interakcji z `Random.default_rng()` (PKT-04, D-14). Implementacja `rand(rng, Punkt2D, n)` (D-13)
z fallback comprehension w razie A1 fail (Pitfall 6).

Purpose: PKT-01..04 + LANG-01. To jest jedyna pełnosprawna funkcja runtime'u w Phase 1 — Phase 2
będzie konsumować punkty wygenerowane tą funkcją do `oblicz_energie`/`symuluj_krok!`.

Output: `using JuliaCity; generuj_punkty(1000)` zwraca 1000 deterministycznych punktów w `[0,1]²`,
gotowych dla Phase 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-bootstrap-core-types-points/01-CONTEXT.md
@.planning/phases/01-bootstrap-core-types-points/01-RESEARCH.md
@CONTRIBUTING.md
@src/JuliaCity.jl
@src/typy.jl

<interfaces>
<!-- Z plan 04 (już zaimplementowane):
     const Punkt2D = Point2{Float64}        # alias
     mutable struct StanSymulacji{R<:AbstractRNG} ...

     Z Random stdlib:
     abstract type AbstractRNG end
     mutable struct Xoshiro <: AbstractRNG end       # Xoshiro(seed::Integer) konstruktor
     Random.default_rng()::TaskLocalRNG              # zwraca task-local RNG, NIE chcemy go ruszać

     Z GeometryBasics + StaticArrays inheritance:
     rand(::AbstractRNG, ::Type{<:StaticVector}, n::Int) -> Vector
     ASUMPCJA A1 (research-flagged MEDIUM): rand(rng, Point2{Float64}, n) zwraca Vector{Point2{Float64}}.
       Smoke test broni; comprehension fallback gotowy.

     Plan 05 dodaje export generuj_punkty do src/JuliaCity.jl. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stwórz `src/punkty.jl` z dwiema metodami `generuj_punkty`</name>
  <files>src/punkty.jl</files>
  <read_first>
    - .planning/phases/01-bootstrap-core-types-points/01-CONTEXT.md (D-11: dwie metody; D-12: defaults n=1000, seed=42; D-13: rand(rng, Punkt2D, n); D-14: brak default_rng mutation)
    - .planning/phases/01-bootstrap-core-types-points/01-RESEARCH.md (Pattern 4: generuj_punkty implementation; Pitfall 6: rand może zwrócić SVector zamiast Point2 — fallback comprehension)
    - src/typy.jl (Punkt2D alias musi być w scope'ie modułu)
    - CONTRIBUTING.md (komentarze polski, asercje angielski)
  </read_first>
  <action>
    Stwórz `src/punkty.jl` z DOKŁADNIE TĄ TREŚCIĄ:

    ```julia
    # Generator losowych punktów testowych — pokrywa REQ PKT-01..04.
    # Lokalny Xoshiro, brak mutacji Random.default_rng() (PKT-04, D-14).
    # Dwie metody (D-11) — friendly default + composable.

    """
        generuj_punkty(n::Int=1000; seed::Integer=42) -> Vector{Punkt2D}

    Generuje `n` losowych punktów 2D w `[0,1]²` używając lokalnego `Xoshiro(seed)`.
    Domyślnie `n = 1000` (PKT-02), `seed = 42` (D-12). Deterministyczne dla danego
    seeda — wielokrotne wywołania z tym samym seedem zwracają identyczne wektory.

    NIE modyfikuje globalnego stanu PRNG (`Random.default_rng()`) — patrz PKT-04.
    Test izolacji w `test/runtests.jl` (Phase 1 plan 06).

    # Examples
    ```jldoctest
    julia> punkty = generuj_punkty(3; seed=42);

    julia> length(punkty)
    3

    julia> all(p -> 0.0 <= p[1] <= 1.0 && 0.0 <= p[2] <= 1.0, punkty)
    true

    julia> generuj_punkty(3; seed=42) == generuj_punkty(3; seed=42)
    true
    ```
    """
    function generuj_punkty(n::Int=1000; seed::Integer=42)
        n > 0 || throw(ArgumentError("n must be positive"))   # asercja po angielsku (LANG-04)
        rng = Xoshiro(seed)
        return generuj_punkty(n, rng)
    end

    """
        generuj_punkty(n::Int, rng::AbstractRNG) -> Vector{Punkt2D}

    Wariant composable — testy mogą podać własny `StableRNG(42)` (Phase 2) dla
    cross-version reproducibility (Pitfall 8 z PITFALLS — `Xoshiro` stream NIE jest
    stabilny między minor versions Julii).

    Argumenty:
    - `n::Int` — liczba punktów, `n > 0` (rzuca `ArgumentError` w przeciwnym razie)
    - `rng::AbstractRNG` — instancja RNG (np. `Xoshiro(42)`, `StableRNG(42)`)
    """
    function generuj_punkty(n::Int, rng::AbstractRNG)
        n > 0 || throw(ArgumentError("n must be positive"))
        # D-13: rand(rng, Punkt2D, n) — Punkt2D dziedziczy z StaticVector przez GeometryBasics,
        # więc StaticArrays daje rand metodę zwracającą Vector{Point2{Float64}}.
        # ASUMPCJA A1 (research-flagged): jeśli rand zwraca Vector{SVector{...}} zamiast
        # Vector{Point2{Float64}}, smoke test w plan 06 wykryje regresję — wówczas
        # zamień na fallback: `[Punkt2D(rand(rng), rand(rng)) for _ in 1:n]`.
        return rand(rng, Punkt2D, n)
    end
    ```

    KRYTYCZNE — nie używamy `Random.seed!` (Pitfall 1 z PITFALLS, anti-pattern):
    - `Random.seed!(42)` mutuje `default_rng()` — łamie PKT-04
    - Zawsze lokalny `Xoshiro(seed)` w funkcji #1 i `rng::AbstractRNG` arg w funkcji #2

    Komentarze po polsku (LANG-01, D-22), asercja `throw(ArgumentError("n must be positive"))` PO ANGIELSKU (LANG-04, D-23).

    Po utworzeniu pliku — natychmiastowy smoke test (przed includem do JuliaCity.jl):

    ```bash
    julia --project=. -e '
        # Standalone include — symuluje co Julia robi przy module load
        # ALE musi być w jakimś module żeby Punkt2D było dostępne.
        # Włóż w temp module:

        module TmpTest
            using GeometryBasics: Point2
            using Random
            const Punkt2D = Point2{Float64}
            include(joinpath(@__DIR__, "src", "punkty.jl"))
        end

        punkty = TmpTest.generuj_punkty(10; seed=42)
        @assert length(punkty) == 10
        @assert eltype(punkty) == TmpTest.Punkt2D || eltype(punkty) <: AbstractVector
        @assert all(p -> 0.0 <= p[1] <= 1.0 && 0.0 <= p[2] <= 1.0, punkty)
        println("OK: standalone smoke test passed")
        println("eltype = ", eltype(punkty))
    '
    ```

    KRYTYCZNE — sprawdź wynik `eltype`:
    - Jeśli `eltype(punkty) == Punkt2D` → A1 jest OK, kontynuuj.
    - Jeśli `eltype(punkty) == SVector{2, Float64}` lub podobne — A1 fail. Zamień implementację
      `rand(rng, Punkt2D, n)` na `[Punkt2D(rand(rng), rand(rng)) for _ in 1:n]` w `src/punkty.jl`.
      To jest documented fallback z Pitfall 6, performance equivalent dla N=1000.
  </action>
  <verify>
    <automated>test -f src/punkty.jl && grep -q "function generuj_punkty(n::Int=1000; seed::Integer=42)" src/punkty.jl && grep -q "function generuj_punkty(n::Int, rng::AbstractRNG)" src/punkty.jl && grep -q "Xoshiro(seed)" src/punkty.jl && grep -q 'throw(ArgumentError("n must be positive"))' src/punkty.jl && ! grep -q "Random.seed!" src/punkty.jl && ! head -c 3 src/punkty.jl | xxd | grep -q "ef bb bf"</automated>
  </verify>
  <acceptance_criteria>
    - `src/punkty.jl` istnieje, UTF-8 bez BOM, kończy się `\n`
    - Plik zawiera DOKŁADNIE 2 definicje funkcji `generuj_punkty` (`grep -c "^function generuj_punkty" src/punkty.jl` = 2)
    - Sygnatura #1: `function generuj_punkty(n::Int=1000; seed::Integer=42)` (D-11, D-12)
    - Sygnatura #2: `function generuj_punkty(n::Int, rng::AbstractRNG)` (D-11)
    - Funkcja #1 wywołuje `Xoshiro(seed)` (lokalny RNG, D-14)
    - Funkcja #2 zawiera `rand(rng, Punkt2D, n)` LUB fallback comprehension `[Punkt2D(rand(rng), rand(rng)) for _ in 1:n]` (jeśli A1 fail)
    - Obydwie funkcje rzucają `ArgumentError("n must be positive")` dla `n ≤ 0`
    - Plik NIE zawiera `Random.seed!` (anti-pattern, Pitfall 1)
    - Komentarze po polsku, docstringi po polsku — plik zawiera literal słowa "punktów", "lokalnego", "modyfikuje"
    - NFC-normalized
    - Standalone smoke test (TmpTest module include) zwraca `OK`, `eltype(punkty) == Punkt2D` (lub fallback zaaplikowany jeśli inne)
  </acceptance_criteria>
  <done>
    - src/punkty.jl utworzony z dwoma metodami
    - Smoke test potwierdza poprawność
    - LANG-01/LANG-04 OK
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire `generuj_punkty` do modułu `JuliaCity` (edit `src/JuliaCity.jl`)</name>
  <files>src/JuliaCity.jl</files>
  <read_first>
    - src/JuliaCity.jl (aktualny stan po plan 04 — `include("typy.jl")` jest, `include("punkty.jl")` brakuje)
    - src/punkty.jl (właśnie utworzone)
    - .planning/phases/01-bootstrap-core-types-points/01-RESEARCH.md (sekcja "Pełen `src/JuliaCity.jl`" — target z `include("punkty.jl")` PO `include("typy.jl")`, `export ... generuj_punkty` na końcu)
  </read_first>
  <action>
    Edytuj `src/JuliaCity.jl`. Dwie zmiany:

    **Zmiana 1:** Dodaj `include("punkty.jl")` BEZPOŚREDNIO PO `include("typy.jl")`. Kolejność jest
    krytyczna — `punkty.jl` używa `Punkt2D` zdefiniowanego w `typy.jl`, więc musi być po nim.

    **Zmiana 2:** Dodaj `generuj_punkty` do listy exportowanych symboli — zmień:

    ```julia
    export Punkt2D, StanSymulacji, Algorytm
    ```

    na:

    ```julia
    export Punkt2D, StanSymulacji, Algorytm, generuj_punkty
    ```

    Po edycji `src/JuliaCity.jl` powinien wyglądać tak (różnicowo od plan 04 output):

    ```julia
    """
        JuliaCity

    Pakiet rozwiązujący problem komiwojażera (TSP) heurystyką inspirowaną fizyką
    błony mydlanej. Publiczne API:
    ...  (docstring bez zmian)
    """
    module JuliaCity

    # Zewnętrzne zależności runtime
    using GeometryBasics: Point2
    using Random

    # Typy domenowe (Punkt2D, Algorytm, StanSymulacji)
    include("typy.jl")

    # Generator punktów testowych (PKT-01..04)
    include("punkty.jl")

    # Eksport publicznego API
    export Punkt2D, StanSymulacji, Algorytm, generuj_punkty

    end # module
    ```

    Zweryfikuj plik kompiluje się i `generuj_punkty` jest eksportowany:

    ```bash
    julia --project=. -e '
        using JuliaCity

        # generuj_punkty jest exported
        @assert :generuj_punkty in names(JuliaCity)

        # Sygnatury obie istnieją
        ms = methods(generuj_punkty)
        @assert length(ms) >= 2 "Expected ≥2 methods, got $(length(ms))"

        # Default n=1000 (PKT-02)
        punkty1k = generuj_punkty()
        @assert length(punkty1k) == 1000

        # Determinizm (PKT-01)
        @assert generuj_punkty(100; seed=42) == generuj_punkty(100; seed=42)
        @assert generuj_punkty(100; seed=42) != generuj_punkty(100; seed=43)

        # Punkty w [0,1]² (PKT-03)
        @assert all(p -> 0.0 <= p[1] <= 1.0 && 0.0 <= p[2] <= 1.0, punkty1k)

        # eltype (smoke test dla A1 — Pitfall 6)
        @assert eltype(punkty1k) == Punkt2D "FAIL: eltype is $(eltype(punkty1k)) — A1 ASSUMPTION FAILED, apply fallback in src/punkty.jl"

        # No global RNG mutation (PKT-04)
        before = copy(Random.default_rng())
        _ = generuj_punkty(1000; seed=42)
        after = copy(Random.default_rng())
        @assert before == after "FAIL: generuj_punkty mutated default_rng()"

        # Composable method (D-11)
        rng = Xoshiro(123)
        punkty_custom = generuj_punkty(50, rng)
        @assert length(punkty_custom) == 50

        # ArgumentError dla n ≤ 0
        try
            generuj_punkty(0)
            error("FAIL: should throw")
        catch e
            @assert e isa ArgumentError
        end
        try
            generuj_punkty(-5)
            error("FAIL: should throw")
        catch e
            @assert e isa ArgumentError
        end

        println("OK: all PKT-01..04 smoke tests passed")
    '
    ```

    Wszystkie asercje muszą przejść. KRYTYCZNE — jeśli asercja `eltype(punkty1k) == Punkt2D` fail
    (research A1 risk realizes), edytuj `src/punkty.jl` Task 1 zamieniając `rand(rng, Punkt2D, n)` na
    fallback `[Punkt2D(rand(rng), rand(rng)) for _ in 1:n]` i powtórz weryfikację.
  </action>
  <verify>
    <automated>grep -q 'include("punkty.jl")' src/JuliaCity.jl && grep -q "export Punkt2D, StanSymulacji, Algorytm, generuj_punkty" src/JuliaCity.jl && julia --project=. -e 'using JuliaCity; using Random; @assert :generuj_punkty in names(JuliaCity); @assert length(generuj_punkty()) == 1000; @assert generuj_punkty(100; seed=42) == generuj_punkty(100; seed=42); @assert eltype(generuj_punkty(10)) == Punkt2D; before = copy(default_rng()); _ = generuj_punkty(100; seed=42); after = copy(default_rng()); @assert before == after; println("OK")'</automated>
  </verify>
  <acceptance_criteria>
    - `src/JuliaCity.jl` zawiera `include("punkty.jl")` PO `include("typy.jl")` (kolejność krytyczna — `grep -n 'include' src/JuliaCity.jl` pokazuje typy.jl line < punkty.jl line)
    - `src/JuliaCity.jl` zawiera literal `export Punkt2D, StanSymulacji, Algorytm, generuj_punkty`
    - `using JuliaCity` w REPL działa bez błędu
    - `:generuj_punkty in names(JuliaCity)` (eksport sprawdzalny przez `names`)
    - `length(generuj_punkty()) == 1000` (PKT-02 default)
    - `length(generuj_punkty(50)) == 50` (n argument)
    - `length(generuj_punkty(50, Xoshiro(7))) == 50` (composable method, D-11)
    - `generuj_punkty(100; seed=42) == generuj_punkty(100; seed=42)` (determinizm, PKT-01)
    - `generuj_punkty(100; seed=42) != generuj_punkty(100; seed=43)` (różne seedy)
    - `all(p -> 0.0 <= p[1] <= 1.0 && 0.0 <= p[2] <= 1.0, generuj_punkty(1000))` (PKT-03)
    - `eltype(generuj_punkty(10)) == Punkt2D` (A1 holds)
    - `before = copy(default_rng()); generuj_punkty(...); after = copy(default_rng()); before == after` (PKT-04, D-14)
    - `generuj_punkty(0)` rzuca `ArgumentError`
    - NFC-normalized
  </acceptance_criteria>
  <done>
    - src/JuliaCity.jl wired do src/punkty.jl przez include + export
    - Wszystkie 4 PKT-* requirements pokryte przez smoke test
    - A1 assumption verified (eltype == Punkt2D) lub fallback aktywowany
    - Phase 2 może bezpiecznie używać `generuj_punkty(1000)` w testach
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User REPL → `generuj_punkty(n; seed)` | User-controlled `n` i `seed` |
| `generuj_punkty` → Random stdlib | Lokalny RNG, brak interakcji z global |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-16 | Denial of Service | `generuj_punkty(typemax(Int))` allocate'uje gigantyczny wektor | accept | Pakiet lokalny, użytkownik świadomy scope (1000 punktów per PROJECT.md). Walidacja `n > 0` chroni przed underflow; górny limit nie jest projektowo wymagany. |
| T-01-17 | Tampering | `Random.seed!` w innym module mógłby zmienić `default_rng` przed `generuj_punkty` | mitigate (architecturally) | `generuj_punkty` używa LOKALNEGO `Xoshiro(seed)` — global state nie wpływa na wynik. PKT-04 verified by test (plan 06). |
| T-01-18 | Information Disclosure | Punkty ujawniają wewnętrzny seed przez statistical analysis | accept | Brak wymagań kryptograficznych — to są punkty testowe TSP, nie nonce/key. Xoshiro nie jest CSPRNG (Julia docs jasno to zaznaczają). |
</threat_model>

<verification>
Pełna walidacja planu 05:

```bash
julia --project=. -e '
    using JuliaCity
    using Random

    # PKT-02: default n=1000
    @assert length(generuj_punkty()) == 1000

    # PKT-01: deterministic for seed=42
    @assert generuj_punkty(100; seed=42) == generuj_punkty(100; seed=42)
    @assert generuj_punkty(100; seed=1) != generuj_punkty(100; seed=2)

    # PKT-03: punkty w [0,1]²
    pts = generuj_punkty(1000)
    @assert all(p -> 0.0 <= p[1] <= 1.0 && 0.0 <= p[2] <= 1.0, pts)

    # PKT-04: no global RNG mutation
    before = copy(default_rng())
    _ = generuj_punkty(1000; seed=42)
    after = copy(default_rng())
    @assert before == after

    # D-11: two methods (composable)
    rng = Xoshiro(7)
    @assert length(generuj_punkty(10, rng)) == 10

    # eltype == Punkt2D (A1 — Pitfall 6 protection)
    @assert eltype(generuj_punkty(10)) == Punkt2D

    # ArgumentError dla n ≤ 0
    @assert try generuj_punkty(0); false catch e; e isa ArgumentError end
    @assert try generuj_punkty(-1); false catch e; e isa ArgumentError end

    println("OK: PKT-01..04 verified")
'
```
</verification>

<success_criteria>
- `src/punkty.jl` zawiera dwie metody `generuj_punkty` per D-11
- `src/JuliaCity.jl` includes punkty.jl + exports `generuj_punkty`
- PKT-01 (deterministic Vector{Punkt2D}), PKT-02 (default n=1000), PKT-03 (punkty w [0,1]²), PKT-04 (no global RNG mutation) wszystkie verified przez smoke test
- A1 assumption (eltype == Punkt2D) verified (lub fallback applied)
- LANG-01 (komentarze polski), LANG-04 (asercje angielski) — pokryte
</success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap-core-types-points/01-05-SUMMARY.md`.
</output>
